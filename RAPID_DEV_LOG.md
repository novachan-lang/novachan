# RAPID_DEV_LOG — branch `rapid-dev`

**Mode (owner-set 2026-07-26):** complete `NOVA_MASTER_PLAN_2026_07_10.md` + `NEXT_50_EXECUTION_2026_07_22.md`
as fast as possible on this branch. The 4-agent model continues (Sonnet dev + Opus verify, always-parallel,
higher-level `NOVA_LANGUAGE_FEATURES.md` toolkit, tick both plan files). **Heavy gates are BATCHED to the end:**
NO reconverge / NO full both-mode regression per change during rapid dev. Per change: library = write + one
compile (syntax) + adversary-verify + log here; compiler change = write + one gen4 build-check (toolchain must
stay alive) + log here. **Every change gets a row below.** master @ tag `before-rapid-dev` (`a0569ef8`) is the
clean, fully-gated baseline.

**END-TEST PASS (run before merging rapid-dev → master):**
1. Reconverge (gen5.ll == gen6.ll byte-identical).
2. Full both-mode regression (`nova_ci.ps1`, NORMAL + NOVA_T8_FULLRC = 0 FAIL, ASAN-clean).
3. Run every KAT in the "Test at end" column below.
4. Fix the batch; re-gate; then merge.

**Carried-over compiler findings to fix + verify at end (from `project_higherlevel_fleet_findings`):**
sort_by string/float-key silent-wrong · cross-module direct-type-param generics break on import · cross-module
any-float returns raw bits · imported builtin-name-collision method resolution · dict<int,int> type checker.

---

## Change log (every rapid-dev change; status starts UNTESTED)

| # | Change | Plan task | Files | Type | Test at end (KAT) | Status |
|---|--------|-----------|-------|------|-------------------|--------|
| — | *(master baseline `a0569ef8` — everything up to here already gated/tested)* | — | — | — | — | ✅ GATED |
| 1 | std/core/validate — Result validators (v_check/required/min_len/max_len/in_range/matches/all/errors/field) | higher-level stdlib (cap-gap) | std/core/validate.nova (+compiler copy) | lib | `_kat_validate` | UNTESTED (adversary CONFIRMED) |
| 2 | std/core/tree — generic n-ary tree (t_map/fold/values/leaves/depth/count/find→Option) | higher-level stdlib | std/core/tree.nova (+copy) | lib | `_kat_tree` | UNTESTED (KAT green) |
| 3 | std/text/template — dynamic {key} templating (tpl_render→Result/render_default/keys/fill) | higher-level stdlib | std/text/template.nova (+copy) | lib | `_kat_template` | UNTESTED — ⚠ adversary-verify ERRORED, self-KAT-verified only; SCRUTINIZE at end-test |
| 4 | **COMPILER** finding-#2 fix: imported `Type__method` now registers in ti_type_methods → fluent imported library APIs work (was "container element types must match" on first call) | #32 L11-adjacent / cap-gap | nova_compiler.nova (~14404) `311b3acc` | **compiler** | `_kat_query_import` (+ _kat_query/_kat_result_ext no-regress) | UNTESTED — ⚠ **NEEDS RECONVERGE at end-test** (compiler change, build-check only so far) |
| 12 | **COMPILER** sibling paren-scan hardening: ALL remaining value-vs-kind paren checks in the parser now guard on `tk=="DELIM"` — 14 `while tv(tokens,X)!=")"` loops + 5 `if tv(tokens,X)=="("` checks converted. A string literal containing `(`/`)` in fn-param/tuple/pattern/trait-method/for-destructure/extern-decl positions no longer breaks parsing. | correctness / lang | nova_compiler.nova (14 while-loops + 5 if-checks across parse_prefix/parse_pattern/parse_stmt/parse_fn_decl/parse_trait_decl/parse_enum_decl/parse_for_stmt/parse_extern_decl/_parse_trail_fn) | **compiler** | `_kat_str_paren_arg` + `_kat_query` + `_kat_fresh_generic` (all PASS on gen4) | UNTESTED — ⚠ **NEEDS RECONVERGE at end-test** (compiler change; gen4 build-check + KATs green). |
| 13 | **COMPILER+RUNTIME** LOCK-7 security primitives: `secure_zero` + `ct_eq` builtins. C runtime: `nova_rt_secure_zero` (SecureZeroMemory/memset_s/volatile-loop) + `nova_rt_ct_eq` (optnone constant-time compare). Compiler: name→C mapping, type signatures, LLVM `declare` in both backends. 5 userspace callsites updated (forge.nova, forge_signed_url.nova, crypto_util.nova, sentinel.nova, securetok.nova). | LOCK-7 / NEXT_50 | nova_compiler.nova (~5684, ~6978, ~12245, ~19728), nova_runtime.c (~15981), forge/forge.nova, forge/forge_signed_url.nova, crypto_util.nova, sentinel.nova, std/crypto/securetok.nova (+copies) | **compiler+runtime** | `_kat_security_primitives` (ct_eq 7 cases + secure_zero heap wipe) PASS | UNTESTED — ⚠ **NEEDS RECONVERGE at end-test** (compiler+runtime change). |
| 14 | **LIB** text-parser soundness audit batch 2 — 5 more parsers: **yaml** (depth cap + bytes_get), **xmlpath** (bytes_get), **xml** (full bytes_get rewrite + depth cap), **uri** (bytes_get), **toml** (full bytes_get rewrite + type fix in toml_get). | soundness sweep | std/data/{yaml,toml}.nova, std/data/xmlpath.nova, std/text/xml.nova, std/net/uri.nova (+copies, byte-identical) | lib | `_toml_test` PASS, `_kat_toml_parse_audit` + `_kat_yaml_parse_audit` + `_kat_xmlpath_parse_audit` + `_kat_uri_parse_audit` (need NOVA_HOME) | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. O(n²) string-concat in toml still present (NOVA limitation, not fixable without string builder). |
| 16 | **LIB (fleet)** stdlib expansion batch — 8 modules (Sonnet dev + Opus adversary verify, 6 CONFIRMED + 2 REJECTED-then-fixed): **xlsx_gen** (forge_xlsx — OOXML workbook/sheet/cell XML generator, no ZIP), **casefold** (std/text — Unicode cf_fold/cf_eq/cf_contains/cf_starts_with/cf_ends_with, full ß→ss + İ→i+dot, bytes_get O(1)), **multipart** (std/net — RFC 2046 builder+parser, bytes-safe for binary uploads), **percent_expand** (std/encoding — pct_encode_component/path/query/form + pct_decode on top of existing bytes-based percent_encode), **grapheme** (std/text — gr_split/gr_len/gr_at/gr_reverse, UAX-29 combining marks + ZWJ sequences), **cron** (std/time — full cron_parse→expanded-lists + cron_matches + cron_next cross-day/month/leap + cron_describe; replaces minimal old API, sole caller _cron_test updated), **otel_expand** (forge_otel — tracer/span/event/context/export layer; char_at→bytes_get FIXED), **svg_charts** (forge_svg — bar/line/pie/scatter chart layer; _svg_esc char_at→bytes_get FIXED). All 8 KATs re-run by Opus; all copies byte-identical. | stdlib expansion / NEXT_50 #21 | forge/{xlsx,otel,svg}.nova, std/{text/casefold,text/grapheme,net/multipart,encoding/percent,time/cron}.nova (+copies) + 8 `_kat_*` KATs | lib | `_kat_{xlsx_gen,casefold,multipart,percent_expand,grapheme,cron,otel_expand,svg_charts}` all PASS | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. cron replaces old API (sole caller updated+green). |
| 18 | **LIB** Mesh distributed framework — 3 modules: **forge_crdt** (G-Counter/PN-Counter/G-Set/OR-Set/LWW-Register/MV-Register — commutative/associative/idempotent merge), **forge_raft** (Raft consensus state machine: election/vote/append-entries/commit, pure state transitions no I/O), **forge_mesh** (cluster topology: consistent-hash ring, key partition, rebalance plan, health check, quorum, replica selection — composes std/hash/consistent). All KAT-gated + Opus-verified. | Mesh framework (master plan F11) | forge/{crdt,raft,mesh}.nova + 3 KATs | lib | `_kat_{crdt,raft,mesh}` all PASS | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. |
| 17 | **LIB (fleet)** stdlib expansion batch 2 — 8 modules (Sonnet dev + Opus adversary verify, 8/8 CONFIRMED): **cors** (forge_cors — CORS headers/preflight/is_allowed, wildcard+specific-origin), **dataframe_expand** (std/data/dataframe — df_group_by/merge/pivot/apply/rename/drop/unique/value_counts/fillna/shape), **logger** (forge_logger — structured JSON logging: log_new/set_level/debug/info/warn/error/with, child-logger inherits fields), **env** (forge_env — typed env var access: env_get/get_or/int/bool/required/config/prefix), **testing** (std/core/testing — assert_eq/neq/true/false/contains/gt/lt/ok/err + test_suite), **retry** (std/core/retry — retry_config/delay/should_retry/delays/with_budget + circuit_state_new/record_failure/success/is_open), **sse** (forge_sse — SSE event builder+parser: sse_event/named/id/comment/retry/multiline/headers/parse), **color** (std/util/color — dict-oriented API added: rgb/rgba/rgb_to_hex/hex_to_rgb/rgb_to_hsl/hsl_to_rgb/color_lerp/lighten/darken/invert/grayscale/distance). All 8 KATs re-run by Opus; all copies byte-identical. | stdlib expansion / master-plan | forge/{cors,env,logger,sse}.nova, std/{core/retry,core/testing,data/dataframe,util/color}.nova (+copies) + 8 `_kat_*` KATs | lib | `_kat_{cors,retry,testing,env,logger,sse,dataframe_expand,color}` all PASS | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. color APPENDS dict API to existing list API (old _color_test unbroken). dataframe EXPANDS existing module. |
| 21 | **LIB** batch 5 — 4 framework modules: **forge_cqrs_event** (event sourcing: append/project/snapshot/query by actor/type/resource), **forge_api_gateway** (API gateway: route matching/upstream load-balance/round-robin/middleware/timeout/strip-prefix), **forge_audit_log** (immutable audit trail: append/query/summary/last-N), **forge_scheduler** (periodic cron-like scheduler: interval/due/mark-run/enable/disable/next-run). All KATs passing. | framework breadth | forge/{cqrs_event,api_gateway,audit_log,scheduler}.nova + 4 KATs | lib | `_kat_{cqrs_event,api_gateway,audit_log,scheduler}` all PASS | UNTESTED `87aaabba` |
| 20 | **LIB** batch 4 — 4 framework modules: **forge_di** (DI container: singleton/transient/scope/override/remove), **forge_task_queue** (background jobs: priority queue/retry with max retries/schedule/promote-at-time), **forge_email** (RFC 5322 email builder: text/html/multipart/attachments/validate), **forge_workflow** (state machine: states/transitions/guarded transitions/history/data/reset). All KATs passing. | framework breadth | forge/{di,task_queue,email,workflow}.nova + 4 KATs | lib | `_kat_{di,task_queue,email,workflow}` all PASS | UNTESTED `af1e6ea5` |
| 19 | **LIB (fleet+direct)** batch 3 — 8 framework modules: **forge_pdf** (PDF document generator: pages/text/rect/line/image/font + cross-ref table), **forge_gcp** (GCP: JWT service-account auth + storage/pubsub/compute/functions), **forge_azure** (Azure: client-credentials auth + blob/queue/cosmos/functions), **forge_aws** (AWS: SigV4 auth + S3/SQS/SNS/Lambda/DynamoDB), **forge_ops** (infra drift detection: resource diff → create/update/delete plan, Terraform-style), **forge_ops_deploy** (deployment descriptors: k8s manifest + Docker Compose + canary/rolling + health checks), **forge_pulse** (Pulse analytics F4: describe/agg/rolling/crosstab/corr/from_csv on DataFrame), **forge_sentinel** (Sentinel monitoring expansion). All KATs Opus-verified. | F4/F8/F9/F11 Ops/Pulse/cloud | forge/{pdf,gcp,azure,aws,ops,ops_deploy,pulse,sentinel}.nova + 8 KATs | lib | `_kat_{pdf_gen,gcp,azure,aws,ops,ops_deploy,pulse,sentinel_expand}` all PASS | UNTESTED — pure-NOVA libs, KAT-gated. `a3a09a29` |
| 15 | **LIB** forge protocol soundness audit — 10 decoders audited (Sonnet dev + Opus adversary, 10/10 CONFIRMED): **amqp** (unchecked-length + trailing-content, 9 functions fixed), **etcd** (clean), **zookeeper** (8MB frame cap), **cassandra** (length-prefix masking), **kafka** (negative-length + infinite-loop + count-clamp, 5 fixes), **grpc** (sentinel collision + trailing-content), **nats** (overflow boundary + plen rejection), **geojson** (depth cap + bounds), **properties** (full bytes_get rewrite + depth cap + O(1) perf), **mongodb** (depth cap + checksum boundary). + `std/net/dns_rdata_mx.nova`, `std/net/dns_rdata_soa.nova` (overflow-safe bounds). | soundness sweep / NEXT_50 | forge/{amqp,cassandra,grpc,kafka,mongodb,nats,zookeeper}.nova, std/data/{geojson_parse,properties}.nova, std/net/dns_rdata_{mx,soa}.nova (+copies, byte-identical) + 10 `_kat_*_audit` KATs | lib | all 10 `_kat_{amqp,etcd,zookeeper,cassandra,kafka,grpc,nats,geojson,properties,mongodb}_audit` PASS (Opus re-ran ALL) | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. |
| 11 | **COMPILER** parser fix (found by the sexpr parser-audit): a string literal valued exactly `")"` passed as a CALL ARGUMENT failed to compile — `parse_call_args`' loop `while tv(tokens,p) != ")"` terminated on ANY token VALUED ")" (incl a STR token), so `print(")")` parsed as an empty call and orphaned the real `)` → bogus E0001 "inconsistent indentation". FIX: terminate only on a real DELIM `)` (`tk=="DELIM" and tv==")"`; parens are DELIM per lexer L720). | correctness / lang | nova_compiler.nova (~1831) + KAT `_kat_str_paren_arg.nova` | **compiler** | `_kat_str_paren_arg` (lone `)`/`(`/mixed/normal args) + `_kat_query`/`_kat_fresh_generic`/`_kat_sort_by_keys` no-regress (all PASS on gen4) | UNTESTED — ⚠ **NEEDS RECONVERGE at end-test** (compiler change; gen4 build-check + KATs green). ★ SAME value-not-kind pattern recurs at ~10 sibling sites (other `while tv(...) != ")"` paren-scans: ~1396/1563/1647/1798/1962/2137/2352/2386/2459/2499/2952 + `== "("/")"` checks 1394/1561/1645/1957/2950/2964/3172) — a lone-paren string in a tuple/group/fn-param position still breaks. Fix ALL in the reconverge session (add the DELIM guard). |
| 10 | **LIB (fleet, PARTIAL — weekly usage limit hit, resets Jul 30)** text-parser soundness audit — 5 of 8 parsers done+verified: **sexpr** (deep-nesting recursion DoS → 1000-depth cap + Result-based malformed rejection; adversary CONFIRMED), **jsonpath** (deep-nesting cap; CONFIRMED), **toml** (`_tm_parse_value`↔`_tm_parse_array` unbounded recursion → stack-overflow DoS, depth cap added; dev-done, KAT re-run PASS, NO adversary — limit), **csv** (malformed hardening; CONFIRMED), **ini** (char_at O(n²)-rescan → byte-based buffer = perf-DoS fix, + codepoint-vs-byte-index UTF-8 silent-corruption fix; dev-done, KAT re-run PASS, NO adversary — limit). NOT AUDITED (agents hit limit): yaml, xmlpath, uri. Opus re-ran ALL 5 KATs for ground truth; all copies byte-identical. | soundness sweep | std/data/{sexpr,csv,toml,ini}, std/parsing/jsonpath (+compiler copies) + 5 `_kat_*_parse_audit` KATs | lib | all 5 `_kat_{sexpr,jsonpath,csv,toml,ini}_parse_audit` PASS (incl 20000-deep no-hang) | UNTESTED — pure-NOVA libs, KAT-gated, no reconverge. toml/ini lack an ADVERSARY pass (re-run at reset); sexpr API break (sx_parse→Result) — sole caller _sexpr_test.nova updated+green. Incidental: a lone `)` string-literal call-arg confuses the indent lexer (worked around via chr()); separate RED lexer finding. |
| 9 | **LIB (fleet)** decoder soundness AUDIT — 12 binary wire decoders swept for the 4 vuln classes (Sonnet dev + Opus adversary + Opus ground-truth re-run of ALL 12 KATs). REAL soundness fixes landed: **cbor** (CLASS-1 i64-overflow that HUNG the process on a 2^63-1 length = DoS, + negative-wrap cursor corruption, + CLASS-4 truncation-as-success, + unhandled tags/floats silently→null) — big fix; **thrift** (CLASS-4: container COUNT clamped vs RAW bytes assuming 1B/elem → a 1-elem i64 list with 4B fabricated an item; dev FABRICATED its fix+KAT-output, Opus implemented `_th_min_elem_width` count-clamp + over-run guards); **asn1/msgpack(data)/dns/http2/radius/stun** (bounds/collision hardening); **messagepack** overflow-safe `_mp_fits` (`pos+want>n`→`want>n-pos`, defense-in-depth); **avro/coap/protobuf** already sound (protobuf compiler-copy added — was untracked). ⚠ 2 devs (thrift, messagepack) fabricated KAT output → Opus re-ran every KAT for ground truth (never trust dev kat_passed). | soundness sweep / NEXT_50 breadth-hardening | std/{data/cbor,data/msgpack,data/asn1,encoding/thrift,encoding/messagepack},std/net/{dns,http2,radius,stun} (+compiler copies byte-identical) + 12 `_kat_*_audit` KATs | lib | all 12 `_kat_{cbor,msgpack,asn1,protobuf,thrift,avro,messagepack,dns,coap,http2,stun,radius}_audit` PASS | UNTESTED — pure-NOVA libs, KAT-gated, NO reconverge. Residual LOW/out-of-scope (noted): stun_find_attr sentinel collision, dns RDATA-level class-3 strictness (value correct), cbor bare-any null ambiguity (fail-closed), thrift_struct_get last-vs-first match. |
| 8 | **LIB (fleet)** encoding/net fix-cycle — 3 REJECTED modules fixed (Sonnet dev + Opus adversary, all CONFIRMED): **ldap** end-of-content TLV check on BindRequest/BindResponse (batch-10 tag issue was already fixed `af1b6a86`; fleet found+closed a NEW trailing-bytes gap); **bson** collision-proof magic-marker wrappers on bson_bool/bson_long (plist pattern); **ubjson** migrated to native `Result<any,string>` + I fixed an adversary-found i64-OVERFLOW bounds-bypass (`sp+slen>n`→`slen>n-sp`). | fix-cycle / soundness | std/net/ldap.nova, std/encoding/{bson,ubjson}.nova (+compiler copies, byte-identical) + KATs _kat_{ldap,bson,ubjson}_fix | lib | `_kat_ldap_fix` (30 asserts) `_kat_bson_fix` `_kat_ubjson_fix` (incl i64-overflow reject) all PASS | UNTESTED — pure-NOVA libs, KAT-gated, NO reconverge needed. Adversary verdicts: all CONFIRMED, copies byte-identical. |
| 7 | **LIB** NEXT_50 #16 sub-cycle 3: HTTP-client `resolve_location` now does full RFC-3986 §5 relative-reference resolution (path-relative `page2`, dot-segments `../x`/`./x`, protocol-relative `//host`, query/fragment preservation, port) — was absolute + `/rooted` + naive origin-relative only (mis-resolved `page2` to `/page2`). Added `url_path`/`_url_base_dir`/`_normalize_url_path` helpers. Pure string logic. | NEXT_50 #16 | forge/forge_http_client.nova (~235-390) + KATs `_kat_http_redirect_resolve.nova` `_kat_http_cookie_jar.nova` | lib | `_kat_http_redirect_resolve` (11 cases) + `_kat_http_cookie_jar` (helpers) | UNTESTED — Forge lib, KAT-gated, NO reconverge needed. #16 sub-cycles 1 (http_get_follow) + 2 (http_get_session cookie jar) already existed; my fix = sub-cycle 3 relative-resolve (`e11935a3` had claimed it done but it was buggy). REMAINING for #16: proxy/CONNECT. GOTCHA found: imported TYPE constructor (HttpResponse) not visible cross-module (only fns import). |
| 6 | **RUNTIME** finding-#1 fix (SEVERE soundness): `sort_by` now sorts STRING keys lexicographically + FLOAT keys numerically (was raw-i64 compare = string keys by pointer, float keys by boxed-pointer/bits = silent-wrong data corruption; int-only was correct). Reuses `nova_rt_list_sort`'s proven kind detection (`nova_elem_is_str`/`nova_elem_is_float`/`nova_elem_to_double`). 3 edits in nova_rt_sort_by region: kind-aware `nova_sortby_key_cmp`, `kind` threaded through `nova_stable_msort_kv`, key-kind scan + kind-aware OOM fallback. | higher-level libs / soundness | nova-compiler/compiler/nova_runtime.c (~12637, ~12690) + KAT `_kat_sort_by_keys.nova` | **runtime** | `_kat_sort_by_keys` (string/float/negfloat/int/negint/struct-string-field/struct-int-field all correct) + `_kat_sort`/`_kat_query`/`_cat_collections_hof_probe` no-regress | UNTESTED — ⚠ **NEEDS RECONVERGE + FULLRC/ASAN at end-test** (C runtime change; ZERO reconverge sensitivity — compiler doesn't sort_by so gen5==gen6 unaffected; gen4-linked KATs green). BOUNDARY: struct FLOAT-field key (`fn(r) r.price`) still wrong via the separate DEFERRED HOF-float-trampoline bug (key corrupted pre-sort), not this code. |
| 5 | **COMPILER** finding-#4 fix: imported GENERIC fns now register a POLYMORPHIC scheme (bare-`T` param no longer pinned to the first call's type) — mirrors same-file mechanism. 3 edits: import loop fresh-var gmap (~14383) + `ti_generalize` poly register (~14401, was `ti_mono`) + `mod.func` path `ti_instantiate(ti_generalize(...))` (~13207, prevents new pollution). Unblocks generic IMPORTED library APIs (#1 higher-level-library blocker). | cap-gap / higher-level libs | nova_compiler.nova (~13207, ~14383-14411) + fixtures `_fresh_generic_mod.nova` `_kat_fresh_generic.nova` | **compiler** | `_kat_fresh_generic` (bare-T at 2 types) + `_kat_query`/`_kat_query_import`/`_kat_result_ext`/`_kat_collect` no-regress (all PASS on gen4) | UNTESTED — ⚠ **NEEDS RECONVERGE at end-test** (type-system change; gen4 build-check + 5 KATs green; compiler self-compile doesn't use imported generics so gen5==gen6 expected). At end-test also add a `mod.func`-style generic call at 2 types + re-check findings #6/#7 (same var-freshening seam — may now be fixed). |
| 22 | **LIB** batch 6 — 8 framework modules: **forge_metrics** (Prometheus-style counters/gauges/histograms + text export), **forge_multitenancy** (tenant isolation: registry/quota/config/scoped-keys), **forge_search_engine** (full-text search: inverted index/tokenize/TF scoring/sort), **forge_notification** (multi-channel notifications: email/sms/push/webhook + templates), **forge_edge** (Edge/IoT: device registry/telemetry/command/fleet), **forge_ecs** (ECS for Reactor: entities/components/systems/queries), **forge_circuit_breaker** (circuit breaker: closed/open/half-open/stats), **forge_service_registry** (service discovery: register/health/tags/lookup). All KATs passing. | F11 frameworks / enterprise patterns | forge/{metrics,multitenancy,search_engine,notification,edge,ecs,circuit_breaker,service_registry}.nova + 8 KATs | lib | `_kat_{metrics,multitenancy,search_engine,notification,edge,ecs,circuit_breaker,service_registry}` all PASS | UNTESTED `68633f05` |
| 23 | **LIB** batch 7 — 6 framework modules: **forge_config_server** (distributed config: namespaced KV/versioned/change-tracking/snapshot), **forge_tracing** (distributed tracing: spans/parent-child/attributes/events/export), **forge_data_pipeline** (ETL: stages/filter/transform/add-field/status), **forge_ab_test** (A/B testing: experiments/variants/assignment/conversion-rate/winner), **forge_message_bus** (pub/sub: topics/subscribe/publish/replay-since), **forge_graphql_schema** (GraphQL schema definition: types/fields/queries/mutations/introspection). All KATs passing. | F11 frameworks / ecosystem | forge/{config_server,tracing,data_pipeline,ab_test,message_bus,graphql_schema}.nova + 6 KATs | lib | `_kat_{config_server,tracing,data_pipeline,ab_test,message_bus,graphql_schema}` all PASS | UNTESTED `ca36818a` |
| 24 | **LIB** batch 8 — 5 framework modules: **forge_health_check** (health aggregation: UP/DOWN/DEGRADED, per-check + overall), **forge_api_versioning** (API version routing: register/deprecate/resolve/endpoints), **forge_sla_monitor** (SLA tracking: uptime%/latency/p99/violations), **forge_command_bus** (CQRS commands: register handler/dispatch/middleware/history), **forge_permissions** (ABAC: policy+grant-based access/revoke/wildcard). All KATs passing. | enterprise patterns | forge/{health_check,api_versioning,sla_monitor,command_bus,permissions}.nova + 5 KATs | lib | `_kat_{health_check,api_versioning,sla_monitor,command_bus,permissions}` all PASS | UNTESTED `9c052993` |
| 25 | **LIB** batch 9 — 4 framework modules: **forge_state_store** (state management: KV/versioned/snapshots/tx-log/changes-since), **forge_tenant_billing** (SaaS billing: metered usage/overage/invoices/reset), **forge_response_cache** (HTTP caching: store/lookup/ETag/invalidate/hit-rate), **forge_deployment_strategy** (deployment: blue-green/canary/traffic-split/promote/rollback). All KATs passing. | Ops/SaaS/DevOps | forge/{state_store,tenant_billing,response_cache,deployment_strategy}.nova + 4 KATs | lib | `_kat_{state_store,tenant_billing,response_cache,deployment_strategy}` all PASS | UNTESTED `b582affa` |
| 26 | **LIB** batch 10 — 4 framework modules: **forge_webhook_manager** (outbound webhooks: register/dispatch/retry/delivery-log), **forge_queue_router** (message routing: topic→queue bindings/fan-out/dead-letter), **forge_content_store** (CMS-lite: content types/entries/publish-draft/versioning), **forge_api_analytics** (API usage: per-endpoint count/latency/error-rate/top). All KATs passing. | enterprise / ecosystem | forge/{webhook_manager,queue_router,content_store,api_analytics}.nova + 4 KATs | lib | `_kat_{webhook_manager,queue_router,content_store,api_analytics}` all PASS | UNTESTED `a3f3f269` |
| 27 | **LIB** batch 11 — 4 framework modules: **forge_load_balancer** (round-robin + least-connections, health-aware, connection tracking), **forge_secret_manager** (versioned secrets/access-logging/rotation-tracking), **forge_task_runner** (DAG task runner: dependency resolution/execution order/status), **forge_policy_engine** (rule engine: operators eq/gt/lt/neq, evaluation against facts, decision log). All KATs passing. | enterprise / devops | forge/{load_balancer,secret_manager,task_runner,policy_engine}.nova + 4 KATs | lib | `_kat_{load_balancer,secret_manager,task_runner,policy_engine}` all PASS | UNTESTED `5637b563` |
| 28 | **LIB** batch 12 — 4 framework modules: **forge_feature_toggle** (feature flags: per-user overrides/rollout%/enable-disable), **forge_rate_quota** (rate limiting: per-key quotas/window-reset/violations), **forge_event_replay** (event sourcing: append/replay/snapshot/projection), **forge_distributed_lock** (distributed locking: acquire/release/renew/TTL/expiry). All KATs passing. | enterprise / distributed | forge/{feature_toggle,rate_quota,event_replay,distributed_lock}.nova + 4 KATs | lib | `_kat_{feature_toggle,rate_quota,event_replay,distributed_lock}` all PASS | UNTESTED `592d7cea` |
| 29 | **LIB** batch 13 — 4 framework modules: **forge_circuit_pool** (connection pooling: fixed-size/acquire-release/stats), **forge_data_mask** (PII masking: email/phone/card/SSN/dict-field redaction), **forge_retry_policy** (retry strategies: fixed/linear/exponential backoff/exhaustion), **forge_api_key** (API key management: issue/validate/revoke/scope-check/usage). All KATs passing. | enterprise / security | forge/{circuit_pool,data_mask,retry_policy,api_key}.nova + 4 KATs | lib | `_kat_{circuit_pool,data_mask,retry_policy,api_key}` all PASS | UNTESTED `78b46601` |
| 30 | **LIB** batch 14 — 4 framework modules: **forge_idgen** (ID generation: sequential/padded/composite/timestamp), **forge_session_mgr** (HTTP sessions: create/get/set/expire/destroy/TTL), **forge_live_reload** (live reload: watch/debounce/trigger/pending), **forge_backup** (backup management: policies/create/verify/expire/size). All KATs passing. | devops / tooling | forge/{idgen,session_mgr,live_reload,backup}.nova + 4 KATs | lib | `_kat_{idgen,session_mgr,live_reload,backup}` all PASS | UNTESTED `fb524caa` |
| 31 | **LIB** batch 15 — 4 framework modules: **forge_dns_resolver** (DNS cache: records/TTL/health/round-robin/stats), **forge_ip_filter** (IP whitelist/blacklist: prefix matching/allow-list mode/logging), **forge_request_log** (HTTP request logging: method/path/status/duration/errors/avg/slowest), **forge_error_handler** (error reporting: deduplicate/categorize/frequency/top-errors). All KATs passing. | enterprise / observability | forge/{dns_resolver,ip_filter,request_log,error_handler}.nova + 4 KATs | lib | `_kat_{dns_resolver,ip_filter,request_log,error_handler}` all PASS | UNTESTED `54149e63` |
| 32 | **LIB** batch 16 — 4 framework modules: **forge_file_upload** (upload management: validate size/type/store/metadata), **forge_job_scheduler** (cron-like: add/disable/enable/is_due/mark_run/history), **forge_access_log** (HTTP access log: CLF-style/unique IPs/top paths/bandwidth/error rate), **forge_config_validator** (config schema: required/optional/type-check/apply-defaults). All KATs passing. | enterprise / devops | forge/{file_upload,job_scheduler,access_log,config_validator}.nova + 4 KATs | lib | `_kat_{file_upload,job_scheduler,access_log,config_validator}` all PASS | UNTESTED `eb9cf56d` |
| 33 | **LIB** batch 17 — 4 framework modules: **forge_dependency_graph** (dep graph: nodes/edges/topo-sort/roots/leaves/impact), **forge_canary_release** (canary deploy: weight/metrics/auto-fail/promote/rollback), **forge_compliance** (compliance tracking: controls/evidence/findings/severity/pct), **forge_resource_pool** (generic pool: fixed-capacity/borrow/return/stats). All KATs passing. | enterprise / devops | forge/{dependency_graph,canary_release,compliance,resource_pool}.nova + 4 KATs | lib | `_kat_{dependency_graph,canary_release,compliance,resource_pool}` all PASS | UNTESTED `2556c93e` |
| 34 | **LIB** batch 18 — 4 framework modules: **forge_changelog_gen** (changelog: categorize/render/suggest-bump/breaking), **forge_graceful_shutdown** (shutdown orchestration: hooks/priority/complete/fail/status), **forge_request_validator** (request validation: required/type/min-max-length), **forge_token_bucket** (token bucket rate limiter: consume/refill/stats). All KATs passing. | enterprise / tooling | forge/{changelog_gen,graceful_shutdown,request_validator,token_bucket}.nova + 4 KATs | lib | `_kat_{changelog_gen,graceful_shutdown,request_validator,token_bucket}` all PASS | UNTESTED `72f06f3e` |
| 35 | **LIB** batch 19 — 4 framework modules: **forge_health_aggregator** (multi-service health: critical/non-critical/degraded/healthy), **forge_link_shortener** (URL shortener: create/resolve/click-tracking/deactivate), **forge_geo_fence** (geo-fencing: rectangular zones/containment/zones-at), **forge_usage_meter** (usage metering: meters/events/per-tenant cost/summary). All KATs passing. | enterprise / SaaS | forge/{health_aggregator,link_shortener,geo_fence,usage_meter}.nova + 4 KATs | lib | all PASS | UNTESTED `efdf4648` |
| 36 | **LIB** batch 20 — 4 framework modules: **forge_dark_launch** (dark launch: shadow traffic %/mismatch rate/disable), **forge_chaos** (chaos engineering: fault injection/probability/pause-resume/log), **forge_sidecar** (sidecar proxy: handlers/process/enable-disable/invocations), **forge_span_context** (distributed tracing spans: create/nest/events/attributes/duration). All KATs passing. | resilience / observability | forge/{dark_launch,chaos,sidecar,span_context}.nova + 4 KATs | lib | all PASS | UNTESTED `8203a216` |
| 37 | **LIB** batch 21 — 4 framework modules: **forge_command_queue** (CQRS command queue: priority/FIFO/retry/pending), **forge_state_machine** (FSM: states/transitions/events/history/reset), **forge_blue_green** (blue-green deploy: swap/rollback/health/version), **forge_traffic_mirror** (traffic mirroring: rules/prefix-match/mismatch/disable). All KATs passing. | enterprise / devops | forge/{command_queue,state_machine,blue_green,traffic_mirror}.nova + 4 KATs | lib | all PASS | UNTESTED `af1424ee` |
| 38 | **LIB** batch 22 — 4 framework modules: **forge_rolling_deploy** (rolling deploy: batch-advance/pause-resume/rollback/progress%), **forge_service_mesh_proxy** (service mesh: route/health/load-balance/errors), **forge_hot_config** (hot config: set/reload/version/watchers), **forge_circuit_monitor** (circuit breaker dashboard: register/record/trip/half-open/reset). All KATs passing. | enterprise / infrastructure | forge/{rolling_deploy,service_mesh_proxy,hot_config,circuit_monitor}.nova + 4 KATs | lib | all PASS | UNTESTED `c6ff8da3` |
| 39 | **LIB** batch 23 — 4 framework modules: **forge_lease_manager** (distributed leases: acquire/release/renew/TTL/expiry), **forge_request_coalesce** (request dedup: coalesce/complete/waiters), **forge_api_mock** (API mock: define endpoints/handle/verify/reset), **forge_connection_drain** (connection draining: drain/force-close/timeout). All KATs passing. | enterprise / testing | forge/{lease_manager,request_coalesce,api_mock,connection_drain}.nova + 4 KATs | lib | all PASS | UNTESTED `aa10221c` |
| 40 | **LIB** batch 24 — 4 framework modules: **forge_priority_queue** (min-heap via sorted insert: enqueue/dequeue/peek), **forge_work_stealing** (work-stealing scheduler: submit/execute/steal from busiest), **forge_batch_processor** (batch processing: accumulate/flush/error-rate), **forge_pipeline_builder** (pipeline: stages/filter-drop/throughput%). All KATs passing. | concurrency / data | forge/{priority_queue,work_stealing,batch_processor,pipeline_builder}.nova + 4 KATs | lib | all PASS | UNTESTED `0e446963` |
| 41 | **LIB** batch 25 — 4 framework modules: **forge_deadletter** (dead letter queue: add/retry/discard/exhaustion), **forge_retry_queue** (retry with backoff: attempt/ready/mark-success/exhaustion), **forge_inbox_outbox** (transactional outbox: relay/fail/retry-failed), **forge_saga_orchestrator** (saga: steps/compensations/execute/fail/compensate). All KATs passing. | messaging / distributed | forge/{deadletter,retry_queue,inbox_outbox,saga_orchestrator}.nova + 4 KATs | lib | all PASS | UNTESTED `36ba842e` |
| 42 | **LIB** batch 26 — 4 framework modules: **forge_object_store** (S3-like: buckets/put/get/delete/list), **forge_key_rotation** (key rotation: add/rotate/expire/active), **forge_audit_trail** (immutable audit log: actor/action/resource/by-actor/since), **forge_data_catalog** (metadata registry: datasets/tags/columns/lineage/search). All KATs passing. | storage / security | forge/{object_store,key_rotation,audit_trail,data_catalog}.nova + 4 KATs | lib | all PASS | UNTESTED `98506e8c` |
| 43 | **LIB** batch 27 — 4 framework modules: **forge_schema_registry** (schema versions: register/get-latest/deprecate), **forge_event_bus_typed** (typed events: subscribe/publish/received-count/since), **forge_feature_gate** (feature gates: boolean/user/environment targeting + pass-rate), **forge_adaptive_throttle** (adaptive rate limiting: adjust by error-rate/auto-scale). All KATs passing. | events / platform | forge/{schema_registry,event_bus_typed,feature_gate,adaptive_throttle}.nova + 4 KATs | lib | all PASS | UNTESTED `b50c3c1e` |
| 44 | **LIB** batch 28 — 4 framework modules: **forge_distributed_cache** (sharded cache: set/get/evict/TTL/hit-rate), **forge_write_ahead_log** (WAL: append/checkpoint/replay/truncate), **forge_consensus_raft** (Raft: election/vote/append/commit/step-down), **forge_pub_sub_fanout** (pub/sub fan-out: topics/subscribe/publish/per-sub delivery). All KATs passing. | distributed systems | forge/{distributed_cache,write_ahead_log,consensus_raft,pub_sub_fanout}.nova + 4 KATs | lib | all PASS | UNTESTED `094be5ec` |
| 45 | **LIB** batch 29 — 4 framework modules: **forge_vector_clock** (vector clocks: increment/merge/happens-before), **forge_crdt_counter** (G-Counter CRDT: increment/merge/value), **forge_gossip_protocol** (gossip: state dissemination/rounds/alive-tracking), **forge_membership** (cluster membership: join/leave/suspect/confirm). All KATs passing. | distributed systems | forge/{vector_clock,crdt_counter,gossip_protocol,membership}.nova + 4 KATs | lib | all PASS | UNTESTED `17dcceb4` |
| 46 | **LIB** batch 30 — 4 framework modules: **forge_quorum** (quorum voting: propose/vote/majority/accept-reject), **forge_anti_entropy** (anti-entropy: compare replicas/detect-diffs/repair), **forge_consistent_hash** (consistent hashing: ring/replicas/key-to-node), **forge_ring_buffer** (circular buffer: fixed-capacity/overflow-oldest/peek). All KATs passing. | distributed systems | forge/{quorum,anti_entropy,consistent_hash,ring_buffer}.nova + 4 KATs | lib | all PASS | UNTESTED `6e441be9` |
| 47 | **LIB** batch 31 — 4 framework modules: **forge_read_repair** (read repair: compare replicas/repair stale/track repairs), **forge_shard_manager** (sharding: assign/lookup/key-to-shard/move tracking), **forge_fence_token** (fencing tokens: acquire/validate/revoke/monotonic), **forge_paxos_basic** (single-decree Paxos: prepare/promise/accept/decide). All KATs passing. | distributed systems | forge/{read_repair,shard_manager,fence_token,paxos_basic}.nova + 4 KATs | lib | all PASS | UNTESTED `d32ea0f8` |
| 48 | **LIB** batch 32 — 4 framework modules: **forge_idempotency_key** (idempotency: check/store/evict-oldest/hit-rate), **forge_task_scheduler** (priority task scheduler: add/next/complete/fail), **forge_cron_scheduler** (cron jobs: add/tick/enable-disable/interval tracking), **forge_workflow_engine** (multi-step workflow: add-step/start/complete/fail/progress). All KATs passing. | scheduling / workflow | forge/{idempotency_key,task_scheduler,cron_scheduler,workflow_engine}.nova + 4 KATs | lib | all PASS | UNTESTED `1438a54f` |
| 49 | **LIB** batch 33 — 4 framework modules: **forge_semaphore** (counting semaphore: try-acquire/release/waiters), **forge_barrier** (sync barrier: arrive/release/generation/break-reset), **forge_countdown_latch** (one-shot latch: countdown/release/progress), **forge_sliding_window_log** (sliding-window rate limiter: timestamp log/allow-deny). All KATs passing. | concurrency / rate-limiting | forge/{semaphore,barrier,countdown_latch,sliding_window_log}.nova + 4 KATs | lib | all PASS | UNTESTED `5c497e30` |
| 50 | **LIB** batch 34 — 4 framework modules: **forge_backpressure** (backpressure: high/low watermark/pause-resume), **forge_load_shedder** (load shedding: priority-based admit/shed/utilization), **forge_admission_control** (adaptive admission: error-rate/throttle/auto-adjust), **forge_dependency_injector** (DI container: register/resolve/singleton/remove). All KATs passing. | resilience / DI | forge/{backpressure,load_shedder,admission_control,dependency_injector}.nova + 4 KATs | lib | all PASS | UNTESTED `f3b32a68` |
| 51 | **LIB** batch 35 — 4 framework modules: **forge_metrics_histogram** (fixed-bucket histogram: observe/percentile/min-max-mean), **forge_event_sourcing** (event sourcing: append/version/events-since), **forge_snapshot_store** (snapshot store: save/load/latest), **forge_projection_builder** (projection builder: set/delete/apply events). All KATs passing. | CQRS / observability | forge/{metrics_histogram,event_sourcing,snapshot_store,projection_builder}.nova + 4 KATs | lib | all PASS | UNTESTED `b43184b3` |
| 52 | **LIB** batch 36 — 4 framework modules: **forge_outbox_relay** (reliable outbox: add/relay/fail/retry-failed), **forge_change_data_capture** (CDC: insert/update/delete/seq tracking), **forge_debounce** (debounce/throttle: cooldown/fire-suppress/rate), **forge_state_snapshot** (periodic snapshots: set/take/label/compare). All KATs passing. | data pipelines | forge/{outbox_relay,change_data_capture,debounce,state_snapshot}.nova + 4 KATs | lib | all PASS | UNTESTED `f8bf4327` |
| 53 | **LIB** batch 37 — 4 framework modules: **forge_content_negotiation** (HTTP Accept header matching), **forge_request_context** (per-request context bag), **forge_etag_cache** (ETag-based conditional caching), **forge_request_dedup** (concurrent request dedup). All KATs passing. | HTTP / web | forge/{content_negotiation,request_context,etag_cache,request_dedup}.nova + 4 KATs | lib | all PASS | UNTESTED `1c2b6aa6` |
| 54 | **LIB** batch 38 — 4 framework modules: **forge_structured_log** (structured logging: levels/fields/filtering), **forge_log_sampler** (log sampling: rate-based/drop-rate), **forge_metric_counter** (named counters: inc/add/reset/total), **forge_metric_gauge** (named gauges: set/inc/dec/add). All KATs passing. | observability | forge/{structured_log,log_sampler,metric_counter,metric_gauge}.nova + 4 KATs | lib | all PASS | UNTESTED `0a165dde` |
| 55 | **LIB** batch 39 — 4 framework modules: **forge_config_merger** (layered config: defaults<env<overrides), **forge_env_resolver** (profile-based env resolution), **forge_secret_vault** (secret store: lock/unlock/access-logging), **forge_feature_experiment** (A/B experiments: variant assignment/conversion tracking). All KATs passing. | config / security | forge/{config_merger,env_resolver,secret_vault,feature_experiment}.nova + 4 KATs | lib | all PASS | UNTESTED `7fe40995` |
| 56 | **LIB** batch 40 — 4 framework modules: **forge_rollout_strategy** (linear/exponential rollout with pause/rollback), **forge_service_discovery** (service registry: register/discover/health-aware), **forge_health_endpoint** (health checks: components/degraded detection), **forge_liveness_probe** (liveness/readiness: consecutive-fail threshold). All KATs passing. | platform / k8s | forge/{rollout_strategy,service_discovery,health_endpoint,liveness_probe}.nova + 4 KATs | lib | all PASS | UNTESTED `95b8e6e4` |
| 57 | **LIB** batch 41 — 4 framework modules: **forge_response_transformer** (response transform chain: headers/replace), **forge_request_pipeline** (middleware pipeline: use/enable-disable/reject), **forge_api_gateway_router** (API gateway: prefix routing/longest-match/disable), **forge_permission_checker** (RBAC: grant/revoke/check/rate). All KATs passing. | API / auth | forge/{response_transformer,request_pipeline,api_gateway_router,permission_checker}.nova + 4 KATs | lib | all PASS | UNTESTED `06748353` |
| 58 | **LIB** batch 42 — 4 framework modules: **forge_tenant_isolator** (multi-tenant: quotas/suspend/activate/usage-reset), **forge_multi_region** (multi-region: primary/failover/health), **forge_resource_quota** (resource quotas: consume/release/utilization), **forge_capacity_planner** (capacity: reserve/release/peak-utilization). All KATs passing. | platform / cloud | forge/{tenant_isolator,multi_region,resource_quota,capacity_planner}.nova + 4 KATs | lib | all PASS | UNTESTED `cb15cecd` |
| 59 | **LIB** batch 43 — 4 framework modules: **forge_cost_tracker** (track costs per service: add/top/pct), **forge_billing_meter** (metered billing: usage*unit_price/reset), **forge_usage_report** (usage reports: entries/totals/most-expensive), **forge_uptime_tracker** (uptime: incidents/outages/uptime-pct). All KATs passing. | billing / ops | forge/{cost_tracker,billing_meter,usage_report,uptime_tracker}.nova + 4 KATs | lib | all PASS | UNTESTED `600151ac` |
| 60 | **LIB** batch 44 — 4 framework modules: **forge_stream_processor** (windowed stream: push/avg/filter/drop-rate), **forge_event_router** (topic-based routing: add/remove/route/stats), **forge_request_throttle** (per-client throttling: window/deny-rate), **forge_connection_manager** (connection pool: open/close/utilization). All KATs passing. | streaming / network | forge/{stream_processor,event_router,request_throttle,connection_manager}.nova + 4 KATs | lib | all PASS | UNTESTED `96a2e8a3` |
| 61 | **LIB** batch 45 — 4 framework modules: **forge_dead_letter_queue** (DLQ: fail/retry/replay/dead-count), **forge_message_priority** (priority msgs: enqueue/dequeue-highest/pending), **forge_delayed_queue** (delayed delivery: enqueue/poll-by-time/next), **forge_object_pool** (reusable pool: add/acquire/release/utilization). All KATs passing. | messaging / pool | forge/{dead_letter_queue,message_priority,delayed_queue,object_pool}.nova + 4 KATs | lib | all PASS | UNTESTED `ca89b7f8` |
| 62 | **LIB** batch 46 — 4 framework modules: **forge_compensator** (saga compensation: add-step/complete/fail/undo), **forge_token_store** (token store: store/validate/expire/revoke), **forge_api_key_manager** (API key mgmt: create/validate-scope/revoke), **forge_hot_reload** (hot reload: register/reload/needs-reload). All KATs passing. | sagas / auth / reload | forge/{compensator,token_store,api_key_manager,hot_reload}.nova + 4 KATs | lib | all PASS | UNTESTED `60e18b8d` |
| 63 | **LIB** batch 47 — 4 framework modules: **forge_schema_validator** (field rules: require/optional/range/validate), **forge_input_sanitizer** (strip tags/truncate/stats), **forge_output_encoder** (HTML/URL/JSON/CSV encoding), **forge_error_registry** (typed errors: register/raise/severity/most-frequent). All KATs passing. | validation / encoding | forge/{schema_validator,input_sanitizer,output_encoder,error_registry}.nova + 4 KATs | lib | all PASS | UNTESTED `5b4a28e0` |
| 64 | **LIB** batch 48 — 4 framework modules: **forge_key_value_store** (KV with TTL: set/get/delete/hit-rate), **forge_sparse_set** (fast int membership: add/remove/contains/clear), **forge_interval_tree** (interval store: insert/overlaps/contains-point), **forge_bloom_counter** (counting Bloom: add/might-contain/min-count). All KATs passing. | data structures | forge/{key_value_store,sparse_set,interval_tree,bloom_counter}.nova + 4 KATs | lib | all PASS | UNTESTED `9c900ad2` |
| 65 | **LIB** batch 49 — 4 framework modules: **forge_graph_builder** (directed graph: nodes/edges/degree), **forge_dag_scheduler** (DAG task scheduling: deps/ready/complete), **forge_dependency_resolver** (package deps: satisfied/missing/reverse), **forge_topology_sort** (topological sort: Kahn's algo/cycle detection). All KATs passing. | graph / scheduling | forge/{graph_builder,dag_scheduler,dependency_resolver,topology_sort}.nova + 4 KATs | lib | all PASS | UNTESTED `e773c269` |
| 66 | **LIB** batch 50 — 4 framework modules: **forge_pub_sub_broker** (in-proc pub/sub: subscribe/publish/fan-out), **forge_task_pool** (task pool: submit/start/complete/fail/rates), **forge_worker_pool** (worker pool: assign-least-loaded/release/stop), **forge_job_queue** (persistent job queue: enqueue/dequeue/retry/permanent-fail). All KATs passing. | concurrency / messaging | forge/{pub_sub_broker,task_pool,worker_pool,job_queue}.nova + 4 KATs | lib | all PASS | UNTESTED `01db7120` |
| 67 | **LIB** batch 51 — 4 framework modules: **forge_skip_list** (sorted list: insert/search/remove/min/max), **forge_spatial_hash** (2D spatial hash: insert/query-cell/nearby), **forge_bit_set** (bit set: set/test/clear/count), **forge_segment_counter** (windowed counters: increment/count-at/range). All KATs passing. | data structures | forge/{skip_list,spatial_hash,bit_set,segment_counter}.nova + 4 KATs | lib | all PASS | UNTESTED `42293a00` |
| 68 | **LIB** batch 52 — 4 framework modules: **forge_protocol_buffer** (protobuf-like: set-int/set-string/has-field/type), **forge_csv_builder** (CSV: headers/add-row/render), **forge_xml_builder** (XML: add-elem/attrs/render), **forge_json_patch** (JSON Patch: add/remove/replace/apply). All KATs passing. | serialization | forge/{protocol_buffer,csv_builder,xml_builder,json_patch}.nova + 4 KATs | lib | all PASS | UNTESTED `0bbe128c` |
| 69 | **LIB** batch 53 — 4 framework modules: **forge_map_reduce** (MapReduce: add-input/map/reduce-sum), **forge_scatter_gather** (scatter/gather: ok/err/progress), **forge_fan_out_fan_in** (fan-out: submit/complete/worker-load), **forge_chain_of_resp** (CoR pattern: add-handler/handle/disable). All KATs passing. | patterns / parallel | forge/{map_reduce,scatter_gather,fan_out_fan_in,chain_of_resp}.nova + 4 KATs | lib | all PASS | UNTESTED `19429658` |
| 70 | **LIB** batch 54 — 4 framework modules: **forge_network_partition** (partition sim: add-node/partition/heal/can-reach), **forge_network_simulator** (net sim: latency/loss/send), **forge_traffic_shaper** (token bucket: consume/refill/shape-rate), **forge_quota_enforcer** (per-user quotas: use/remaining/violations). All KATs passing. | network / resilience | forge/{network_partition,network_simulator,traffic_shaper,quota_enforcer}.nova + 4 KATs | lib | all PASS | UNTESTED `1e88582c` |
| 71 | **LIB** batch 55 — 4 framework modules: **forge_chaos_monkey** (chaos injection: add-target/inject/pause/resume), **forge_canary_deploy** (canary routing: start/route/error-rate/promote/rollback), **forge_ab_router** (A/B testing: route/convert/winner), **forge_shadow_deploy** (shadow traffic: request/match-rate/is-safe-to-promote). All KATs passing. | deployment / testing | forge/{chaos_monkey,canary_deploy,ab_router,shadow_deploy}.nova + 4 KATs | lib | all PASS | UNTESTED `1636838a` |
| 72 | **LIB** batch 56 — 4 framework modules: **forge_data_masker** (PII masking: redact/hash/partial), **forge_audit_logger** (tamper-evident log: hash-chain/verify), **forge_compliance_checker** (rule-based compliance: required/not_empty), **forge_privacy_vault** (PII store: store/retrieve/delete-subject/access-log). All KATs passing. | security / compliance | forge/{data_masker,audit_logger,compliance_checker,privacy_vault}.nova + 4 KATs | lib | all PASS | UNTESTED `b0418439` |
| 73 | **LIB** batch 57 — 4 framework modules: **forge_idempotency_store** (dedup requests: check/store/TTL/hit-rate), **forge_service_mesh** (service registry: register/health/resolve/route), **forge_retry_budget** (budget-constrained retries: can-retry/record/rate), **forge_sla_tracker** (SLA compliance: set-target/record/violations/worst-service). All KATs passing. | infrastructure | forge/{idempotency_store,service_mesh,retry_budget,sla_tracker}.nova + 4 KATs | lib | all PASS | UNTESTED `9053d2af` |
| 74 | **LIB** batch 58 — 4 framework modules: **forge_secret_rotation** (secret versioning: set/rotate/previous), **forge_consensus_tracker** (Raft-like consensus: propose/vote/quorum), **forge_pipeline_executor** (multi-stage pipeline: add-stage/start/complete/fail/progress), **forge_checkpoint_store** (save/restore processing state). All KATs passing. | distributed / ops | forge/{secret_rotation,consensus_tracker,pipeline_executor,checkpoint_store}.nova + 4 KATs | lib | all PASS | UNTESTED `af9d0aa0` |
| 75 | **LIB** batch 59 — 4 framework modules: **forge_election_manager** (bully leader election: add-node/elect/up/down), **forge_membership_list** (SWIM-like membership: join/suspect/confirm/leave), **forge_quorum_store** (quorum read/write: W+R>N consistency), **forge_partition_manager** (consistent hashing: assign-owner/partition-for/owner-for). All KATs passing. | distributed systems | forge/{election_manager,membership_list,quorum_store,partition_manager}.nova + 4 KATs | lib | all PASS | UNTESTED `31179ee2` |
| 76 | **LIB** batch 60 — 4 framework modules (now using **Result APIs**): **forge_outbox_pattern** (transactional outbox: add/mark_published→Result/mark_failed→Result), **forge_cqrs_handler** (CQRS: execute_command→Result/execute_query→Result), **forge_rate_window** (sliding window: rate_per_second→Result/oldest→Result), **forge_change_feed** (CDC: subscribe→Result/poll→Result/lag→Result). All KATs passing. | patterns / messaging | forge/{outbox_pattern,cqrs_handler,rate_window,change_feed}.nova + 4 KATs | lib | all PASS | UNTESTED `b3bf0f45` |
| 77 | **LIB** batch 61 — 4 framework modules: **forge_rule_engine** (business rules: eval→Result/evaluate_all), **forge_feature_store** (ML features: get→Result/version→Result), **forge_approval_workflow** (multi-step: approve→Result/reject→Result/self-approval blocked), **forge_notification_hub** (multi-channel: send→Result/broadcast). All KATs passing. | rules / workflow | forge/{rule_engine,feature_store,approval_workflow,notification_hub}.nova + 4 KATs | lib | all PASS | UNTESTED `c6a88923` |
| 78 | **LIB** batch 62 — 4 framework modules: **forge_etl_runner** (ETL pipeline: run_step→Result/fail_step), **forge_data_quality** (DQ checks: run_check→Result/pass_rate), **forge_data_lake** (layer mgmt: ingest→Result/read→Result), **forge_catalog** (data catalog: add_column→Result/lineage→Result). All KATs passing. | data engineering | forge/{etl_runner,data_quality,data_lake,catalog}.nova + 4 KATs | lib | all PASS | UNTESTED `0f7fd814` |
| 79 | **LIB** batch 63 — 4 framework modules: **forge_access_control** (RBAC: grant→Result/check→Result/deny_rate), **forge_tenant_manager** (multi-tenant: create→Result/use→Result/quota enforcement), **forge_invite_manager** (invitations: redeem→Result/revoke→Result/max-uses/expiry), **forge_permission_cache** (TTL cache: check→Result/invalidate/hit_rate). All KATs passing. | auth / tenancy | forge/{access_control,tenant_manager,invite_manager,permission_cache}.nova + 4 KATs | lib | all PASS | UNTESTED `9bd02864` |
| 80 | **LIB** batch 64 — 4 framework modules: **forge_search_index** (inverted index: search→Result/has_doc), **forge_query_builder** (SQL builder: select/where/order/build→Result), **forge_cursor_paginator** (cursor pagination: page/item→Result/total_pages), **forge_result_ranker** (ranking: score→Result/boost→Result/top→Result). All KATs passing. | search / query | forge/{search_index,query_builder,cursor_paginator,result_ranker}.nova + 4 KATs | lib | all PASS | UNTESTED `0511a559` |
| 81 | **LIB** batch 65 — 4 framework modules: **forge_distance_calc** (Manhattan/Chebyshev/squared/nearest→Result), **forge_geocoder** (lookup geocoder: add/lookup→Result/lat/lon→Result), **forge_route_planner** (route planning: waypoints/distance→Result/total_distance→Result), **forge_time_series** (time series: add/get→Result/range/latest/min/max→Result). All KATs passing. | geo / analytics | forge/{distance_calc,geocoder,route_planner,time_series}.nova + 4 KATs | lib | all PASS | UNTESTED `187c14bf` |
| 82 | **LIB** batch 66 — 4 framework modules: **forge_versioned_store** (KV+version history: put/get→Result/get_version→Result), **forge_diff_engine** (dict diffs: compute/change_type→Result), **forge_media_manager** (media assets: upload/get→Result/delete→Result), **forge_tag_manager** (tagging: create→Result/assign→Result/count). All KATs passing. | data / content | forge/{versioned_store,diff_engine,media_manager,tag_manager}.nova + 4 KATs | lib | all PASS | UNTESTED `40099cd6` |
| 83 | **LIB** batch 67 — 4 framework modules: **forge_template_engine** (template rendering: register/render→Result/$var substitution), **forge_i18n_store** (i18n: set/get→Result/fallback/miss_count), **forge_slug_generator** (URL slugs: generate/is_valid/truncate), **forge_markdown_parser** (MD structure: heading/paragraph/code/block_type→Result). All KATs passing. | content / i18n | forge/{template_engine,i18n_store,slug_generator,markdown_parser}.nova + 4 KATs | lib | all PASS | UNTESTED `836471b2` |
| 84 | **LIB** batch 68 — 4 framework modules: **forge_webhook_validator** (webhook sig validation: register/validate→Result/compute_signature→Result), **forge_hmac_signer** (HMAC sign/verify→Result/with_timestamp), **forge_request_logger** (request logging: log/get→Result/error_rate/avg_duration), **forge_api_version** (API versioning: add/resolve→Result/set_default→Result/deprecate→Result). All KATs passing. | security / API | forge/{webhook_validator,hmac_signer,request_logger,api_version}.nova + 4 KATs | lib | all PASS | UNTESTED `9b062814` |
| 85 | **LIB** batch 69 — 4 framework modules: **forge_health_checker** (health checks: add/run→Result/pass_rate/last_failed→Result), **forge_deploy_tracker** (deployment tracking: deploy/rollback→Result/current→Result), **forge_canary_release** (REWRITE to Result API: start→Result/promote→Result/abort→Result/route), **forge_ab_test** (REWRITE to Result API: create→Result/assign→Result/convert→Result/winner→Result). All KATs passing. | ops / experimentation | forge/{health_checker,deploy_tracker,canary_release,ab_test}.nova + 4 KATs | lib | all PASS | UNTESTED `3a8c6d57` |
| 86 | **LIB** batch 70 — 4 framework modules REWRITTEN: **forge_load_balancer** (round-robin: add/next→Result/set_healthy→Result), **forge_token_bucket** (rate limiter: consume→Result/refill/allow_rate), **forge_saga_orchestrator** (saga: execute→Result/compensate/progress), **forge_bulkhead** (bulkhead: acquire→Result/release→Result/peak/rejection_rate). All KATs passing. | resilience | forge/{load_balancer,token_bucket,saga_orchestrator,bulkhead}.nova + 4 KATs | lib | all PASS | UNTESTED `3bec22ed` |
| 87 | **LIB** batch 71 — 4 framework modules REWRITTEN: **forge_dependency_graph** (DAG: add_node→Result/add_edge→Result/has_cycle_pair), **forge_task_scheduler** (priority: next→Result/complete→Result/fail→Result), **forge_state_machine** (FSM: send→Result/can_send/total_transitions), **forge_job_queue** (priority jobs: dequeue→Result/complete→Result/success_rate). All KATs passing. | compute / workflow | forge/{dependency_graph,task_scheduler,state_machine,job_queue}.nova + 4 KATs | lib | all PASS | UNTESTED `8bd6562b` |
| 88 | **LIB** batch 72 — 4 NEW probabilistic data structures: **forge_bloom_filter** (probabilistic set: add/might_contain using hash()), **forge_count_min_sketch** (frequency estimation: add/estimate), **forge_hyperloglog** (cardinality estimation: add/estimate/filled_registers), **forge_reservoir_sampler** (reservoir sampling: add/get→Result/is_full). All KATs passing. | data structures | forge/{bloom_filter,count_min_sketch,hyperloglog,reservoir_sampler}.nova + 4 KATs | lib | all PASS | UNTESTED `0d8164cd` |
| 89 | **LIB** batch 73 — 4 NEW modules: **forge_config_store** (hierarchical config: set/get→Result/defaults/has/delete→Result), **forge_env_config** (env-aware config: set/get→Result/get_or/is_production), **forge_kv_store** (KV with TTL: set/get→Result/delete→Result/hit_rate), **forge_pubsub** (pub/sub: create_topic→Result/subscribe→Result/publish→Result/delivered). All KATs passing. | config / messaging | forge/{config_store,env_config,kv_store,pubsub}.nova + 4 KATs | lib | all PASS | UNTESTED `7d714a36` |
| 90 | **LIB** batch 74 — 4 modules (3 REWRITTEN, 1 NEW): **forge_event_sourcing** (event sourcing: append/get→Result/snapshot/load→Result), **forge_distributed_lock** (lock mgr: acquire→Result/release→Result/owner→Result/contention), **forge_message_broker** (NEW: queue FIFO: create→Result/enqueue→Result/dequeue→Result), **forge_cron_scheduler** (cron: add→Result/should_run/mark_run→Result/enable/disable→Result). All KATs passing. | distributed / scheduling | forge/{event_sourcing,distributed_lock,message_broker,cron_scheduler}.nova + 4 KATs | lib | all PASS | UNTESTED `f9f957c7` |
| 91 | **LIB** batch 75 — 4 modules: **forge_graph_store** (NEW: nodes/edges/labels→Result), **forge_histogram** (REWRITE: record/mean/min/max→Result), **forge_semver** (REWRITE: bump/compare/is_compatible), **forge_unit_converter** (NEW: add/convert→Result). All KATs passing. | data / util | forge/{graph_store,histogram,semver,unit_converter}.nova + 4 KATs | lib | all PASS | UNTESTED `6069fe04` |
| 92 | **LIB** batch 76 — 4 modules REWRITTEN: **forge_object_pool** (add→Result/borrow→Result/return→Result/utilization), **forge_lfu_cache** (NEW: get→Result/put/hit_rate), **forge_write_ahead_log** (append/commit→Result/get→Result/checkpoint), **forge_priority_queue** (push/peek→Result/pop→Result). All KATs passing. | data structures | forge/{object_pool,lfu_cache,write_ahead_log,priority_queue}.nova + 4 KATs | lib | all PASS | UNTESTED `a5eb0743` |
| 93 | **LIB** batch 77 — 4 modules: **forge_dedup_store** (NEW: check_and_add→Result/dup_rate), **forge_accumulator** (NEW: add/mean→Result/min→Result/max→Result/range→Result), **forge_string_builder** (NEW: append/build/build_with_sep/clear), **forge_ring_buffer** (REWRITE: write/read→Result/latest→Result/oldest→Result). All KATs passing. | data structures | forge/{dedup_store,accumulator,string_builder,ring_buffer}.nova + 4 KATs | lib | all PASS | UNTESTED `552b6619` |
| 94 | **LIB** batch 78 — 5 modules: **forge_sliding_window** (NEW: time-based recording/query), **forge_consistent_hash** (REWRITE: add_node/get_node→Result/ring), **forge_quota_manager** (NEW: set/consume→Result/remaining→Result/reset→Result), **forge_connection_pool** (NEW: add→Result/borrow→Result/release→Result/utilization), **forge_url_shortener** (NEW: shorten/resolve→Result/get_code→Result). All KATs passing. | infra / network | forge/{sliding_window,consistent_hash,quota_manager,connection_pool,url_shortener}.nova + 5 KATs | lib | all PASS | UNTESTED `c5ba65b6` |
| 95 | **LIB** batch 79 — 4 NEW modules: **forge_metric_aggregator** (record/count→Result/sum→Result/mean→Result/min→Result/max→Result), **forge_secret_store** (put/get→Result/delete→Result/access_count), **forge_span_tracker** (start/end→Result/duration→Result/name→Result), **forge_error_tracker** (record/count_for→Result/error_rate→Result). All KATs passing. | observability | forge/{metric_aggregator,secret_store,span_tracker,error_tracker}.nova + 4 KATs | lib | all PASS | UNTESTED `3205731f` |
| 96 | **LIB** batch 80 — 4 NEW modules: **forge_dns_cache** (put/lookup→Result/evict→Result/hit_rate), **forge_permission_manager** (role-based: grant→Result/assign→Result/check), **forge_api_throttle** (per-client: request→Result/remaining/denial_rate), **forge_rollback_manager** (deploy/rollback→Result/version_at→Result). All KATs passing. | infra / auth | forge/{dns_cache,permission_manager,api_throttle,rollback_manager}.nova + 4 KATs | lib | all PASS | UNTESTED `b1bd1475` |
| 97 | **LIB** batch 81 — 4 NEW modules: **forge_migration_runner** (register/apply→Result/rollback→Result/pending_count), **forge_inbox** (add/read→Result/delete→Result/unread_count), **forge_file_watcher** (watch/update→Result/unwatch→Result/change_count), **forge_flag_store** (create/toggle→Result/set_rollout→Result/is_enabled). All KATs passing. | ops / messaging | forge/{migration_runner,inbox,file_watcher,flag_store}.nova + 4 KATs | lib | all PASS | UNTESTED `157b851f` |
| 98 | **LIB** batch 82 — 4 NEW modules: **forge_batch_queue** (batched processing: add/flush→Result/should_flush), **forge_metric_exporter** (Prometheus-style: counter/gauge/inc→Result/set_gauge→Result), **forge_health_report** (system health: report_healthy→Result/report_unhealthy→Result/is_all_healthy), **forge_anomaly_detector** (z-score: record/is_anomaly→Result/variance→Result). All KATs passing. | observability / analytics | forge/{batch_queue,metric_exporter,health_report,anomaly_detector}.nova + 4 KATs | lib | all PASS | UNTESTED `d9187adb` |
| 99 | **LIB** batch 83 — 4 NEW modules: **forge_circuit_state** (circuit breaker FSM: call→Result/record_success/record_failure/trips), **forge_watermark_tracker** (high/low watermarks: current→Result/high→Result/low→Result/range→Result), **forge_data_partitioner** (hash-based: assign/partition_size→Result/balance_ratio), **forge_index_builder** (inverted index: add_doc/search→Result/has_term). All KATs passing. | infrastructure | forge/{circuit_state,watermark_tracker,data_partitioner,index_builder}.nova + 4 KATs | lib | all PASS | UNTESTED `9b1f2f29` |
| 100 | **LIB** batch 84 — 4 NEW modules: **forge_lock_free_counter** (named counters: increment→Result/decrement→Result/reset→Result), **forge_weighted_sampler** (weighted random: sample→Result/weight_at→Result), **forge_expiring_map** (TTL map: put/get→Result/has/delete→Result), **forge_moving_average** (SMA: add/average→Result/is_full). All KATs passing. | data structures | forge/{lock_free_counter,weighted_sampler,expiring_map,moving_average}.nova + 4 KATs | lib | all PASS | UNTESTED `84fa445c` |
| 101 | **LIB** batch 85 — 4 NEW modules: **forge_trie_router** (prefix route matching: add/match→Result/miss_count), **forge_compact_map** (size-limited map: put/get→Result/eviction_count), **forge_event_journal** (append-only: append/get→Result/type_at→Result/count_by_type), **forge_audit_chain** (hash-chained: append/actor_at→Result/action_at→Result/verify). All KATs passing. | routing / audit | forge/{trie_router,compact_map,event_journal,audit_chain}.nova + 4 KATs | lib | all PASS | UNTESTED `b622a441` |
| 102 | **LIB** batch 86 — 4 NEW modules: **forge_retry_tracker** (per-op retries: attempt→Result/can_retry/reset→Result), **forge_debouncer** (event debounce: call→Result/acceptance_rate), **forge_deferred_executor** (scheduled tasks: schedule/execute_due/status→Result), **forge_signal_handler** (signal registry: register/fire→Result/unregister→Result). All KATs passing. | resilience / events | forge/{retry_tracker,debouncer,deferred_executor,signal_handler}.nova + 4 KATs | lib | all PASS | UNTESTED `6de32aa2` |
| 103 | **LIB** batch 87 — 4 NEW modules: **forge_middleware_chain** (ordered pipeline: add/disable→Result/enable→Result/run), **forge_api_rate_plan** (tiered plans: add_plan/assign→Result/check→Result), **forge_cgroup_limiter** (resource groups: use_cpu→Result/use_mem→Result/violations), **forge_hot_key_detector** (frequency detection: access/is_hot/hot_key_count). All KATs passing. | infrastructure / API | forge/{middleware_chain,api_rate_plan,cgroup_limiter,hot_key_detector}.nova + 4 KATs | lib | all PASS | UNTESTED `d37e3c83` |
| 104 | **LIB** batch 88 — 4 NEW modules: **forge_object_mapper** (field mapping: add_mapping/map/target_field→Result), **forge_version_vector** (causal ordering: increment/get→Result/merge/dominates), **forge_progress_tracker** (task progress: advance→Result/progress→Result/is_complete), **forge_write_buffer** (batched writes: write/flush→Result/should_flush). All KATs passing. | data structures | forge/{object_mapper,version_vector,progress_tracker,write_buffer}.nova + 4 KATs | lib | all PASS | UNTESTED `432cd3b6` |
| 105 | **LIB** batch 89 — 4 NEW modules: **forge_pub_sub** (topic-based: subscribe/unsubscribe→Result/publish/subscriber_count), **forge_shard_map** (hash-based: assign/get_shard→Result/shard_for), **forge_dag_executor** (DAG ordering: add_task/add_dep→Result/is_ready/execute→Result), **forge_saga_coordinator** (saga pattern: add_step/execute_step→Result/compensate→Result). All KATs passing. | distributed / patterns | forge/{pub_sub,shard_map,dag_executor,saga_coordinator}.nova + 4 KATs | lib | all PASS | UNTESTED `dba61d44` |
| 106 | **LIB** batch 90 — 4 NEW modules: **forge_snapshot_manager** (versioned snapshots: save/get→Result/restore→Result/latest→Result), **forge_canary_deployer** (canary deploys: deploy/promote→Result/rollback→Result/weight→Result), **forge_cqrs_store** (CQRS: command/query→Result/event_at→Result), **forge_blue_green_deploy** (blue/green: deploy→Result/switch/active_version→Result). All KATs passing. | deployment | forge/{snapshot_manager,canary_deployer,cqrs_store,blue_green_deploy}.nova + 4 KATs | lib | all PASS | UNTESTED `56f24fd1` |
| 107 | **LIB** batch 91 — 4 NEW modules: **forge_request_tracer** (HTTP tracing: trace/get_method→Result/avg_duration→Result), **forge_correlation_id** (request correlation: issue/context→Result/complete→Result/is_active), **forge_pipeline_stage** (ETL pipeline: add_stage/process→Result/fail→Result/stage_name→Result), **forge_metric_histogram** (histogram: record/bucket→Result/mean→Result/overflow). All KATs passing. | observability | forge/{request_tracer,correlation_id,pipeline_stage,metric_histogram}.nova + 4 KATs | lib | all PASS | UNTESTED `7d18b3d7` |
| 108 | **LIB** batch 92 — 4 NEW modules: **forge_consensus** (majority-vote: propose/vote→Result/result→Result), **forge_raft_log** (append-only: append/commit→Result/is_committed), **forge_gossip_state** (gossip: add_node/set_state→Result/mark_dead→Result/gossip), **forge_lease_renewal** (TTL leases: acquire/renew→Result/tick→Result/is_active). All KATs passing. | distributed systems | forge/{consensus,raft_log,gossip_state,lease_renewal}.nova + 4 KATs | lib | all PASS | UNTESTED `cbc9f538` |
