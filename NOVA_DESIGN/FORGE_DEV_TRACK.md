# FORGE DEV TRACK — rapid-dev phase (functional testing deferred to the end)

> **Baseline:** clean gate 621 PASS / 0 FAIL both RC modes (2026-07-02). From here we DEVELOP every remaining
> Forge feature to beat Spring Boot / Django / Phoenix, WITHOUT per-feature functional testing — to move
> fast. Each feature is written to a high NOVA standard, **double-checked** (correct? will it break anything
> downstream? blast-radius?), **syntax/compile-checked** (gen3 build, no run — NOT a functional test), and
> committed with a row below. At the end we run the **track-driven test pass**: go top-to-bottom, test each
> row, flip Status `untested` → `tested` (or `FIXED <commit>` if a bug is found + fixed).

## Rules
1. **Correctness twice.** Before every commit: is it correct per NOVA semantics, and does it break any
   existing feature? forge.nova / runtime / compiler edits (high blast-radius) = Opus, extra-careful; new
   LEAF modules = Sonnet 4.6 under an Opus spec + Opus review.
2. **Syntax check every no-test commit** (compile a minimal importer via gen3, no run). A parse error in a
   shared module cascades — never commit a non-compiling change.
3. **One row per feature** here, with the commit hash + exactly what the final test pass must verify.
4. NOVA gotchas to honor (hardened this session): `type Name`+indented fields (NOT `struct` unless brace
   form); NO multi-line list literals (build via push); indent-significant (mind copy/paste indent);
   closures capture BY VALUE (no shared-mutable via capture); dynamic-key dicts must be born in the loop;
   `\r\n` are literal escapes in source; unused `let` is a COMPILE ERROR; `1<<64` is broken; reserved word
   `unsafe`; extern string-RETURN unproven (return int, compare in C).

## Track table

| # | Feature (competitor beat) | Module | Commit | What the test pass must verify | Status |
|---|---|---|---|---|---|
| 0 | Baseline (rapid-dev phase begins) | — | 8a2eee4 | full gate was 621/0 before phase | ✅ gated |
| 1 | RFC 9457 problem+json (beat Spring @ControllerAdvice) | forge.nova | 72c8ff4 | problem(404,"x")→status 404 + Content-Type application/problem+json + body {type,title,status,detail}; problem_full adds instance when non-empty; _problem_title maps common codes | TESTED ✓ 07-02 |
| 2 | File/blob storage abstraction (beat Django storages / Spring Resource) | forge_storage.nova (leaf) | 72c8ff4 | storage_local(root); put/get/exists/delete round-trip on local disk; storage_url→/storage/key; _storage_safe_key strips traversal ("../../etc/x"→"etc/x", "a//b"→"a/b", ".."→"") | TESTED ✓ 07-02 |
| 3 | Cursor (keyset) pagination (beat OFFSET pagination) | forge.nova | grep:page_build | cursor_encode/decode round-trip (base64url); page_build(limit+1 rows, limit, last_key)→has_more=true+next_cursor+drops sentinel; ≤limit rows→has_more=false, cursor "" | TESTED ✓ 07-02 (forge_page_test) |
| 4 | In-proc cache (beat Spring @Cacheable / Django cache) | forge_cache.nova (leaf) | grep:cache_new | cache_new/put/get/delete/clear/stop + cache_get_or; single-owner actor (N>1-safe); [val] present / [] tombstone; get on missing/deleted→err("cache: miss") | TESTED ✓ 07-02 |
| 5 | Service discovery + client-side round-robin LB (beat Eureka+Ribbon) | forge_discovery.nova (leaf) | grep:discovery_new | register(svc,ep)/resolve(round-robin advances per-service)/list/stop; unknown service→""; 2 endpoints→idx 0,1,0,... | TESTED ✓ 07-02 |
| 6 | Saga orchestration (local txns + compensations; beat 2PC / hand-rolled rollback) | forge_saga.nova (leaf) | grep:saga_run | saga_new/step/run; steps run in order threading ctx; a failing step compensates completed steps in REVERSE; ok(final ctx) on full success, err on failure; fn-values stored in step lists + called | TESTED ✓ 07-02 |
| 7 | Message broker + DLQ (beat Kafka/Rabbit/NATS single-node infra) | forge_mq.nova (leaf) | grep:mq_new | mq_new/publish/consume(FIFO head-index)/depth/dead_letter/dlq/stop; publish 2→depth 2; consume→ok(o1) FIFO; drained→err("mq: empty"); dead_letter→dlq list | TESTED ✓ 07-02 (regression fixed: restored channel-based broker + added DLQ) |
| 8 | BFF/gateway fan-out (http_get_all) | forge_http_client.nova (leaf) | grep:http_get_all | http_get_all([urls]) returns a list of Result<HttpResponse>, one per URL in order (list comprehension); loopback: 2 URLs→2 results | VERIFIED-BY-COMPOSITION (http_get proven in forge_http_client_test; http_get_all = [http_get(u) for u in urls]; live loopback socket test skipped — flaky per memory) |
| 9 | Deadline/budget propagation (the resilience pattern teams botch) | forge.nova | grep:deadline_in | deadline_in(5000)→abs ms; deadline_remaining decreases, floors at 0 when past; deadline_exceeded flips true past the deadline | TESTED ✓ 07-02 |
| 10 | Bulkhead: outbound concurrency cap (beat Resilience4j Bulkhead) | forge_bulkhead.nova (leaf) | grep:bulkhead_new | bulkhead_new(2); try_acquire→1,1 then 0 at cap; release frees one; available count; actor N>1-safe | TESTED ✓ 07-02 |
| 11 | Runtime feature flags + A/B variants (beat hard-coded conditionals / LaunchDarkly-simple) | forge_flags.nova (leaf) | grep:flags_new | flag_set/enable/disable/value/on; enable→flag_on true; unset→"" + flag_on false; variant strings via flag_value | TESTED ✓ 07-02 |
| 12 | REST status helpers (201 Created+Location / 202 / 204) | forge.nova | grep:resp_created | resp_created(loc,body)→201 + Location header; resp_accepted→202; resp_no_content→204 empty | TESTED ✓ 07-02 |
| 13 | Protobuf wire codec (gRPC message substrate; no .proto) | forge_protobuf.nova (leaf) | grep:pb_encode_varint | varint 300→[172,2] + decode; zigzag(-1)→1,(-2)→3, decode(3)→-2; tag(1,0)→8,(2,2)→18; field varint/bytes encoders | TESTED ✓ 07-02 |
| 14 | Composable query DSL (HIGH-LEVEL NOVA: comprehensions+join; beat Ecto macros/JPA Criteria) | forge_query.nova (leaf) | grep:q_build | q_qualify("u",[id,name])→[u.id,u.name]; q_eq_conds→[f = ?]; q_where_all AND-joins; q_in(id,3)→id IN (?, ?, ?); q_build assembles SELECT...FROM...WHERE...ORDER BY...LIMIT; params bound (injection-safe) | TESTED ✓ 07-02 |
| 15 | CSV export RFC 4180 (HIGH-LEVEL NOVA: comprehensions+join) | forge.nova | grep:csv_of | csv_row([a,b])→"a,b"; field with comma/quote/CRLF gets quoted+quote-doubled; csv_of CRLF-joins rows; resp_csv→200 text/csv attachment | TESTED ✓ 07-02 |
| 16 | NDJSON streaming/bulk (HIGH-LEVEL NOVA: comprehension+join) | forge.nova | grep:ndjson_of | ndjson_of([{a:1},{a:2}])→two JSON lines LF-joined; resp_ndjson→200 application/x-ndjson | TESTED ✓ 07-02 |
| 17 | Proactive media-type negotiation RFC 7231 (HIGH-LEVEL: comprehension, permille q) | forge.nova | grep:negotiate_type | Accept:application/json;q=0.9,text/html → offered[json,html] picks html (q=1.0>0.9); q=0 excludes; most-specific range wins its q; no match→"" (caller 406); absent Accept→offered[0] | TESTED ✓ 07-02 |
| 18 | JSON-RPC 2.0 codec (request/notification/success/error/batch/parse+accessors) | forge_jsonrpc.nova | _fdb_one forge_jsonrpc_test (to write) | jsonrpc_request("sum",[1,2],1)→{jsonrpc:2.0,method,params,id}; error nests code+message; is_notification=no id; parse round-trips; batch=JSON array | TESTED ✓ 07-02 |
| 19 | Per-key token-bucket rate limiter (ACTOR, monotonic clock, N>1-safe) | forge_ratelimit.nova | _fdb_one forge_ratelimit_test (to write) | cap=5 rate=10/s: 5 allows then deny; +100ms→1 token→1 allow; reset refills; unseen key=full. LIMITATION: unbounded key cardinality (no LRU/TTL evict — bound before untrusted per-IP) | TESTED ✓ 07-02 |
| 20 | gRPC wire framing + path routing + status/trailers (leaf, protobuf-agnostic) | forge_grpc.nova | _fdb_one forge_grpc_test (to write) | grpc_frame([1,2,3])→[0,0,0,0,3,1,2,3]; deframe round-trips→[[1,2,3],8,0]; path_service("/echo.Echo/Say")→"echo.Echo", method→"Say"; trailers dict; 10 status codes | TESTED ✓ 07-02 |
| 21 | Transactional outbox (ACTOR, at-least-once, poll-then-ack) | forge_outbox.nova | _fdb_one forge_outbox_test (to write) | add 2→ids 1,2 pending=2; poll(10) both; ack(1)→pending=1; poll returns only id2; poll does NOT mark delivered (crash re-delivers) | TESTED ✓ 07-02 |
| 22 | W3C Trace Context propagation (traceparent parse/child/continue, CSPRNG ids) | forge.nova | grep:trace_continue | trace_start(true)→00-<32hex>-<16hex>-01; parse validates hex+non-zero+sampled bit; trace_child keeps trace_id+flags new span_id; malformed→fresh; trace_id_of for log correlation | TESTED ✓ 07-02 |
| 23 | gRPC unary dispatch (service map, fn-value handlers, envelopes) | forge_grpc.nova | _fdb_one forge_grpc_test (to write) | register "/echo.Echo/Say"→echo handler; dispatch(framed[1,2,3])→{framed:[0,0,0,0,3,1,2,3],status:0}; unknown path→status 12; response_ok/error envelopes. grpc_deframe now BOUNDS-CHECKED (bee5245-follow): short/lying frame → malformed sentinel [[],off,-1]; grpc_dispatch returns INVALID_ARGUMENT (no OOB read). SECURITY GAP CLOSED | TESTED ✓ 07-02 |
| 24 | Model-driven admin: cols auto-derived from struct + UPDATE/edit (Django-signature) | forge_admin.nova | _fdb_one forge_admin_test | admin_cols_of(User())→field_names; admin_form_cols filters id; admin_resource_model wires list(edit/delete/new)+create+edit+update+delete; UPDATE parameterized; esc handles ' and " (attr-injection-safe). Existing admin_render_table untouched (byte-stable for its test) | TESTED ✓ 07-02 |
| 25 | Circuit breaker (ACTOR, CLOSED/OPEN/HALF_OPEN, monotonic) | forge_circuit.nova | _fdb_one forge_circuit_test (to write) | threshold=3: 3 failures→open; allow→0 while open; after reset_ms allow→1 (half-open probe); probe success→closed, probe fail→open; circuit_state does NOT advance timer (only allow does) | TESTED ✓ 07-02 |
| 26 | Retry exponential backoff (PURE, cap, caller-jitter, schedule) | forge_retry.nova | _fdb_one forge_retry_test (to write) | base=100 max=2000: delays 100,200,400,800,1600,2000-cap; retry_should(2,3)=T (3,3)=F; apply_jitter(800,500)=400; schedule(4,100,2000)=[100,200,400,800]; total_ms=1500. sum not a builtin (fold) | TESTED ✓ 07-02 |
| 27 | GraphQL engine: tokenizer + recursive-descent parser + executor + DoS limits (S8 frontier) | forge_graphql.nova | _fdb_one forge_graphql_test (to write) | gql_execute("{todos{title done}}", resolvers, 0)→{data:{todos:[{title,done}...]}}; alias a:b; scalar args; nested selections; list-map; trivial resolver reads dict/struct; gql_execute_limited rejects over-depth/complexity→{errors}. **USAGE: queries come from req.body at runtime (NOVA string literals can't hold { } — interpolation); build literal test queries via chr(123)/chr(125). v1 = queries + VARIABLES ($x) [row 28]; no fragments/mutations yet** | TESTED ✓ 07-02 |
| 28 | GraphQL variables ($x) — production-usable | forge_graphql.nova | _fdb_one forge_graphql_test | tokenizer emits var token for $name; arg $id → {__var:id} marker; _gql_apply_vars substitutes from variables dict before resolve (missing→""); gql_execute_vars / gql_execute_limited_vars; execute/limited delegate with {} | TESTED ✓ 07-02 |
| 29 | GraphQL-over-HTTP endpoint (wire engine → Forge route) | forge_graphql_http.nova | _fdb_one forge_graphql_http_test (to write) | graphql_route(app,"/graphql",resolvers,0) registers POST; handler reads {query,variables} from body_json, runs gql_execute_limited_vars (depth 10/complexity 500 default), returns json_of(200,{data|errors}); use with serve_app_n (dict path). graphql_route_limited for custom caps | TESTED ✓ 07-02 |
| 30 | JSON-RPC 2.0-over-HTTP endpoint (codec → Forge route, batch+notifications) | forge_jsonrpc_http.nova | _fdb_one forge_jsonrpc_http_test (to write) | jsonrpc_route(app,"/rpc",methods); single call→200 success/error; unknown method→-32601; notification(no id)→204 silent; batch array→JSON array of non-notification responses (all-notif→204); rides plain HTTP (no HTTP/2). jsonrpc_methods/register helpers | TESTED ✓ 07-02 |
| 31 | HPACK Huffman (RFC 7541 §5.2 + Appendix B) — HTTP/2 D2.2 START-HERE | forge_hpack.nova | _fdb_one forge_hpack_huffman_test | encode("www.example.com")=RFC C.4.1 bytes byte-exact; C.4.2 "no-cache"; round-trip; hpack_decode_string now decodes H=1 literals | TESTED ✓ 07-02 |
| 32 | ORM struct-insert auto-increment PK (§12 hero DB capability) | forge_orm.nova | _fdb_one forge_orm_hero_test | orm_insert skips id when id==0 -> SQLite auto-assigns; Todo(0,..)x2 -> orm_get typed returns id 1/2 + fields; explicit non-zero id still inserted verbatim (forge_orm_test green) | TESTED ✓ 07-02 |
| 33 | LiveView on_info — server-push re-render (Phoenix handle_info parity) | forge_live.nova | _fdb_one forge_live_oninfo_test | live_view_info(view,update,info,state); live_send_ev drives update_fn, live_send_info drives info_fn; both re-render+diff+emit patch (tagged envelopes, no select); client event->slot0, server push->slot1 verified | TESTED ✓ 07-02 |
| 34 | gRPC-over-h2c serving — dispatch grpc onto HTTP/2 frames (S8 payoff, no ALPN) | forge_grpc_h2.nova | _fdb_one forge_grpc_h2_test | grpc_h2_handle(HEADERS(:path)+DATA(framed msg))->reply HEADERS(:status 200)+DATA(framed reply)+trailers(grpc-status); echo dispatch->status 0 + [0,0,0,0,3,1,2,3]; unknown->12. Core in-process tested AND serve_grpc_h2c SOCKET-VERIFIED (forge_grpc_h2c_serve_test loopback: NOVA-as-client). Serve-loop path-capture bug found+fixed by the test. Ext-grpc-client (grpcurl) interop = only remaining | TESTED ✓ (socket) 07-02 |
| 35 | Auto-admin search + sort (D7.2, Django list parity) | forge_admin.nova | grep:admin_search_sql | admin_search_sql builds LIKE-param WHERE over search_cols (%q%) + whitelisted ORDER BY (?sort= injection-safe); admin_resource_searchable wires ?q=/?sort=; admin_search_box | untested (syntax✓) |
| 36 | GraphQL DataLoader (D4.4, N+1 batching) | forge_graphql.nova | grep:gql_load_many | gql_loader(batch_fn); gql_load_many([1,2,3])→ONE batch call, per-key cache (str keys); gql_load(k); gql_loader_clear per-request | untested (syntax✓) |
| 37 | GraphQL schema-from-types SDL (D4.1) | forge_gql_schema.nova | grep:gql_type_sdl | gql_type_sdl(name, StructSample())→"type Name { field: Int/String/Boolean/Float }" via field_names+type_of; gql_schema(defs) joins types; braces via chr() | untested (syntax✓) |
| 38 | Typed MQ publish/consume (D5.1, JSON codec over broker) | forge_mq.nova | grep:mq_publish_typed | mq_publish_typed(b,name,struct/dict)→json_stringify onto queue; mq_consume_typed(consumer)→json_decode(mq_pull); struct IS the message (RTTI), broker codec swaps later | untested (syntax✓) |
| 39 | Struct-diff migrations (D9.2, derive-from-struct, add-only) | forge_migrate.nova | grep:migrate_plan | migrate_create_sql(struct)→CREATE with id PK + typed cols; migrate_add_columns_sql(existing,struct)→ALTER ADD for new fields; migrate_plan([]) creates / (existing) alters; scalar→SQL type map. Drop/rename need annotation (documented) | untested (syntax✓) |
| 40 | Admin FK picker + bulk actions (D7.3) | forge_admin.nova | grep:admin_fk_select | admin_fk_select(field,options,selected)→<select> of related rows; admin_bulk_form(table,action,rows)→checkbox-per-row POST /bulk/<action>; admin_action_ids(req)→checked ids (list or single). esc-safe | untested (syntax✓) |
| 41 | API versioning + content negotiation (D9.4) | forge.nova | grep:version_group | version_group(app,"v1")→/v1 route-group prefix (versions coexist); respond_negotiated(req,value,offered)→negotiate_type match or 406, JSON-first serialize | untested (syntax✓) |
| 42 | Admin object-perms + audit log (D7.4/D7.5) | forge_admin.nova | grep:admin_can | admin_can(perms_fn/0,actor,obj,action)→gate (0=allow); admin_audit_ddl/record(parameterized)/all — a LogEntry row per mutation (Django parity) | untested (syntax✓) |
| 43 | graphql-ws subscription codec (D4.3) | forge_graphql_ws.nova | grep:gqlws_next | gqlws_connection_ack/next(id,data→payload.data)/error(id,msg→payload[])/complete(id) + parse + accessors type/id/query/variables; all JSON via dicts (no literal braces). Pair with forge WS + gql executor | untested (syntax✓) |
| 44 | MQ retry-then-DLQ + prefetch helpers (D5.4-lite) | forge_mq.nova | grep:mq_requeue_or_dlq | mq_requeue_or_dlq(b,name,msg,attempt,max)→requeue(1)/dead-letter(0); mq_prefetch_ok(inflight,max). Full ack/nack wire = later broker increment | untested (syntax✓) |
| 45 | Cluster global registry (D6.3, distributed name→handle) | clusterx.nova | _fdb_one cluster_test-style multi-node (defer) | global_register(info,name,value) stores locally; global_whereis(info,name) resolves local-first then fan-out "whereis" to LIVE peers (bounded via tcp_wait_readable). Additive: new manager reg/lookup cmds + whereis peer msg; membership/gossip untouched | untested (syntax✓) |
| 46 | Cluster PubSub (D6.5, publish reaches subscribers on every node) | clusterx.nova | multi-node (defer) | cluster_subscribe(info,topic)→channel; cluster_broadcast(info,topic,msg) delivers to LOCAL subs + fans out remote "pub" to LIVE peers→their local subs. Additive: subs dict + sub/pub_local cmds + pub peer msg | untested (syntax✓) |
| 47 | Distributed monitor (D6.4, remote DOWN signal) | clusterx.nova | multi-node (defer) | cluster_monitor(info,node)→channel; a spawned poll-loop over cluster_status fires one {DOWN,node} when the node goes down, then exits (no leak). Supervisor receive()s + restarts elsewhere | untested (syntax✓) |
| 48 | S14 config + profiles + service registry + request ctx (DI ergonomic) | forge_config.nova | grep:config_for_profile | config_get/int/bool + config_from_env (env wins) + config_profile (NOVA_PROFILE) + config_merge/for_profile (overlay); registry provide/resolve/has_service (app singletons); ctx_new/set/get/has (request-scoped). Typed struct binding composes with from_json_safe<T> | untested (syntax✓) |
| 49 | S15 declarative cross-cutting decorators (AOP substitute) | forge_aspects.nova | grep:cached | cached(store,key,producer) @Cacheable; cache_forget @CacheEvict; retried(max,base,action) @Retryable w/ exp backoff; timed(action)→[result,ms] @Timed; every(interval,action) @Scheduled(fixedRate). HOF over a 0-arg closure, no proxies | untested (syntax✓) |
| 50 | S16 repository data depth: derived queries + auditing + soft-delete + optimistic lock | forge_repo.nova | grep:repo_where_eq | repo_where_eq/find_sql (Spring Data findByXAndY, param-bound); repo_audit_stamp (created/updated_at); repo_soft_delete_sql + repo_active_clause/find_active_sql; repo_versioned_update_sql (@Version, affected=0→conflict). Pure builders, injection-safe | untested (syntax✓) |
| 51 | S17 template engine (Django-templates/Thymeleaf parity) | forge_template.nova | grep:template_render | template_render(source,ctx)→{{key}} AUTO-ESCAPED interpolation (XSS-safe); template_layout(layout,blocks)→{{blockname}} RAW block insertion (layout inheritance). Runtime template strings (no NOVA-brace trap). Control-flow {%for%}/{%if%} = follow-up | untested (syntax✓) |
| 52 | S18 application event bus (Spring @EventListener parity) | forge_events.nova | grep:on_event | events_new; on_event(bus,event,listener) many-per-event; emit(sync, ordered, returns count); emit_async(spawns each listener, payload deep-copied). Decouples producers from consumers | untested (syntax✓) |
| 53 | S17 form objects (Django Form/ModelForm parity) | forge_forms.nova | grep:form_validate | form_define(fields [name,type,required]); form_validate(form,data)→{valid,errors} (required/int/email); form_render(form,values,errors)→HTML inputs w/ current values + per-field error spans (failed-submit re-render). All esc-safe | untested (syntax✓) |
| 54 | S18 i18n (message bundles + locale + {param} interp) | forge_i18n.nova | grep:i18n_ti | i18n_new/add; i18n_t (locale→en→default fallback); i18n_ti (+{param} interpolation, missing param kept); i18n_locale(Accept-Language→primary subtag match). Messages are runtime strings (brace trap) | untested (syntax✓) |
| 55 | S17 global exception-handler registry (@ControllerAdvice parity) | forge_errmap.nova | grep:errmap_handle | errmap_new/on(tag,handler)/default; errmap_handle(tag,detail) 3-priority: specific→default→last-resort string. NOVA has no exceptions→error TAGS from Result err; response-type-agnostic | untested (syntax✓) |
| 56 | S17 HATEOAS hypermedia (HAL, Spring HATEOAS parity) | forge_hateoas.nova | grep:hal_resource | hal_resource(data)→envelope w/ _links; hal_link(rel,href)/hal_self; json_stringify→application/hal+json. Client navigates by rel not hard-coded URLs | untested (syntax✓) |
| 57 | S18 account-lockout / brute-force guard | forge_lockout.nova | grep:lockout_locked | lockout_fail(key)→count; lockout_locked(key,max); lockout_reset(on success); lockout_remaining. Credential-stuffing defense (Spring lockout / django-axes) | untested (syntax✓) |
| 58 | S18 method-level security (@PreAuthorize/@Secured parity) | forge_method_security.nova | grep:pre_authorize | has_role/has_any_role/has_all_roles(principal.roles); pre_authorize(principal,role,action)→{allowed,value} (runs action() on pass); secured(any-role). Guard a service action BEFORE it runs, caller→403 on deny | untested (syntax✓) |
| 59 | S19 actuator introspection endpoints (Spring Actuator parity) | forge_actuator.nova | grep:actuator_routes | actuator_routes(app)→[{method,path}] (/mappings); actuator_info (/info); actuator_env(cfg)→redacts secret/password/token/key (/env). Serve as JSON at /actuator/* | untested (syntax✓) |
| 60 | S19 batch/chunk processing (Spring Batch parity) | forge_batch.nova | grep:batch_process | batch_chunks(items,size); batch_process(items,size,process_fn) per-chunk (bounded memory, independent commit); batch_map (processor). ETL reader→processor→writer | untested (syntax✓) |
| 61 | S18 test harness (Spring MockMvc / Django Client parity) | forge_test.nova | grep:test_get | test_get/post/request→forge.dispatch (real router, no socket); assert_status/contains/ok, expect, run_case. Deterministic, kill-on-timeout-safe | untested (syntax✓) |
| 62 | S16 DB seeding / fixtures (Rails db/seeds, Django fixtures) | forge_seed.nova | grep:seed_row_sql | seed_row_sql(table,row)→param INSERT; seed_all_sql(rows)→batch. Caller runs via orm_exec. Injection-safe | untested (syntax✓) |
| 63 | S16 model lifecycle hooks (Rails callbacks / Django signals / JPA @PrePersist) | forge_hooks.nova | grep:run_hooks | hooks_new/on_hook(table,event,hook); run_hooks threads row through before_save/after_save/before_delete/after_delete hooks (before_* may transform: slugs/defaults/audit stamps) | untested (syntax✓) |
| 64 | S16 versioned migrations (Rails/Django/Flyway history table) | forge_migrate.nova | grep:migration_history_ddl | migration_history_ddl (schema_migrations table); migration_check_sql (applied?); migration_record_sql. Idempotent ordered up-migrations alongside the struct-diff auto path | untested (syntax✓) |
| 65 | Multi-tenancy (SaaS tenant isolation) | forge_tenant.nova | grep:tenant_scope | tenant_from_host(subdomain)/header/path resolution; tenant_scope(conds,params,id) appends tenant_id=? (cross-tenant leak impossible by construction); tenant_find_sql; tenant_stamp (writes tenant-owned). Shared-DB strategy | untested (syntax✓) |
| 66 | RFC 5988 pagination Link headers (GitHub-API convention) | forge_links.nova | grep:pagination_links | link_rel(url,rel); link_header(links); pagination_links(base,page,per,total)→prev/next/first/last Link header. Clients follow links | untested (syntax✓) |
| 67 | Content-Security-Policy builder (XSS defense-in-depth) | forge_csp.nova | grep:csp_build | csp_directive/csp_build(directives)→policy string; csp_strict() sane default (self-only, no inline objects/framing). Serve as Content-Security-Policy header | untested (syntax✓) |
| 68 | Deep field validators (JSR-380 / Django validators parity) | forge_validators.nova | grep:v_email | 14 pure predicates: required/min_len/max_len/length_between/is_int/int_min/max/range/digits/alnum/matches_charset/email/url/one_of. Compose in routes/forms, map false→error | untested (syntax✓) |
| 69 | URL slug generation (Rails friendly_id / Django slugify) | forge_slug.nova | grep:slugify | slugify(title)→lowercase, non-alnum→hyphen, collapse, no lead/trail dash; slugify_n(title,n) uniqueness suffix | untested (syntax✓) |
| 70 | Pluggable session store (actor-backed, N>1-safe) | forge_session_store.nova | grep:session_store_new | session_store_new/set/get/destroy/stop; actor owns the session dict (Redis/DB backend swaps behind same API) | untested (syntax✓) |
| 71 | API key management (Stripe-style prefix_random + store) | forge_apikey.nova | grep:apikey_generate | apikey_generate(prefix)→prefix_<48hex CSPRNG>; store register/valid/meta/revoke; apikey_extract (Bearer/raw). Per-key auth + metadata (owner/scopes/rate-limit). Prod: store key HASH (documented) | untested (syntax✓) |
| 72 | Webhooks — signed outbound delivery + verify (Stripe/GitHub-style) | forge_webhook.nova | grep:webhook_prepare | webhook_prepare(secret,event,data)→[body,headers] w/ X-Webhook-Signature=hmac_sha256(secret,body); webhook_verify (receiver auth). Caller POSTs via http_client + retries | untested (syntax✓) |
| 73 | Money handling (integer cents, no float bugs) | forge_money.nova | grep:money_format | money_format(cents,symbol)→"$12.34" (neg-aware); money_add (exact); money_percent(cents,basis_points) tax/fees. Store minor units | untested (syntax✓) |
| 74 | CQRS command/query bus (Axon/MediatR/NestJS-CQRS) | forge_cqrs.nova | grep:cqrs_dispatch | cqrs_new/register(name,handler); cqrs_dispatch(name,payload)→handler (err if none). ONE dispatch point to wrap w/ logging/tx/validation (forge_aspects) | untested (syntax✓) |
| 75 | Multi-channel notifications (Laravel Notifications / ActionMailer) | forge_notify.nova | grep:notify_broadcast | notify_channel(name,sender fn(recipient,subject,body)); notify_send(one channel); notify_broadcast(all channels fan-out). Decouples notify from HOW (email/sms/push) | untested (syntax✓) |
| 76 | Cron expression matching (Quartz/Spring @Scheduled cron) | forge_cron.nova | grep:cron_matches | cron_matches(expr,min,hour,dom,month,dow) — 5-field, supports * / N / */K / A-B / A,B,C. Pair with a 60s scheduler tick for declarative cron | untested (syntax✓) |
| 77 | Full-text search (tokenize + inverted index + TF ranking) | forge_search.nova | grep:search_query | search_new/add(docid,text); search_query→[docid,score] (summed term-frequency, caller sorts). PG tsvector / Elasticsearch for single-node | untested (syntax✓) |
| 78 | Named-lock / mutex manager (actor, FIFO-fair) | forge_lock.nova | grep:lock_acquire | lock_new; lock_acquire(name) PARKS until free (FIFO waiter queue); lock_release hands to next waiter or frees. Race-free (one actor owns lock state). Coordinate shared resource among green tasks | untested (syntax✓) |
| 79 | Markdown→HTML renderer (Rails ActionText / rich text) | forge_markdown.nova | grep:markdown_to_html | markdown_to_html(src): #/##/### headings, - / * lists, paragraphs, **bold**/*italic*/`code`/[text](url). XSS-safe (esc before markup). Sonnet under Opus spec | untested (syntax✓) |
| 80 | Composite health checks (Spring Actuator HealthIndicator aggregation) | forge_health.nova | grep:health_check | health_indicator(name,check fn()->bool); health_check→{status UP/DOWN, components{name:{status}}}. DOWN if any down. Real dependency health (DB/cache/downstream) | untested (syntax✓) |
| 81 | Bulk DB insert (multi-row VALUES, saveAll/bulk_create) | forge_bulk.nova | grep:bulk_insert_sql | bulk_insert_sql(table,cols,rows)→ONE INSERT with N value-groups (far fewer round-trips). Param-bound, injection-safe. Run via orm_exec | untested (syntax✓) |
| 82 | Standard API response envelope (JSON:API-lite) | forge_envelope.nova | grep:envelope_ok | envelope_ok(data)/ok_meta/error(code,msg)/page(items,page,per,total)→consistent {data,meta}/{error} shape across all endpoints | untested (syntax✓) |
| 83 | Declarative validation schema (Pydantic/Zod/JSON-Schema) | forge_schema.nova | grep:schema_validate | schema_validate({field:[[validator_fn,message]...]}, data)→{valid,errors} first-error-per-field. Composes forge_validators | untested (syntax✓) |
| 84 | Consistent hashing (Ketama sharding) | forge_hashring.nova | grep:hashring_node_for | hashring_add(node); hashring_node_for(key)→next node clockwise on the hash ring (FNV-1a), minimal remap on node change (~1/N). Cassandra/Riak/memcached sharding | untested (syntax✓) |
| 85 | Bounded LRU cache (Caffeine/Guava eviction) | forge_lru.nova | grep:lru_put | lru_new(cap)/put/get/has; least-recently-used eviction when full (access-order list). Bounded-memory cache (vs unbounded forge_cache) | untested (syntax✓) |
| 86 | HTML sanitizer (whitelist, OWASP-antisamy/DOMPurify/Rails sanitize) | forge_sanitize.nova | grep:sanitize | sanitize(html): keep safe-tag whitelist, STRIP all attributes (kills onclick/style/js-urls), DROP dangerous tags (script etc.); strip_tags→plain text. For user rich text | untested (syntax✓) |
| 87 | XML serializer (SOAP/RSS/sitemap/legacy content-negotiation) | forge_xml.nova | grep:xml_element | xml_element(tag,v) dict→child elements, list→<item>s, scalars esc'd; xml_document (decl+root); xml_attr_element (attributes). Injection-safe (esc) | untested (syntax✓) |
| 88 | Feature-flag targeting + % rollout (LaunchDarkly/Unleash) | forge_targeting.nova | grep:rollout_on | rollout_on(feature,user,percent) STABLE hash-bucket (same user→same answer, % only adds users); target_attr(attrs,attr,allowed); feature_on combines. Beyond simple flags | untested (syntax✓) |
| 89 | Audit field-diff (change history) | forge_diff.nova | grep:dict_diff | dict_diff(before,after)→[{field,old,new}] added/changed/removed; dict_changed. Pair w/ hooks+admin_audit to record WHAT changed | untested (syntax✓) |
| 90 | Delayed jobs (Sidekiq perform_in / Celery countdown) | forge_delayed.nova | grep:delay_run | delay_run(ms,action) spawns a green task that sleeps then runs (fire-and-forget); thousands of pending jobs ~free. Recurring→every, cron→forge_cron | untested (syntax✓) |
| 91 | Credit-card validation (Luhn + brand + PCI mask) | forge_card.nova | grep:luhn_valid | luhn_valid(number) checksum; card_brand (visa/mastercard/amex/discover by prefix+len); card_mask→****last4 (PCI-safe, never log full). Cleans spaces/dashes | untested (syntax✓) |
| 92 | Bloom filter (probabilistic membership; Cassandra SSTable / CDN skip) | forge_bloom.nova | grep:bloom_maybe | bloom_new(size,k)/bloom_add/bloom_maybe: false=DEFINITELY absent (skip the expensive lookup), true=maybe present. Sparse dict bit-set, k seed-differentiated FNV hashes; no false negatives | untested (syntax✓) |
| 93 | Phone validation + E.164 (libphonenumber-lite) | forge_phone.nova | grep:phone_e164 | phone_valid: 7-15 digit E.164 range; phone_e164 normalizes to +<cc><national> (keeps a leading +, else prepends default_cc). Strips all formatting | untested (syntax✓) |
| 94 | Duration humanizing + relative time (Rails distance_of_time / moment fromNow) | forge_duration.nova | grep:time_ago | humanize_ms→"1m 30s"; time_ago(secs)→just now / N minutes ago / N hours ago / N days ago. For UIs + logs | untested (syntax✓) |
| 95 | Circuit breaker + registry (Resilience4j/Hystrix/Spring Cloud) | forge_circuit.nova | grep:cb_allow | cb_new/allow/success/failure: closed→open after N fails (reject fast), cooldown→half_open trial, success→closed. cb_registry/cb_get = one named breaker per dependency | untested (syntax✓) |
| 96 | Retry backoff calculators (AWS backoff-and-jitter) | forge_backoff.nova | grep:backoff_exp | backoff_exp (base*2^n capped), backoff_linear, backoff_decorrelated (herd-spreading jitter in [base,prev*3], caller-seeded → deterministic). Pairs w/ aspects.retried() | untested (syntax✓) |
| 97 | ULID sortable ids (better DB PK than UUIDv4) | forge_ulid.nova | grep:ulid_timestamp | ulid()/ulid_at: 26-char Crockford-base32 = 48-bit ms + 80-bit CSPRNG. Sorts in creation order as a plain string (no random-insert B-tree page splits); timestamp recoverable | untested (syntax✓) |
| 98 | Fixed-window rate limiter (X-RateLimit quotas) | forge_ratewin.nova | grep:ratewin_allow | ratewin_new/allow/remaining/reset_at: `limit` req per fixed window, snaps to 0 at boundary. The '1000 req/hour' quota algorithm; complements token-bucket. key->[start,count] | untested (syntax✓) |
| 99 | Semver parse/compare/satisfies (npm semver / Cargo) | forge_semver.nova | grep:semver_satisfies | semver_parse/cmp/eq/gt/lt; satisfies caret ^ / tilde ~ / >= / exact; correct prerelease precedence (1.0.0-beta<1.0.0, numeric<alpha). Build metadata ignored | untested (syntax✓) |
| 100 | Pagination metadata + page-window (Spring Page / Django Paginator) | forge_pagination.nova | grep:page_meta | page_meta(total/page/per_page→total_pages/offset/has_prev/has_next/from/to, clamped); page_window centered+edge-shifted numeric pager; page_offset/page_total_pages | untested (syntax✓) |
| 101 | Idempotency keys (Stripe Idempotency-Key, no double-charge) | forge_idempotency.nova | grep:idem_begin | idem_begin claims a key (false=replay/in-flight→409); idem_complete stores response; idem_done/idem_response replay it; idem_release frees a failed attempt. Redis/DB-backable | untested (syntax✓) |
| 102 | Cursor (keyset) pagination (GraphQL Relay / Stripe starting_after) | forge_cursor.nova | grep:cursor_where | cursor_encode/decode opaque length-prefixed [sort,id]; cursor_where→parameterized `(sort,id)>(?,?)` keyset predicate. O(log n) seek, stable under concurrent inserts | untested (syntax✓) |
| 103 | HTTP ETag + conditional requests (RFC 7232, 304 Not Modified) | forge_etag.nova | grep:etag_for | etag_for fingerprints a body (32-bit FNV, quoted); etag_matches honors a comma list + '*' + weak W/; etag_not_modified→short-circuit 304, bandwidth-saving conditional GET | untested (syntax✓) |
| 104 | IPv4 CIDR matching (allow/deny, trusted-proxy, abuse block) | forge_ipcidr.nova | grep:cidr_contains | ip_to_int dotted-quad→32-bit (strict, rejects bad octets); cidr_contains masks via boundary-floor (no bitwise-AND); ip_in_any over a list. /0 matches all | untested (syntax✓) |
| 105 | Bulkhead concurrency isolation (Resilience4j Bulkhead) | forge_bulkhead.nova | grep:bulkhead_try | bulkhead_new(max)/try/release counting semaphore: cap concurrent calls to a slow dependency, reject-fast over limit (shed load). Single-process; own inside an actor for cross-task | untested (syntax✓) |
| 106 | PII masking for logs (GDPR/CCPA, Rails filter_parameters) | forge_mask.nova | grep:mask_fields | mask_email (a***@domain), mask_tail (****1111), mask_secret (***), mask_fields→non-destructive redacted COPY. Keep secrets/PII out of log sinks | untested (syntax✓) |
| 107 | Content negotiation (Accept header, Spring produces / Rails respond_to) | forge_negotiate.nova | grep:accept_best | accept_best picks best offered by client q-weight (0..1000 ints, no floats), wildcard specificity exact>type/*>*/*, server-preference tie-break; accept_q; wants_json | untested (syntax✓) |
| 108 | Query-string build/parse + percent-encoding (WHATWG / Rack::Utils) | forge_qs.nova | grep:qs_build | qs_encode (unreserved passthrough, %20 not +), qs_decode (%HH + '+'→space), qs_build dict→k=v&…, qs_parse→dict. Injection-safe URL param handling | untested (syntax✓) |
| 109 | Set-Cookie builder + Cookie parse (HttpOnly/Secure/SameSite) | forge_cookie.nova | grep:cookie_serialize | cookie_serialize attribute-aware; cookie_session hardened default (HttpOnly+Secure+SameSite=Lax); cookie_delete; cookie_parse. Complements core cookie_get (read-side) | untested (syntax✓) |
| 110 | NDJSON / JSON Lines streaming (Elasticsearch _bulk, log shipping) | forge_ndjson.nova | grep:ndjson_encode | ndjson_encode(list)/ndjson_line(row)/ndjson_decode: one JSON value per line → stream without buffering the whole array; blank-line-lenient decode | untested (syntax✓) |
| 111 | Sliding-window-log rate limiter (no boundary burst) | forge_sliding.nova | grep:sliding_allow | sliding_new/allow/remaining: timestamp log per key, counts only hits in the trailing window (exact per-instant limit, unlike fixed-window's 2x edge burst). O(hits) mem, lazy eviction | untested (syntax✓) |
| 112 | Base32 encode/decode (RFC 4648) | forge_base32.nova | grep:base32_encode | base32_encode/decode, case-insensitive, '=' padding; bit-accumulator masked to leftover bits (i64-safe, no 1<<64). Underpins TOTP secrets. TESTED ✓ (TOTP decode path) | tested✓ (via TOTP KAT) |
| 113 | UUID v4 + v7 (RFC 9562) | forge_uuid.nova | grep:uuid7 | uuid4 (random), uuid7 (time-ordered, string-sortable DB keys), uuid7_at, uuid_is_valid. 16 CSPRNG bytes→32 nibbles, variant 8/9/a/b unbiased, no bit-shifts | untested (syntax✓) |
| 114 | JSON Pointer (RFC 6901) | forge_jsonpointer.nova | grep:jptr_get | jptr_tokens/has/get walk nested dict/list by /a/0/b; ~1→/ ~0→~ unescape; dict-key or all-digit array-index descent via type_of. Config lookup / patch targeting | untested (syntax✓) |
| 115 | TOTP/HOTP 2FA (RFC 6238/4226) + HMAC-SHA1 | forge_totp.nova + forge_crypto.hmac_sha1_bytes | grep:fn totp | hotp/totp/totp_now/totp_verify(skew)/totp_secret/totp_uri, base32 secret, Google-Authenticator compatible. RFC KAT VERIFIED: C0..4=755224/287082/359152/969429/338314, T59=94287082, T1111111109=07081804 | TESTED ✓ RFC KAT |
| 116 | DataLoader / N+1 query killer (Facebook DataLoader, Spring @BatchMapping) | forge_dataloader.nova | grep:dl_dispatch | dataloader_new(batch_fn); dl_load queues keys, dl_dispatch loads them in ONE batch + memoizes, dl_get reads cache; dl_load_all convenience. Per-request lifetime | untested (syntax✓) |
| 117 | Outbound multipart/form-data builder (client upload/webhook) | forge_multipart_build.nova | grep:multipart_build | multipart_build(boundary,fields,files)→[content_type,body]; text fields + file parts, RFC 7578 CRLF, matching Content-Type. Forge already PARSES inbound multipart | untested (syntax✓) |
| 118 | Signed/expiring URLs + tokens (S3-presigned / Rails signed_id) | forge_signed_url.nova | grep:sign_token | sign_token/verify_token HMAC-SHA256 payload+expiry, stateless tamper-proof self-expiring links; constant-time compare, fail-closed; signed_url appends ?t=. Reset/download/unsub links | untested (syntax✓) |
| 119 | Password strength policy + entropy (OWASP ASVS / NIST / zxcvbn-lite) | forge_pwpolicy.nova | grep:pw_check | pw_classes/pw_entropy(floor-log2 pool)/pw_check(len+classes+blocklist)/pw_ok/pw_score(0-4). Complements forge_auth (hashing). Decides ACCEPT at signup | untested (syntax✓) |
| 120 | MIME type by extension (static-file Content-Type; nginx mime.types) | forge_mime.nova | grep:mime_type | mime_type(~42 web types→octet-stream fallback), mime_ext (basename-aware, dotted-dir safe), mime_is_text (charset/compression). For static serving | untested (syntax✓) |
| 121 | CSV parser RFC 4180 (inbound; forge core has csv_row/csv_of build) | forge_csv_parse.nova | grep:csv_parse | csv_parse state machine (quotes/embedded-commas/newlines/""→"), 
| 122 | Identifier case conversion (snake/camel/Pascal/kebab/CONSTANT/Title) | forge_case.nova | grep:to_snake | word-split ANY input (separators + camelCase + acronym-run boundaries) then rejoin. to_snake/camel/pascal/kebab/constant/title. API JSON<->struct field mapping, codegen, DB columns | untested (syntax✓) |
| 123 | Number/byte humanizing (Rails number_to_human / Django intcomma) | forge_humanize.nova | grep:humanize_int | comma_int (1,234,567), humanize_int (1.5M K/M/B/T), humanize_bytes (1.5 KB base-1024). Integer-only, overflow-safe 1-decimal | untested (syntax✓) |
| 124 | Finite state machine (Spring StateMachine / AASM / XState) | forge_fsm.nova | grep:fsm_fire | fsm_new/add (event,from->to)/fsm_fire ENFORCES legality (illegal rejected, state never corrupted)/fsm_can/fsm_events. Order+workflow lifecycles | untested (syntax✓) |
| 125 | Base62 short-id encoding (URL shorteners, opaque public ids) | forge_base62.nova | grep:base62_encode | base62_encode/decode over 0-9a-zA-Z, URL-safe (no +/=). Hide a sequential PK as a bit.ly/YouTube-style code | untested (syntax✓) |
| 126 | OAuth2 PKCE (RFC 7636, public-client auth-code security) | forge_oauth_pkce.nova | grep:pkce_challenge | pkce_verifier (CSPRNG base64url), pkce_challenge (S256 via forge_crypto sha256), pkce_auth_url (qs_encoded), pkce_state_ok (CSRF). SPA/mobile/CLI login | untested (syntax✓) |
| 127 | Rate-limit response headers (X-RateLimit-* + Retry-After) | forge_ratelimit_headers.nova | grep:ratelimit_headers | ratelimit_headers(limit,remaining,reset,now,blocked)->header dict; Retry-After only when blocked. Ties ratewin/sliding into HTTP | untested (syntax✓) |
| 128 | Deep dict merge + dotted-path get/set (Spring property layering / lodash) | forge_deepmerge.nova | grep:deep_merge | deep_merge (recursive, non-mutating, nested overrides win at leaf), deep_get/deep_set/deep_has by 'a.b.c' path. Config layering primitive | untested (syntax✓) |
| 129 | English inflector (Rails Inflector) | forge_inflect.nova | grep:fn pluralize | pluralize/singularize (irregulars/uncountables/ies/es/oes/ves rules) + ordinalize (1st/2nd/11th). Auto REST resource + table names | untested (syntax✓) |
| 130 | Civil-date arithmetic (chrono days_from_civil / ISO 8601) | forge_date.nova | grep:date_add_days | Hinnant integer-exact days_from/civil_from_days: ISO parse/format, weekday(+name), add/diff days, days_in_month, is_leap, epoch<->date, date_today. UTC, no floats/libc | untested (syntax✓) |
| 131 | RBAC role hierarchy (Spring Security / Django perms / Casbin-lite) | forge_rbac.nova | grep:rbac_can | rbac_grant(role,perm)+rbac_inherit(role,parent)+rbac_assign(user,role); rbac_can walks the role graph (cycle-safe visited set) incl inherited perms; rbac_has_role | untested (syntax✓) |
| 132 | HTTP-date IMF-fixdate (RFC 7231 Date/Expires/Last-Modified) | forge_httpdate.nova | grep:httpdate_format | httpdate_format(epoch)->"Sun, 06 Nov 1994 08:49:37 GMT"; httpdate_parse->epoch (IMF only). Composes forge_date; pairs with etag conditional requests | untested (syntax✓) |
| 133 | IBAN validation (ISO 13616/7064 MOD-97) | forge_iban.nova | grep:iban_valid | iban_valid: clean+rearrange+letter->10..35+incremental mod97==1 (no bigint); iban_country + iban_format (groups of 4). SEPA/wire typo-catch | untested (syntax✓) |
| 134 | User-Agent classifier (browser/OS/device/bot; ua-parser-lite) | forge_useragent.nova | grep:ua_info | ua_browser (ordered: Edge>Opera>Chrome>FF>Safari>IE), ua_os (Android before Linux), ua_is_mobile/ua_is_bot, ua_device, ua_info summary. Analytics/adaptive/bot-filter | untested (syntax✓) |
| 135 | URL parse/build (WHATWG URL / urllib.parse) | forge_url.nova | grep:url_parse | url_parse progressive-peel -> scheme/host/port/path/query/fragment/userinfo (all keys, host->path '/'); url_build inverse; url_query_get. NOVA supports else-if + not | untested (syntax✓) |
| 136 | Redis client RESP2 (caching/session/ratelimit/pubsub backend) | forge_redis.nova | grep:resp_parse | resp_encode+resp_parse PURE codec (str/err/int/bulk/nil/array/incomplete) KAT-VERIFIED; socket layer redis_connect/command (pump tcp_recv) + get/set/del/exists/incr/expire/ping over tcp_*. Live-server test deferred | codec TESTED ✓ / live deferred |
| 136b | JSON-RPC server dispatch (ADDITIVE to existing forge_jsonrpc codec) | forge_jsonrpc.nova | grep:jsonrpc_dispatch | jsonrpc_dispatch(handlers,text): parse->handler-map lookup->invoke->response (notif->"", unknown->-32601) + dispatch_batch. Reuses existing parse/method/params/id/success/error. (grep-verify caught a near-overwrite; kept richer original) | untested (syntax✓) |
| 137 | HTTP Range / 206 partial content (RFC 7233) | forge_range.nova | grep:range_parse | range_parse(header,total)->[ok,start,end] for explicit/open-ended/suffix forms (416 on unsatisfiable); range_content_range/range_unsatisfiable/range_length. Video seek + resumable downloads | untested (syntax✓) |
| 138 | Content-Disposition downloads (RFC 6266/5987, Unicode filenames) | forge_content_disposition.nova | grep:content_disposition | attachment/inline + filename* UTF-8 pct-encoded for non-ASCII + ASCII fallback; header-injection-safe (control/quote strip). Save-As with correct unicode names | untested (syntax✓) |
| 139 | Security headers bundle (Helmet.js / Spring Security / Django) | forge_security_headers.nova | grep:security_headers | HSTS/X-Content-Type-Options/X-Frame-Options/Referrer-Policy/COOP/CORP + strict (preload+Permissions-Policy); X-XSS-Protection=0 (modern). apply_headers merge. Complements forge_csp | untested (syntax✓) |
| 140 | Cache-Control directive builder | forge_cachecontrol.nova | grep:cache_public | cache_public/private/immutable/no_store/swr(stale-while-revalidate)/shared(s-maxage) value builders → feed core cache_control(resp,value). Correct caching policy in one call | untested (syntax✓) |
| 141 | Geo distance/bbox/within (haversine; PostGIS ST_Distance-lite) | forge_geo.nova | grep:geo_distance | geo_distance (KAT London->Paris 343556m ✓), geo_bbox radius prefilter, geo_within. Uses sin/cos/asin/sqrt. geo_bearing OMITTED (compiler float-codegen bug, gated) | TESTED ✓ (distance/bbox/within) |
| 142 | EAN-13/UPC-A/EAN-8 barcode checksum (GS1) | forge_ean.nova | grep:ean_valid | ean_check_digit/ean_valid/ean_complete, mod-10 weighted (3,1 from right). KAT 5901234123457 valid ✓ | TESTED ✓ |
| 143 | MessagePack encode/decode (binary JSON; Redis/Fluentd/RPC) | forge_msgpack.nova | grep:msgpack_encode | over the SOUND bytes type (NOT char-string: found NUL-truncation makes char-bytes unsound). fixint/int64/str/str16/array/map. KAT round-trips int/int64/str/list/map/nil ✓ | TESTED ✓ (round-trip KAT) |
| 144 | iCalendar .ics events (RFC 5545, Add-to-Calendar) | forge_ical.nova | grep:ical_event | ical_event VEVENT (start/end/summary/desc/location, TEXT-escaped) + ical_calendar VCALENDAR; composes forge_date for UTC timestamps. Booking/invites | untested (syntax✓) |
| 145 | 2FA recovery/backup codes | forge_recovery_codes.nova | grep:recovery_codes | recovery_codes(n,groups,glen) over unambiguous alphabet (no 0/O/1/I/L), recovery_consume single-use. NUL-SAFE (bytes_get->char, no raw-byte concat). Pairs w/ forge_totp | untested (syntax✓) |
| 146 | .well-known discovery (OIDC/OAuth metadata, security.txt, JWKS) | forge_wellknown.nova | grep:oidc_discovery | oidc_discovery (openid-configuration), oauth_metadata (RFC 8414), jwks wrapper, security_txt (RFC 9116). Makes a service an auto-discoverable OIDC/OAuth provider | untested (syntax✓) |
| 147 | Open Graph + Twitter Card meta tags (social link previews) | forge_opengraph.nova | grep:opengraph | opengraph(title/desc/image/url/type)->og:* + twitter:card meta tags (FB/X/LinkedIn/Slack/iMessage unfurl), HTML-attr-escaped | untested (syntax✓) |
| 148 | RSS 2.0 + Atom 1.0 feed generation | forge_feed.nova | grep:feed_rss | feed_rss/feed_atom from item dicts + feed_escape (single-pass, & first, no double-escape). Blog/news/podcast syndication | untested (syntax✓) |
| 149 | Honeypot form spam prevention (no-CAPTCHA, accessible) | forge_honeypot.nova | grep:honeypot_is_bot | honeypot_field_name trap + honeypot_is_bot/ok: hidden-field (bots fill, humans don't) + min render-to-submit timing. WCAG-friendly, zero friction | untested (syntax✓) |
| 150 | AWS Signature V4 request signing (S3/DynamoDB/Bedrock, no SDK) | forge_awssigv4.nova | grep:sigv4_signature | sigv4_signing_key (HMAC chain) + canonical_request/string_to_sign/signature/auth_header; composes forge_crypto. KAT-VERIFIED vs AWS official vector (sig 5d672d79...a6f2b5d7) | TESTED ✓ AWS KAT |
| 151 | Inbound webhook signature verify (Stripe/GitHub/Slack) | forge_webhook_verify.nova | grep:stripe_webhook_verify | github(sha256=)/stripe(t=,v1=+replay)/slack(v0:) HMAC-SHA256, constant-time. KAT: valid→accept, replay→reject. (forge_webhook=outbound) | TESTED ✓ KAT |
| 152 | Math CAPTCHA (stateless signed token) | forge_captcha.nova | grep:captcha_verify | captcha_math→[question,token]; captcha_verify HMAC constant-time + expiry + answer match, fail-closed. No session storage | untested (syntax✓) |
| 153 | vCard 4.0 contact cards (RFC 6350, Add-to-Contacts) | forge_vcard.nova | grep:vcard_build | vcard_build from name/org/email/phone/url/note/address (TEXT-escaped, structured N/ADR) + vcard_simple. .vcf for phones/CRMs | untested (syntax✓) |
| 154 | Signed S3 object requests (GET/PUT/DELETE, no SDK) | forge_s3.nova | grep:s3_request | s3_request/get/put/delete: S3 canonical request (virtual-host, host/x-amz-date/x-amz-content-sha256 signed) via KAT-verified sigv4 → request dict. forge_storage cloud backend. Live-S3 deferred | untested (syntax✓) |
| 155 | Signed DynamoDB requests (PutItem/GetItem/Query) | forge_dynamodb.nova | grep:dynamodb_request | X-Amz-Target op + JSON body, SigV4-signed (5 sorted canonical headers) → request dict. Serverless NoSQL backend. Composes KAT sigv4. Live deferred | untested (syntax✓) |
| 156 | CLDR plural rules (correct pluralized i18n) | forge_plural.nova | grep:plural_category | plural_category en/fr/ru/uk/pl/ar/cs/sk/CJK (one/two/few/many/zero/other) + plural_select. ICU MessageFormat/ngettext parity, complements forge_i18n | untested (syntax✓) |
| 157 | Schema.org JSON-LD structured data (SEO rich results) | forge_jsonld.nova | grep:jsonld_article | Article/Organization/BreadcrumbList/Product(Offer)/WebSite(SearchAction) + jsonld_script wrapper. @context/@type via json_stringify. Google/Bing rich cards | TESTED ✓ (agent ran, @type verified) |
| 158 | Color utils + WCAG contrast (a11y/theming) | forge_color.nova | grep:color_contrast | hex<->RGB(+3-digit), RGB->HSL, WCAG luminance+contrast+passes_aa(4.5:1). VERIFIED black/white=21.0, #777/#fff=4.478. Confirms int/round/floor/ceil builtins | TESTED ✓ |
| 159 | Levenshtein fuzzy-match (did-you-mean / dedup) | forge_levenshtein.nova | grep:levenshtein | edit distance (KAT kitten/sitting=3) + similarity 0-100 + closest_match + fuzzy_match. O(n*m) rolling-row DP. Typo-tolerant search | TESTED ✓ |
| 160 | Text content helpers (truncate/word-count/reading-time/highlight) | forge_text.nova | grep:reading_time | truncate_words/excerpt (word-boundary+ellipsis), word_count, reading_time, highlight (<mark>, case-preserving). CMS/blog/search-results. KAT verified | TESTED ✓ |
| 161 | Snowflake distributed IDs (Twitter, coordination-free) | forge_snowflake.nova | grep:snowflake_next | 64-bit time-ordered id (41-bit ts|10-bit machine|12-bit seq), snowflake_next + timestamp/machine/sequence decode. KAT round-trip exact. Sortable, 1024 machines x 4096/ms | TESTED ✓ |
| 162 | Geohash proximity encoding (Redis GEO / tile index) | forge_geohash.nova | grep:geohash_encode | lat/lon bit-interleave -> base32 shared-prefix cells. KAT (57.64911,10.40744)=u4pruydqqvj exact. Complements forge_geo haversine | TESTED ✓ |
| 163 | .env config parsing (12-factor / dotenv) | forge_dotenv.nova | grep:dotenv_parse | dotenv_parse (export prefix, #comments, single/double quotes + 
	\ escapes, inline comments) + dotenv_get default. KAT verified. Feeds forge_config | TESTED ✓ |
| 164 | Soundex phonetic matching (name sounds-alike search/dedup) | forge_phonetic.nova | grep:fn soundex | soundex (letter+3 digits, h/w-aware collapse) + soundex_match. KAT: Robert/Rupert=R163, Ashcraft=A261, Pfister=P236, Smith/Smyth=S530 all exact | TESTED ✓ |
| 165 | LCS line diff (git/PR change review) | forge_diff_text.nova | grep:diff_lines | diff_lines LCS-aligned [op,line] (same/add/del, flat 1D DP i*w+j) + diff_unified (+/-/space) + diff_changes. KAT: beta->delta=2 changes, correct marks. Version history/audit | TESTED ✓ |
| 166 | NanoID compact URL-safe ids | forge_nanoid.nova | grep:fn nanoid | nanoid(size)/nanoid_default(21)/nanoid_custom over 64-symbol unbiased alphabet. NUL-safe (bytes_get). KAT len 21/10. Completes id family (ulid/uuid/snowflake/nanoid) | TESTED ✓ |
| 167 | INI config parser (git config/php.ini/systemd) | forge_ini.nova | grep:ini_parse | [section] key=value -> nested dict (top keys under ""), ; and # comments, quoted values, ini_get default. KAT verified | TESTED ✓ |
| 168 | Jaro-Winkler similarity (fuzzy name/record matching) | forge_jaro.nova | grep:jaro_winkler | jaro + jaro_winkler (0..1, transposition-aware, prefix-boosted). KAT exact MARTHA/MARHTA=0.944/0.961, DIXON/DICKSONX=0.813. Completes fuzzy toolkit (levenshtein+phonetic+jaro) | TESTED ✓ |
| 169 | Deterministic default avatars (initials + color) | forge_avatar.nova | grep:avatar_initials | avatar_initials (up to 2, GitHub/Slack style) + avatar_color (stable palette pick, FNV hash) + avatar dict. Same user->same avatar, no image storage. KAT verified | TESTED ✓ |
| 170 | Descriptive statistics (dashboards/monitoring/SLA) | forge_stats.nova | grep:stats_stddev | sum/mean/min/max/median/variance/stddev/percentile over int|float lists (insertion sort + sqrt). KAT mean=5/stddev=2/median=4.5. p95/p99 latency SLAs | TESTED ✓ |
| 171 | HTML entity decode (feed/scraped content, inverse of esc) | forge_htmlentities.nova | grep:html_decode | html_decode named (17: amp/lt/copy/euro/mdash/...) + decimal &#NNN; + hex &#xHH;, correct UTF-8 for BMP (/,% no shifts). KAT ©=2B €=3B &#x41;=A passthrough | TESTED ✓ |
| 172 | TOML config parser (subset) | forge_toml.nova | grep:toml_parse | toml_parse->nested dict ([table]/[a.b] nesting, quoted/literal strings+escapes, single-line arrays, #comments) + toml_get dotted-path + toml_is_int. Scalars as STRINGS (dotenv/ini convention; parse_int-through-any coercion hit a codegen edge). KAT verified | TESTED ✓ |
| 173 | DAG topological sort + cycle detection (task/build/migration order) | forge_dag.nova | grep:dag_topo_sort | dag_add(node depends on dep) + dag_topo_sort (deps-first order, [] on cycle) + dag_has_cycle. Kahn-style. KAT: a->b->c order valid, cycle detected | TESTED ✓ |
| 174 | Binary min-heap / priority queue | forge_heap.nova | grep:heap_pop | heap_push/heap_pop (O(log n) sift up/down) + peek/size/empty. Array-backed, explicit logical size (no list-shrink). Scheduling/Dijkstra/top-K. KAT pop order dbac by priority | TESTED ✓ |
| 175 | Fixed-capacity ring/circular buffer | forge_ring.nova | grep:ring_push | ring_push (overwrite oldest when full) + ring_to_list (oldest->newest) + size/full/oldest. Pre-sized array+head/count, O(1). Log tailing / sliding windows / metric sampling. KAT cap-3+5=cde | TESTED ✓ |
| 176 | Union-Find / disjoint-set (connected components, clustering, MST) | forge_union_find.nova | grep:uf_union | uf_union/uf_find (path compression + union by size) + uf_connected + uf_size. KAT: connectivity+sizes 3/2/5 correct. Kruskal MST / dedup grouping | TESTED ✓ |
| 177 | Trie / prefix tree (autocomplete, type-ahead) | forge_trie.nova | grep:trie_complete | trie_insert/contains/has_prefix/complete (nested-dict, $end marker). KAT: contains car=true ca=false, complete car=2 ca=3. Search suggestions/command completion | TESTED ✓ |
| 178 | Glob wildcard matching (routes/ACLs/topics) | forge_wildcard.nova | grep:wildcard_match | wildcard_match (* run, ? one char, iterative backtrack no recursion) + wildcard_any (glob list). Route patterns/file filters/ACL/pub-sub topics. KAT all correct | TESTED ✓ |
| 179 | POSIX shell-argument quoting (command-injection-safe) | forge_shellquote.nova | grep:shell_quote | shell_quote (single-quote wrap, embedded ' -> ''' escape) + shell_join + shell_is_safe. Safe subprocess/CLI command lines. KAT: a'b -> 'a'''b' exact | TESTED ✓ |
| 180 | Compact bit set / bit vector | forge_bitset.nova | grep:bitset_set | bitset_set/test/clear + popcount + union/intersect. 63-bit words (dodges 1<<64). Feature flags/permission masks/sieve/presence. KAT cross-word bits + set-ops correct | TESTED ✓ |
| 181 | Natural / human sort (file2<file10, version-aware) | forge_natsort.nova | grep:natcompare | natcompare/natless: digit-runs compared numerically (strip leading zeros), else lexical. File managers, version lists. KAT file2<file10, v1.9<v1.10, x007==x7 | TESTED ✓ |
| 182 | Unweighted graph BFS shortest-path + reachability | forge_graph.nova | grep:graph_bfs_path | graph_edge/biedge + graph_bfs_path (shortest, head-index queue) + reachable + distance. Degrees-of-separation, dependency reachability, routing. KAT a-e-d=2 beats a-b-c-d=3 | TESTED ✓ |
| 183 | POSIX path utilities (basename/dirname/extname/join/normalize) | forge_path.nova | grep:path_normalize | path_basename/dirname/extname/join + normalize (resolves ./.. collapses //). URL paths/storage keys/routes. KAT /a/./b/../c=/a/c, a/b/../../c=c | TESTED ✓ |
| 184 | CRC-32 checksum (data integrity) | forge_checksum.nova | grep:crc32 | crc32 + crc32_hex, bitwise IEEE 802.3 (poly 0xEDB88320, no table). File/blob integrity, network frames, dedup. KAT EXACT vs standard: 123456789=0xCBF43926, hello=0x3610a686 | TESTED ✓ |
| 185 | Hex encode/decode + hexdump | forge_hex.nova | grep:hex_encode | hex_encode/hex_decode (byte<->2-hex) + hex_spaced. Hexdump/wire-debug/binary-in-text. NUL-decode caveat noted. KAT Hi!=486921 round-trip. Complements base32/62/64 | TESTED ✓ |
| 186 | Fake seed-data generator (Faker/factory_bot) | forge_faker.nova | grep:fake_name | fake_name/email/username/sentence, deterministic from a seed (reproducible or varied). Seed dev DBs, demos, load tests. KAT deterministic+varied | TESTED ✓ |
| 187 | Password + passphrase generator (CSPRNG) | forge_pwgen.nova | grep:fn pwgen | pwgen/pwgen_alnum/pwgen_digits + Diceware passphrase. NUL-safe byte->char, shell/JSON-safe symbols, modulo-bias documented. Generator side of forge_pwpolicy. KAT verified | TESTED ✓ |
| 188 | Duration-string parse ('2h30m'->ms) | forge_duration_parse.nova | grep:parse_duration_ms | parse_duration_ms/seconds (ms/s/m/h/d/w, ms-before-m). Inverse of forge_duration humanize. Config timeouts/TTLs. KAT 2h30m=9000000, 1d=86400s | TESTED ✓ |
| 189 | Run-length encoding | forge_rle.nova | grep:rle_encode | rle_encode/rle_decode (aaabbc<->3a2b1c). Sparse bitmaps, telemetry, sprite/terminal data. KAT round-trip | TESTED ✓ |
| 190 | Base58 encoding (Bitcoin/IPFS, transcribable) | forge_base58.nova | grep:base58_encode | base58_encode/decode (drops ambiguous 0OIl). Crypto addresses, content IDs, shareable tokens. KAT round-trip + ambiguous-char reject. Completes base32/58/62/64 | TESTED ✓ |
| 191 | ISBN-10/13 book identifiers (validate + convert) | forge_isbn.nova | grep:isbn13_valid | isbn10_valid (mod-11,X) + isbn13_valid (EAN mod-10) + isbn_valid + isbn10_to_13. Library/bookstore/publishing. KAT valid/invalid + conversion | TESTED ✓ |
| 192 | SWIFT/BIC bank code validation (ISO 9362) | forge_bic.nova | grep:bic_valid | bic_valid (8/11 chars, letter bank+country, alnum location/branch) + bic_bank/bic_country. Pairs w/ forge_iban for wire/SEPA. KAT verified | TESTED ✓ |
| 193 | GDPR/CCPA consent management | forge_consent.nova | grep:consent_allowed | consent_new (deny-default) + grant/revoke + consent_allowed (necessary always, else opt-in) + encode/decode cookie. Gate analytics/marketing on consent. KAT round-trip | TESTED ✓ |
| 194 | UTM campaign attribution (parse/build) | forge_utm.nova | grep:utm_parse | utm_parse (strip utm_ prefix->dict) + utm_build (campaign-tagged URL) + utm_source. Marketing attribution. KAT verified | TESTED ✓ |
| 195 | Referrer/traffic-source classification | forge_referrer.nova | grep:referrer_channel | referrer_channel (search/social/direct/referral by known domains) + referrer_domain. Analytics attribution. KAT all channels | TESTED ✓ |
| 196 | Gravatar avatar URLs | forge_gravatar.nova | grep:gravatar_hash | gravatar_hash (md5 trim+lowercase email) + gravatar_url (size+default style) + gravatar. Composes forge_crypto md5. KAT EXACT vs documented: MyEmailAddress@example.com=0bc83cb571cd1c50ba6f3e8a78ef1346 | TESTED ✓ |
| 197 | Timezone offset parse/apply (fixed-offset ISO-8601) | forge_tzoffset.nova | grep:tz_offset_minutes | tz_offset_minutes (+05:30/-0800/Z) + tz_to_local/utc + tz_format. User-zone display, offset timestamps. KAT +05:30=330 -0800=-480 | TESTED ✓ |
| 198 | Percentage/money math (discount/tax/change, integer cents) | forge_percent.nova | grep:percent_of | percent_of/bps_of/apply_discount/apply_tax(_bps)/percent_change, integer cents+rounding (no float money errors). KAT 8.25%bps=10825, signed change | TESTED ✓ |
| 199 | YAML config parser (practical subset) | forge_yaml.nova | grep:yaml_parse | yaml_parse->nested dict/list (mappings/nested/scalar-seq/mapping-seq, auto indent step) + yaml_get. Scalars-as-strings. CI/K8s-lite/app config. KAT full nesting incl services list-of-dicts | TESTED ✓ |
| 200 | Locale currency formatting (US/EU from cents) | forge_money_fmt.nova | grep:money_us | money_us (\$1,234.56) + money_eu (1.234,56 EUR): symbol placement, thousands grouping, decimal style, integer cents. Complements forge_money/percent. KAT US/EU/cents/neg/millions | TESTED ✓ |
| 201 | Promo/discount coupons | forge_coupon.nova | grep:coupon_discount | coupon_percent/fixed + coupon_valid (min-order+expiry) + coupon_discount (rounded pct / capped fixed) + coupon_apply. Integer cents. KAT 20%=2000, cap, expiry, below-min | TESTED ✓ |
| 202 | Ordered allow/deny ACL (glob patterns, first-match) | forge_acl.nova | grep:acl_check | acl_allow/acl_deny + acl_check (first-match-wins, default-deny). AWS-IAM/firewall fine-grained resource perms. Composes forge_wildcard, complements forge_rbac. KAT verified | TESTED ✓ |
| 203 | Env-var expansion in strings (config templating) | forge_env_expand.nova | grep:env_expand | env_expand: substitute ${VAR}/$VAR from a vars dict, unknown->'', lone-$ literal. dotenv interpolation/command templates. chr()-built braces. KAT braced/bare/mixed/unknown | TESTED ✓ |
| 204 | JSONPath-lite query ($.a[0].b) | forge_jsonpath.nova | grep:jsonpath_get | jsonpath_get/has (dict-key + array-index + quoted-bracket-key) + jsonpath_tokens. Config lookups, webhook extraction, API mining. Distinct from jsonpointer (RFC 6901). KAT nested/index/tokens | TESTED ✓ |
| 205 | Email normalization for dedup | forge_email_normalize.nova | grep:email_normalize | email_normalize (lowercase+strip +tag; Gmail drops dots+unify domain) + email_same. Prevents duplicate signups. KAT gmail plus/dots/googlemail collapse | TESTED ✓ |
| 206 | robots.txt generation (crawler control) | forge_robots.nova | grep:robots_render | robots_agent/allow/disallow/crawl_delay/sitemap + robots_render. Search-engine + bot crawl control. KAT directives present | TESTED ✓ |
| 207 | sitemap.xml generation (SEO page discovery) | forge_sitemap.nova | grep:sitemap_render | sitemap_add (loc/lastmod/changefreq/priority) + sitemap_render (<urlset>, XML-escaped URLs). Pairs w/ robots. KAT escaping+priority | TESTED ✓ |
| 208 | US SSN validation + PII masking | forge_ssn.nova | grep:ssn_valid | ssn_valid (SSA rules: area!=000/666/900+, group!=00, serial!=0000) + ssn_mask (***-**-1234) + ssn_format. KAT valid+exclusions+mask | TESTED ✓ |
| 209 | URL shortener (bit.ly-style) | forge_shortlink.nova | grep:shortlink_code | shortlink_code/id (id<->base62) + shortlink_url + shortlink_resolve. Composes forge_base62. KAT 1000000<->4c92 + URL build/resolve | TESTED ✓ |
| 210 | Business-day arithmetic (weekend-skipping) | forge_business_days.nova | grep:add_business_days | is_business_day + business_days_between + add_business_days (skip Sat/Sun). SLA deadlines, delivery/invoice dates. Composes forge_date. KAT Fri+1biz=Mon, Fri+5biz=next-Fri | TESTED ✓ |
| 211 | Age from birthdate + age-gating | forge_age.nova | grep:age_years | age_years (birthday-passed-aware) + age_at_least (18+/21+ gating). Eligibility, age-gates, profiles. KAT birthday-today=26/tomorrow=25/exactly-18 | TESTED ✓ |
| 212 | Countdown / deadline labels | forge_countdown.nova | grep:days_until | days_until + is_past + countdown_label (today/tomorrow/yesterday/in N days/N days ago). Deadlines, trial expiry, timers. Composes forge_date. KAT | TESTED ✓ |
| 213 | ISO 3166 country data + validation | forge_country.nova | grep:country_name | country_valid/name/alpha3/calling_code/from_alpha3 (50 countries, alpha2<->alpha3<->name<->tel). Address forms, dropdowns, shipping/tax, phone codes. KAT | TESTED ✓ |
| 214 | HTTP Basic + Bearer auth header parse/build | forge_basic_auth.nova | grep:basic_auth_parse | basic_auth_header (Basic base64) + basic_auth_parse (->[user,pass]) + bearer_from_header (renamed vs core bearer_token(req)). RFC 7617/6750. KAT base64 round-trip | TESTED ✓ |
| 215 | ISO 4217 currency data | forge_currency.nova | grep:currency_decimals | currency_valid/symbol/name/decimals (26 currencies; JPY/KRW=0, BHD/KWD=3, else 2). Multi-currency checkout, price display, rounding. Complements money/country. KAT | TESTED ✓ |
| 216 | Country-code -> flag emoji | forge_flag.nova | grep:flag_emoji | flag_emoji: 2-letter code -> flag via regional-indicator symbols (4-byte UTF-8 astral, /,% no shifts). Country pickers/locale badges. KAT US/GB=8 bytes, 0xF0 lead. Distinct from forge_flags | TESTED ✓ |
| 217 | IPv6 address validation (:: compression) | forge_ip6.nova | grep:ip6_valid | ip6_valid (8-group hex + single :: zero-compression) + ip6_is_loopback. Complements IPv4 forge_ipcidr. KAT full/compressed/::/bad-hex/double-:: | TESTED ✓ |
| 218 | ISO 639 language data + RTL | forge_language.nova | grep:language_name | language_valid/name/native + language_is_rtl (ar/he/fa/ur/yi) + language_dir (rtl/ltr). 41 languages. i18n pickers, Accept-Language, HTML dir. Complements forge_country | TESTED ✓ |
| 219 | Have-I-Been-Pwned k-anonymity password check | forge_pwned.nova | grep:pwned_prefix | pwned_prefix (5-char range query) + pwned_suffix (35-char local match) + pwned_count/is_breached. Password never leaves client. Composes forge_crypto sha1. KAT EXACT SHA1('password')=5BAA6... | TESTED ✓ |
| 220 | Multimap / group-by (key->list) | forge_multimap.nova | grep:mm_group_by | mm_add/get/count/keys + mm_group_by (group record dicts by a field). Inverted indexes, grouping, dup collection. KAT multi-value + group_by | TESTED ✓ |
| 221 | Frequency counter (Counter) | forge_counter.nova | grep:counter_most_common | counter_add/add_n/get/most_common/of/distinct. Word freq, tag counts, top-N, histograms. KAT a=3/most-common/add_n. Confirms int-in-dict increment sound | TESTED ✓ |
| 222 | String set (union/intersect/diff) | forge_set.nova | grep:set_union | set_new/add/has/remove/size/of + union/intersect/diff. Dedup, tag/permission sets. Dict-backed. KAT dedup + set algebra (union=4/inter=2/diff=1) | TESTED ✓ |
| 223 | In-memory list utilities (page/chunk/flatten/unique) | forge_listutil.nova | grep:list_chunk | list_page (paginate loaded array) + list_chunk (batch) + list_flatten + list_unique (order-preserving dedup). Pairs w/ forge_pagination. KAT all + nested-list access | TESTED ✓ |
| 224 | US state data (50 + DC) | forge_us_states.nova | grep:state_name | state_valid/name/code (2-letter<->full, reverse) + state_count=51. Address forms, dropdowns, shipping/tax. Complements forge_country. KAT ca=California/Texas=TX/51 | TESTED ✓ |
| 225 | Deep structural equality | forge_deep_equal.nova | grep:deep_equal | deep_equal recursive over nested dicts/lists/scalars (dict order-insensitive, list order-sensitive). Test assertions, change detection, cache keys. KAT nested eq true->false | TESTED ✓ |
| 226 | Numeric helpers (clamp/map-range) | forge_numeric.nova | grep:map_range | clamp/num_min/num_max/map_range/percent_in_range. UI layout, gauges, progress bars, bounds. KAT clamp=10/map=50/pct=38 | TESTED ✓ |
| 227 | Unit conversions (temp/distance/weight) | forge_units.nova | grep:c_to_f | c_to_f/f_to_c/c_to_k + mi_to_km/km_to_mi + lb_to_kg/kg_to_lb (integer-rounded). Weather/fitness/international. KAT 100C=212F/5mi=8km/100lb=45kg | TESTED ✓ |
| 228 | HTTP status reason phrases + categories | forge_http_status.nova | grep:status_reason | status_reason (25 codes) + status_category + is_success/redirect/client_error/server_error + is_retryable (408/429/5xx!=501). Response building, retry logic, logging. KAT | TESTED ✓ |
| 229 | Content-Type header parse/build | forge_content_type.nova | grep:content_type_parse | content_type_parse (media + charset/boundary params, unquoted) + content_type_media/charset + content_type_build. Complements forge_mime/negotiate. KAT | TESTED ✓ |
| 230 | Retry-After header parse (seconds/HTTP-date) | forge_retry_after.nova | grep:retry_after_seconds | retry_after_seconds: digits->seconds, HTTP-date->(date-now) floored, -1 unparseable. Client backoff 429/503. Composes forge_httpdate. KAT 120s/delta=777/past=0/bad=-1 | TESTED ✓ |
| 231 | Canonical query string (sort params) | forge_query_canon.nova | grep:query_canon | query_canon (sort params -> stable) + query_equal (order-insensitive). Cache keys, request signatures, canonical URLs, dedup. Complements forge_qs. KAT | TESTED ✓ |
| 232 | In-memory KV store with TTL expiry | forge_kv_ttl.nova | grep:kv_ttl_remaining | kv_new/set(ttl)/get/has/ttl_remaining/delete, lazy eviction. Request caches, short-lived tokens, OTP/nonce, rate-limit state. KAT fresh/expired/ttl=70 | TESTED ✓ |
| 233 | Navigation breadcrumbs from path | forge_breadcrumb.nova | grep:breadcrumbs | breadcrumbs(path)->[label,url] (title-cased labels nike-air->Nike Air, cumulative URLs, Home prepended). Nav UIs + JSON-LD BreadcrumbList. KAT | TESTED ✓ |
| 234 | Word-wrap to max width | forge_wordwrap.nova | grep:word_wrap | word_wrap: break lines at word boundaries to a max width, preserve newlines. Terminal/email/plain-text/chat. KAT: width-10 wrap, no line exceeds | TESTED ✓ |
| 235 | Aligned monospace table rendering | forge_table.nova | grep:table_render | table_render(headers,rows): auto column widths + cell padding + separator line. CLI reports, logs, plain-text email. KAT: alignment + dashes | TESTED ✓ |
| 236 | ASCII progress bar | forge_progress.nova | grep:progress_bar | progress_bar (current/total/width -> '[#####-----] 50%', clamped 0-100) + progress_percent. CLI progress, log status, job dashboards. KAT half/full/empty/over/pct | TESTED ✓ |
| 237 | ANSI terminal colors + strip | forge_ansi.nova | grep:ansi_strip | ansi_color/red/green/yellow/blue/bold (SGR+reset) + ansi_strip (non-TTY/logs). CLI tools, test runners. KAT colorize + strip round-trip. Completes CLI toolkit (table/progress/wordwrap/ansi) | TESTED ✓ |
| 238 | Indent/dedent text blocks | forge_indent.nova | grep:dedent | indent (add N spaces to non-empty lines) + dedent (remove up to N leading spaces). Codegen, nested logging, quoting. KAT indent/dedent verified | TESTED ✓ |
| 239 | Reversible sequential-ID obfuscation | forge_obfuscate_id.nova | grep:obfuscate_encode | obfuscate_encode/decode: modular-multiplicative permutation over 2^31-1, inverse via extended Euclid. Hide row order/count in public URLs, no lookup. Bijective, pair w/ base62. KAT round-trip | TESTED ✓ |
| 240 | TSV parse/build (spreadsheet/export) | forge_tsv.nova | grep:tsv_parse | tsv_parse (rows of fields, 
| 241 | Nested dict flatten/unflatten (dotted keys) | forge_json_flatten.nova | grep:json_flatten | json_flatten (nested -> {a.b.c: value}) + json_unflatten. Config diffing, form serialization, analytics events. KAT a.b.c round-trip | TESTED ✓ |
| 242 | CSPRNG random selection (pick/shuffle/sample) | forge_random_pick.nova | grep:pick_one | pick_one + shuffle (Fisher-Yates) + sample (without replacement). A/B bucketing, raffles, sampling, randomized order. secure_bytes. KAT pick/shuffle/sample | TESTED ✓ |
| 243 | Monotonic stopwatch (profiling/benchmarks) | forge_stopwatch.nova | grep:sw_elapsed_ms | sw_start + sw_elapsed_ms/us/ns via clock_ns (monotonic, NTP-immune). Profiling, benchmarks, latency. KAT: real sleep(50)->>=40ms | TESTED ✓ |
| 244 | Boolean string parsing | forge_boolean.nova | grep:parse_bool | parse_bool (yes/true/1/on/y/t->true) + parse_bool_default (unknown->default) + bool_str. Config, checkboxes, env vars. Complements dotenv. KAT truthy/falsy/default | TESTED ✓ |
| 245 | Cron next-fire-time computation | forge_cron_next.nova | grep:cron_next | cron_next (next epoch-minute matching expr, bounded) + cron_minutes_until. Composes forge_cron cron_matches + forge_date. Job schedulers, next-run displays. KAT: every-min->+1, hourly<=60, Feb30->-1 | TESTED ✓ |
| 246 | Semver version bumping | forge_semver_bump.nova | grep:bump_major | bump_major/minor/patch (semver rules, prerelease dropped). Release tooling. Composes forge_semver. KAT 1.2.3->2.0.0/1.3.0/1.2.4, v-prefix | TESTED ✓ |
| 247 | Time-of-day formatting (HH:MM:SS / 12-hour) | forge_clock.nova | grep:clock_12h | clock_hms/hm/clock_12h (AM/PM) + clock_from_epoch. Complements forge_date/duration. KAT 45296s=12:34:56, noon/midnight/1PM/9AM. Completes date/time domain | TESTED ✓ |
| 248 | Keep-a-Changelog builder | forge_changelog.nova | grep:changelog_render | changelog_new/add (Added/Changed/Deprecated/Removed/Fixed/Security) + changelog_render (versioned markdown, non-empty only). Release notes, pairs w/ semver_bump. KAT | TESTED ✓ |
| 249 | JSON pretty-printer (indented) | forge_json_pretty.nova | grep:json_pretty | json_pretty (parse+re-emit indented, recursive) + json_pretty_value. Config, API debugging, log inspection. Braces via chr(), bool->0/1 noted. KAT nested indent | TESTED ✓ |
| 250 | Recursive PII masking (nested payloads) | forge_json_mask.nova | grep:json_mask | json_mask: deep-redact sensitive keys to *** at ANY depth (dict/list), non-destructive copy. Deep GDPR logging of nested payloads. Complements flat forge_mask. KAT top+nested masked, original intact | TESTED ✓ |
| 251 | Day-of-year / week-of-year | forge_ordinal_date.nova | grep:date_day_of_year | date_day_of_year (1-366) + date_week_of_year + date_days_left_in_year (leap-aware). Progress-of-year, week numbering. Composes forge_date. KAT Jul3=184/Dec31=365/leap366 | TESTED ✓ |
| 252 | IPv4 subnet math (network/broadcast/netmask) | forge_subnet.nova | grep:subnet_network | subnet_size/network/broadcast/netmask from CIDR (boundary-floor, no bitwise-AND). Network planning, IP allocation. Complements forge_ipcidr. KAT /24 + /16 | TESTED ✓ |
| 253 | MAC address validation/normalization | forge_mac.nova | grep:mac_normalize | mac_valid (colon/hyphen/Cisco-dot) + mac_normalize (lowercase colon) + mac_oui (vendor prefix). Device tracking, DHCP, ARP. KAT 3 forms + normalize + OUI | TESTED ✓ |
| 254 | RFC 1123 hostname/domain validation | forge_hostname.nova | grep:hostname_valid | hostname_valid (labels 1-63, no leading/trailing hyphen, <=253, FQDN-tolerant) + hostname_tld. URL/email-domain, TLS SNI, allow-lists. KAT valid/hyphen/empty/FQDN/TLD | TESTED ✓ |
| 255 | Port validation + service names + scheme defaults | forge_port.nova | grep:port_for_scheme | port_valid (1-65535) + port_service (well-known) + port_for_scheme (http=80/https=443/redis=6379). URL parsing, firewall, config. KAT | TESTED ✓ |
| 256 | Deterministic UUIDv5 (name-based) | forge_uuid5.nova | grep:uuid5 | uuid5 (SHA-1 namespace||name, v5+variant) + uuid5_ns_dns/url + uuid5_dns. Idempotent storage-free ids. Composes forge_crypto sha1. KAT EXACT vs RFC: uuid5(DNS,'example.com')=cfbff0d1-...ae17 | TESTED ✓ |
| 257 | Practical email validation | forge_email_validate.nova | grep:email_valid | email_valid (local + one @ + valid domain-with-TLD via forge_hostname) + email_domain. Signup/contact forms. Complements email_normalize. KAT valid/complex + rejections | TESTED ✓ |
| 258 | Deterministic identicon pattern | forge_identicon.nova | grep:identicon | identicon(seed)->[color_hex, 5x5 symmetric 0/1 grid]. GitHub-style avatars, render SVG/CSS, no storage. KAT 5x5/mirror-symmetry/determinism. Completes avatar domain | TESTED ✓ |
| 259 | base64url encode/decode (JWT/URL tokens) | forge_base64url.nova | grep:base64url_encode | base64url_encode/decode (RFC 4648 §5, +->- /->_ no padding, re-pad on decode). JWT/JWS/WebAuthn/URL tokens. KAT no-padding+round-trip+URL-safe. Completes encoding family | TESTED ✓ |
| 260 | JWT unverified decode/inspect | forge_jwt_decode.nova | grep:jwt_decode_payload | jwt_decode_payload/header (base64url+json_decode, NO verify) + jwt_is_expired. Claim inspection/debug/gateway tokens. forge.nova jwt_verify=signed. Composes base64url. KAT sub/alg/exp | TESTED ✓ |
| 261 | Flesch readability scoring | forge_readability.nova | grep:flesch_reading_ease | flesch_reading_ease + flesch_kincaid_grade (syllable-group estimate, integer). Content/SEO/education/UX-writing quality. KAT: simple=116 exact, dense=far lower | TESTED ✓ |
| 262 | HTML to plain text | forge_html_to_text.nova | grep:html_to_text | html_to_text: strip tags + block-close->newline + entity decode. Email plain-text fallback, search indexing, previews. Composes forge_htmlentities. KAT text/tags/entities/newlines | TESTED ✓ |
| 263 | ASCII/control-char helpers | forge_ascii.nova | grep:has_control | is_ascii/is_printable + strip_control + has_control (rejects control incl ESC/DEL for header-injection safety). Input sanitization, protocol validation. KAT control-detection/strip/printable | TESTED ✓ |
| 264 | Byte-size string parsing | forge_bytesize.nova | grep:parse_bytesize | parse_bytesize ('1.5GB'/'500MB'->bytes, base-1024, 1 decimal). Inverse of humanize_bytes. Config max-upload/memory/quota limits. KAT 1.5GB=1610612736/TB/spaced | TESTED ✓ |
| 265 | Tip/gratuity + bill-split math | forge_tip.nova | grep:split_bill | tip_amount/tip_total + split_bill (per-person + remainder surfaced) + tip_split. Integer cents, no rounding loss. Restaurant/bill-split apps. KAT 18%=1800, \$100/3=3333r1 | TESTED ✓ |
| 266 | Roman numeral encode/decode | forge_roman.nova | grep:to_roman | to_roman (1-3999, subtractive) + from_roman. Copyright years, outlines, clock faces. KAT 1994<->MCMXCIV, 2024=MMXXIV, round-trip | TESTED ✓ |
| 267 | base36 encode/decode (case-insensitive) | forge_base36.nova | grep:base36_encode | base36_encode/decode (0-9a-z, case-insensitive). Reddit-style ids, transcribable short codes. Completes radix family (32/36/58/62/64). KAT 1295<->zz round-trip | TESTED ✓ |
| 268 | Integer math (gcd/lcm/fraction/isqrt) | forge_math.nova | grep:reduce_fraction | gcd + lcm + reduce_fraction + isqrt (Newton). Ratios, aspect ratios (1920x1080->16:9), LCM scheduling. KAT gcd=6/lcm=12/16:9/isqrt | TESTED ✓ |
| 269 | int<->binary + popcount | forge_binary.nova | grep:to_binary | to_binary/padded/from_binary + popcount. Bit flags/bitmask display, protocol fields. KAT 10=1010, round-trip, popcount 7=3/255=8 | TESTED ✓ |
| 270 | Aspect-ratio + fit-to-box | forge_ratio.nova | grep:fit_within | fit_within (scale preserving aspect into box) + ratio_simplify + ratio_string (16:9). Image/video sizing, thumbnails, layout. Composes forge_math gcd. KAT fit + 16:9/4:3 | TESTED ✓ |
| 271 | RGB color blend/lighten/darken | forge_color_blend.nova | grep:color_blend | color_blend (linear RGB interp) + color_lighten/darken. Gradients, tints, hover states, overlays. Composes forge_color. KAT blend (floor)/darken-exact/100%=hex2 | TESTED ✓ |
| 272 | Color gradient + heatmap lookup | forge_gradient.nova | grep:gradient_at | gradient (N interpolated colors) + gradient_at (value->color heatmap, clamped). Data viz, heatmaps, chart series. Composes forge_color_blend. KAT endpoints+heatmap. Completes color domain | TESTED ✓ |
| 273 | Server-side SVG builders | forge_svg.nova | grep:svg_document | svg_rect/circle/line/text/document (element+doc, XML-escaped text). Charts, badges, icons, identicon render, sparklines. KAT svg/rect/circle/escaped/closed | TESTED ✓ |
| 274 | Unicode block-char sparklines | forge_sparkline.nova | grep:sparkline | sparkline(list)->U+2581..U+2588 inline chart, auto-scaled 8 levels; sparkline_points(). Dashboards/log-trends/CLI metrics. KAT exact-byte glyphs (ramp/flat/single/empty/neg/desc) | TESTED ✓ |
| 275 | ORM/REST naming inflections | forge_naming.nova | grep:tableize | classify(table->model)/tableize(model->table)/humanize_label(col->label)/foreign_key(assoc->fk). Composes forge_case+forge_inflect. KAT 12 Rails vectors | TESTED ✓ |
| 276 | CloudEvents v1.0 (CNCF) envelopes | forge_cloudevents.nova | grep:cloudevent | vendor-neutral event envelope (Knative/EventBridge/Kafka interop). build/full/valid(required-attrs)/to_json/from_json structured mode/is(routing). KAT 12: attrs serialized, invalid rejected, round-trip | TESTED ✓ |
| 277 | logfmt structured logging | forge_logfmt.nova | grep:logfmt_encode | logfmt_encode(dict)->key=value line + logfmt_decode(line)->dict; selective quoting (space/=/quote), backslash-escape, bare-key->true. KAT 10 round-trip+quoting | TESTED ✓ |
| 278 | Stable A/B/n variant assignment | forge_targeting.nova | grep:ab_variant | ab_variant(feature,user,variants) sticky per-subject arm, no store, cross-node consistent. Complements rollout_on(bool%). KAT deterministic/in-set/uniform 500-500 + 300-299-301 | TESTED ✓ |
| 279 | Test-harness response decomposition + assertions | forge_test.nova | grep:assert_json_field | response_status/response_body + assert_header(header-scoped)/assert_not_contains/assert_json_field(jsonPath-style). MockMvc/Django-Client depth. KAT 10 synthetic-response | TESTED ✓ |
| 280 | CloudEvents HTTP binary content mode | forge_cloudevents.nova | grep:cloudevent_to_headers | cloudevent_to_headers(attrs->ce-* + datacontenttype->content-type, data=body) + from_headers reverse. Knative/EventBridge HTTP delivery. KAT 12 round-trip | TESTED ✓ |
| 281 | JSON:API v1.0 document builder | forge_jsonapi.nova | grep:jsonapi_resource | resource objects (type/id/attributes) + to-one relationships + data/errors documents + vnd.api+json media type. jsonapi.org hypermedia (alongside HAL). KAT 12 serialize | TESTED ✓ |
| 282 | W3C Server-Timing header | forge_server_timing.nova | grep:st_header | st_metric/st_metric_desc/st_name + st_header join -> Server-Timing value (backend timings to browser DevTools). Integer-ms. KAT 6 grammar+join+edges | TESTED ✓ |
| 283 | RFC 9530 Content-Digest (SHA-256) | forge_content_digest.nova | grep:content_digest_sha256 | content_digest_sha256(body)->sha-256=:b64: + verify(). Body integrity (webhooks/uploads). Composes forge_crypto. KAT vs independent SHA-256 b64 vectors + tamper | TESTED ✓ |
| 284 | WWW-Authenticate Bearer/Basic challenge | forge_www_authenticate.nova | grep:bearer_error | bearer_challenge/bearer_error(realm,error,desc,scope)/basic_challenge. RFC 6750/7617 401 challenge, quoted+escaped params, empty optionals omitted. KAT 5 | TESTED ✓ |
| 285 | Permissions-Policy + Clear-Site-Data builders | forge_browser_policy.nova | grep:permissions_policy | pp_none/self/any/allow(origins)+permissions_policy join (precise feature allowlists beyond static security_headers) + clear_site_data(logout purge). KAT 9 | TESTED ✓ |
| 286 | Covariance + Pearson correlation + linear regression | forge_stats.nova | grep:stats_correlation | stats_covariance/correlation(Pearson r)/linreg_slope/linreg_intercept (OLS). /n matches variance so ratios cancel; zero-var+len guards. KAT 12 hand-computed incl r=0.7746 | TESTED ✓ |
| 287 | CBOR encoder (RFC 8949) | forge_cbor.nova | grep:cbor_encode | cbor_uint/int(major0/1 min-width)/text/array(recursive)/bool/null. Binary encoding under COSE/WebAuthn/IoT (distinct from msgpack). KAT 20 vs RFC 8949 Appendix A | TESTED ✓ |
| 288 | Base45 encoder (RFC 9285) | forge_base45.nova | grep:base45_encode | base45_encode(byte-list) QR-alphanumeric encoding (EU Digital COVID Cert payload). 2 bytes->3 digits LSB-first. Encoding family w/ base32/58/62/64. KAT 6 vs RFC 9285 | TESTED ✓ |
| 289 | HSL-inverse + HSV<->RGB color conversions | forge_color.nova | grep:color_hsl_to_rgb | color_hsl_to_rgb (inverse of rgb_to_hsl) + color_rgb_to_hsv + color_hsv_to_rgb (picker/theme model). KAT 16 exact + 4 round-trips within ±2 (integer HSL/HSV lossy) | TESTED ✓ |
| 290 | Ascii85 (base-85) encoder | forge_ascii85.nova | grep:ascii85_encode | ascii85_encode(byte-list): 4B->5 chars '!'..'u', zero-group->z, partial->n+1. PDF/PostScript/git-diff encoding. KAT 6 vs PUBLISHED 'Man '->'9jqo^' + partial/multi | TESTED ✓ |
| 291 | ISIN validation (ISO 6166) | forge_isin.nova | grep:isin_valid | isin_valid + isin_check_digit for securities IDs (Luhn-mod-10 after A=10..Z=35 expansion). Fintech w/ IBAN/BIC/card. KAT real Apple/BAE valid + hand-verified cd=5 + 4 reject classes | TESTED ✓ |
| 292 | CUSIP validation | forge_cusip.nova | grep:cusip_valid | cusip_valid + cusip_check_digit (North-American securities id, companion to ISIN). Sum-digits Luhn, even-doubling, */@/# specials. KAT published Apple/Cisco + hand-verified cd 0/2 + rejects | TESTED ✓ |
| 293 | Point-in-polygon (ray casting) + area | forge_polygon.nova | grep:point_in_polygon | point_in_polygon(even-odd, convex+concave) + polygon_area(shoelace). Geofencing forge_geo lacks (geo_within=circle only). KAT 13 incl concave-notch-outside + exact areas | TESTED ✓ |
| 294 | VIN validation (ISO 3779) | forge_vin.nova | grep:vin_valid | vin_valid + vin_check_char (17-char, position-9 weighted-mod-11 check, X=10, I/O/Q forbidden). Automotive/logistics; w/ ISIN/CUSIP/IBAN. KAT classic 1HGCM82633A004352 + rejects | TESTED ✓ |
| 295 | Dense matrix linear algebra | forge_matrix.nova | grep:mat_mul | mat_mul(conformable/non-square)/transpose/identity/add + mat_det2/det3. Row-major, exact int results. Graphics/physics/ML host math. KAT 9 known products+dets(-2/-306/1) | TESTED ✓ |
| 296 | 3D vector math (vec3) | forge_vec3.nova | grep:vec3_cross | vec3_add/sub/scale/dot/cross/length/normalize. 3D graphics/physics/games (Reactor) + geometric ML; companion to matrix. Zero-safe. KAT 11 incl right-handed cross + orthogonality | TESTED ✓ |
| 297 | Complex-number arithmetic | forge_complex.nova | grep:complex_mul | complex_add/sub/mul/div/conj/abs/arg over [re,im]. FFT/signal-processing/control/AC math; companion to vec3/matrix. KAT 10: (1+2i)(3+4i)=-5+10i, i^2=-1, |z|=5, div round-trip, arg(i)=pi/2 | TESTED ✓ |
| 298 | CRC-16 (CCITT-FALSE + XMODEM) | forge_crc16.nova | grep:crc16_ccitt | crc16_ccitt(init FFFF)/crc16_xmodem(init 0)/crc16_hex, poly 0x1021 MSB-first. 16-bit companion to CRC-32 for Modbus/XMODEM/USB. KAT std check '123456789'=0x29B1/0x31C3 | TESTED ✓ |
| 299 | Damerau-Levenshtein edit distance | forge_damerau.nova | grep:damerau_levenshtein | damerau_levenshtein (Levenshtein + adjacent-transposition=1 edit, OSA). Better fuzzy/spell-check than plain Levenshtein. Flat 1D DP (sound). KAT 9 incl teh->the=1 | TESTED ✓ |
| 300 | Quaternion 3D rotation math | forge_quat.nova | grep:quat_mul | quat_identity/mul(Hamilton)/conj/norm/normalize/from_axis_angle/rotate_vec. Gimbal-free 3D rotation (Reactor); completes vec3/matrix/complex. KAT 8 incl i*j=k, rotate-90/180-z. Found+fixed a sqrt float-return codegen edge | TESTED ✓ |
| 301 | Base58Check encoding (Bitcoin addresses) | forge_base58check.nova | grep:base58check_encode | base58_encode_bytes(byte-array bignum, leading-zero->1) + base58check_encode(version+payload+double-SHA256 checksum). Composes forge_crypto. KAT vs CANONICAL genesis address 1A1zP1eP5... | TESTED ✓ |
| 302 | Canonical hexdump -C byte dump | forge_hexdump.nova | grep:hexdump | hexdump(byte-list)->offset+16 hex(two 8-cols)+ASCII gutter(printable/dot). Binary-payload debugging. Avoids 1<<64 edge. KAT 8 offset/hex/gap/gutter/dots/multirow | TESTED ✓ |
| 303 | Bech32 encoding (BIP 173) | forge_bech32.nova | grep:bech32_encode | bech32_encode(hrp,5-bit data) + BCH-GF(32) 6-symbol checksum. SegWit bc1/BOLT11/Nostr/Cosmos. Avoids 1<<64 edge. KAT exact BIP173 a12uel5l + abcdef1...mqqqxw + max-len 90 | TESTED ✓ |
| 304 | Porter stemmer (1980) | forge_stemmer.nova | grep:fn stem | stem(word) English root reduction (running->run, generalization->gener) for search-index term unification. Faithful 5-step spec (measure/*v*/*d/*o). KAT 29 canonical Porter pairs, all steps | TESTED ✓ |
| 305 | Search tokenizer + stop-words + n-grams | forge_tokenize.nova | grep:text_tokenize | text_tokenize + is_stopword/remove_stopwords + ngrams. Completes search-index pipeline w/ Porter stemmer (tokenize->stopwords->stem->index). KAT 9 | TESTED ✓ |
| 306 | Cosine similarity (document similarity) | forge_cosine.nova | grep:cosine_similarity | cosine_similarity(token lists) term-freq vectors -> (A.B)/(|A||B|), length-independent. Caps search pipeline (more-like-this/near-dup/clustering). sqrt-accumulator dodges float edge. KAT 6 | TESTED ✓ |
| 307 | Set-similarity metrics (Jaccard/Dice/overlap) | forge_setsim.nova | grep:jaccard | jaccard/dice_coef/overlap_coef over token sets (membership not frequency). Tag similarity/near-dup/recs; set-based complement to cosine. KAT 9 | TESTED ✓ |
| 308 | Dijkstra weighted shortest path | forge_dijkstra.nova | grep:dijkstra_dist | wgraph_new/edge + dijkstra_dist + dijkstra_path (weighted least-cost, vs forge_graph unweighted BFS). Routing/network/game-AI pathfinding. KAT least-cost route + unreachable + self | TESTED ✓ |
| 309 | Easing/tweening functions | forge_easing.nova | grep:ease_in_out_cubic | ease_linear + quad/cubic in/out/in-out + lerp. Natural UI transitions + game animation (Reactor); animate via a+(b-a)*ease(t). KAT 14 curve values + endpoints + lerp | TESTED ✓ |
| 310 | GFM strikethrough (markdown) | forge_markdown.nova | grep:_md_inline | ~~text~~ -> <del> added to markdown_to_html inline pipeline (reuses _md_apply_rule). KAT 6 strikethrough + no regression | TESTED ✓ |
| 311 | Bezier curves (quadratic/cubic) | forge_bezier.nova | grep:bezier_cubic | bezier_quad/bezier_cubic/bezier_cubic_path over [x,y]. SVG paths/fonts/animation motion (Reactor); complements easing/vec3. KAT 9 endpoints+midpoints+path | TESTED ✓ |
| 312 | AABB collision + spatial tests | forge_aabb.nova | grep:aabb_intersects | aabb_intersects/contains_point/contains/intersection over [x,y,w,h]. O(1) broad-phase for game physics (Reactor)/hit-testing/spatial index. KAT 10. FOUND mixed float-var-vs-int compare bug | TESTED ✓ |
| 313 | International Morse Code | forge_morse.nova | grep:morse_encode | morse_encode/morse_decode (A-Z 0-9, letters space-sep, words ' / '). Accessibility/ham-radio/reversible codec. KAT 8 SOS/words/roundtrip | TESTED ✓ |
| 314 | Deterministic Lorem Ipsum generator | forge_lorem.nova | grep:lorem_words | lorem_words/sentence/paragraph, deterministic (cycles pool) for stable snapshots. UI mockups/demo-data. KAT 9 | TESTED ✓ |
| 315 | Password strength estimator | forge_pwstrength.nova | grep:password_strength | password_strength(0-4)+label, length+class-diversity heuristic w/ short/all-same penalty. Registration meter; distinct from pwpolicy/pwgen. KAT scoring+ordering+labels | TESTED ✓ |
| 316 | PID controller | forge_pid.nova | grep:pid_step | pid_new/step/control/reset (P+I+D feedback loop) for control systems/robotics/embedded. Float-in-list state persists by ref. KAT 8 incl integral accumulation 2/4/6 | TESTED ✓ |
| 317 | Catmull-Rom spline | forge_spline.nova | grep:catmull_rom | catmull_rom + catmull_rom_path, passes THROUGH control points (vs Bezier). Camera paths/waypoints/data-smoothing. Companion to bezier. KAT 7 through-points + collinear-line | TESTED ✓ |
| 318 | CSS named colors name<->hex | forge_color_name.nova | grep:color_name_to_hex | color_name_to_hex + color_hex_to_name (16 HTML + common CSS names). Theme config/CSS gen/admin UIs. Companion to forge_color. KAT 9 both directions | TESTED ✓ |
| 319 | UI text helpers (list/count/abbreviate) | forge_uitext.nova | grep:abbreviate_number | humanize_list('a, b and c') + pluralize_count('2 files', composes inflect) + abbreviate_number(1.5K/2M/3.2B). Dashboard/notification copy. KAT 16 | TESTED ✓ |
| 320 | Fenwick tree (Binary Indexed Tree) | forge_fenwick.nova | grep:fenwick_add | fenwick_new/add/prefix_sum/range_sum, O(log n) update+query via i&(-i). Leaderboards/frequency/cumulative stats. KAT 9 | TESTED ✓ |
| 321 | Segment tree (range-min-query) | forge_segtree.nova | grep:segtree_query_min | segtree_build/query_min/update, O(log n) RMQ + point update (min not invertible, complements Fenwick sums). Sliding-window min/RMQ. KAT 8 | TESTED ✓ |
| 322 | Count-Min Sketch (frequency estimation) | forge_countmin.nova | grep:cms_estimate | cms_new/add/estimate, fixed-memory per-item count estimate (vs exact-unbounded counter), min-across-rows never under-counts. Heavy-hitters/rate-limit at scale. KAT 7 | TESTED ✓ |
| 323 | Aho-Corasick multi-pattern search | forge_aho.nova | grep:ac_search | ac_new/add/build/search, find ALL of a keyword set in one O(text+matches) pass (vs per-keyword find). Filters/highlighting/IDS. Trie+BFS fail links. KAT classic ushers + multi + empty | TESTED ✓ |
| 324 | Reservoir sampling (Algorithm R) | forge_reservoir.nova | grep:reservoir_sample | reservoir_sample(items,k,seed): uniform k-sample from unknown-length stream, O(k) mem, seeded/reproducible. Log sampling/subsampling. KAT 7 properties | TESTED ✓ |
| 325 | HyperLogLog cardinality estimation | forge_hll.nova | grep:hll_count | hll_new/add/count, distinct-count in KB (unique visitors at scale). Cardinality companion to countmin/reservoir. Harmonic estimator + linear-counting via hand-rolled ln (_hll_ln atanh series). KAT 10000->9407, 500->533 | TESTED ✓ |
| 326 | Deque (double-ended queue, O(1) both ends) | forge_deque.nova | grep:deque_push_front | deque_new/from_list + push/pop/peek _front/_back (all TRUE O(1)) + len/is_empty/to_list/clear. Repr [head,tail,slots-dict], cursors grow monotonically, keys del'd on pop (no leak), no ring wraparound. Work queues/BFS/sliding-window/LRU. Empty->null (single-exit null-init dodges the return-null mistag bug). KAT 25: FIFO/LIFO/both-ends/peek/empty-guards/from_list/clear+reuse/200-elt adversarial drain/mixed-types | TESTED ✓ |
| 327 | Interval scheduling (merge/overlap/gaps) | forge_interval.nova | grep:interval_merge | interval_overlaps/contains/intersect(->null)/merge/insert/total_coverage/gaps over integer [start,end] inclusive ranges. Calendars/booking/availability/rate-windows/HTTP-Range/line-ranges. Insertion-sort by start, touching ranges coalesce. KAT 22: overlap/touch/contains/intersect+null/merge sorted+unsorted+touch+empty+single/insert gap+merge/coverage disjoint+overlap/gaps normal+full+none+edge | TESTED ✓ |
| 328 | Ordered map (sorted key->value, range/floor/ceiling) | forge_omap.nova | grep:omap_floor | omap_new/set/get/has/del/floor/ceiling/min/max/keys/values/range over int OR string keys (NOVA std::map/TreeMap). Binary-search lower_bound O(log n), shift on set/del. Leaderboards/time-series buckets/CIDR-floor/autocomplete/nearest-key. Empty->null (single-exit). KAT 34: sorted insert/update/get-null/floor(below/exact/above)/ceiling/min/max/range/del/empty-queries/STRING-key lexicographic floor+ceiling | TESTED ✓ |
| 329 | MinHash near-duplicate similarity | forge_minhash.nova | grep:minhash_signature | minhash_signature/similarity/estimate: reduce a token set to N ints, fraction-of-matching-slots estimates Jaccard -> scalable near-dup/dedup/plagiarism/clustering (vs O(n^2) exact cosine/setsim). Deterministic hash family h_i(x)=(a_i*x+b_i)%(2^31-1), splitmix coeffs, FNV-1a tokens. GOTCHA: `match` is a RESERVED keyword -> silent mis-codegen as a var (dangling label); renamed to `matches`. KAT 8: siglen/determinism/set-semantics/disjoint-low/Jaccard0.34 est 0.28@128 0.33@256/mismatch-len-0/convenience | TESTED ✓ |
| 330 | Weighted random selection | forge_wrand.nova | grep:wrand_sample_at | wrand_build (cumulative table O(n)) + sample_at(target)/sample_seeded(LCG)/sample_n. O(log n) binary-search upper_bound. A/B-split, canary %, weighted load-balance, loot tables, sample-by-frequency. Integer weights (no float rounding); zero-weight bucket never picked; seeded reproducible. KAT: cum/total/exact target->index mapping/single/zero-weight-skip/determinism/40000-draw distribution EXACT 10000-10000-20000 for [1,1,2] | TESTED ✓ |
| 331 | Streaming top-K by score | forge_topk.nova | grep:topk_threshold | topk_new/add/size/entries/items/scores/threshold: retain the K highest-scored (item,score) from a stream in O(K)/entry, bounded memory (vs O(n log n) full sort). Trending/leaderboards/most-expensive-N/best-N-hits/heavy-hitters shortlist. Entries kept best-first; below-threshold dropped instantly; ties keep earlier. Int scores. KAT 14: top-3 stream/threshold/below-ignored/above-displaces/partial-fill/empty-null/tie-earlier/entry-payload | TESTED ✓ |
| 332 | Number theory (primes/factor/modpow/totient) | forge_primes.nova | grep:mod_pow | is_prime/next_prime/prime_factors/prime_sieve/mod_pow/euler_totient. Crypto (RSA-style a^b mod m), hash-table prime sizing, checksums, math. Trial-division (correct all n, O(sqrt n)); mod_pow uses binary Russian-peasant mulmod -> NO 64-bit overflow up to ~2^62 mod. forge_math already has gcd/lcm; this adds the prime+modular layer. KAT 30: primality incl 1000000007, next_prime, factor 360=[2,2,2,3,3,5], sieve(30), mod_pow, FERMAT a^(p-1)%p==1 @ 10-digit prime, totient | TESTED ✓ |
| 333 | Combinatorics (factorial/nPr/nCr/Catalan) | forge_combinatorics.nova | grep:n_choose | factorial/n_perm/n_choose/n_multichoose/catalan. Counting/odds/probability/A-B-design/subset counts. n_choose = EXACT multiplicative binomial over min(r,n-r) -> integer-exact + overflow-avoiding (returns C(52,5)=2598960 without forming 52!). factorial exact through 20!. KAT 28: factorial 0..20!/nPr/nCr(52,5)+symmetry+edges/multichoose/Catalan 0..5+catalan(10)=16796 | TESTED ✓ |
| 334 | Check digits (Damm + Verhoeff) | forge_checkdigit.nova | grep:damm_generate | damm_generate/validate + verhoeff_generate/validate. Stronger than Luhn (forge_card): catch ALL single-digit errors AND ALL adjacent transpositions (Luhn misses 09<->90). Account/order/national IDs, hand-keyed/spoken numbers. Damm=1 quasigroup table; Verhoeff=D5 dihedral (d/p/inv tables). GOTCHA: NOVA has NO multi-line list literals -> tables MUST be one line each. KAT: Damm 572->4, Verhoeff 236->3 & 12345->1, both reject typo+transposition, roundtrips | TESTED ✓ |
| 335 | Google Encoded Polyline | forge_polyline.nova | grep:polyline_encode | polyline_encode/decode: pack a list of [lat,lng] into a compact URL-safe ASCII string (Google Maps/Directions/Roads format; Leaflet/Mapbox decode it). Store GPS tracks, share routes in a link, shrink payloads. Scale 1e5 -> deltas -> zig-zag -> 5-bit chunks+63. Integer after scaling (no float-codegen risk); ~1e-5 precision. KAT: EXACT canonical Google vector (38.5,-120.2)... -> _p~iF..., decode within tol, roundtrip, single/empty/near-origin track | TESTED ✓ |
| 336 | Online/streaming statistics (Welford) | forge_onlinestat.nova | grep:ostat_add | ostat_new/add + count/mean/variance/sample_variance/stddev/sample_stddev/min/max/zscore. Running stats in O(1) memory single-pass (vs forge_stats batch) - live metrics/telemetry/latencies + anomaly detection via z-score. Welford (numerically stable, no catastrophic cancellation). GOTCHA: float-return sqrt bug -> stddev needed init-0.0-then-reassign-in-branch (bind-to-let alone returned garbage 3e-156). KAT 22: classic 2,4,4,4,5,5,7,9 -> mean5/var4/stddev2/sample-var4.571, z-scores, min/max, single/empty/negative-floats | TESTED ✓ |
| 337 | Moving averages (SMA window + EMA) | forge_movingavg.nova | grep:sma_add | sma_new/add/value/count/full (ring-buffer O(1) rolling mean of last N) + ema_new/ema_new_span/add/value (exponential MA, single number, no history). Smooth a stream - dashboards, sensor readings, trading lines, load smoothing. Complements forge_onlinestat (cumulative) with LOCAL trend. KAT 20: SMA win3 [1,2,3,4,5]->1/1.5/2/3/4 + partial + float; EMA alpha0.5 [1,2,3]->1/1.5/2.25 + span-convention + convergence-to-constant | TESTED ✓ |
| 338 | Financial math (compound/PV/loan/NPV/IRR) | forge_finance.nova | grep:loan_payment | future_value/present_value/compound_interest/loan_payment/amortization_schedule/npv/irr. Fintech - mortgage/loan calcs, subscription billing, savings projection, investment analysis. Rates per-period decimal; pow-based; IRR via bisection. KAT 15: FV 1000@5%x10=1628.89 + PV inverse, real 30yr mortgage 200k@0.5%=1199.10/mo, zero-rate loan, amortization (principal-sum=P, final-bal~0, interest decreasing), NPV=243.43, IRR=0.2337 (NPV@IRR~0) | TESTED ✓ |
| 339 | 2D bounding box toolkit | forge_bbox.nova | grep:bbox_intersects | bbox_from_points/extend/contains/intersects/union/intersection/center/width/height/area over [minx,miny,maxx,maxy]. Map viewport zoom-to-fit, click-in-region, viewport culling / broad-phase collision, spatial-join prefilter. Geographic [lng,lat] or screen [x,y]. General box toolkit (forge_geo.geo_bbox is circle->box). Empty/no-overlap->null. KAT 20: from-points/extend/contains(in,edge,out)/intersects(yes,no,touch)/union/intersection+null/center/area/int-coerce | TESTED ✓ |
| 340 | LEB128 varint encoding (WASM/DWARF) | forge_leb128.nova | grep:uleb128_encode | uleb128_encode/decode + sleb128_encode/decode (signed = sign-extended, the WASM/DWARF convention, distinct from protobuf zig-zag). NOVA compiles to WASM -> this is that target's wire int format; also compact binary protocols/files. Output = list of bytes; decode reads at an offset (chainable). KAT: exact vectors u624485->[229,142,38], u300->[172,2], s(-2)->[126], s(-128)->[128,127], s127->[255,0]; decode; roundtrips ±2e9; sequential offset decode | TESTED ✓ |
| 341 | Secure client-IP extraction (X-Forwarded-For) | forge_clientip.nova | grep:client_ip | parse_forwarded_for + client_ip (SECURE: walk chain right-to-left past trusted-proxy CIDRs, first non-trusted = real client - unspoofable) + client_ip_leftmost (naive, labelled insecure) + is_trusted_proxy/proxy_hop_count. Prevents XFF spoofing of rate-limit/geo/audit identity (Rails/Express trust-proxy, nginx real_ip). Imports forge_ipcidr (ip_in_any). KAT 12: parse, both-trusted, SPOOF-DEFENSE (forged left entries ignored), direct, all-trusted fallback, untrusted-peer ignores XFF, naive, helpers | TESTED ✓ |
| 342 | HTTP Prefer / Preference-Applied (RFC 7240) | forge_prefer.nova | grep:parse_prefer | parse_prefer -> dict + prefer_get/has/wait + preference_applied builder. Client asks server to tweak handling: return=minimal, respond-async, wait=N, handling=lenient (OData/batch/bulk/async REST). Case-insensitive lookups, quoted-value unquoting, token-only prefs map to "". KAT 15: get/default/case-insensitive, token presence, present-but-empty, wait=int + absent=-1, quoted unquote, empty header, Preference-Applied build | TESTED ✓ |
| 343 | k-nearest-neighbors (classify/regress/similarity) | forge_knn.nova | grep:knn_nearest | knn_distance_sq (squared euclidean, no sqrt) + knn_nearest (k closest indices) + knn_classify (majority label, tie->nearest) + knn_regress (avg of k). No-training ML: churn/fraud classification, recommendations, nearest-store, dedup. GOTCHA: nested-list [dist,idx] insertion-sort corrupted (holding a list-elem ref across list-elem reassign) -> use PARALLEL SCALAR arrays; any-float compare needs *1.0 coerce. KAT 12: dist, classify A/B/k1/3D, nearest+clamp, regress, int-coerce | TESTED ✓ |
| 344 | k-means clustering (Lloyd + farthest-point init) | forge_kmeans.nova | grep:kmeans | kmeans -> [centroids, assignments] + kmeans_assign + kmeans_inertia. Unsupervised ML: segmentation, color quantization, grouping vectors/docs, k representatives. Lloyd's algo (assign to nearest center -> move center to member mean -> repeat, early-stop on convergence); DETERMINISTIC farthest-point init (no random seed), empty clusters keep old centroid. Imports forge_knn (distance). KAT 12: 2-cluster (members together, distinct, centroids at true means, small inertia, reassign) + 3-cluster (3 pairs, all distinct) | TESTED ✓ |
| 345 | Naive Bayes text classifier | forge_bayes.nova | grep:bayes_classify | bayes_new/train/classify/score + classes/doc_count. Multinomial naive Bayes: spam-vs-ham, sentiment, topic/intent routing, language detection - fast, tiny, few examples/class. Laplace add-one smoothing (unseen word doesn't zero a class); sums LOG probs (no underflow). Flat "class|token" keys (no nested dicts). Completes ML suite w/ minhash+knn+kmeans. KAT 13: spam/ham classify, unseen-words-defined, score ordering, 2nd sentiment model pos/neg, untrained->"" | TESTED ✓ |
| 346 | Classification eval metrics (P/R/F1/confusion) | forge_evalmetrics.nova | grep:eval_confusion | eval_accuracy/confusion([TP,FP,FN,TN])/precision/recall/f1 + macro_f1/macro_precision. Evaluate a classifier - raw accuracy LIES on imbalanced data; precision/recall/F1/macro-avg tell the truth. Completes ML story (train->classify->EVALUATE) for forge_bayes/knn. KAT 16: acc/confusion/P/R/F1 = 2/3, perfect=1.0, all-wrong=0, IMBALANCED (acc 0.9 but spam-recall 0, macro-F1<0.5), empty | TESTED ✓ |
| 347 | Information theory (entropy/gini/info-gain) | forge_infotheory.nova | grep:shannon_entropy | shannon_entropy(bits)/normalized_entropy/gini_impurity/information_gain + gini_coefficient (inequality). Decision-tree splits (info gain, CART gini), feature selection, diversity, randomness; Gini COEFFICIENT = income/load inequality (0=equal, ->1=concentrated). log2-based. KAT 17 exact: coin=1bit, 4-equal=2bits, 75/25=0.811, gini-impurity coin=0.5/4equal=0.75, info-gain perfect-split=1.0/useless=0, gini-coeff equal=0/maxineq=0.75 | TESTED ✓ |
| 348 | TF-IDF term weighting | forge_tfidf.nova | grep:tfidf_fit | tfidf_fit(corpus)->idf dict + tfidf_vector/tfidf_score/tfidf_idf. Score word importance to a doc within a collection: search relevance ranking, keyword/tag extraction, more-like-this, text->numeric vectors for knn/kmeans/bayes. TF within doc * IDF=ln(N/df); ubiquitous term (idf 0) drops out. KAT 13 exact: idf the=0/sat=ln3=1.099/cat=ln1.5=0.405, tfidf scores, repeated-term higher tf, vector drops "the", empty | TESTED ✓ |
| 349 | Feature scaling (min-max/z-score/unit) | forge_scale.nova | grep:scale_zscore | scale_minmax(+apply)/scale_zscore(+apply)/scale_unit + scale_bounds/scale_stats. ML preprocessing - distance methods (knn/kmeans) are dominated by the largest-range feature; scaling levels them. fit-then-apply for consistent train/test scaling. GOTCHA: scale_unit needed INLINE sqrt (stored/returned sqrt-of-accumulator gave 3e-156 garbage; init-reassign+list-wrap both failed - layout-dependent). KAT 21: minmax [0,5,10]->0/.5/1, zscore mean0/std1, unit [3,4]->.6/.8 len=1, apply, int-coerce | TESTED ✓ |
| 350 | One-hot / label encoding (categorical->numeric) | forge_onehot.nova | grep:onehot_fit | onehot_fit/size/vector/encode/decode + label_encode/onehot_categories. Turn labels (red/green/blue, US/UK) into 0/1 columns so categories stay equidistant/orderless - the correct ML input for knn/kmeans/bayes (vs fake 1/2/3 ordering). label_encode (int index) for the target/tree models. Fit vocab once, encode consistently (unseen -> all-zero / -1). Clean integer. KAT 18: fit first-seen order, label/vector/encode/decode, unknown->zeros, integer categories | TESTED ✓ |
| 351 | A* grid pathfinding | forge_astar.nova | grep:astar_grid | astar_grid (shortest 4-dir path around obstacles) + astar_distance. Games/Reactor unit movement, robot/map navigation, tile-map routing. Heuristic-guided (Manhattan) -> faster point-to-point than Dijkstra, still optimal. Grid 0=open/else blocked; returns [[r,c]...] or []. Open set = PARALLEL scalar arrays + "r,c" string-key dicts (dodges list-of-lists corruption). KAT 10: open dist4/len5, wall-detour dist6, unreachable=-1/[], start==goal=0, contiguous path, maze | TESTED ✓ |
| 352 | Minimum spanning tree (Kruskal) | forge_mst.nova | grep:mst_kruskal | mst_kruskal->[edges,total] + mst_total_weight/mst_edges/mst_spans_all. Cheapest edge set to connect all nodes, no cycles: min-cost network/cabling/road layout, cluster by cutting expensive links, perfect-maze generation. Sort edges by weight (parallel scalar arrays) + union-find cycle check. Edges [u,v,w] string ids/int weights. Imports forge_union_find. KAT 11: triangle total3, classic 4-node total6 (cheap edges, spans), disconnected FOREST, order-independence, single edge | TESTED ✓ |
| 353 | Softmax (logits->probabilities) | forge_softmax.nova | grep:softmax | softmax + softmax_temperature + argmax + softmax_confidence. Turn raw scores into a probability distribution summing to 1 - the final layer of classifiers/NNs, calibrated confidence from bayes/knn scores, weighted sampling; TEMPERATURE knob (softer/sharper, the LLM-sampling knob). Subtracts max for numerical stability. Uses INLINE exp (sum into accumulator + exp inline in division; dodges float-return bug). KAT 14: argmax, uniform-for-equal, known [2,1,0]->[.665,.245,.090] sum=1, monotonic, stability@1000s, temperature softer/sharper, confidence | TESTED ✓ |
| 354 | PageRank (graph node importance) | forge_pagerank.nova | grep:pagerank | pagerank(edges,damping,iters)->dict + pagerank_top. Google's power-iteration ranking - important nodes are pointed to by important nodes: rank web pages/influential users/key papers/co-purchase graphs/trust networks. Damping 0.85 (random surfer); dangling nodes spread rank uniformly (no leak); ranks sum to 1. Edges [from,to] string ids. Reliable float (no transcendentals). KAT 12: cycle->equal .333, hub->highest, star authority (dangling, sum=1), importance flow, empty | TESTED ✓ |
| 355 | DBSCAN density clustering + outliers | forge_dbscan.nova | grep:dbscan | dbscan(points,eps,min_pts)->cluster-ids (-1=noise) + dbscan_num_clusters/noise_count. Finds clusters of ANY shape and flags OUTLIERS, no k needed (vs kmeans's k + round blobs) - anomaly detection, irregular spatial/geo grouping. Core point = >=min_pts neighbors within eps; grows to density-reachable; unreachable = noise. Squared distance (no sqrt). Imports forge_knn. Completes clustering w/ kmeans. KAT 13: 2 clusters+outlier(=noise), all-noise sparse, LINE=1 cluster (kmeans would split) | TESTED ✓ |
| 356 | IQR outlier detection (Tukey/box-plot) | forge_iqr.nova | grep:iqr_bounds | iqr_quartiles([Q1,med,Q3])/iqr_range/iqr_bounds/iqr_is_outlier/iqr_outliers. ROBUST outlier detection (box-plot whiskers): flag values >1.5*IQR beyond Q1/Q3. Rank-based -> extremes don't distort thresholds like they inflate mean/stddev (z-score); right for skewed data, cleaning, honest anomaly detection. Quartiles via linear interp. KAT 15: [1..9] Q1=3/med=5/Q3=7, +50 outlier (fence 14.5), robust unsorted+negative, k=3 far-out, single | TESTED ✓ |
| 357 | Binomial + Poisson distributions | forge_binomial.nova | grep:binom_pmf | binom_pmf/cdf/mean/variance + poisson_pmf/cdf. "How likely is exactly k successes?" - binomial for n yes/no trials (A/B conversion counts, defects, "k of n servers up"), Poisson for rare events at rate lambda (arrivals/sec, requests, typos). Binomial via n_choose*p^k*q^(n-k); Poisson incremental product + INLINE exp (no factorial overflow, no float-return bug). Imports forge_combinatorics. KAT 18 exact: b(10,5,.5)=.246, cdf=.623, mean5/var2.5, sums-to-1; Poisson lambda2 pmf .135/.271/.271/.180, cdf .677, sums-to-1 | TESTED ✓ |
| 358 | Dynamic Time Warping (sequence similarity) | forge_dtw.nova | grep:dtw_distance | dtw_distance + dtw_normalized + dtw_similar. Match two time series even when shifted/stretched (Euclidean fails on lag) - sensor/IoT traces, stock patterns, gesture/handwriting/speech recognition, nearest-historical-series. DP table (flat 1D, dodges nested-list writes) of local dist + cheapest reach (left/below/diag); repeats align for free. KAT 14: identical=0, time-warp-free=0, [1,3]vs[1,2,3]=1, symmetric, shifted-signal-small (beats Euclidean), normalized, threshold, int-coerce | TESTED ✓ |
| 359 | K-fold cross-validation + train/test split | forge_kfold.nova | grep:kfold_split | kfold_split->[train,test] + kfold_test_indices + train_test_split + shuffle_indices. Honest ML evaluation - never score on training data. k-fold partitions into k folds (every row tested once, average k scores w/ evalmetrics = stable estimate); train_test_split = single random holdout. Returns INDEX sets; reproducible via seed (Fisher-Yates LCG). Completes ML workflow (split->train->classify->evaluate). Integer/reliable. KAT 15: fold blocks, remainder [3,2,2,2,2], partition (each idx 1 test fold, covers all), holdout 8/2 disjoint, reproducible, shuffle permutation | TESTED ✓ |
| 360 | Technical indicators (RSI/Bollinger/MACD) | forge_indicators.nova | grep:bollinger | rsi (0-100 momentum, overbought/oversold) + bollinger([upper,mid,lower] +/-k*std) + bollinger_bandwidth + macd_line (fast-EMA minus slow-EMA). Turn a price/KPI/latency series into momentum + volatility signals for trading/monitoring dashboards. Bollinger uses INLINE sqrt; MACD imports forge_movingavg (EMA). KAT 14: RSI all-up=100/all-down=0/balanced=50/75, Bollinger [2,4,4,4,5,5,7,9]->[9,5,1] bandwidth 8, constant->0-width, MACD rising+/falling-/flat~0 | TESTED ✓ |
| 361 | SimHash near-duplicate fingerprinting | forge_simhash.nova | grep:simhash | simhash->32-bit fingerprint + simhash_distance (Hamming) + simhash_similar + simhash_token_distance. Charikar LSH (Google web-dedup): similar docs -> similar fingerprints (small Hamming dist), compare ONE integer instead of full docs. Captures weighted/cosine similarity (vs minhash's Jaccard). Near-dup/content-clustering/plagiarism/change-detection. Each token votes per bit; column sign = fingerprint bit. Integer bit-manip (1<<b, b<=31). KAT 16: identical=0, reordered=0 (order-indep), near-dup small, far>near, symmetric, [0,32], Hamming hand-checks 5^6=2/0^7=3 | TESTED ✓ |
| 362 | Spearman rank correlation | forge_spearman.nova | grep:spearman | spearman(x,y)->rho in [-1,1]. Monotonic (non-parametric) correlation - does Y rise as X does, even non-linearly? Compares RANKS not raw values -> robust to outliers + catches curves Pearson (forge_stats) misses. Feature relevance on skewed/ordinal data, survey/A-B analysis. Rank-difference formula 1-6*Sd2/(n(n2-1)) (NO sqrt, reliable); averaged ranks for ties; parallel-array rank sort. KAT 12: perfect +1/-1, y=x^2 (non-linear monotonic)=1, classic=0.8, unsorted, ties, n<2/mismatch=0, int-coerce | TESTED ✓ |
| 363 | Linear regression + predict + R^2 | forge_linreg.nova | grep:linreg_fit | linreg_fit->[slope,intercept,r2] + linreg_predict/slope/intercept/r2. Fuller least-squares than forge_stats (which has only raw slope/intercept): fit once, PREDICT any x, read R^2 (variance explained; 1=exact, 0=no better than mean). Trend lines/forecasting, calibration, "does X drive Y", what-if prediction. All sums (NO sqrt, reliable). KAT 14: y=2x+1 slope2/int1/r2=1/predict(5)=11, negative slope, flat, noisy r2 in(0,1), known slope0.6/int2.2, n<2=0, int-coerce | TESTED ✓ |
| 364 | Max subarray (Kadane) + product + buy/sell | forge_subarray.nova | grep:max_subarray | max_subarray_sum + max_subarray([sum,start,end]) + max_product_subarray + best_buy_sell. Best contiguous run in O(n): most-profitable window, strongest signal segment, busiest span. Max-product tracks running min too (neg*neg flips positive); buy/sell = one trade max profit. Integer/reliable. KAT 14: Kadane 6@3..6, all-neg->largest, product [2,3,-2,4]=6/negs-flip=12/zero-reset=0/24, buy-sell [7,1,5,3,6,4]=5/falling=0/rising=4 | TESTED ✓ |
| 365 | Lexicographic permutations | forge_permute.nova | grep:next_permutation | next_permutation (constant-mem step, like C++ std::next_permutation) + all_permutations + permutation_count. ENUMERATES orderings (vs combinatorics COUNTING): brute-force small search (schedules/seatings/TSP-few-cities), anagrams, test-input orderings. Elements compared via < (int or string); DISTINCT perms with dups ([1,1,2]->3 not 6). Scalar swaps. KAT 15: 123->132->213->312, last->empty, all [1,2,3] lex (6), 4!=24, distinct-dups, from-unsorted, single/empty, string anagrams abc..cba | TESTED ✓ |
| 366 | Sliding-window max/min (monotonic deque) | forge_slidingmax.nova | grep:sliding_max | sliding_max + sliding_min: max/min of every width-k window in O(n) TOTAL (not O(nk)) via a monotonic deque of candidate indices. Rolling highs/lows on price/sensor series, peak detection over a moving window, moving-max signal processing, "best in the last k". Returns n-k+1 values. COMPOSES forge_deque (O(1) both ends). Integer. KAT 12: classic [1,3,-1,-3,5,3,6,7] k3 max=[3,3,5,5,6,7]/min=[-1,-3,-3,-3,3,3], k1=self, k=n=single, incr/decr/equal, k>n=empty, count=n-k+1 | TESTED ✓ |
| 367 | k-sum (two-sum + three-sum) | forge_ksum.nova | grep:three_sum | two_sum(indices O(n) hash) + has_pair_sum + two_sum_all(distinct value pairs) + three_sum(DISTINCT triples, sort+two-pointer O(n^2)). Find elements adding to a target: reconciliation, budget matching, subset checks, puzzles. Dedups. Integer. KAT 12: two_sum [2,7,11,15]@9=[0,1], none=-1, has-pair, all-pairs, three_sum classic [-1,0,1,2,-1,-4]@0=[-1,-1,2]|[-1,0,1], @9, none, [0,0,0,0]->single | TESTED ✓ |
| 368 | Edit operations (Levenshtein backtrace) | forge_editops.nova | grep:edit_operations | edit_distance + edit_operations (the exact keep/sub/ins/del steps A->B, not just distance like forge_levenshtein). Visual diff, autocorrect suggestions that explain the fix, spell highlighting, text/DNA alignment. Fills Levenshtein DP (flat 1D) then BACKTRACES cheapest path; ops = "keep X"/"sub X Y"/"del X"/"ins Y"; non-keep count == distance. Integer/reliable. KAT 12: kitten->sitting dist3, sub/ins/del/from-empty/to-empty/identical ops, non-keep=dist, RECONSTRUCT b from ops | TESTED ✓ |
| 369 | Convex hull (Andrew monotone chain) | forge_convexhull.nova | grep:convex_hull | convex_hull([x,y] verts CCW) + hull_area (shoelace) + hull_size. Smallest convex polygon enclosing 2D points - shape/track footprint, collision broad-phase boundary, cluster extent, "outer-edge points". Monotone chain: sort + sweep lower/upper chains dropping non-left turns. Only test = cross-product sign (NO sqrt, exact on ints). Index-sort + index-stacks dodge list-of-lists corruption. KAT 14: square+interior->4 corners area9, triangle+interiors->3 area18, collinear dropped, <3 pts, pentagon all-5, float coords | TESTED ✓ |
| 370 | Knapsack + coin change (DP optimization) | forge_knapsack.nova | grep:knapsack_01 | coin_change_min/ways + knapsack_01 + knapsack_unbounded. "Best choice under a budget": fewest coins / ways to make an amount (change machines, reach-target-from-step-sizes), and 0/1 knapsack max value under a weight/cost cap (cargo, budget allocation, feature/portfolio selection under a cap). Rolling integer DP O(n*capacity), exact, no floats. KAT 15: min([1,2,5]@11)=3/[3,7]@12=4/impossible=-1, ways([1,2,5]@5)=4/[2,3,5,6]@10=5, knap01([1,3,4,5],[1,4,5,7]@7)=9, unbounded reuse 130 | TESTED ✓ |
| 371 | Longest increasing subsequence | forge_lis.nova | grep:lis_length | lis_length (patience-sort O(n log n) binary search) + lis (O(n^2) reconstruction of one actual subsequence). Longest upward-trending chain (order-preserving, not adjacent): longest streak of records/improvements, max nesting boxes/tasks, patience solitaire, diff/scheduling building block. Strict < (equal doesn't extend). Integer/reliable. KAT 13: classic [10,9,2,5,3,7,101,18]=4, [0,1,0,3,2,3]=4, all-incr=5, decr=1, all-equal=1, single/empty, reconstruct [3,1,4,1,5,9,2,6]->[3,4,5,9] increasing | TESTED ✓ |
| 372 | Longest common subsequence (string) | forge_lcs.nova | grep:lcs_length | lcs_length + lcs (the shared subsequence string) + lcs_ratio [0,1]. Longest chars in BOTH strings in order (not adjacent) - the shared CONTENT (vs editops counting changes): text/file diff (LCS=unchanged), VCS merge, plagiarism/similarity, DNA/protein alignment. DP table + backtrace. Integer/reliable. KAT 15: ABCBDAB/BDCAB=4, AGGTAB/GXTXAYB->GTAB, identical ratio1.0, none=0/"", empty, subseq ace, night/nacht ratio0.6, kitten/sitting=4 | TESTED ✓ |
| 373 | Value noise (procedural generation) | forge_noise.nova | grep:value_noise_1d | value_noise_1d/2d + fractal_noise_1d (fBm octaves). SMOOTH pseudo-random (vs jagged random): terrain heightmaps, cloud/marble/wood textures, organic animation wobble, world-gen (Reactor). Hash integer lattice -> smoothstep interpolate; fractal layers octaves. Deterministic per seed -> reproducible worlds. [0,1), no sqrt/transcendentals. KAT 12: determ 1d/2d, range[0,1) incl negative, continuity (nearby~nearby), seed-varies, integer-stable, fractal range+determ, 2d-varies | TESTED ✓ |
| 374 | Gray code (reflected binary) | forge_gray.nova | grep:gray_encode | gray_encode (n^(n>>1)) + gray_decode + gray_sequence + gray_bitdiff. Ordering where successive values differ in EXACTLY ONE bit - rotary/position encoders, ADCs, Karnaugh maps (no glitch while bits settle), minimal-transition test sequences, error-tolerant counters. Integer bit math. KAT 12: known enc 2->3/3->2/4->6/7->4, decode inverse, roundtrip 0..100, seq 2bit=[0,1,3,2]/3bit=[0,1,3,2,6,7,5,4], 4-bit single-bit-change cycle (incl wraparound), bitdiff | TESTED ✓ |
| 375 | Rasterization (Bresenham line + circle) | forge_raster.nova | grep:_r_abs | line (Bresenham all-directions) + circle (Jesko midpoint) + rect (outline) -> lists of [x,y] PIXELS. Rasterize (vs forge_svg vector): draw on pixel/LED matrix or tile grid, char-canvas plotting, line-of-sight/laser path across a game map, image-buffer shapes. Pure integer (no float). GOTCHA: naive midpoint-circle variant stepped x too fast (pixel at r=3.6) -> use Jesko err=1-2r. KAT 12: diagonal/horiz/vert/shallow/reversed/point lines, circle pixels within +/-1 radius, rect perimeter=8 | TESTED ✓ |
| 376 | Flood fill + connected components | forge_floodfill.nova | grep:flood_fill | flood_fill (paint bucket, in place, returns count) + region_size (non-destructive) + count_regions. Spread from a cell to all same-value 4-connected neighbors: image-editor bucket, region labeling/blob size, connected-component/island counting, maze reachability, territory scoring. Grid = list of rows of ints; parallel scalar stacks (no list-of-lists corruption); nested grid[r][c] writes verified. KAT 12: fill count3+grid, no-op, whole-grid, region_size non-destructive, count_regions 1s=2/0s=1, OOB=0, single | TESTED ✓ |
| 377 | 2D vector math | forge_vec2.nova | grep:vec2_dot | vec2_add/sub/scale/negate/dot/cross(scalar)/length_sq/length/distance/normalize/lerp/perp. Everyday 2D algebra (forge_vec3 is 3D): UI layout, sprites/physics, plotting, mouse/touch, map work. Vector=[x,y]; cross=scalar (turn dir/signed area); length_sq no-sqrt for magnitude compares. Magnitude fns use INLINE sqrt (dodges float-return edge). KAT 21: add/sub/scale/negate, dot(11)/perp0, cross(±1), length_sq25/length5/distance5, normalize [.6,.8]+unit-len, lerp mid/ends, perp orthogonal | TESTED ✓ |
| 378 | Line segment intersection | forge_segintersect.nova | grep:segments_intersect | segments_intersect(bool) + intersection_point([x,y] or null). Do two 2D segments cross + where: path-hits-wall detection, polygon/route self-intersection, map/CAD overlap, ray/laser hits, wire-crossing, clipping. Cross-product orientation + on-segment (no sqrt); point via determinant (one division). KAT 13: X-cross->(2,2), parallel->no/null, disjoint, T-junction, collinear overlap yes / disjoint no, shared endpoint, V+H->(3,4), near-miss | TESTED ✓ |
| 379 | Spatial hash grid (broad-phase) | forge_spatialhash.nova | grep:spatial_insert | spatial_new/insert/cell/neighbors/query/cell_count. Fast "what's near here" over many 2D objects: bucket by grid cell so you only compare same/adjacent cells (~O(N) vs O(N^2)) - game/physics collision broad-phase, particles, boids neighbor lookup, click hit-test of 1000s of sprites, point clustering. Repr [cell_size, dict "cx,cy"->id list] (dict-of-lists verified). Int/float coords. KAT 14: cell buckets, 3x3 neighbors (excl far), box query, far-only, empty, cell-count, negative/float coords | TESTED ✓ |
| 380 | Morton code (Z-order curve) | forge_morton.nova | grep:morton_encode | morton_encode/decode + morton_x/y. Interleave 2D (x,y) bits into ONE integer (and back) so spatially-close points get 1D-close codes - store 2D data in a sorted array/B-tree with locality: spatial DB/image indexing, quadtree cell keys, GPU Z-order texture tiling (cache), locality sort of points. 16-bit coords (0..65535) -> 32-bit code. Integer bit-interleave. KAT 16: known 3,5<->39/1,0->1/0,1->2, decode inverse, roundtrip range + 16-bit edge, locality (nearby codes closer than far) | TESTED ✓ |
| 381 | Barycentric coords + point-in-triangle | forge_barycentric.nova | grep:barycentric | barycentric([u,v,w] u+v+w=1) + point_in_triangle + triangle_area + bary_interpolate. Blend of a triangle's 3 corners - inside test (all weights>=0) + how graphics interpolate color/texture/depth/lighting across a face during rasterization, FEM interpolation, terrain-height on a triangulated mesh, triangular hit-testing. Dot/cross + 1 division (NO sqrt). KAT 14: vertices->unit, centroid->1/3, sum=1, in/on-edge/on-vertex/outside, area8/degenerate0, interpolate centroid=10/vertex-exact | TESTED ✓ |
| 382 | Number properties (digital-root/perfect/etc) | forge_numprops.nova | grep:digital_root | digit_sum + digital_root + is_perfect/armstrong/happy/palindrome_number/triangular. Practical (digital_root = casting-out-nines checksum; palindrome/digit-sum validation) + puzzle/education/Project-Euler. Exact integer routines. KAT 33: digit_sum15/droot6, perfect 6/28/496 vs 12/1, Armstrong 153/370/9474 vs 154, happy 19/7 vs 4/2, palindrome 12321 vs 123/neg, triangular 10/15 vs 11/2 | TESTED ✓ |
| 383 | Hilbert space-filling curve | forge_hilbert.nova | grep:hilbert_xy2d | hilbert_xy2d + hilbert_d2xy. Number a 2D grid along one path where CONSECUTIVE numbers are ALWAYS adjacent (unlike Morton's occasional jumps) - strictly better locality for spatial DB/R-trees, cache-friendly image/matrix traversal, dithering, IP/geo heatmaps, 2D->1D where neighbors stay neighbors. order = 2^k side. Integer bit math + quadrant rotation. KAT 12: order-2 U-shape, xy2d inverse, 8x8 roundtrip, ALWAYS-ADJACENT (Manhattan=1) defining property, covers all 16 of 4x4 | TESTED ✓ |
| 384 | Pascal's triangle | forge_pascal.nova | grep:pascal_row | pascal_row + pascal_triangle + pascal_value(C(n,k)) + pascal_row_sum. Grid of binomial coefficients (each = sum of two above); row n = (a+b)^n coefficients = "n choose k" - expand binomials, read combination counts, build binomial distributions w/o factorials. Whole ROWS/triangle (vs combinatorics single C(n,k)) via multiplicative recurrence. Integer exact. KAT 15: rows 0/1/4/6, triangle, C(5,2)=10/C(10,5)=252/symm/oor, Pascal's-rule (interior=sum-above), row-sum=2^n | TESTED ✓ |
| 385 | Polygon clipping (Sutherland-Hodgman) | forge_polyclip.nova | grep:clip_polygon | clip_polygon (vs convex region) + clip_rect. Keep only the part of a polygon inside a boundary: crop map features to a viewport/tile, restrict drawing to a window, region-vs-bbox overlap, cull off-screen geometry before render. Edge-by-edge: keep inside vertices + insert boundary-crossing points. Clip region must be CONVEX. Cross-product side test + line intersection (NO sqrt). KAT 12: big-square->inner-rect (4 verts, extent 2..8), fully-inside unchanged, fully-outside empty, triangle-partly-out in-bounds, convex triangle clip | TESTED ✓ |
| 386 | Modular arithmetic (extgcd/modinv/CRT) | forge_modarith.nova | grep:mod_inverse | ext_gcd([g,x,y] Bezout) + gcd + mod_inverse + crt (Chinese Remainder). Crypto (RSA exponent inverse mod phi, DH/ECC), rolling hashes, calendar/cycle math, "solve x under divisibility constraints". Completes number-theory layer w/ primes+combinatorics. Integer (moderate moduli). KAT 15: extgcd 30,18->6 Bezout, modinv 3@11=4/7@26=15/none=-1/verify, CRT Sun-Tzu [2,3,2]@[3,5,7]=23, non-coprime=-1, multi-verify | TESTED ✓ |
| 387 | Polynomial operations (Horner) | forge_poly.nova | grep:poly_eval | poly_eval (Horner) + poly_degree/add/mul(convolution)/derivative/scale over coeff lists [a0,a1,...]. Curve/trend fitting, interpolation, filter/control transfer functions, computer algebra, cheap function approximation, CRC bytes-as-polynomials. eval float; algebra keeps int coeffs exact. KAT 17: eval 17/25/const/x0/negative, degree, add [4,6,5], mul (x+1)^2=[1,2,1]/(x+1)(x-1)=[-1,0,1], derivative [3,4], scale, eval(mul) consistency | TESTED ✓ |
| 388 | Strongly connected components (Kosaraju) | forge_scc.nova | grep:scc | scc(node-list components) + scc_count + scc_same. Maximal mutually-reachable node groups (cycles) in a directed graph: circular imports, deadlock cycles, condense package/build deps into units, feedback loops in state machines/citations. Kosaraju (finish-order DFS + reversed-graph DFS); iterative (no recursion limit), parallel scalar stacks + dict adj/radj. Edges [from,to] string ids. KAT 13: cycle->1, DAG->N, two-cycles+bridge->2, classic 2x3-cycle, self-loop, partition covers all | TESTED ✓ |
| 389 | Linear system solver (Gaussian elim) | forge_linsolve.nova | grep:solve | solve(A,b)->x or null. Solve n equations n unknowns by Gaussian elimination w/ partial pivoting: curve/plane fitting, balance chemical eqns, circuit node voltages (Kirchhoff), truss forces, least-squares normal equations, graphics transforms. A=list of rows, b=vector; singular->null. Content-swap pivoting (dodges list-of-lists ref swap); nested matrix writes. KAT 12: 2x2 [1,3], identity, 3x3 [2,3,-1], zero-first-pivot, singular->null, residual~0, 1x1, fractional [.5,.5] | TESTED ✓ |
| (skip) | Numerical calculus (derivative/integrate/newton) | ABANDONED - not shipped | HOF-float bug | forge_calculus needs f(x) to return a float through a function VALUE param, which the compiler mis-reads as garbage (~2e37). Known-unsound HOF area. Documented in reference_nova_module_authoring_gotchas #9. Deleted, not committed. |
| 390 | Bipartite graph check + 2-coloring | forge_bipartite.nova | grep:is_bipartite | is_bipartite + bipartite_coloring (dict node->0/1 or null). Split a graph into 2 conflict-free groups: 2-shift scheduling, 2-bin sorting w/ forbidden pairs, two-sided (job/applicant) matching feasibility, border 2-coloring. Bipartite iff NO odd cycle; BFS 2-color. Undirected [u,v] string ids. KAT 12: even-cycle yes+valid, triangle/5-cycle no+null, tree, jobs/applicants, disconnected(+odd=no / all=yes), single edge | TESTED ✓ |
| 391 | Eulerian path/circuit | forge_euler.nova | grep:has_euler_path | has_euler_circuit + has_euler_path + euler_odd_count. Trace a graph in ONE stroke crossing every edge once (Seven Bridges of Konigsberg): mail/snowplow/sweeper route covering every road w/o repeats (Chinese Postman core), draw-without-lifting-pen, DNA assembly, single-pass wiring/pipe inspection. Euler's theorem: connected + all-even-degree=circuit, + 0-or-2-odd=path. Degree count + BFS connectivity. Undirected string ids. KAT 13: square/triangle circuit, path a-b-c (2 odd), KONIGSBERG 7-bridges 4-odd=no, disconnected=no, single-edge, figure-8 | TESTED ✓ |
| 392 | Maximum flow (Edmonds-Karp) | forge_maxflow.nova | grep:max_flow | max_flow(edges,source,sink)->int. Max throughput through a capacitated network - and via max-flow-min-cut ALSO solves maximum BIPARTITE MATCHING (jobs->workers), MINIMUM CUT (cheapest links to sever = bottleneck), scheduling, transportation. Edmonds-Karp: BFS shortest augmenting paths in the residual graph. Edges [from,to,capacity] string ids, int caps; dict residual+adjacency. KAT 8: chain=2, parallel=2, diamond=4, CLRS classic=23, mid-bottleneck=1, no-path=0, parallel-edges-merge=5, bipartite-matching-via-flow=3 | TESTED ✓ |
| 393 | Connected components (undirected) | forge_components.nova | grep:connected_components | connected_components + component_count + largest_component_size + same_component + is_connected. Undirected "islands": social circles, unreachable network segments, entity-resolution/dedup clusters, disjoint map/mesh regions, is-graph-fragmented. Every edge two-way (vs directed forge_scc). BFS flood per unvisited node. Undirected string ids. KAT 15: two-clusters (count2/largest3/same/not-same/not-connected), chain=1, undirected-direction-agnostic, three-islands, star=1(size5), cycle=1, partition-covers-all | TESTED ✓ |
| 394 | Greedy graph coloring | forge_coloring.nova | grep:greedy_coloring | greedy_coloring(node->color) + color_count + is_proper_coloring. Assign colors so no adjacent nodes clash, few colors: register allocation, exam/room/shift scheduling, radio-frequency assignment, map coloring. Generalizes forge_bipartite (2-color) to any count. Greedy (lowest color no neighbor uses) - fast upper bound. Undirected string ids, int colors. KAT 15: K3=3, even-cycle=2, path=2, K4=4, star=2, single-edge=2, all-proper, catches-bad-coloring | TESTED ✓ |
| 395 | Reachability / transitive closure | forge_reach.nova | grep:reachable_from | reachable_from + can_reach + reachable_count. DIRECTED "who can reach whom": downstream tasks of a change, packages a build pulls in, permission/trust reachability, states a state machine can enter, "does A lead to B" in dependency/call/flow graphs. BFS out-edges transitively. Directed [from,to] string ids. KAT 14: chain a reaches 3 / d reaches 0 (directional), self-reach, branching, cycle mutual, disconnected no-reach, unknown->0 | TESTED ✓ |
| 396 | Graph metrics (degree/density) | forge_graphmetrics.nova | grep:degree_sequence | degrees + node_degree + degree_sequence + max/min/avg_degree + graph_density. Structural measurements of an undirected graph: hubs (max degree), sparse-vs-dense, average connectivity, degree-sequence pattern (scale-free hubs). Social/network analysis, generated-graph sanity, centrality heuristics, dense/sparse algorithm choice. Density=2E/(V(V-1)). KAT 15: triangle seq[2,2,2] density1, path [2,1,1] density.667, star [4,1,1,1,1], K4 density1, sparse<0.5, unknown->0 | TESTED ✓ |
| 397 | Critical path method (project scheduling) | forge_cpm.nova | grep:critical_path_length | critical_path_length + earliest_finish. Tasks with DURATIONS + dependencies -> earliest each finishes + min project time = the CRITICAL PATH (dependent chain w/ no slack). PERT/CPM/Gantt project mgmt; also build/pipeline duration (longest compile-dependency chain), assembly/workflow planning. Kahn topological order + earliest-finish DP; cycle->-1. Durations dict task->int, deps [before,after]. KAT 12: A/B->C->D crit-path=8, chain=9, no-deps=max8, parallel-join=5, cycle=-1, single=7 | TESTED ✓ |
| 398 | Interval scheduling (activity + weighted) | forge_intervalsched.nova | grep:max_weight_schedule | max_nonoverlapping (greedy activity selection - MOST non-overlapping jobs) + max_weight_schedule (weighted-interval-scheduling DP - MAX total value). Resource allocation: room/CPU/equipment booking, ad placement, event scheduling, non-conflicting-subset selection. forge_interval merges ranges; this OPTIMIZES a selection. Sort-by-end + greedy/DP (parallel scalar arrays). [start,end] or [start,end,weight] int. KAT 10: activity 2/4/all-overlap1/disjoint3/touching3, weighted 14/bigone100/two-beats-one6/sum60/single42 | TESTED ✓ |
| 399 | Order statistics (quickselect/median) | forge_select.nova | grep:kth_smallest | kth_smallest + kth_largest + median. Find the k-th smallest/largest or the median WITHOUT full sort - quickselect partitions like quicksort but recurses one side (O(n) avg vs O(n log n)). Median/percentile, "3rd cheapest", top-k thresholds, robust statistics. Works on a copy (input untouched); 0-indexed k. Iterative Lomuto partition, scalar swaps. KAT 16: k0/k3/k7/k5, oor->0, kth_largest, input-unchanged, median odd/even, single/dups/negatives, EXHAUSTIVE vs sorted | TESTED ✓ |
| 400 | Transitive reduction (dependency skeleton) | forge_transreduce.nova | grep:transitive_reduction | transitive_reduction + reduction_edge_count. Strip a directed graph to the FEWEST edges keeping the same reachability - if A->B->C then a direct A->C is redundant. The minimal dependency skeleton: clean dependency DIAGRAM (only essential/direct edges), simplify build/package graph, Hasse diagram of a partial order, direct-prerequisite structure. Keep an edge only if its end is NOT reachable from its start another way. DAG->unique. Directed [from,to] string ids. KAT 12: triangle drops a->c(2), chain keeps all(3), multi drops a->d(4), diamond keeps all(4), long-redundancy->chain(3), empty/single | TESTED ✓ |
| 401 | Vertex cover (2-approximation) | forge_vertexcover.nova | grep:vertex_cover | vertex_cover + vertex_cover_size + is_vertex_cover. Small node set touching every edge: place monitoring sensors/cameras so every link is watched, guards covering hallways, accounts to audit catching every risky relationship, road checkpoints. Minimum is NP-hard -> classic greedy 2-approx (take BOTH endpoints of each uncovered edge, <=2x optimal). Undirected [u,v] string ids. KAT 13: triangle size2+valid, star/path/complex all valid covers, single=2, empty=0, is_cover yes/no/full, 2-approx bound, disconnected=4 | TESTED ✓ |
| 402 | Fibonacci + Lucas sequences | forge_fib.nova | grep:fibonacci | fibonacci + fib_sequence + is_fibonacci + lucas. Fibonacci (0,1,1,2,3,5,...) - natural growth, golden-ratio design/UI proportions, algorithm analysis (Fib heaps, Euclid gcd worst case), trading retracements; Lucas (2,1,3,4,7,...) same rule, different seeds. Iterative, exact through F(92). KAT 22: fib 0/1/10=55/20=6765/30=832040, sequence, is_fibonacci 13/144 vs 14/100/neg, lucas 2/1/3/11/123, identity L(n)=F(n-1)+F(n+1) | TESTED ✓ |
| 403 | Base-N conversion (radix 2-36) | forge_basen.nova | grep:to_base | to_base(int->digit string) + from_base(string->int) for any radix 2-36. Binary/octal/hex debugging + bit work, compact URL-safe IDs/serials (base36 of a counter). (forge_base64/basex=byte<->text; this=integer<->digit-string radix.) Negatives via leading '-'; case-insensitive parse. KAT 17: bin1010/hex ff/oct10/base36 zz/dec/zero/neg, from_base incl UPPER, EXHAUSTIVE roundtrip bases 2-36, neg-roundtrip, big hex f4240 | TESTED ✓ |
| 404 | Statistical mode + frequency | forge_mode.nova | grep:frequency | mode + modes (multi-modal) + frequency + count_of. The MODE = 3rd average w/ mean(stats)/median(select); the ONLY one for CATEGORICAL data: most common category/survey answer/busiest hour/top page/winning vote/distribution peak. frequency = full tally for histograms/analytics. Int or string elements; single-exit null (dodges return-null mistag). KAT 14: mode 3/string b, count_of, frequency, bimodal [1,2], single, all-distinct=all, empty null/0, tie first-to-reach | TESTED ✓ |
| 405 | Adler-32 + Fletcher-16 checksums | forge_adler.nova | grep:adler32 | adler32 (zlib/gzip's checksum) + fletcher16 + adler32_verify. Fast integrity checksums to detect ACCIDENTAL corruption (flipped bit, garbled packet, bad disk read) - position-sensitive (catch reordering, unlike a byte sum), much cheaper than CRC-32/crypto-hash (why zlib uses Adler-32 per deflate block). For error detection NOT forgery defense (use HMAC). KAT 11: CANONICAL Adler-32("")=1/("Wikipedia")=300286872, Fletcher-16 abcde=51440/abcdef=8279/abcdefgh=1575, position-sensitive, deterministic, verify ok+corrupt | TESTED ✓ |
| 406 | Hamming distance + code min-distance | forge_hamming.nova | grep:hamming_distance | hamming_distance (equal-len strings) + hamming_int (bit diffs) + min_distance (over codewords). "How many single changes apart" - foundation of error-detecting/correcting codes (min-distance d detects d-1, corrects (d-1)/2), DNA/protein mutation count, hash/fingerprint compare (simhash near-dup), fixed-record field-change count. Positions compared directly (vs editops ins/del). KAT 15: karolin/kathrin=3, binary=2, identical0/all-diff/mismatch-1/empty, int 5^6=2/0^255=8, min-dist parity2/repetition3/dup0/single-1 | TESTED ✓ |
| 407 | Knuth-Morris-Pratt string search | forge_kmp.nova | grep:kmp_search | kmp_search (first index) + kmp_find_all (all overlapping) + kmp_count + kmp_failure (LPS table) + kmp_period (shortest repeating unit). LINEAR O(n+m) substring search - precomputes a failure/LPS table so a mismatch slides the pattern forward WITHOUT ever moving the text cursor back (each text char read once), avoiding the naive O(n*m) blowup on "aaaa..a"/"aaa..ab". For find-in-file, log scanning, DNA motif finding, dedup, and as a streaming-matcher primitive; the failure table also reveals a string's shortest period. KAT 20: abxabcabcaby/abcaby=6, absent-1/at0/atend/empty0/single/full/longer-1, all-overlap banana/ana=[1,3] aaaa/aa=[0,1,2], counts, LPS ababaca=[0,0,1,2,3,0,1]/aaaa=[0,1,2,3], period abcabcabc=3/abab=2/abcd=4 | TESTED ✓ |
| 408 | Manacher longest palindromic substring | forge_manacher.nova | grep:longest_palindrome | longest_palindrome (substring) + longest_palindrome_len + is_palindrome + count_palindromes. LINEAR O(n) longest-palindrome via Manacher - mirrors already-discovered palindrome radii across the current center so each position is amortized O(1), beating the O(n^2) expand-around-every-center. '#'-interleaved guarded transform handles odd AND even palindromes uniformly. For longest-palindrome queries, palindrome counting, text/DNA symmetry analysis. KAT 22: racecar/abacaba(embedded)/bb/geeksskeeg substrings, len babad3/racecar7/empty0, is_palindrome level/noon/empty, count aaa=6/abc=3/aba=4 | TESTED ✓ |
| 409 | Z-algorithm string matching | forge_zalgo.nova | grep:z_function | z_function (Z-array) + z_search + z_find_all + longest_repeated_prefix. For each i, the longest common prefix of s and s[i..], computed in linear O(n) via a reused [l,r) prefix-window. KMP's cousin: pattern+chr(1)+text then any position whose Z=pattern-len is a match -> O(n+m) search. Z-array also reveals borders/periods and the longest reappearing prefix. KAT 18: Z aaaaa=[0,4,3,2,1]/aabaab=[0,1,0,3,1,0], search basic6/absent-1/at0/atend/empty0/full/longer-1, find_all aaaa/aa=[0,1,2] banana/ana=[1,3] abcabcabc/abc=[0,3,6], lrp aaaa=3/aabaab=3/abcd=0 | TESTED ✓ |
| 410 | Run-length encoding (RLE) | forge_runlength.nova | grep:rle_encode | rle_encode/rle_decode (compact "<count><char>" text form, multi-digit counts) + rle_pairs/rle_from_pairs (digit-safe [char,count] list, round-trips ANY string) + run_count. Simplest lossless compression - collapse a run of the same char to (count,char); shines on long runs (fax/bitmap/sprite, sparse logs, DNA) and is the first stage of bigger schemes (BWT+RLE=bzip2, PackBits/TIFF). KAT 22: aaabbc<->3a2b1c, 12w multidigit, single/empty, roundtrip, pairs digit-safe 11122/a1a1bb99, nested struct p[0]=[a,3], run_count aaabbc=3/ababab=6/aaaaa=1 | TESTED ✓ |
| 411 | Rabin-Karp rolling-hash search | forge_rabinkarp.nova | grep:rk_search | rk_hash (polynomial/Horner mod 1e9+7) + rk_search + rk_find_all + rk_count. Slides a window across text, updating its hash in O(1) (add entering char, subtract leaving) instead of recomputing; on a hash match verifies the actual chars. Avg O(n+m); its superpower is MULTI-PATTERN/2D and content-defined chunking (plagiarism/virus-sig/rsync-dedup fingerprints). base 256, products < 2^63 (no overflow). KAT 20: exact hashes A=65/AB=16706/ABC=4276803/empty0, search matches KMP/Z (basic6/absent-1/at0/atend/full/single), overlapping find_all aaaa/banana/abcabcabc, stress 100-char abab.. -> 49 abab | TESTED ✓ |
| 412 | Bit-manipulation primitives | forge_bitops.nova | grep:next_power_of_two | bit_length + is_power_of_two + next/prev_power_of_two + count_trailing_zeros (ctz) + count_leading_zeros (clz, width-relative) + reverse_bits + rotate_left/rotate_right + parity. The Hacker's-Delight scalar bit toolkit powering hash-table sizing (round capacity up to power-of-2), allocators (align/find-free-slot via ctz), codecs (bit reverse/rotate fields), FFT bit-reversal, parity error bits. Non-negative ints, width 1..63 (NOVA 1<<64 broken). Complements forge_binary (int<->string+popcount) + forge_bitset. KAT 39: bit_length 255=8/256=9, p2 16/17/0, next 5->8/1000->1024, prev 1000->512, ctz 8=3/0=-1, clz 1@8=7, reverse 1@8=128/11@4=13, rotL/rotR/wrap, parity 3=0/7=1 | TESTED ✓ |
| 413 | Counting + radix sort (linear) | forge_intsort.nova | grep:counting_sort | counting_sort (O(n+k), stable, small key range) + radix_sort (LSD base-256, stable, O(d*(n+256)), large ranges) + is_sorted. Non-comparison sorting beats the O(n log n) comparison bound by using the key AS an index: counting sort tallies + prefix-sums + places; radix sort does one stable counting pass per byte, LSB-first, so it scales past counting sort's O(max) limit. STABLE (equal keys keep input order). Non-negative ints. Workhorse behind fast integer/string sorting. KAT 17: cs basic/dups/rev/zero, rs wiki [170,45,..]->sorted, big 1e6 range, byte-boundary 255/256, both agree up to 65535 (multi-pass), is_sorted | TESTED ✓ |
| 414 | Sparse table (O(1) range min/max) | forge_sparsetable.nova | grep:sparse_build_min | sparse_build_min/max + sparse_query_min/max. O(1) range-min/max over a STATIC array after O(n log n) build - exploits min/max IDEMPOTENCE: cover [l,r] with two OVERLAPPING power-of-two blocks (largest 2^k that fits, anchored at both ends), so each query is one comparison of two precomputed lookups. For static RMQ, sliding analytics over a fixed log, LCA via Euler tour, offline range stats. (Mutable -> forge_segtree; prefix sums -> forge_fenwick since sum isn't idempotent.) KAT: min/max spot queries on [1,3,2,7,9,11,3,5,6] + EXHAUSTIVE brute-force cross-check of all 45 [l,r] ranges (min AND max) vs linear scan = 0 mismatches | TESTED ✓ |
| 415 | Binary search + bisect boundaries | forge_bisect.nova | grep:lower_bound | lower_bound/upper_bound (Python bisect_left/right) + binary_search (first occ, -1 absent) + bisect_contains + count_sorted (dup count) + count_range ([lo,hi)) + insert_sorted + closest. O(log n) over a SORTED int list. The boundaries answer what real code asks: insertion point to stay sorted, how many copies of a value (upper-lower), how many in a range, nearest entry - fast ordered lookups/range-counts/percentiles/closest-match without a tree. KAT 33: lb/ub on [1,2,4,4,4,7,9] (4->lb2/ub5), binary_search first-occ/absent, count_sorted 4=3, count_range [2,8)=5, insert_sorted 5/0/10/4, closest 6->7/3->2(tie-smaller)/100->9 | TESTED ✓ |
| 416 | Boyer-Moore-Horspool search | forge_boyermoore.nova | grep:bm_search | bm_search + bm_find_all + bm_count + bm_contains. The practically-FASTEST single-pattern search (core of grep/editor-find/memmem): compares the pattern RIGHT-to-left and on a mismatch JUMPS via a bad-character table - often by the whole pattern length - so it's SUBLINEAR (touches far fewer than n chars) on typical text; longer pattern + bigger alphabet = bigger skips. Horspool = clean bad-char-only variant keyed on the text char at the pattern's last position. Completes the search family (KMP/Z/Rabin-Karp/BM). KAT 17: results match KMP/Z (basic6/absent-1/at0/atend/full/single), classic EXAMPLE->17, prose brown->10, overlapping find_all aaaa/banana/abcabcabc, contains case-sensitive | TESTED ✓ |
| 417 | Inversion count + merge sort | forge_inversions.nova | grep:count_inversions | count_inversions (out-of-order pairs i<j, xs[i]>xs[j]) + merge_sort (stable, handles NEGATIVES unlike counting/radix) + kendall_tau_distance. O(n log n) inversion counting during the merge (right-half element jumping ahead = that many inversions in one shot) vs naive O(n^2). Exact measure of "how unsorted" - the engine behind Kendall's tau rank correlation (recommenders, A/B ranking, search quality), distance-from-sorted, near-sorted detection. KAT 20: rev5=10/mix=3/dups=4/two=1, merge_sort negatives [-10,-1,0,3,5], brute-force cross-check on 10-elem, kendall same0/reversed6/one-swap1 | TESTED ✓ |
| 418 | LZW dictionary compression | forge_lzw.nova | grep:lzw_encode | lzw_encode (string -> int codes) + lzw_decode + lzw_compressed_size. Lempel-Ziv-Welch, the algorithm in GIF/TIFF/PDF/Unix-compress. Unlike RLE (repeats of ONE char), LZW finds repeated SUBSTRINGS: it grows a dictionary of seen sequences on the fly so a repeated phrase becomes one code; encoder AND decoder rebuild the SAME dictionary from the data, so no table is transmitted (incl the KwKwK case where a code is used the instant before it's defined). Seeded with 256 single-byte codes (no NUL). KAT 14: exact codes ab=[97,98]/aaa=[97,256], textbook TOBEORNOTTOBEORTOBEORNOT=[84,79,66,69,79,82,78,79,84,256,258,260,265,259,261,263], round-trips incl KwKwK aaaaaaaa/abab.., compresses 40-char abab.. to <40 codes | TESTED ✓ |
| 419 | Burrows-Wheeler Transform | forge_bwt.nova | grep:bwt_transform | bwt_transform (-> [last_column, primary_index]) + bwt_inverse. The reversible permutation at bzip2's heart: it REORDERS a string so chars with the same following context cluster together (long runs -> RLE/entropy coders squeeze hard), yet is perfectly INVERTIBLE from just the transformed string + one integer via the LF-mapping (pairs each last-column char with its first-column twin). Forward = sort all rotations, take last column; inverse = O(n) counting + LF walk. Basis of bzip2 and the FM-index (compressed full-text search / DNA read alignment). KAT 14: canonical banana->["nnbaaa",3] exact, inverse recovers, round-trips mississippi/abracadabra/hello world/digits/mixed-case-punct/reverse/all-same/empty | TESTED ✓ |
| 420 | Move-To-Front coding | forge_mtf.nova | grep:mtf_encode | mtf_encode (string -> indices) + mtf_decode. The transform between BWT and the entropy coder in bzip2: keep a list of all bytes, output each char's CURRENT position then move it to front, so recently-used bytes get small numbers and BWT's long runs become a stream dominated by ZEROS - exactly the skew RLE + Huffman exploit. Fully reversible with the identical list; also a general "recency" transform (caches, adaptive coders). KAT 12: exact aaa=[97,0,0]/aba=[97,98,1], BWT-synergy nnbaaa->[110,0,99,99,0,0], round-trips, + FULL bzip2-style pipeline BWT->MTF->decode->inverse-BWT recovers "abracadabra_abracadabra" | TESTED ✓ |
| 421 | Huffman coding (entropy coder) | forge_huffman.nova | grep:huffman_build | huffman_build (codebook from frequencies) + huffman_encode + huffman_decode + huffman_encoded_bits. The optimal PREFIX-FREE entropy coder ending gzip/bzip2/JPEG/MP3: frequent symbols get SHORT bit-codes, rare ones longer, provably minimizing total bits; builds a tree by repeatedly merging the two least-frequent nodes so every symbol is a leaf -> no code prefixes another -> unambiguous with no separators. Compresses the zero-heavy BWT+MTF stream. Iterative DFS (pop-stack) fills the codebook; decode greedy-matches the prefix-free reverse map. KAT 15: round-trips hello/mississippi/abracadabra/mixed/long, single-char code "0"+"0000", freq a(4)=1bit < c(1)=2bit, prefix-free ambiguous-looking input, compresses <8bit/char | TESTED ✓ |
| 422 | Fast non-crypto hashes | forge_fasthash.nova | grep:fnv1a_32 | fnv1a_32 (recommended default) + djb2 + sdbm + hash_combine (Boost-style tuple mixer) + hash_bucket. The workhorse hashes behind hash tables, bloom filters, sharding/consistent-hashing, cache keys, dedup - turn a string/sequence into a well-spread 32-bit int FAST. NOT for security (reversible/collidable by design; use forge_crypto SHA/HMAC). Centralizes+KAT-verifies what was inline-copied in 7 modules; djb2/sdbm give a 2nd independent hash for double-hashing. KAT 18: FNV-1a canonical ""=2166136261/"a"=3826002220/"foobar"=3214735720 (32-bit mask byte-exact), djb2 ""=5381/"a"=177670/"hi"=5863446, sdbm ""=0/"a"=97/"hi"=6822401, 32-bit range, combine order-sensitive, bucket in [0,n) | TESTED ✓ |
| 423 | Arbitrary-precision integers (bignum) | forge_bignum.nova | grep:big_mul | big_add + big_sub (a>=b) + big_mul + big_cmp + big_factorial + big_pow, on NON-NEGATIVE integers as decimal strings. NOVA's native int is 64-bit and silently WRAPS past ~9.2e18; this computes with numbers of ANY size - 100!, 2^1000, RSA-scale moduli, exact factorials/binomials/running totals. Base-10 digit strings (trivial to read/print/store/compare); schoolbook algorithms digit-by-digit; square-and-multiply pow. (Division/modulo intentionally deferred.) KAT 26 famous-value exact: 50!=304140932...(65 digits), 2^100=1267650600228229401496703205376, 25!=15511210043330985984000000, 3^40, 20-nines squared, carry/borrow/norm, cmp, cross-check 10^20 two ways | TESTED ✓ |
| 424 | Bignum division + modular exponentiation | forge_bignum.nova | grep:big_divmod | ADDITIVE to forge_bignum: big_divmod (schoolbook long division -> [quotient,remainder]) + big_div + big_mod + big_powmod. Completes arbitrary-precision arithmetic with the crypto payoff: big_powmod is (base^exp) mod m via square-and-multiply with reduction each step - THE primitive behind RSA/Diffie-Hellman and Miller-Rabin. KAT 17: divmod 100/7=[14,2], div/mod exact, divisor>dividend, RECONSTRUCTION q*b+r==a for 2^100 and 50!/25!, remainder<divisor, powmod 3^5%7=5/2^10%1000=24/7^2%5=4, powmod==pow-then-mod for 2^100 % 1e9+7 | TESTED ✓ |
| 425 | Miller-Rabin primality testing | forge_primality.nova | grep:is_prime | is_prime (deterministic for n<3.3e24 via 12 fixed witnesses) + next_prime. Imports forge_bignum. Trial division dies past ~1e12; Miller-Rabin asks a few modular-exponentiation questions instead and correctly REJECTS Carmichael numbers (561/1105/1729) that fool the Fermat test - the whole reason it exists. The primality engine behind RSA/DH key generation; next_prime finds the next prime >= a value (how generators pick a prime near a random seed). Exponent is itself a bignum -> walked bit-by-bit via bignum division. KAT 20: tiny 0/1/4 composite, 2/3/17/97 prime, Carmichael 561/1105/1729 composite, 7919/104729/1000000007/mersenne-2147483647 prime, 1000000011 composite, next_prime 14->17/100->101/0->2/13->13 | TESTED ✓ |
| 426 | Bignum number theory (GCD/LCM/modinv) | forge_bigmath.nova | grep:big_modinv | big_gcd + big_lcm + big_modinv, on arbitrary-precision integers (imports forge_bignum). The MODULAR INVERSE (x with a*x=1 mod m) unlocks real modular arithmetic - how RSA derives the private exponent, how you "divide" mod m, and how CRT/ECC recombine. Extended Euclidean kept entirely in NON-NEGATIVE bignums by doing every subtraction MODULO m (forge_bignum has no sign). GCD/LCM = classic Euclid on any-size numbers. KAT 15: gcd 48/36=12, 1071/462=21, gcd(2^100,6)=2, lcm 4/6=12, modinv 3^-1 mod 11=4, 10^-1 mod 17=12, non-coprime->0, VERIFY a*inv==1 mod 1000000007 (a=65537) and mod 998244353 | TESTED ✓ |
| 427 | Calendar algorithms (weekday/Easter/Julian-day) | forge_calendar.nova | grep:julian_day | day_of_week (Sakamoto 0=Sun) + day_name + is_weekend + easter (Anonymous Gregorian Computus) + julian_day (Fliegel-Van Flandern) + from_julian_day + days_between + add_days. Exact integer formulas, no lookup tables; Julian Day Numbers make date arithmetic trivial subtraction/addition (no month-length/leap bookkeeping). Complements forge_date (parse/format/leap). KAT 24: dow 2000-01-01=Sat/1970=Thu/moon-landing=Sun, Easter 2024=3/31,2025=4/20,2000=4/23,2008=3/23, JDN 2000=2451545/1970=2440588(unix), inverse round-trips, days_between 365/366-leap, add_days across month+leap+backward boundaries | TESTED ✓ |
| 428 | Quoted-Printable (MIME) | forge_qp.nova | grep:qp_encode | qp_encode + qp_decode. MIME quoted-printable (RFC 2045), the email transfer encoding for mostly-ASCII text: unlike base64 (blows 3->4 bytes, unreadable), it leaves printable ASCII UNTOUCHED and only escapes special bytes as "=XX" uppercase hex (so "café"->"caf=E9"), staying human-readable/compact for Latin text with occasional accents. Decode reverses it and drops soft line breaks (=\n, =\r\n). KAT 16: plain passthrough, =->=3D, café=caf=E9, newline=0A, high byte=FF, space/tab literal, decode incl lowercase hex + malformed-= literal + both soft-break forms, round-trips ASCII/accented(naïve/café)/symbols | TESTED ✓ |
| 429 | Classic ciphers (Caesar/Vigenere/Atbash/Rail-Fence) | forge_classiccipher.nova | grep:vigenere_encrypt | caesar_encrypt/decrypt + rot13 + atbash + vigenere_encrypt/decrypt + railfence_encrypt/decrypt. The historical pen-and-paper ciphers for teaching/puzzles/CTF (NOT secure - use forge_crypto): demonstrates the two ideas every modern cipher builds on - SUBSTITUTION (Caesar shifts, Atbash mirrors, Vigenere shifts by repeating key) and TRANSPOSITION (Rail-Fence zigzag). Preserves case + non-letters. KAT 20: Caesar Hello,World!->Khoor,Zruog!/XYZ-wrap, rot13/atbash self-inverse, Vigenere textbook ATTACKATDAWN/LEMON->LXFOPVEFRNHR both ways, Rail-Fence canonical WEAREDISCOVEREDFLEEATONCE/3->WECRLTEERDSOEEFEAOCAIVDEN both ways + 4-rail round-trip | TESTED ✓ |
| 430 | Exact rational arithmetic (fractions) | forge_rational.nova | grep:rat_add | rat (reduced fraction) + rat_add/sub/mul/div + rat_neg/abs/recip + rat_cmp/rat_eq + accessors + rat_to_str. EXACT numerator/denominator arithmetic - floats can't hold 1/3 or 1/10 (0.1+0.2=0.30000000000000004) but rationals stay perfectly precise (1/3+1/6 = exactly 1/2). Auto-reduced to lowest terms, positive denominator, sign on numerator -> one canonical form, exact comparison. For money/tax splits, odds, ratios, symbolic math, and dodging NOVA float edge cases. [num,den] pair (64-bit; compose forge_bignum for huge). KAT 24: reduce 2/4=1/2, sign norm, 1/3+1/6=1/2, 0.1+0.2->3/10, 1/2+1/3+1/6=1, harmonic 1+1/2+1/3+1/4=25/12, cmp/eq/recip | TESTED ✓ |
| 431 | Floyd-Warshall all-pairs shortest path | forge_floydwarshall.nova | grep:floyd_warshall | floyd_warshall (O(V^3) all-pairs distance matrix) + fw_inf + fw_has_negative_cycle + fw_reachable. Finds shortest distance between EVERY pair in one pass; unlike Dijkstra handles NEGATIVE edges + detects negative cycles. 3-line DP: allow routing through node 0, then 1, ..., then n-1. For small/dense graphs needing the whole matrix: routing tables, transitive-closure/reachability, all-pairs queries, graph diameter. Adjacency matrix (0 diagonal, fw_inf() no-edge); flat 1-D internal array avoids 2-D writes. KAT 15: full 4-node matrix (0->3=6 via 1->2), negative edge 0->1->2=2 beats direct 5, negative-cycle detect true/false, reachability disconnected node | TESTED ✓ |
| 432 | Bellman-Ford single-source shortest path | forge_bellmanford.nova | grep:bellman_ford | bellman_ford (dist from source) + bf_inf + bf_has_negative_cycle + bf_reachable. Dijkstra is faster but breaks on NEGATIVE edges; Bellman-Ford handles them and DETECTS negative cycles (a loop you ride forever getting cheaper) instead of returning nonsense. Relaxes every edge V-1 times (a shortest path has <=V-1 edges); a V-th improving pass = negative cycle. Behind currency-arbitrage detection (negative cycle in log-rates = free money), distance-vector routing (RIP). Edge list of [from,to,weight]. Pairs with forge_floydwarshall (all-pairs). KAT 8: classic CLRS graph dist=[0,2,4,7,-2], positive graph 0->2->1->3=4, negative-cycle detect, reachability disconnected node | TESTED ✓ |
| 433 | Continued fractions (best rational approx) | forge_contfrac.nova | grep:cf_expand | cf_expand (p/q -> [a0,a1,...] Euclidean quotients) + cf_convergents (successive best rational approximations as [num,den]) + cf_to_rational + cf_best_approx. The mathematically OPTIMAL way to approximate by a simple fraction - pi's convergents are 3, 22/7, 355/113 (each the closest fraction with that small a denominator). Drives best rational approximation (gear/aspect/musical ratios), calendar leap rules, number theory. Exact integer; composes with forge_rational. KAT 12: 415/93=[4,2,6,7], 355/113=[3,7,16], convergents of pi-proxy = 3/1,22/7,355/113, reconstruct, best_approx truncations | TESTED ✓ |
| 434 | Boyer-Moore majority voting | forge_majority.nova | grep:majority_element | majority_element (>n/2, as [found,value]) + has_majority + frequent_over_third (>n/3, up to 2 values). Finds dominant elements in ONE pass, O(1) space: candidates and opposing votes CANCEL so a true majority survives; a verification pass rejects false candidates when no majority exists. For fault-tolerant consensus (value a majority of replicas agree on), stream/log dominant value, tallying. frequent_over_third = 2-counter generalization. KAT 13: majority 3 in [3,3,4,2,3,3,3], no-majority, exactly-half NOT majority, single/all, >n/3 returns 0/1/2 values ascending | TESTED ✓ |
| 435 | Prefix sums + difference arrays | forge_prefixsum.nova | grep:prefix_1d | prefix_1d + range_sum_1d + prefix_2d (summed-area table) + range_sum_2d + diff_build/diff_add/diff_apply. Precompute running totals ONCE -> any range sum in O(1) by one subtraction (turns a million "sum l..r" queries from O(n) each to O(1)). 2-D version (integral image) = fast box blur, Haar face detection, heatmap analytics. Difference array is the dual: RANGE UPDATES in O(1) (add to [l,r] by touching 2 endpoints), materialize once - for overlapping increments (seat booking, interval coverage). Flat internal arrays avoid 2-D writes. KAT 14: 1D prefix/range, 2D submatrix full=45/br=28/blocks, diff range-add->[0,2,5,5,3], interval coverage->[1,2,2,2,2,1] | TESTED ✓ |
| 436 | Articulation points + bridges (Tarjan) | forge_articulation.nova | grep:articulation_points | articulation_points (cut vertices) + find_bridges (cut edges). Tarjan single-DFS O(V+E): a node/edge whose removal DISCONNECTS the graph = single points of failure (routers/links/roads that split the network). Uses discovery-time + low-link (earliest node reachable via a back edge); a subtree that can't reach above its entry hangs off a cut vertex/edge. For network-reliability/redundancy analysis, infra weak spots, biconnected decomposition. Undirected adjacency list. Recursion + shared mutable state (validated). KAT 8: classic 5-node aps={0,3} bridges={0-3,3-4}, triangle none, path internal-nodes+all-edges, star center+all-edges | TESTED ✓ |
| 437 | Number to English words | forge_numwords.nova | grep:number_to_words | number_to_words(n) spells any 64-bit integer in English ("one thousand two hundred thirty-four"). Behind cheque writing, invoice/legal totals in words (anti-tampering), accessible/screen-reader text, voice UIs. Splits into 3-digit groups from the right, names each (hundreds + special-cased 0-99), appends scale (thousand/million/.../quintillion for full i64). American style, hyphenated tens. KAT 21: zero, teens/tens boundary (19/20), forty-two, 305=three hundred five, 999, 1234, 1234567, billion/trillion, negatives, internal-zero groups 1000001=one million one / 2000015=two million fifteen, check-amount 2547 | TESTED ✓ |
| 438 | Lowest Common Ancestor (binary lifting) | forge_lca.nova | grep:lca_build | lca_build (O(n log n) preprocess) + lca_query (O(log n)) + lca_distance. The deepest shared ancestor of two tree nodes - nearest common manager, most-recent common ancestor of two species, closest shared taxonomy category - and the key to node-to-node DISTANCE in a tree. Binary lifting precomputes each node's 2^k-th ancestor, then a query raises the deeper node to the other's depth and lifts both in lockstep. Adjacency list + root; BFS parent/depth; flat 2-D lift table. KAT 15: LCA queries (3,4->1; 6,7->4; ancestor 4,1->1; cross-subtree->root; self), distances (6,7=2; 3,5=4; 0,7=3) | TESTED ✓ |
| 439 | Shell-style tokenization (shlex) | forge_shlex.nova | grep:shlex_split | shlex_split + shlex_count. Split a command line into arguments the POSIX-shell way, honoring single quotes, double quotes and backslash escapes - `rm 'My Documents'` is ONE arg, not two. For building REPLs/prompts, parsing stored command strings, config that embeds commands, safely interpreting user arg lists. Mirrors Python shlex: whitespace separates; single quotes literal; double quotes group + backslash escapes "/\\; bare backslash escapes next char; adjacent quoted+unquoted fuse (foo"bar"->foobar). KAT 14: basic/extra-ws/empty, single+double quotes, empty-quoted=1 token, fusion, escaped space, escaped-quote-in-dq, single-quote literal backslash, realistic rm/echo commands | TESTED ✓ |
| 440 | BK-tree fuzzy string search | forge_bktree.nova | grep:bk_query | bk_new + bk_insert + bk_query (all words within edit distance) + bk_size. The index behind spell-checkers/"did you mean?"/autocorrect/near-dup detection: finds every stored word within a tolerance WITHOUT scanning the dictionary, pruning via the triangle inequality on edit distance (acceptable subtree words attach via an edge length in [d-t,d+t]). Each node keys children by exact distance; self-contained Levenshtein. Validates nested dict-mutation-through-index. KAT 9: dup ignored (size 7), within-1 of book={boo,book,books,cook}, exact-only dist0, typo caqe->{cake,cape}, cook->{book,cook}, none, wide tolerance, carte->cart, cake@2={cake,cape,cart} | TESTED ✓ |
| 441 | Arithmetic expression evaluator | forge_calc.nova | grep:calc | calc(expr) evaluates an arithmetic string with correct PRECEDENCE + parentheses (1+2*3=7, (1+2)*3=9). The engine behind calculators, spreadsheet cells, formula/config fields, rule expressions - turns human-written math into a value. Textbook recursive-descent: expr=+/- over terms, term=*/% over factors, factor=number|(expr)|unary-+/-, encoding precedence + left-assoc in the call structure (shared position box; recursion validated). +-*/% , parens, unary minus, whitespace; 64-bit integer (/ = integer div); div/mod by zero skipped. KAT 18: precedence/assoc, 17%5=2, 20/3=6, unary -(3+4)=-7, 2*-3, nested parens, 1e6*1e6, formula (100+2)*3-6/2=303 | TESTED ✓ |
| 442 | Regex matcher (. * + ? subset) | forge_regexmatch.nova | grep:is_match | is_match(text, pattern) full-string match supporting '.' (any char), '*' (zero+), '+' (one+), '?' (zero-or-one) - the everyday pattern operators for grep-style filtering, validation, routing/log matching, WITHOUT a full regex engine. Clean recursive backtracker; '+' desugared to "item item*" so the core only handles */?. The classic textbook regex-matching problem. (Char classes/anchors NOT supported - see glob for shell wildcards.) KAT 24: aa/a*=true aa/a=false, .* , mississippi patterns, a.c, empty cases, ab+ one-or-more, colou?r optional, a?/aaa=false, hello.* prefix | TESTED ✓ |
| 443 | Bencode serialization (BitTorrent) | forge_bencode.nova | grep:bencode_encode | bencode_encode + bencode_decode over int/string/list/dict. The compact format BitTorrent uses for .torrent files + tracker messages: i42e=integer, 4:spam=string, l..e=list, d..e=dict with SORTED keys -> CANONICAL (same data = byte-identical output, hashable for integrity/dedup). A tiny dependency-free deterministic JSON alternative (cache keys, message framing). Recursive encode via type_of dispatch; recursive parser decode (heterogeneous any-return validated). KAT 18: i42e/i-3e/i0e, 4:spam/0:, l4:spami42ee/le, canonical d3:cow3:moo4:spam4:eggse, round-trips incl nested dict+list, decoded access, length-prefixed ':' safe | TESTED ✓ |
| 444 | Suffix array (full-text index) | forge_suffixarray.nova | grep:sa_build | sa_build (sorted suffixes) + sa_contains (binary-search substring) + sa_lcp + sa_longest_repeated + sa_count_distinct. Builds a string into a reusable INDEX: unlike KMP/BM which rescan per pattern, build ONCE then answer "does P occur?" in O(|P| log n) - for many queries on the same text (search engines, autocomplete, read mapping). With the LCP array yields the longest repeated substring + exact distinct-substring count. KAT 18: banana SA=[5,3,1,0,4,2] LCP=[0,1,3,0,0,2], substring search, longest-repeated banana->ana/mississippi->issi, distinct banana=15/aaa=3/abab=7 | TESTED ✓ |
| 445 | Counter total/top-k/merge/subtract | forge_counter.nova | grep:counter_top_k | ADDITIVE to forge_counter (Python-Counter multiset): counter_total (sum of counts) + counter_subtract + counter_top_k (list of [key,count] ranked by count desc, tie->key asc; existing counter_most_common only returns the single top key) + counter_merge (combine two counters, add counts). Completes the Counter with running totals, top-N leaderboards, and counter arithmetic. KAT 12: total=6, top2=a:3,b:2, tie-break x:5,y:2,z:2, subtract, merge a/b/c=2 each, empty=0 | TESTED ✓ |
| 446 | Sequence alignment (Needleman-Wunsch) | forge_align.nova | grep:align_global | align_global (-> [score, aligned_a, aligned_b]) + align_score. Global sequence alignment: edit distance gives a single number, alignment shows HOW two strings line up - padding both with "-" gaps so matching symbols share a column, making every match/mismatch/insert/delete visible. For DNA/protein aligners, file-diff, spell-correction explanations, plagiarism/version compare. DP over a chosen scoring scheme (match>0, mismatch/gap<0) + traceback (flat table, diag>up>left tie-break). GOTCHA: `match` is reserved -> LINK FAIL, param renamed matchv. KAT 15: identical score 7, AB/ABC->AB-/ABC score 0, empty all-gaps, de-gap reconstructs originals + equal length, NW GCATGCU/GATTACA=0, align_score | TESTED ✓ |
| 447 | Combinatorial enumeration | forge_enumerate.nova | grep:cartesian_product | combinations (all k-subsets) + power_set (all 2^n subsets) + cartesian_product (one-from-each-list tuples) + combination_count. LISTS combinatorial collections (vs forge_combinatorics which COUNTS) - drives exhaustive search + test generation: every pair, every feature-flag subset, every {browser}x{os}x{locale} matrix, coalition/knapsack enumeration, parameter grids. Backtracking combinations (shared buffer + COPY on emit -> no aliasing); bitmask power set; iterative product. KAT 16: C(4,2) exact 6-list, k=0->[[]]/k>n->[], power_set(2)=[[],1,2,1-2] size 2^n, cartesian 2x2 + 3-list, counts C(5,2)=10/C(52,5)=2598960 | TESTED ✓ |
| 448 | Number Theoretic Transform (integer FFT) | forge_ntt.nova | grep:ntt_multiply | ntt_multiply (O(n log n) polynomial mult / convolution) + in-place _ntt_transform. An integer-only FFT: same divide-and-conquer as the complex FFT but in modular arithmetic under prime 998244353 (=119*2^23+1, root 3) -> EXACT (no rounding) and no floats, ideal for NOVA. Engine behind fast big-integer multiplication, signal/sequence convolution, fast polynomial arithmetic, string-matching tricks. Supports sizes up to 2^23; exact while each output coeff < ~1e9. Bit-reversal + butterflies + inverse scaling. FLAGSHIP: exact fast convolution in pure integer NOVA. KAT 8: (1+2x+3x^2)(4+5x+6x^2)=[4,13,28,27,18], (1+x)^3=[1,3,3,1] chained, shift, + 2 brute-force cross-checks vs naive O(n^2) conv | TESTED ✓ |
| 449 | Bit-level reader/writer (bit I/O) | forge_bitio.nova | grep:bw_write_bits | bw_new/bw_write_bits/bw_finish/bw_bitcount + br_new/br_read_bits/br_remaining. Read+write values of ANY bit width packed tightly into bytes - what byte-oriented APIs can't do: a 3-bit flag + 5-bit count + 12-bit offset back-to-back, Huffman's variable-length codes, protocol/bitmap/pixel fields off byte boundaries. Writer accumulates MSB-first + flushes/pads; reader walks it back. Dict state (mutation validated). Distinct from bitset (array) + bitops (scalar). KAT 21: 3+5bit->byte180, nibbles->0xAB, 12-bit spans [171,192], 1+7bit flag->0xAA, 5-field record round-trip, remaining-count | TESTED ✓ |
| 450 | Matrix exponentiation (linear recurrences) | forge_matexp.nova | grep:mat_pow | mat_identity + mat_mul + mat_pow (repeated squaring) + mat_fib + rec_nth (arbitrary constant-coeff recurrence). Raise a matrix to a HUGE power in O(k^3 log n) - jump far ahead in any LINEAR RECURRENCE in log time: write a_n=... as a companion-matrix product, then matrix power does n steps with ~log2(n) multiplies -> the 10^18-th term (mod prime) instantly. Also counts length-k graph walks (adjacency^k). Exact integer under a modulus (no floats). KAT 15: [[1,2],[3,4]]x[[5,6],[7,8]]=[[19,22],[43,50]], A^0=I, fib 10=55/20=6765, fib(1000)+fib(100000) mod 1e9+7 match naive, rec_nth Fibonacci/Tribonacci=81/Pell=70/geometric 2^10=1024 | TESTED ✓ |
| 451 | Traveling salesman - exact (Held-Karp) | forge_tsp.nova | grep:tsp_shortest_cycle | tsp_shortest_cycle + tsp_shortest_path + tsp_tour (order reconstruction). EXACT TSP via Held-Karp bitmask DP: shortest route visiting every city - core of vehicle routing, delivery planning, PCB drilling, DNA fragment ordering. Brute force is n! (12 cities=479M); Held-Karp is O(2^n*n^2) by remembering, per SUBSET+endpoint, the cheapest visit ending there -> runnable to ~15-18 cities, TRUE optimum not heuristic. Integer distance matrix, city 0 = start; parent traceback for the tour. KAT 13: classic 4-city cycle=80/path=65, tour reconstructs (starts 0, all 4, cost 80), triangle/2-city/1-city, asymmetric directed cycle=3 | TESTED ✓ |
| 452 | 2-SAT solver (implication graph + SCC) | forge_2sat.nova | grep:two_sat | two_sat(n, clauses) -> [satisfiable, assignment]. Decides if a set of "A OR B" 2-literal constraints can ALL hold at once, and returns a satisfying assignment, in LINEAR time (general SAT is NP-hard; the 2-literal case is fast). Powerful modeling: any "for each pair, at least one of two choices must hold" (on/off adjacency, either-or scheduling, 2-coloring, conflict resolution, 2-group assignment). Each (a OR b) = implications (¬a->b),(¬b->a); build the 2n-node implication graph, find SCCs (recursive Tarjan), UNSAT iff a variable shares a component with its negation, SCC order gives the assignment. Signed literals (+v/-v), vars 1..n. KAT 13: SAT instances with VERIFIED-valid assignments, unsat x1^¬x1 + classic 4-clause, forced-true variables | TESTED ✓ |
| 453 | Farey sequences + mediants | forge_farey.nova | grep:farey | farey(n) (all reduced fractions in [0,1] with denom<=n, increasing) + mediant + farey_count. Consecutive Farey terms a/b<c/d satisfy bc-ad=1, and the simplest fraction between them is the MEDIANT (a+c)/(b+d) - how to insert into sorted fractions with the SMALLEST new denominator. Underlies bounded-denominator best-approximation, "simplest fraction in an interval", Stern-Brocot tree, gear ratios, Ford circles. O(1)/term neighbor recurrence (no sort/gcd per term); length via Euler totient sum. KAT 17: mediant 1/3+1/2=2/5, F1-F5 exact, counts 2/3/5/7/11/23 matching generation, bc-ad=1 verified across F7 | TESTED ✓ |
| 454 | Assignment problem (Hungarian algorithm) | forge_hungarian.nova | grep:hungarian | hungarian (-> [total_cost, assignment]) + assignment_cost. Kuhn-Munkres for the ASSIGNMENT PROBLEM: min-total-cost one-to-one pairing of n agents to n tasks. Brute force is n! (10=3.6M); Hungarian is O(n^3) via row/col dual potentials + shortest augmenting paths. Workhorse of task/resource scheduling, driver-rider/bidder-item matching, min wiring/transport cost, multi-object tracking (detections->tracks). Non-negative int square matrix. KAT 11: classic 3x3 cost5/assign[1,0,2], brute-force cross-checks on 3x3/4x4/5x5 (proven optimal), assignment-validity, 1x1/2x2 | TESTED ✓ |
| 455 | Global minimum cut (Stoer-Wagner) | forge_stoerwagner.nova | grep:min_cut | min_cut(graph) = cheapest edge set whose removal splits the graph, minimized over ALL partitions. Unlike max-flow/min-cut (fixed source+sink), Stoer-Wagner finds the single weakest split with NO s/t to choose - for network reliability ("how hard to disconnect at all?"), graph clustering, image segmentation, community detection. O(V^3) via repeated min-cut phases: grow a set by always adding the most tightly connected vertex (max adjacency search), record the last vertex's connection, merge the last two, repeat; smallest candidate = global min cut. Symmetric int weight matrix; flat working matrix. KAT 8: 4-node cut=5, 4-cycle=2, bridge-between-triangles=1, star=2, 2-node, brute-force cross-checks | TESTED ✓ |
| 456 | Maximum bipartite matching (Kuhn) | forge_bimatch.nova | grep:max_bipartite_matching | max_bipartite_matching (-> [count, match_right]) + matching_size. Pair up as MANY items across two groups as possible when only certain pairings are allowed, each used once - workers->jobs, students->projects, slots->meetings, taxis->riders. Unlike Hungarian (min COST over a full assignment), maximizes the COUNT of unweighted pairings. Kuhn's augmenting-path: for each left item find a path that reshuffles matches to free a partner (+1 each success; recursion+shared state). By Konig max matching = min vertex cover; perfect iff Hall holds. [left,right] edges. KAT 11: perfect3, augmenting classic=3 valid, contended->1, Hall->1, 4x4=4 valid, empty->0, asymmetric 2x3=2 | TESTED ✓ |
| 457 | Ordered set with order statistics | forge_orderset.nova | grep:os_rank | os_new/add/remove/contains + os_kth (select) + os_rank + os_floor/ceiling + os_range/count_range + os_size. A sorted set of distinct ints that answers what a plain set/dict CAN'T: k-th smallest (select) and how many are less than x (rank) - powering leaderboards ("what rank is this score? who's 5th?"), live percentiles/medians, threshold counts, floor/ceiling nearest lookups. Sorted-array + binary search: query O(log n) (select O(1)), add/remove O(n) shifts - ideal for query-heavy sets. Distinct from forge_omap (a MAP, no rank/select). floor/ceiling return [found,value] (no null sentinel). KAT 25: dedup+sort, kth/rank, floor4=2/ceil3=5, range, remove, leaderboard rank+top | TESTED ✓ |
| 458 | Min + circular max subarray | forge_subarray.nova | grep:max_circular_subarray | ADDITIVE to forge_subarray: min_subarray_sum (Kadane for minimums) + max_circular_subarray (max contiguous sum in a CIRCULAR/wrap-around array). Circular = max(best non-wrapping Kadane, total - best-middle-to-remove); all-negative uses the non-wrapping answer so it isn't a spurious empty sum. For circular buffers, cyclic scheduling, wrap-around optimization. KAT 11: min -2/-5/allpos, circular non-wrap 3, wrap [5,-3,5]=10 / [3,4,8]=15, all-negative=-2/-1 | TESTED ✓ |
| 459 | Divisors + sigma/tau + perfect numbers | forge_divisors.nova | grep:num_divisors | divisors (all, ascending) + num_divisors (tau/d(n)) + sum_divisors (sigma) + aliquot_sum + is_perfect/is_abundant/is_deficient. Where forge_primes gives PRIME factors, this lists ALL divisors and the classic quantities: count, sum, proper-divisor sum -> classifies every n as perfect (=proper-sum: 6,28,496), abundant, or deficient. For scheduling/tiling (which group sizes divide evenly), fraction simplification, crypto, recreational math. O(sqrt n) trial division pairing i with n/i. KAT 24: div12=[1,2,3,4,6,12]/div36, tau 60=12/square16=5, sigma 12=28/28=56, aliquot 12=16/6=6, perfect 6/28/496 vs 12, abundant/deficient | TESTED ✓ |
| 460 | Seedable deterministic PRNG | forge_prng.nova | grep:prng_new | prng_new/next/int/bool + prng_shuffle/sample/choice. A SEEDABLE deterministic PRNG (xorshift32): same seed -> same stream, for REPRODUCIBLE simulations/Monte-Carlo, deterministic tests exercising randomness, procedural generation (same seed=same terrain), replayable randomized algorithms, shareable puzzle seeds. Opposite of forge_random_pick's OS CSPRNG - predictability is the FEATURE; NOT for passwords/tokens/keys. State threaded through calls. KAT 12: determinism (same seed=same 5-stream), different-seed differs, range bounds, coverage (all 0..5 hit), permutation-preserving shuffle, reproducible shuffle, sample distinct, choice | TESTED ✓ |
| 461 | Dice notation parser + roller | forge_dice.nova | grep:dice_roll | dice_parse (-> [count,sides,modifier]) + dice_min/max/mean_x2 + dice_roll(spec,seed). Parses tabletop DICE NOTATION "2d6+3" (also "dS", "NdS", +/-M): count, die size, flat modifier -> range + expected + a roll. Rolls go through forge_prng so same spec+seed = same result (reproducible loot/combat replays, shareable procedural seeds). For game logic, simulations, random stat/damage. Imports forge_prng. KAT 14: parse 2d6+3/d20/3d8-2, min5/max15/mean_x2=20, d1 deterministic (2d1+3=5/1d1=1), roll determinism + range [5,15] over 50 seeds | TESTED ✓ |
| 462 | Hamming(7,4) error-correcting code | forge_hammingcode.nova | grep:ham_encode | ham_encode (4 data bits -> 7) + ham_decode (-> [data4, error_pos]) + ham_data. Single-error CORRECTING code: CRCs/checksums DETECT corruption, Hamming REPAIRS a single flipped bit with no retransmission (ECC RAM, spacecraft telemetry, QR codes, flash). 3 parity bits at power-of-two positions each cover a bit subset, so failing checks spell the error position in binary (the syndrome). Distinct from forge_hamming (distance) + forge_crc (detection). Bit lists. KAT 8: encode 1011->0110011, clean round-trip ALL 16 patterns, single-error CORRECTION at EVERY of 7 positions (exhaustive) with reported position, clean syndrome=0 | TESTED ✓ |
| 463 | Closest pair of points (D&C) | forge_closestpair.nova | grep:closest_pair_dist2 | closest_pair_dist2(points) = squared distance of the two closest points, O(n log n) divide-and-conquer. Splits by x, solves halves, checks only a thin vertical STRIP around the divide (each point has O(1) closer neighbors) - beats O(n^2) all-pairs. For collision/proximity detection, deduping near-identical coords, nearest cities/sensors, clustering seeds. Exact + float-free via SQUARED distances (isqrt for actual). Index-based sorting avoids the list-of-lists sort hazard. KAT 7: small=2, tight-pair=1, duplicates=0, two=100, <2=-1, + 2 brute-force cross-checks incl a pair spanning the divide | TESTED ✓ |
| 464 | LFU (least-frequently-used) cache | forge_lfu.nova | grep:lfu_put | lfu_new/put/get/contains/size/freq. Bounded cache that evicts the LEAST-FREQUENTLY-accessed entry when full (vs forge_lru's least-recently-used). LRU can drop a popular item just for being idle briefly; LFU keeps often-hit items - fits stable popularity (CDN edges, hot DB rows/queries, frequent config/lookup). Each entry counts accesses (get+put bump); smallest count evicts, ties -> LRU so a cold newcomer doesn't displace an established key. Rebuild-dict eviction (forge_lru pattern). KAT 17: freq eviction (hot a survives / b out), LRU tie-break, update-no-evict, freq counting, hot key surviving 3 evictions | TESTED ✓ |
| 465 | DP classics (matrix-chain/rod-cut/egg-drop) | forge_dpclassics.nova | grep:matrix_chain_order | matrix_chain_order (optimal parenthesization of a matrix product) + rod_cutting (max revenue cutting a rod by per-length prices) + egg_drop (min guaranteed trials to find a threshold). Three canonical DP SHAPES: best-split-point (also DB join ordering / expression eval), unbounded-knapsack/partition (cutting stock, max-value change), minimax worst-case search (A/B threshold, stress-to-break). Integer/exact, flat DP arrays. KAT 17: matrix-chain [1,2,3,4]=18/GfG=26000/CLRS[30,35,15,5,10,20,25]=15125, rod 8=22/4=10, egg (2,10)=4/(2,100)=14/(3,14)=4/(2,36)=8 | TESTED ✓ |
| 466 | Word break / segmentation (DP) | forge_wordbreak.nova | grep:word_break | can_break (can s split entirely into dictionary words?) + word_break (recover one segmentation). Splits run-together strings: hashtags (thisisahashtag->this is a hashtag), squashed domains/slugs, unspaced languages (CJK/Thai), old all-caps, and as a step in spell-correction/OCR cleanup. Naive is exponential; DP is O(n^2*lookup): dp[i]=first i chars segment iff some dp[j] true AND s[j..i) is a word; back-pointers reconstruct. Dict = word list (set internally). KAT 12: leetcode/applepenapple/hashtag true, catsandog false, empty/single, reconstruction validity (concat==input) | TESTED ✓ |
| 467 | De Bruijn sequences (FKM) | forge_debruijn.nova | grep:de_bruijn | de_bruijn(k,n) + de_bruijn_string. The SHORTEST cyclic sequence over a k-symbol alphabet containing every length-n string exactly once (binary n=3 = "00010111", length 8). The trick behind minimal-keypress PIN brute-forcing, exhaustive combinatorial test vectors in minimal space, the O(1) lowest-set-bit hack (De Bruijn constant + lookup), and rotary-encoder position encoding. Built by Frank-Ruskey (FKM), emitting the lexicographically smallest sequence by concatenating length-dividing Lyndon words (recursion + shared state). Length exactly k^n. KAT 12: exact B(2,3)/B(2,4)=0000100110101111, lengths k^n, + full-coverage verify (every cyclic window distinct + all present) for (2,3)/(2,4)/(3,2)/(3,3)/(4,2) | TESTED ✓ |
| 468 | Permutation rank/unrank (factoradic) | forge_factoradic.nova | grep:perm_unrank | perm_rank (permutation -> lexicographic index) + perm_unrank (index -> k-th permutation, no enumeration) + to/from_factoradic + num_permutations. Gap-free integer NUMBERING of all n! arrangements: pick a uniform-random permutation from a random index, store a shuffle as one number, resume an exhaustive search, stripe permutations across workers by index. Factorial number system (digit at place i is 0..i, weight i!) = the Lehmer code. O(1) random access vs forge_permute's streaming. KAT 17: n!=24/120, factoradic 5->[2,1,0], unrank/rank, EXHAUSTIVE round-trip rank(unrank)==k over all 24 (n=4) AND all 120 (n=5) | TESTED ✓ |
| 469 | Josephus problem | forge_josephus.nova | grep:josephus | josephus(n,k) (survivor, O(n) recurrence, no simulation) + josephus_order (full elimination order) + josephus_survivor_sim. Circle of n, every k-th eliminated - who survives + in what order. For fair round-robin elimination/tournament seeding, leader counting-out, deterministic load-shedding, any skip-k-remove-repeat process. Survivor via J(1)=0, J(i)=(J(i-1)+k) mod i; order via direct simulation. 0-based. KAT 11: survivor 7,3=3/10,2=4/100,1=99, order 7,3=[2,5,1,6,4,0,3], simulation matches recurrence across 180 (n,k) pairs (n=1..30,k=1..6) | TESTED ✓ |
| 470 | Zeckendorf + Fibonacci coding | forge_zeckendorf.nova | grep:zeckendorf | zeckendorf(n) (unique non-consecutive Fibonacci sum) + fib_encode/fib_decode (Fibonacci code). Zeckendorf: every positive int = unique sum of non-consecutive Fibonaccis (10=8+2), built greedily. As bits -> Fibonacci coding: a variable-length prefix-free UNIVERSAL integer code ending every word in "11" (never appears inside since consecutive Fibs aren't both used) - more bit-error-robust than binary (a flip can't cascade past the next 11) + self-synchronizing, used in small-int compression. KAT 13: z10=[8,2]/z100=[89,8,3]/z12=[8,3,1], sum-property n=1..200, enc 1=11/10=010011, round-trip ALL n=1..300, ends-in-11 | TESTED ✓ |
| 471 | Integer nth-roots + perfect-power | forge_introots.nova | grep:iroot | iroot(x,n) (floor nth root, binary search + overflow-safe capped power) + is_perfect_square/cube + is_perfect_power (-> [found,base,maximal-exponent]). EXACT integer roots, no float (avoids the classic sqrt(16)=3.9999 cast bugs). Perfect-power test = required first step of AKS primality + factoring; floor-root in Pollard rho, tiling, hashing. Full 64-bit safe. Distinct from forge_math isqrt (n=2 only). KAT 23: roots 100/2=10, 27/3=3, 26/3=2, 1e18/3=1e6, psq 144, pcube 1000, perfect-power 64=[2,6]/81=[3,4]/1000=[10,3], non-powers 7/2 | TESTED ✓ |
| 472 | Collatz (3n+1) sequences | forge_collatz.nova | grep:collatz_steps | collatz_steps (stopping time) + collatz_sequence (full path) + collatz_max (peak). The famous 3n+1 hailstone process: halve if even, triple-plus-one if odd, always (unproven) falling to 1. For teaching iteration/recursion, a performance micro-benchmark, chaotic-deterministic test data, and exploring a notorious open problem. KAT 13: steps 6=8/27=111(famous)/7=16, seq 6=[6,3,10,5,16,8,4,2,1], peak 27=9232/7=52, seq-len=steps+1, all n=1..50 reach 1 | TESTED ✓ |
| 473 | Three-way merge (diff3) | forge_merge3.nova | grep:merge3 | merge3(base, ours, theirs) -> [merged_lines, conflict_count]. Line-based THREE-WAY MERGE at the heart of git-merge, config reconciliation, collaborative editing: one-sided changes taken automatically, identical changes on both sides collapse, only overlapping DIFFERENT edits become conflicts (<<<<<<< ======= >>>>>>> markers). Aligns all three via common lines: LCS base-vs-each side, keep lines matched in BOTH as stable anchors, resolve each inter-anchor region by which side changed it. KAT 14: no-change, ours-only/theirs-only auto, NON-OVERLAPPING both merge cleanly A|b|C, same-change collapse, conflict markers+count, insert, both-delete | TESTED ✓ |
| 474 | Running median (two-heap streaming) | forge_runningmedian.nova | grep:rm_add | rm_new/add/size + rm_median_x2 + rm_median_lower. Keeps the MEDIAN of a growing stream in O(log n)/add, O(1) lookup via the classic two-heap trick (smaller half in a max-heap, larger half in a min-heap, kept balanced). The median is the ROBUST middle - shrugs off outliers unlike the mean (onlinestat), right for latency p50, spiky sensors, "typical value so far" over a live feed without re-sorting. Even counts reported as lower-middle or 2x-median (exact integer, no fractions). Max-heap = min-heap of negated values. KAT 13: streaming sequence, odd/even, duplicates, + brute-force cross-check after EVERY insertion of a 15-element stream | TESTED ✓ |
| 475 | Histogram (data binning) | forge_histogram.nova | grep:histogram | histogram(data,bins)->[counts,min,max] + histo_range (explicit range) + histo_bin (single value) + histo_edges. Bins CONTINUOUS data into equal-width buckets - unlike frequency/mode (exact-value counts), shows the SHAPE of a distribution (clustering, skew, outliers). Behind distribution charts, image tone histograms, latency buckets (Prometheus), data sanity checks. Auto or explicit range; last bucket inclusive of max; below/above clamp to end bins. KAT 15: uniform [2,2,2,2,2], explicit range, total preserved, bin lo/hi/mid/clamp, edges 0..100/5, skew [4,0,0,1], all-same | TESTED ✓ |
| 476 | 2-D k-d tree nearest neighbor | forge_kdtree.nova | grep:kd_nearest | kd_build + kd_nearest (nearest point index) + kd_nearest_dist2. Preprocess points once, answer "closest stored point to this query?" in ~O(log n) vs scanning all. Recursively splits the plane alternating x/y so a query PRUNES subtrees whose region is already farther than the best found. For nearest-facility lookup (store/charger/tower), kNN acceleration, game proximity culling, map snapping. Adapts to clusters (unlike grid spatialhash); serves many queries vs one-shot closestpair. Exact SQUARED int distances. Node-pool + recursion. KAT 9: spot queries, self=0, + EXHAUSTIVE brute-force cross-check over ~340 grid queries across 2 point sets, single-point | TESTED ✓ |
| 477 | 2-D Fenwick tree (BIT) | forge_fenwick2d.nova | grep:fw2_update | fw2_new/fw2_update (point add) + fw2_query (prefix rectangle) + fw2_rect_sum (any sub-block). A 2-D Binary Indexed Tree: add to any cell AND sum any rectangle, each O(log R * log C), on a LIVE grid that keeps mutating - where static 2-D prefix sums (forge_prefixsum) would need a full rebuild per change. For continuously-updated heatmaps/analytics, counting points in a moving rectangle, editable integral images, event-in-region-so-far streams. Each tree cell covers a lowest-set-bit range; rectangle = inclusion-exclusion of 4 prefixes. Lowbit via i-(i&(i-1)) (no negatives). KAT 8: updates/queries, negative delta, + EXHAUSTIVE cross-check of EVERY rectangle vs naive grid after 7 updates to a 6x7 board | TESTED ✓ |
| 478 | Lazy-propagation segment tree | forge_lazysegtree.nova | grep:lseg_range_add | lseg_build + lseg_range_add (add to a whole RANGE) + lseg_range_sum, both O(log n). Fenwick updates ONE cell; this adds to every element in [i,j] in one log-op via LAZY tags: an internal node remembers a pending amount and only pushes it to children when a query looks inside, keeping bulk updates cheap. Workhorse for interval bookkeeping: seat/inventory reservations over date ranges, cumulative range increments, span brightness/volume, range toggles. Range-ADD/range-SUM variant. KAT 9: sums, range-add (overlapping), + EXHAUSTIVE cross-check of EVERY range sum vs naive array after 6 range-adds to a 10-elem array | TESTED ✓ |
| 479 | Augmented interval tree (stabbing/overlap) | forge_intervaltree.nova | grep:itree_overlap | itree_build + itree_overlap (intervals overlapping a range) + itree_stab (intervals containing a point). Balanced tree keyed on start where each node records the MAX end in its subtree -> queries PRUNE branches that can't reach, O(log n + k). For calendar/meeting conflicts, IP/port range lookup (firewall/router), genomic feature queries, range-matching rule engines. Indexes a COLLECTION for repeated queries (complements forge_interval's 2-interval ops). Inclusive int [start,end]. Index-sort avoids list-of-lists hazard. KAT 9: stab/overlap spot queries + EXHAUSTIVE brute-force cross-check (~275 range + ~106 point queries incl nested intervals) | TESTED ✓ |
| 480 | Treap (randomized balanced BST) | forge_treap.nova | grep:treap_insert | treap_new/insert/erase/contains + treap_size + treap_rank + treap_select. A treap stays balanced through RANDOMNESS not rotations: each key gets a random priority, the tree is a heap on priorities + BST on keys -> O(log n) height whp with tiny code. Ordered SET with O(log n) insert/erase/membership PLUS order statistics (rank=#keys<x, select=k-th smallest) via cached subtree sizes. Unlike forge_orderset (sorted array, O(n) insert) it mutates in true log time - for large constantly-changing sets with ranking/percentile queries: live leaderboards, order books, sliding-window stats. Split/merge impl, deterministic hash priorities (reproducible). KAT 12: insert-with-dups, select for EVERY index + rank for ALL 0..100 vs sorted reference, erase+re-check, oob=-1, empty | TESTED ✓ |
| 481 | Peak/valley finding (unimodal) | forge_peakfind.nova | grep:find_peak | find_peak (a local max, O(log n) even on UNSORTED arrays) + find_valley + is_mountain + mountain_peak. Binary search normally needs sorted data, but a peak can be found in log time in any array: at a midpoint, the higher neighbour must lead uphill to a peak. For a mountain (strictly up then down) it pinpoints the summit - the max of a unimodal cost/latency curve, brightest scan sample, trajectory turning point, best-setting-when-metric-rises-then-falls. Distinct from monotonic bisect. Boundaries = -inf so an end can peak. KAT 19: find_peak mountain/mono/single/empty + valid-peak property on unsorted, find_valley, is_mountain (plateau/short/mono rejected), mountain_peak | TESTED ✓ |
| 482 | Maximum-sum submatrix (2D Kadane) | forge_kadane2d.nova | grep:max_submatrix_sum | max_submatrix_sum + max_submatrix_rect (corners [top,left,bottom,right]). The rectangular sub-block with the largest total - 2D generalization of Kadane (forge_subarray). Trick: fix top+bottom rows, collapse each column between them into a 1-D array, run 1-D Kadane -> O(rows^2*cols) vs O((rows*cols)^2) brute. For brightest/darkest image region, most-profitable heatmap/spreadsheet zone, densest 2-D signal cluster, best rectangle in scored maps. Integer matrix. KAT 7: classic GfG=29, all-pos whole, all-neg single, single cell, rect-corners sum to max, brute-force cross-checks | TESTED ✓ |
| 483 | Largest rectangle (histogram + all-1s grid) | forge_maxrect.nova | grep:largest_rectangle_histogram | largest_rectangle_histogram (biggest rect under bars, O(n) monotonic stack) + maximal_rectangle (largest all-1s block in a 0/1 grid). Monotonic stack of increasing bars closes off taller ones when a shorter appears; maximal_rectangle accumulates per-column 1-run heights row by row then runs the histogram solver. For largest empty/whitespace region on a page/UI, biggest free rectangle in packing/floorplan, maximal foreground/background image areas, skyline problems. Distinct from kadane2d (max SUM). KAT 12: histogram classic=10/valley=12/incr=9 + brute-checks, maximal LeetCode=6/all-zeros/all-ones/single-row | TESTED ✓ |
| 484 | Trapping rain water + max container | forge_rainwater.nova | grep:trapping_water | trapping_water (water pooled in an elevation profile) + max_container_area (most water between 2 lines). Water sits above a bar up to min(tallest-left-wall, tallest-right-wall) minus its height; two-pointer walks inward advancing the smaller-wall side (whose level is then fixed) in one O(n) pass, O(1) memory. Models basin/terrain retention, container fill between barriers, valley capacity over any 1-D height series. KAT 14: classic=6/[4,2,0,3,2,5]=9/[3,0,2,0,4]=7, mono/flat/small=0, brute-force cross-check on 5 profiles, container-most-water=49 | TESTED ✓ |
| 485 | Next-greater element (monotonic stack) | forge_nextgreater.nova | grep:next_greater | next_greater + next_smaller (to the right) + previous_greater (to the left) + stock_span. For each element find the nearest LARGER/smaller neighbor - a monotonic stack answers all at once in O(n) by keeping only elements that could still be someone's answer. Engine behind daily-temperatures (days until warmer), stock-span (prior days with price <= today), skyline/histogram rendering, span/nearest-boundary analytics. -1/0 where none. KAT 12: ng [4,2,4,-1,-1]/desc/asc/single, next_smaller, previous_greater, stock_span classic=[1,1,1,2,1,4,6], brute-force cross-check on 4 arrays | TESTED ✓ |
| 486 | Dutch national flag / 3-way partition | forge_dutchflag.nova | grep:dutch_flag | dutch_flag (sort an array of only 0/1/2 in ONE pass) + partition3 (rearrange into [<pivot | ==pivot | >pivot], returns [array,lt,eq]). Dijkstra's three-pointer sweep, O(n)/O(1). 3-way partition is the heart of a QUICKSORT that stays fast with MANY DUPLICATE keys (2-way degrades to O(n^2)); also for below/at/above bucketing, ternary grouping, moving zeros/nulls to an end. In place. KAT 13: sort-colors variants, partition3 lt/eq counts + region property (<,==,> verified), pivot-absent, many-duplicate-pivot | TESTED ✓ |
| 487 | Subarrays summing to a target | forge_subarraysum.nova | grep:subarray_sum_count | subarray_sum_count + subarray_sum_exists + subarray_sum_indices. Count/find contiguous subarrays summing to exactly k in O(n) via PREFIX SUMS + hashmap: (i..j] sums to k iff prefix[j]-prefix[i]==k, so sweep asking "have I seen (current prefix - k)?". Crucially handles NEGATIVES (where sliding-window breaks). For "how many windows net exactly this amount", target-run detection (score/calories/budget), zero-sum segments, time-series pattern counts. KAT 12: count [1,1,1]/2=2, negatives, all-zero=6, classic=4, exists, indices sum-verify, brute-force cross-check w/ negatives | TESTED ✓ |
| 488 | Longest consecutive run | forge_longestrun.nova | grep:longest_consecutive | longest_consecutive (longest run of consecutive integers in an UNSORTED array) + longest_consecutive_start + max_consecutive_ones. O(n) via hash set - drop all values in, only count runs from a true run-start (predecessor absent), each element visited <=2x, beating O(n log n) sorting. For gap/coverage analysis (longest unbroken span of IDs/days/ports), streak detection, defrag heuristics. Duplicates ignored. KAT 12: [100,4,200,1,3,2]=4 start 1, 0..8=9, empty/single/dups/negatives, max-consecutive-ones=3 | TESTED ✓ |
| 489 | Product except self (prefix/suffix) | forge_productexcept.nova | grep:product_except_self | product_except_self: for each i, the product of every OTHER element, WITHOUT division. The "total/this" shortcut explodes on zeros; the O(n) trick is two sweeps - fill each slot with the product to its LEFT (prefix), then sweep back multiplying the product to its RIGHT (suffix). Handles zeros perfectly (one zero -> only that slot non-zero; two+ -> all zero). Archetypal prefix/suffix-product pattern: leave-one-out aggregates, normalize weights without the current item, division-free numerics. KAT 9: [1,2,3,4]=[24,12,8,6], one/two zeros, negatives, single/empty, brute-force cross-check | TESTED ✓ |
| 490 | Matrix transforms (rotate/transpose/spiral/flip) | forge_matrixtransform.nova | grep:rotate_cw | transpose + rotate_cw/rotate_ccw (90 deg) + flip_horizontal/flip_vertical + spiral (clockwise readout). The geometric transforms for a grid that's a PICTURE or BOARD (vs forge_matrix's LA): rotate a sprite/photo/tile, quarter-turn a game board, transpose a table, walk a grid spirally to print/serialize. Each returns a NEW matrix (input untouched) and they compose (2 rotations=180, transpose+flip=rotate). Any equal-length rows, any elements. KAT 13: transpose/cw/ccw/flip on 2x2, composition (cw x2=180, cw+ccw=identity), non-square 2x3, spiral 3x3=[1,2,3,6,9,8,7,4,5]/2x3/3x4 | TESTED ✓ |
| 491 | N-Queens (backtracking) | forge_nqueens.nova | grep:nqueens_count | nqueens_count (# solutions) + nqueens_solution (one placement). Place N queens so none attacks another - the canonical BACKTRACKING demo: try each column of a row, recurse, abandon the instant a placement conflicts, pruning huge swaths vs checking all N^N boards. O(1) conflict check via column + two-diagonal occupied flags (/ = row+col, \ = row-col). Template for constraint satisfaction / combinatorial search (scheduling with exclusions, placement, exact cover). KAT 15: famous count sequence 1,0,0,2,10,4,40,92 (n=1..8), solutions valid+complete for n=4/8, no-solution n=2/3 empty | TESTED ✓ |
| 492 | Sudoku solver + validator | forge_sudoku.nova | grep:sudoku_solve | sudoku_solve (fills a 9x9, 0=blank) + sudoku_valid + sudoku_is_complete. Same BACKTRACKING engine as N-Queens: find next empty cell, try each 1-9 not already in its row/column/3x3 box, recurse, undo on a dead end - racing past what a 9^81 brute force never could. Compact demo of constraint propagation + search that transfers to scheduling/timetabling/packing. Flat-81 internal board + recursion. KAT 12: canonical Wikipedia puzzle solved with KNOWN solution cells verified ([0][0]=5/[0][2]=4/[8][8]=9/[4][4]=5), complete+valid, two-5s invalid, unsolvable->empty | TESTED ✓ |
| 493 | Conway's Game of Life | forge_gameoflife.nova | grep:next_generation | next_generation (one step) + life_run (N steps) + life_population. The famous cellular automaton: a 0/1 grid evolves by 4 rules on each cell's 8 neighbours (live w/ 2-3 survives, else dies; dead w/ exactly 3 born) - trivial local rules, complex emergent behaviour. Used for real work: procedural cave/terrain generation (smooth random noise into caverns), image erosion/dilation, crowd/epidemic toy models, generative art. Finite board (edges dead), returns new grid. KAT 10: blinker oscillator (h->v->h period-2), block still-life, single-cell dies, birth rule (L-tromino->block), population | TESTED ✓ |
| 494 | Word search in a grid (DFS) | forge_wordsearch.nova | grep:word_search | word_search(grid, word): can a word be traced through a letter grid via 4-directional adjacent steps, never reusing a cell? Classic Boggle/word-search puzzle - a clean DFS-with-BACKTRACKING showcase: from each matching start, walk to a neighbour matching the next letter, mark cells used so the path can't cross itself, unmark on dead-ends, short-circuit on match. Same shape as constrained grid path search, cell-template matching, snake/route validation. Grid = list of equal-length row strings, case-sensitive, no diagonals. KAT 11: LeetCode ABCCED/SEE=true, ABCB=false(reuse), single/absent, corner-turning path, too-long, single-row, AA/AAA no-reuse | TESTED ✓ |
| 495 | Subset sum + partition | forge_subsetsum.nova | grep:subset_sum_exists | subset_sum_exists + count_subsets_sum + can_partition. Pick SOME (non-adjacent) subset summing to a target - NP-complete in general, but O(n*target) DP tracks reachable totals: sweep numbers, mark every reachable-total + this-number as reachable. Distinct from contiguous forge_subarraysum. Underpins the PARTITION problem (split into two equal-sum halves - balance teams/loads/a bill), exact change/budget from a fixed set, scheduling/allocation feasibility. Non-negative ints. KAT 15: exists 9=true/30=false/edges, count 10->3 / four-ones-sum2->6, can_partition [1,5,11,5]=true/odd-total=false | TESTED ✓ |
| 496 | Greedy classics (jump/gas/house-robber) | forge_greedy.nova | grep:jump_game | jump_game (reach the end?) + min_jumps (fewest hops) + gas_station (circular-route start) + max_non_adjacent_sum (house robber). One-pass GREEDY where local-optimal reaches global-optimal, O(n) no DP table: jump tracks farthest reachable; min_jumps is greedy BFS by jump-levels; gas_station starts just past where the tank last went negative (if total fuel>=cost); house-robber keeps incl/excl running maxima. For reachability/hop-count routing, circular scheduling/logistics, non-conflicting max-value selection. KAT 15: jump yes/no/single/zeros, min_jumps 2/2/ones=3, gas classic=3/none=-1/start=4, robber 12/4/edges | TESTED ✓ |
| 497 | Search in rotated sorted array | forge_rotatedsearch.nova | grep:search_rotated | search_rotated (find target, O(log n)) + find_rotation_point (index of min/wrap) + find_min_rotated. Binary search in a sorted array that was ROTATED (two sorted runs joined at a wrap point): at each midpoint work out WHICH half is properly sorted, then decide if the target is in that half's range - still O(log n). For circular buffers/ring logs (wrapped data), a clock/schedule starting mid-day, sorted data whose start offset drifted. Distinct from bisect (non-rotated). Distinct ints. KAT 12: spot searches, rotation-point/min, + EXHAUSTIVE cross-check over ALL 7 rotations (every element + absent + min) | TESTED ✓ |
| 498 | Find unique - single/missing/duplicate | forge_finduniq.nova | grep:single_number | single_number (XOR - the loner when all else appears twice, O(1) space) + missing_number (XOR indices^values, overflow-safe) + find_duplicate (Floyd cycle detection - value-as-next-pointer, the duplicate is the cycle entrance, O(1) space, array unmodified). Beat hash-set/sum approaches on time+memory. For data-integrity checks (corrupted/duplicated record), id-set reconciliation, classic array puzzles. KAT 13: single 1/4/loner, missing 2/8/first-0, duplicate Floyd 2/3/9 | TESTED ✓ |
| 499 | Anagram detection + grouping | forge_anagram.nova | grep:is_anagram | is_anagram + anagram_signature (canonical sorted-char key) + group_anagrams (partition words into mutual-anagram groups). Two strings are anagrams iff same character multiset; the signature collapses every anagram to one key. For word games (Scrabble/"what words from these letters"), dedup of differently-ordered token sets, clustering by char content. GOTCHA FOUND: sorting a list of char_at() single-char strings by index-shift CORRUPTS (they alias) -> signature built by integer char-COUNTING sort (ord) instead. KAT 15: listen/silent=true, dormitory/dirtyroom, signatures, group [eat,tea,ate|tan,nat|bat]=3 groups + membership | TESTED ✓ |
| 500 | Longest palindromic subsequence (DP) | forge_lps.nova | grep:longest_palindromic_subseq | longest_palindromic_subseq (longest same-forwards-backwards run when DELETIONS allowed) + min_insertions_palindrome. Unlike a palindromic SUBSTRING (forge_manacher, contiguous), the letters can be scattered - LPS of "character" is "carac"=5. O(n^2) DP: dp[i][j] grows by matching both ends (s[i]==s[j] -> keep both, recurse inward) or dropping one. Consequences: min chars to INSERT to make a palindrome = n - LPS; LPS = LCS of s with its reverse. For DNA/RNA secondary structure, approximate/palindromic matching, edit similarity. KAT 13: bbbab=4/character=5/racecar=7/abc=1, min-insertions abc=2/leetcode=5 | TESTED ✓ |
| 501 | Longest common substring (DP) | forge_lcsubstring.nova | grep:longest_common_substring | longest_common_substring (length) + longest_common_substring_str (the text). The longest CONTIGUOUS run appearing in both strings - unlike longest common SUBSEQUENCE (may skip chars), the match must be unbroken (abcdef/zabcf share "abc"=3). O(n*m) DP: dp[i][j]=length of common substring ENDING at a[i-1],b[j-1] - extend the diagonal on match, reset to 0 on mismatch, answer = grid max. Core similarity/overlap primitive: near-dup/plagiarism, shared portion of filenames/URLs/DNA reads, fuzzy joins, diff heuristics. KAT 10: GeeksforGeeks/GeeksQuiz->"Geeks", empty/none/same, brute-force cross-check on 4 pairs | TESTED ✓ |
| 502 | Stock profit (multi-transaction DP) | forge_stockprofit.nova | grep:max_profit_unlimited | max_profit_unlimited + max_profit_with_fee + max_profit_k. Max buy/sell profit under richer rules than single-transaction best_buy_sell: unlimited trades (greedy - capture every upward step), unlimited with a per-trade FEE (state-machine DP on cash/hold), and AT MOST k transactions (DP over transaction count, falls back to unlimited when k unconstraining). Core of trading backtests + any "enter/exit a position repeatedly for max gain with switching costs or limited moves". KAT 12: unlimited 7/asc4/desc0, fee=2->8, k=2->7/k=1->5/k-large->unlimited/k=0->0 | TESTED ✓ |
| 503 | Meeting rooms / max concurrent overlap | forge_meetingrooms.nova | grep:min_meeting_rooms | min_meeting_rooms (peak simultaneous overlap = rooms/servers/lanes needed) + can_attend_all (does 1 suffice?). Two-pointer SWEEP: sort starts + ends separately, walk a clock - each start before the next end needs a room, each end frees one, high-water mark = answer, O(n log n) no per-minute sim. Core of scheduling/capacity planning: room/resource allocation, connection-pool sizing, concurrency limits, double-booking detection. End-exclusive (meeting ending at t frees for one starting at t). KAT 11: classic=2/no-overlap=1/all-overlap=4/touching=1, can_attend, single/empty, brute-force cross-check | TESTED ✓ |
| 504 | Merge sorted sequences (+ median) | forge_mergesorted.nova | grep:merge_sorted | merge_sorted (two-way, O(m+n)) + merge_k_sorted + median_of_two_x2. Two-pointer merge of ascending lists - the merge step of merge sort + the standard way to union ordered result sets without re-sorting. k-way for combining leaderboards, merging DB shards, external sort. median_of_two_x2 = 2x median (exact int). For pre-sorted data: ordered log/time-series streams, paginated sorted API pages, sorted index intersect/union - far cheaper than concat+re-sort. KAT 12: merge basic/empty/dups/uneven, k=3/with-empty, median odd=4/even=5, sorted-property | TESTED ✓ |
| 505 | Digit utilities (count/reverse/nth) | forge_numprops.nova | grep:reverse_digits | ADDITIVE to forge_numprops: digit_count (# decimal digits, 0->1) + reverse_digits (reverse digits, sign preserved, 1230->321) + nth_digit (k-th digit from the right, 0-indexed). Everyday integer digit manipulation for checksums, formatting/validation, number puzzles (the classic "reverse integer"). Complements existing digit_sum/digital_root/is_palindrome_number. KAT 17: digit_count 0=1/4567=4/1e6=7, reverse 123=321/1230=321/-45=-54/palindrome, nth 4567[0..3]=7654/oob=0/neg | TESTED ✓ |
| 506 | String/word reversal | forge_strreverse.nova | grep:reverse_words | reverse_string (flip end-to-end) + reverse_words (reverse word ORDER, collapse spaces: "the quick brown fox"->"fox brown quick the") + reverse_each_word (letters within each word). For palindrome checks, buffer reversal, right-to-left word streams, flipping "Last, First" names, re-ordering path/URL segments, simple obfuscation. Normalizes whitespace, no stray separators. KAT 14: reverse_string basic/empty/palindrome, reverse_words extra-space-collapse/name-reorder/spaces-only, reverse_each_word | TESTED ✓ |
| 507 | K closest points | forge_kclosest.nova | grep:k_closest_points | k_closest_points (to origin) + k_closest_to (to a target), ranked by distance. Batch "closest k" over a plain list (vs single-nearest kdtree): N nearest stores/drivers/friends, kNN classifier's k voters, spatial recommendations, closest-few map results. Exact + float-free via SQUARED distance; sorts an INDEX array (coords never corrupted), ties keep original order. Increasing-distance output. KAT 8: closest-1/2/all, k>count, to-target with tie-stability, nearest-first/farthest-last property | TESTED ✓ |
| 508 | Missing ranges / gap detection | forge_missingranges.nova | grep:missing_ranges | missing_ranges (inclusive [start,end] holes in [lo,hi] not in a sorted list) + count_missing + is_fully_covered. Walk the sorted list once, note each jump of 2+ between consecutive present values plus the edges, O(n). The everyday "what's NOT covered": unallocated IP/port/ID blocks, missing sequence numbers/pages (which packets/records didn't arrive), free slots between busy ranges, coverage-map gaps. Sorted input in [lo,hi]. KAT 12: classic [2-2,4-49,51-74,76-99], empty/full/edges/single-gap, count_missing=95, is_fully_covered | TESTED ✓ |
| 509 | List chunk/windows/pairwise/flatten | forge_listchunk.nova | grep:fn chunk | chunk (fixed-size batches, last may be short) + windows (every contiguous k-run) + pairwise (k=2) + flatten (one level). The itertools-style list reshaping constantly needed but easy to get subtly wrong at boundaries: chunk for paging/API batching/grid layout, windows for moving averages/n-grams/context features, pairwise for diffs/adjacent deltas, flatten to merge lists of lists. Any element type. KAT 13: chunk uneven [1,2|3,4|5]/oversize/empty, windows 2/3/too-big/exact, pairwise, flatten with empties | TESTED ✓ |
| 510 | Set algebra on lists | forge_setops.nova | grep:list_union | unique + list_union (either) + list_intersection (both) + list_difference (A not B) + symmetric_difference (exactly one). Reconciliation over collections: what's in either/both/A-only/changed. Hash-set makes each O(n+m) vs naive O(n*m); de-dups naturally. For merging id lists, shared tags/followers, diffing permission/feature sets, added/removed records between exports, cleaning duplicates. FIRST-SEEN order (stable, not sorted). KAT 15: unique first-seen, union/dups/disjoint, intersection order-preserved, difference all/none, symdiff same->empty | TESTED ✓ |
| 511 | Text stats (sentences/chars/longest word) | forge_text.nova | grep:sentence_count | ADDITIVE to forge_text: sentence_count (runs of ./!/? as one boundary; trailing text counts) + char_count (with/without spaces) + longest_word. Content-analysis stats for editors, readability/SEO, writing tools. Complements existing word_count/reading_time/truncate/excerpt/highlight. KAT 14: sentences 3/ellipsis=2/no-trailing=3/no-punct=1, char_count with=5/without=3, longest_word tie/leading-spaces/empty | TESTED ✓ |
| 512 | String padding/alignment | forge_pad.nova | grep:pad_left | pad_left (right-justify) + pad_right (left-justify) + center + zfill (zero-fill, keeps leading '-'). Fixed-width output workhorse: aligned terminal/report columns, right-aligned numbers/currency, zero-filled sortable ids/dates/versions, centered headings. Over-width strings returned UNCHANGED (never truncated). Single-char fill. KAT 14: pad_left 42->00042/exact/over, pad_right hi..., center even **hi**/odd *hi**, zfill 42->0042 / -7->-007 | TESTED ✓ |
| 513 | ASCII character classification | forge_charclass.nova | grep:is_alpha | is_digit/is_alpha/is_alnum/is_upper/is_lower/is_whitespace/is_punct + to_upper_char/to_lower_char. The tiny predicates every lexer/parser/validator/sanitizer is built from (C ctype.h / the character half of a regex class): tokenize source/config, validate identifiers/usernames, split on word boundaries, strip/detect control chars, custom trim/case. Case helpers only touch letters. 7-bit ASCII, single-char strings. KAT 25: all classifications + case conversions incl non-letters unchanged | TESTED ✓ |
| 514 | Substring find utilities | forge_strfind.nova | grep:index_of | index_of (first) + last_index_of + count_occurrences (non-overlapping) + str_contains + starts_with + ends_with. Bread-and-butter substring queries: parse a field before a delimiter, check a file extension/URL scheme, count a token in a log line, validate a prefix, gate a heavier match. Plain direct scanners (for repeated search of one long text use KMP/Boyer-Moore). Empty needle -> 0. Case-sensitive. KAT 20: index_of world=6/first/absent/empty, last_index_of, count 3/overlap-nonoverlap=2/mississippi-s=4, contains, starts/ends_with incl too-long | TESTED ✓ |
| 515 | Trim / strip string ends | forge_trim.nova | grep:trim_chars | trim (both ends) + ltrim + rtrim + trim_chars (custom character set). Strip whitespace or chosen characters from the ENDS - one of the most-used string ops: clean user input before validate/store, tidy a file line, drop a trailing newline, strip surrounding quotes/slashes, normalize so " yes " == "yes". Interior whitespace preserved. (forge_ascii.strip_control removes control bytes ANYWHERE; this is end-trimming.) KAT 15: trim spaces/tabs/interior-preserved/all-ws, ltrim/rtrim, trim_chars "xxhelloxx"->hello / "/-path-/"->path multi-set | TESTED ✓ |
| 516 | Skyline problem (upper envelope) | forge_skyline.nova | grep:skyline | skyline(buildings [left,right,height]) -> [x,height] key points. The union/upper-envelope of overlapping rectangles via SWEEP: turn each building into start+end events, sweep left processing all events at each x together, tally active heights, emit a corner whenever the tallest active height changes. Powers city-silhouette/layered-chart rendering, interval/rectangle union outlines, collision broad-phase, max-coverage-over-time from overlapping [start,end,value] spans. Grouping events per-x handles tie-breaking; index-sort avoids list-of-lists hazard. KAT 5: classic LeetCode [2:10,3:15,7:12,12:0,15:10,20:8,24:0] exact, single/identical-merge, + EXHAUSTIVE brute-force cross-check of height at every x on 2 sets | TESTED ✓ |
| 517 | Cuckoo filter (membership + DELETE) | forge_cuckoofilter.nova | grep:cuckoo_insert | cuckoo_new/insert/contains/delete/count. Approximate-membership filter like Bloom but SUPPORTS DELETION + more space-efficient at low FP rates. No false negatives, small tunable false-positive rate, far less memory than storing items. Stores a short FINGERPRINT in one of two candidate buckets; when both full, kicks an existing fp to ITS other bucket (cuckoo move) - and the alt bucket = i XOR hash(fp) so no item remembers which bucket it's in, making delete trivial (erase fp from either). For dedup-with-expiry, evictable membership caches, set reconciliation. prng eviction; power-of-2 buckets. KAT 12: NO false negatives (40 inserted all found), count, delete-all->empty, delete absent=false, low FP rate (<20/200) | TESTED ✓ |
| 518 | Modular sqrt + discrete log (Tonelli-Shanks, BSGS) | forge_modsolve.nova | grep:mod_sqrt | legendre + is_quadratic_residue + mod_sqrt (Tonelli-Shanks) + discrete_log (baby-step giant-step). The two "solve for x" problems forge_modarith's ext_gcd/mod_inverse do NOT do: x*x=n mod p (modular SQUARE ROOT) and g^x=h mod p (modular LOGARITHM) - the number-theory core of crypto. mod_sqrt decompresses EC points from x-coord, tests/solves quadratic residues; uses the p==3(mod4) direct formula else general Tonelli-Shanks. discrete_log is meet-in-the-middle in ~sqrt(p) work/memory (generic DH/ElGamal attack, Pohlig-Hellman). legendre gates whether a root exists. Integer-only, p up to ~3e9 (products fit i64). KAT: legendre QR/non-residue/divides, mod_sqrt both parity paths (7 fast / 13,17,41,10009 Tonelli) w/ EXHAUSTIVE root^2==n brute-check over full residue set, discrete_log primitive-root 3 mod7 + subgroup-only 2 mod7 (no-log cases) + self-check 2 mod101 x=0..99 | TESTED ✓ |
| 519 | Multiplicative group mod n (Moebius, order, primitive root) | forge_multgroup.nova | grep:primitive_root | mobius + multiplicative_order + is_primitive_root + primitive_root. The structure of the units mod n, sitting directly beneath forge_modsolve's discrete_log: a primitive root IS a generator, an element's order IS its cyclic-subgroup size. mobius = squarefree sign function (Moebius inversion, Mertens, inclusion-exclusion/coprime sieves). multiplicative_order = smallest k with a^k=1 mod n (period of a modular sequence, repeating-decimal length = ord(10 mod n), Carmichael/RSA reasoning); -1 if not coprime. primitive_root = smallest generator reaching every unit (the base for a DH group / discrete-log). is_primitive_root uses the fast test g^((p-1)/q)!=1 for each prime q|p-1. Integer-only, moderate n. KAT: mobius fixed + EXHAUSTIVE divisor-sum identity sum_{d|n}mu(d)=[n==1] over n=1..40; order fixed + non-coprime=-1 + a^order==1 self-check over all coprime a of {5,7,9,11,13,16}; primroot 2..101 incl 23->5 + order-(p-1) self-check | TESTED ✓ |
| 520 | Combinatorial counting numbers (Bell, Stirling, derangements, partitions) | forge_combnums.nova | grep:stirling2 | bell + stirling2 + stirling1 + derangement + partitions. The counting numbers BEYOND forge_combinatorics' factorial/nCr/Catalan. bell(n)=set partitions of n labelled items (equivalence relations); stirling2(n,k)=partitions into exactly k blocks (surjection count); stirling1(n,k)=permutations with exactly k cycles (falling-factorial<->power coeffs); derangement(n)=permutations with NO fixed point (hat-check, ~1/e); partitions(n)=unordered integer sums (coin-change with all denominations). Exact integer DP (Bell triangle, S(n,k)=k*S(n-1,k)+S(n-1,k-1), c(n,k)=(n-1)*c(n-1,k)+c(n-1,k-1), D(n)=(n-1)(D(n-1)+D(n-2)), unbounded-knapsack). KAT: fixed sequences B(0..10)/der(0..9)/p(0..10) + BIG p(50)=204226 p(100)=190569292 bell(10)=115975 + cross-identities sum_k S2(n,k)==bell(n) [n=0..12] and sum_k S1(n,k)==n! [n=1..8] | TESTED ✓ |
| 521 | Linear algebra over GF(2) (XOR equation systems) | forge_gf2.nova | grep:gf2_solve | gf2_rank + gf2_solve + gf2_xor_subset. Solve systems of XOR equations over the two-element field (add=XOR, mul=AND) - the reduction behind Lights Out & parity puzzles, linear error-correcting-code decoding, the final subset-XOR stage of quadratic-sieve/Dixon factoring, and boolean-matrix rank/nullspace. Over GF(2) Gaussian elimination is far simpler than over reals: no fractions, no stability pivoting, and a whole matrix ROW packs into ONE integer so eliminating a variable = a single XOR of two ints. gf2_rank via XOR basis (keyed by highest set bit); gf2_solve does full RREF on augmented int-rows (RHS at bit n), returns one particular solution (free vars=0) or empty=inconsistent; gf2_xor_subset finds a subset XORing to a target. n<=62 (augmented bit stays positive i64). Bit-vs-== precedence parenthesized. KAT: rank cases (3^5=6 dependent, staircase); determined/inconsistent/underdetermined solve; EXHAUSTIVE brute self-check - build system from every known x in {0,1}^4 over 5 eqns, solve, verify A x==b; xor_subset reachable/impossible(bit0 unreachable)/target-0 | TESTED ✓ |
| 522 | Linear Diophantine equation a*x+b*y=c over integers | forge_diophantine.nova | grep:diophantine | diophantine + diophantine_min_x + dio_check. Solve a*x+b*y=c in WHOLE numbers and ALL of them - the math behind coin/postage problems, cycle alignment, tiling counts, and the constructive core of CRT/modular-inverse. A solution exists iff gcd(a,b)|c; when it does there are infinitely many forming an arithmetic progression. diophantine returns [x0,y0,dx,dy] so every solution is (x0+k*dx, y0+k*dy), dx=b/g dy=-a/g; empty if none. diophantine_min_x picks the smallest x>=0. Handles both-zero (all pairs iff c=0), one-zero, and NEGATIVE coefficients (Bezout sign-normalized). Built on extended Euclid. KAT: 3x+5y=1 (dx=5,dy=-3, stepping k=-3..3 all valid), unsolvable gcd(4,6)!/5, common-factor 4x+6y=10, min_x in [0,dx), negative -3x+5y=1, + EXHAUSTIVE brute over a,b in[2,9] c in[0,20]: base satisfies + -+step stay solutions | TESTED ✓ |
| 523 | Pythagorean triples (Euclid parametric formula) | forge_pythagoras.nova | grep:pythagorean_triples | is_pythagorean + pythagorean_triples + primitive_pythagorean_triples. Whole-number right-triangle sides a*a+b*b=c*c, generated via Euclid's formula (a=m^2-n^2, b=2mn, c=m^2+n^2, primitive when m,n coprime opposite-parity) then scaled - O(answer) not O(limit^2) brute. For exact right angles in construction/layout, integer-exact graphics distances, lattice geometry, puzzle generation. Output FLAT [a,b,c,...] a<b<c sorted by (c,b,a) via INT-key encode/sort/decode (sidesteps list-of-lists sorting hazard). KAT: is_pythagorean; known members up to c=25 present; every emitted triple valid+ordered+c<=max+globally-sorted; DECISIVE cross-check Euclid-count==brute-enumeration-count at c<=30/60/100; no duplicates @60; primitive variant all gcd(a,b)==1 + excludes multiples (6,8,10) + includes 20,21,29 | TESTED ✓ |
| 524 | Frobenius / Chicken McNugget coin problem | forge_frobenius.nova | grep:frobenius_number | frobenius_number + is_representable + count_non_representable. Which amounts can't be made from unlimited coins of given denominations, and the LARGEST such (McNugget 6,9,20 -> 43). Additive cousin of forge_diophantine (there: exact solutions to one equation; here: any non-negative combination). A largest gap exists iff gcd(coins)==1 (else infinitely many unreachable). frobenius_number sweeps upward and provably stops after min-coin CONSECUTIVE reachable values (last_false = answer); is_representable = reachability DP; count_non_representable = genus. Returns -1 when no finite answer (gcd>1 or a coin of 1). KAT: classic 3,5=7 / 4,7=17 / 2,3=1 / McNugget 6,9,20=43 / 3,5,7=4 / 5,8,11=17 / 6,10,15=29; gcd>1 and coin-1 -> -1; genus 3,5=4; all-above-frob representable; frob-itself-unreachable + frob+1-reachable; + DECISIVE Sylvester cross-check over ALL coprime pairs a in[2,12] b in[a+1,13]: frob==ab-a-b AND genus==(a-1)(b-1)/2 | TESTED ✓ |
| 525 | Pell's equation x^2 - D*y^2 = 1 | forge_pell.nova | grep:pell_fundamental | pell_check + pell_fundamental + pell_solutions. Solve Pell's equation in positive integers (studied by Brahmagupta/Bhaskara pre-Pell). For non-square D infinitely many solutions; the fundamental (smallest) generates all others by recurrence. Gives best rational approximations x/y to sqrt(D), underlies square-triangular numbers, continued-fraction theory, some factoring. Fundamental read off the periodic continued fraction of sqrt(D): iterate convergents h/k, stop at first h^2-D*k^2==1 (integer isqrt, NO float). pell_solutions chains via (x1*xn+D*y1*yn, x1*yn+y1*xn). i64-exact while x^2 fits (D up to ~61+). KAT: fundamentals 2->(3,2) 3->(2,1) 5->(9,4) 6->(5,2) 7->(8,3) 13->(649,180) 61->(1766319049,226153980); perfect-square/<=0 -> empty; SWEEP every non-square D in[2,60] satisfies x^2-Dy^2==1 & positive; generated chains D=2 (3,2)(17,12).. & D=3 (2,1)(7,4)(26,15) all satisfy | TESTED ✓ |
| 526 | Egyptian fractions (Fibonacci-Sylvester greedy) | forge_egyptian.nova | grep:egyptian_fraction | egyptian_fraction + egyptian_check + all_distinct. Write a proper fraction as a sum of DISTINCT unit fractions (2/3=1/2+1/6) the ancient-Egyptian way, via the greedy algorithm: repeatedly subtract the largest unit fraction 1/ceil(b/a) that fits. Always terminates (numerator strictly shrinks) with strictly-increasing (distinct) denominators. Classic recreational/number-theory topic, exact-rational (no floats/repeating decimals), a provably-correct greedy that can blow up (5/121, 31/311). egyptian_check verifies a decomposition sums back EXACTLY via integer fraction accumulation + cross-multiply (no rounding). Input proper 0<a<b. KAT: 2/3->[2,6] 3/4->[2,4] 5/6->[2,3] 4/13->[4,18,468] 1/5->[5]; improper/0/equal -> empty; wrong decomposition rejected; + EXHAUSTIVE over EVERY reduced a/b with b<=40: sums back exactly + distinct + strictly-increasing + all denominators >=2 | TESTED ✓ |
| 527 | Exact integer matrix determinant (Bareiss) + int matmul | forge_intmatrix.nova | grep:det_bareiss | mat_mul + mat_identity + det (Bareiss) + det_cofactor. EXACT integer linear algebra - every other matrix routine (forge_matrix/forge_poly) is FLOAT and can report 0.0000001 for a singular matrix or 5.9999999 for det 6. Needed when the answer feeds an exact decision: invertible? vectors dependent? lattice unimodular? integer polygon/simplex signed area? polynomial resultant? det() uses the BAREISS fraction-free algorithm (Gaussian elimination rearranged so every intermediate stays an exact integer, each division guaranteed even) - avoids both fractions and cofactor's factorial blow-up; with row-swap pivoting + sign. det_cofactor (Laplace, O(n!)) is an independent reference. Uses NOVA nested-list mutation via row-reference idiom (deep-copied first). KAT: fixed 1x1/2x2=-2/diag/3x3=-306/triangular=24/singular=0/I4=I6=1; mat_mul [[19,22],[43,50]] + A*I==A; + BIG cross-check over 40 random matrices (n=2..4, entries [-4,4]): Bareiss==cofactor AND det(A*B)==det(A)*det(B) | TESTED ✓ |
| 528 | Gaussian integers Z[i] (norm, mul, Euclidean gcd) | forge_gaussint.nova | grep:gint_gcd | gint_norm/add/sub/mul/conj/div/mod/gcd/divides. Arithmetic on complex integers a+bi (a,b in Z), a Euclidean domain -> exact division-with-remainder and gcd. The natural setting for sum-of-two-squares (p=a^2+b^2 iff p=2 or p=1 mod4, gcd hands you a,b), Gaussian primes, factoring in Z[i], exact complex lattice math. Each is a [real,imag] list. NORM a^2+b^2 = multiplicative size measure; gint_div rounds the true complex quotient to NEAREST Gaussian int (guaranteeing norm(r)<norm(w)); gcd returns canonical first-quadrant associate. KAT: norm; (1+i)^2=2i, (2+i)(2-i)=5; divides; gcd(5,2+i) norm 5 divides both; + 3 BIG self-checks over 60 random pairs: norm(z*w)==norm(z)*norm(w), z==q*w+r with norm(r)<norm(w), gcd divides both operands | TESTED ✓ |
| 529 | GF(2^8) finite field arithmetic (AES/Reed-Solomon field) | forge_gf256.nova | grep:gf_mul | gf_add/mul/pow/inv/div + gf_exp_table/gf_log_table. The 256-element byte field under AES, Reed-Solomon (QR/CD/DVD/RAID-6), BCH. ADD=XOR (no carries, self-inverse), MUL=carry-less poly multiply reduced mod AES poly 0x11B (Russian-peasant, no tables) -> a genuine field: every nonzero byte has an inverse (a^-1=a^254 since a^255=1), so you can divide; commutative/associative/distributive hold. Exact overflow-free byte arithmetic where 1/x always exists. Generator-3 log/exp tables for fast bulk RS math. KAT: FIPS-197 vector 0x57*0x83==0xC1 (pins the AES field, not just any GF256) + 0x57*0x13==0xFE + xtime; EXHAUSTIVE a*inv(a)==1 for ALL 255 nonzero bytes; commutativity+distributivity over spread; div inverts mul (sampled all 256 a); log/exp consistency exp[log[z]]==z all z, order-255 (exp[255]==1), log-mul==gf_mul. Unlocks Reed-Solomon | TESTED ✓ |
| 530 | Reed-Solomon ECC encode + error detection (over GF256) | forge_rs.nova | grep:rs_encode | rs_generator_poly + rs_encode + rs_syndromes + rs_has_errors. The redundancy behind QR codes/CD/DVD/RAID-6/deep-space. Over GF(2^8) (imports forge_gf256): rs_encode appends nsym parity bytes so the codeword-as-polynomial is an exact multiple of generator g(x)=prod(x-3^i), i in 0..nsym-1. That algebraic fact IS the code: a clean codeword evaluates to ZERO at every root (all syndromes 0), and ANY corruption makes a syndrome nonzero -> detects up to nsym bad symbols with certainty. SYSTEMATIC (message = first k symbols unchanged + nsym parity). Full CORRECTION (Berlekamp-Massey/Chien/Forney) = deeper follow-on; this = encode+detect foundation. First non-leaf of the cluster (forge-only import, _fdb_one resolves transitively). KAT: gen poly length nsym+1 + monic; systematic preserves message; FUNDAMENTAL clean->all-syndromes-zero; BIG self-check 50 random (msg,nsym): clean=zero-syndrome + single corruption always detected; 4-symbol corruption detected | TESTED ✓ |
| 531 | Gale-Shapley stable matching (stable marriage) | forge_stablematch.nova | grep:stable_matching | stable_matching + is_stable + is_perfect. The algorithm behind the US medical-residency Match, school-choice, and market-clearinghouses (Roth/Shapley Nobel 2012). Given two equal-size ranked groups, produces a pairing with NO blocking pair (no proposer+receiver who'd both rather swap). Proposers propose down their list; receivers hold their best-so-far and trade up; provably terminates with a stable, perfect, proposer-optimal matching. Rank-table for O(1) preference compare; free-proposer stack via pop(). is_stable independently audits for blocking pairs. KAT: n=2 fixed + swapped-matching-detected-UNSTABLE (proves is_stable non-trivial); 3x3; + BIG randomized self-check 60 instances sizes 2..8 GS output ALWAYS perfect AND stable; size-mismatch -> empty. GOTCHA reconfirmed: `match` reserved -> using as var = misleading 'expected )' EOF parse error | TESTED ✓ |
| 532 | Galois LFSR (linear-feedback shift register) | forge_lfsr.nova | grep:lfsr_next | lfsr_next + lfsr_bits + lfsr_period + lfsr_is_maximal. The XOR-gate engines behind CRC, data scramblers (Ethernet/SATA/PCIe/DVB), spread-spectrum/GPS gold codes, flash/RF whitening, on-chip BIST test patterns, and classic stream-cipher keystreams. Galois form: shift right, XOR the whole register by the tap mask when the shifted-out bit is 1 (one-branch fast variant). With maximal taps an n-bit register cycles through ALL 2^n-1 nonzero states (m-sequence). Taps = int bitmask; seed nonzero (all-zero = dead fixed point). KAT: 8-bit 0xB8 period exactly 255 + DECISIVE maximality proof - one full period visits EVERY nonzero 8-bit state exactly once, never hits 0, returns to seed; balance 128 ones over 255 bits (2^7); 16-bit 0xB400 period 65535; non-maximal tap 0x80 correctly rejected | TESTED ✓ |
| 533 | Hamiltonian path / cycle (backtracking) | forge_hamiltonian.nova | grep:hamiltonian_path | build_adj + hamiltonian_path + hamiltonian_cycle + is_hamiltonian. Visit EVERY vertex once (cycle: + return to start) on an ARBITRARY graph - existence question, unlike weighted forge_tsp/forge_heldkarp (cheapest tour on complete graph). Route/tour planning, DNA fragment assembly (OLC), snake/board puzzles, PCB drill / CNC tool-path order, the yes/no core under TSP. NP-complete -> backtracking with edge-pruning (extend along real edge to unvisited vertex, undo when stuck); recursion + shared-mutable path/visited + pop() backtrack (all NOVA-safe). Adjacency matrix. KAT: K4 both; path-graph path-yes/cycle-NO; cycle-graph cycle-yes; disconnected none; star K1,3 none; DECISIVE Petersen (10v) Ham-PATH-yes but Ham-CYCLE-NO; K7 cycle; every result verified via is_hamiltonian | TESTED ✓ |
| 534 | Sprague-Grundy combinatorial game theory (Nim) | forge_grundy.nova | grep:grundy_subtraction | mex + nim_value + nim_is_winning + grundy_subtraction + grundy_xor. The Sprague-Grundy theorem: EVERY impartial game (same moves for both, last-to-move wins) equals a single Nim pile of its "Grundy number", and a sum of independent games is won/lost by XOR-ing their Grundy numbers - reducing Nim/take-away/Kayles/coin-turning/puzzle-endgames to arithmetic (position LOSING for mover iff Grundy/XOR == 0). Engine = mex (minimum excludant). nim_value = XOR of pile sizes; grundy_subtraction tabulates a take-away game's Grundy numbers via DP+mex; grundy_xor combines games. KAT: mex; Nim 1,2,3=0(P-pos)/1,2,4=7(win)/equal-piles-cancel k=0..30; subtraction {1,2,3} Grundy(i)==i%4 & {1,2}==i%3 over 0..40 (CLOSED-FORM cross-check); {2,3} mex-self-consistency; SG theorem sum-of-two-{1,2,3}-piles == (a%4)^(b%4) & losing iff 0 over all a,b in 0..12. GOTCHA hit: `{` in a STRING literal triggers interpolation -> parse error (use plain text in labels) | TESTED ✓ |

) + tsv_build + tsv_parse_dicts (header-keyed). Spreadsheet paste, data export. Complements forge_csv_parse. KAT round-trip + dicts | TESTED ✓ |

 handling, no spurious trailing row; csv_parse_dicts (header→dict); csv_field safe accessor | untested (syntax✓) |
