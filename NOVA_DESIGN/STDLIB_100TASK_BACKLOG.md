# STDLIB 100-TASK CAMPAIGN — Backlog & Ledger

**Rhythm (owner, 2026-07-13):** develop tasks ONE BY ONE, continuously, NO stopping. Per-module
compile+run+KAT during build (fast, correctness gate). **NO mid-campaign full arc** — run the heavy
`nova_ci` both-mode (NORMAL+FULLRC) arc **ONCE at the end of ~100 tasks.** A task = 1 coherent library
(~12 modules). Modules written to BOTH `std/<cat>/` AND `nova-compiler/std/<cat>/` (byte-identical);
KAT tests in `nova-compiler/test_programs/_<mod>_test.nova`; add to `_orphan_coverage_manifest.txt`.

**Sourcing:** backlog is grep-verified against the existing std/ tree (gap-analysis workflow `wwr9euk59`,
12 domain surveyors vs JDK/Python/Go/Rust). std/ is very mature (math 72, text 65, collections 48…) —
every task below was dup-checked absent. Ordered LOW-RISK / high-value first.

## DONE (per-module KAT-gated at build; final both-mode arc deferred to campaign end — see rhythm)
## Campaign at **47 tasks committed** (2026-07-16). Strategic note: owner directed libraries are CONTINUOUS/
## open-ended — build high-value ones, then SHIFT to the NOVA_MASTER_PLAN (language ceilings + frameworks).
## Pivot underway after the last high-value general libraries (crypto batteries).
| # | Task | Modules | Commit |
|---|---|---|---|
| 1 | std/compress/ | 12 | cdac0a6b |
| 2 | std/finance/ | 12 | f8a20eb3 |
| 3 | std/color/ | 12 | bf7b7c05 |
| 4 | std/automata/ | 12 | 9e0cfc5d |
| 5 | std/validation/ | 12 | e2c38ad7 |
| 6 | std/dsp/ | 12 | 0d8b9765 |
| 7 | std/testing/ | 12 | ec43eec6 |
| 8 | std/media/ (image+audio codecs: ppm pgm pbm pam bmp tga qoi farbfeld png xpm ico wav) | 12 | df3d1d7b |
| 9 | std/net/dns (RFC 1035 message codec: name-compression/header/question/rr/rdata-a/txt/mx/soa/srv/message/query-EDNS0) | 12 | 1fb6637f |
| 10 | std/time/ depth (isoweek/period/strftime/strptime/holiday-easter/businessday/pduration/age/quarter/daycount/weeknum/reltime) | 12 | cbde0113 |
| 11 | std/net/ inetproto (ipv4/ipv6/tcp/tcpopts/udp/icmp/arp/eth/pseudo/ntp/dhcp + inet checksum) | 12 | 77aad8da |
| 12 | std/data/asn1 (DER/BER: tag/length/int/bool/null/oid/octetstring/bitstring/string/time/seq/parse) | 12 | a3e31a17 |
| 13 | std/collections/ heaps (daryheap/skewheap/heapsort/indexedpq/minmaxheap/intervalheap/leftistheap/binomialheap/weakheap/heapselect/kwaymerge/radixheap) | 12 | 08e09122 |
| 14 | std/hash/ breadth (crc8/crc16/crc32c/crc64/fnv64 + xxh32/xxh64/murmur3_32/murmur3_128/siphash/hashcombine/classichash) | 12 | fcdb35ff + bc239644 |
| 15 | std/parsing/ toolkit (calc/exprvars/rpn/pratt/peg/combinators/tokenizer/glob/jsonpath/wildcard/template/numparse) | 12 | 45f2353a + e9736b3d |
| 16 | std/text/ diff+patch (diff_lcs/diff_char/diff_patience/diff_histogram/diff_myers/unified_emit/unified_parse/patch_apply/patch_reverse/diff3_merge/conflict_format/patch_stats) | 12 | bd3e8542 + 53e08098 |

| 17 | std/term/ ANSI (color16/color256/style/cursor/screen/strip/box/progress/spinner/hyperlink/columns/gradient) | 12 | 3dbac697 |
| 18 | std/random/ PRNG engines (mt19937/pcg32/splitmix64/xoshiro256/xoshiro128/lcg/well512/lfsr/wyrand/jsf64/sfc64/lehmer) | 12 | eaf30328 |
| 19 | std/cipher/ classical (playfair/bifid/foursquare/twosquare/hill/columnar/affine/autokey/beaufort/polybius/scytale/adfgx) | 12 | 998728fe |
| 20 | std/midi/ MIDI protocol + SMF | 12 | 58f607f8 |
| 21 | std/net/lineproto application-protocol codecs | 12 | 3a1a0ab1 |
| 22 | std/compress/univcodes universal integer codes | 12 | 03674ade |
| 23 | std/music/theory music-theory | 12 | f5ddcf6b |
| 24 | std/game/cards card-game | 12 | 41e2806e |
| 25 | std/game/maze maze algorithms | 12 | 87e1db74 |
| 26 | std/data/subtitles SRT/WebVTT | 12 | c755af8e |
| 27 | std/net/binproto MQTT/CoAP/STOMP/POP3 | 12 | 5e63a90a |
| 28 | std/data/feeds feed + playlist formats | 12 | ccf6d953 |
| 29 | std/compress/adaptive dictionary+transform coders | 12 | 3c80d921 |
| 30 | std/physics/mechanics | 12 | 5d8f8ae3 |
| 31 | std/chem/formulas | 12 | 13cf1f55 |
| 32 | std/physics/waves_em waves/optics/thermo/EM | 12 | c9bdb5fa |
| 33 | std/geo/spherical | 12 | 377c5a60 |
| 34 | std/audio/synth | 12 | e8fd1504 |
| 35 | std/math/tseries time-series analysis | 12 | 0c549e06 |
| 36 | std/numeric/scientific numerical methods | 12 | 0e892a74 |
| 37 | std/game/rating rating + tournament | 12 | bc1127f3 |
| 38 | std/text/markdown Markdown->HTML renderer | 12 | 9673815c |
| 39 | std/data/geodata geo-format codecs | 12 | 86805af5 |
| 40 | std/game/puzzle puzzle solvers | 12 | 1fcd2220 |
| 41 | std/image image-processing | 12 | 54d7670a |
| 42 | std/calendar calendar-system | 12 | a4d802e7 |
| 43 | std/barcode barcode-symbology | 12 | 18eb418e |
| 44 | std/finance advanced finance | 12 | ab784b9d |
| 45 | std/numeric/distributions probability distributions | 12 | 7892247e |
| 46 | std/os pure path/binary/byte utilities | 12 | 2fc466b2 |
| 47 | binary file-format parsers — os/{elf_header,pe_coff,macho,tar_ustar,ar_archive,cpio} compress/{zip_read,gzip_member} data/{wasm_hdr,java_class,pdf_xref} media/id3 | 12 | 2cf8cc74 |
| 48 | std/crypto/ batteries (cryptanalysis merkle commitment securetok otp pwentropy blockpad randomtest) — round-trip KATs | 8 | 0d660ddc |
| 49 | std/text/ NLP+search (bm25 keyword textrank collocation nlp_eval ngram_lm stopwords langdetect sentiment readability2 summarize spellcorrect) — **INDEPENDENT ADVERSARIAL AUDIT gate added**: 3 KAT-passing modules REJECTED (summarize+spellcorrect silent-sort-corruption, langdetect dead trigram) → FIXED + re-verified with >3-elem probes | 12 | 5c4de071 |
| 50 | std/bio/ bioinformatics (seq codon kmer orf fastx restriction protein align motif phylo population seqstats) — audit: 5 clean + 5 fixed (fastx OOB-read, population hw_chi NaN, seqstats empty-MW, restriction palindrome biology, align REAL NW/SW traceback); re-verified with own probes | 12 | 131a36bc |
| 51 | std/control/ control+robotics (pid statespace transferfn dcmotor so3 quatext trajectory motionprofile discretefilter ratelimit sim1d lqr) — audit: ratelimit REJECT (neg quantize) FIXED, pid/motionprofile guards added, so3/quatext self-audited. **robotkin HELD (13th): confirmed layout-dependent float-ABI codegen bug in rk_fk — cos on raw float param; needs ATTENDED compiler fix** | 12 | e28007d7 |
| 52 | std/units/ conversions (length mass time temperature area volume speed pressure energy power data_size angle_unit) — SAFE domain (pure multiply, no trig). Audit recomputed ALL ~96 factors vs NIST/SI: all correct; 1 doc-only fix (cup label). NOTE: energy test = `_units_energy_test` (avoids `_energy_test` = std/physics/energy) | 12 | 751e97b3 |
| 53 | std/money/ INTEGER money (minor currency format parse round allocate exchange tax discount aggregate accounting validate) — cents-as-int, no floats. Audit caught systematic NEG-numerator half_up bug (exchange/tax/discount) + format sign-position, FIXED + verified with neg probes. NOTE: 5 generic basenames collided (format/round/tax/validate/parse) → tests are `_money_<mod>_test`; 7 std tests | 12 | d36771ca |
| 54 | std/framing/ stream/serial framing (cobs slip hdlc kiss lenprefix delimiter bytestuff ppp_frame crc_frame stxetx escaping reassembly) — byte-oriented, SAFE, UNIQUE basenames (no collisions). Audit: 11 clean + 1 low doc-fix (bs_overhead). Canonical vectors confirmed: COBS paper vectors, CRC16-CCITT-FALSE=0x29B1, CRC32=0xCBF43926, PPP FCS tamper-detect | 12 | bacfa1e9 |
| 55 | std/sketch/ probabilistic structures (countmin cuckoofilter countingbloom hyperloglog distinctcount streamstats topk reservoir bloomscalable minhash simhash) — invariant KATs (no-false-neg, never-underestimate, Welford exact, seed-determinism). Audit stress-tested @200 items: 5 clean + 6 low doc/tuning + topk docstring FIXED. **quotientfilter HELD (12th): not a correct QF (unused run/cluster logic, qf_count 30% under, spurious-dup drops) — needs proper rebuild** | 11 | 3ea7b976 |
| 56 | std/combinatorics/ generators (permutations subsets compositions derangement multiset_perm cartesian nextperm powerset partition_gen catalan_struct lyndon setpartition) — exact counts (n!/C/2^n/D_n/Catalan/Bell/Stirling/p(n)/Lyndon) + no-dups + identities (Euler, conjugate-involution, gray-adjacency) all audit-verified. nextperm REJECT (neg-index sort sentinel in permutation_index) FIXED + re-verified (24/24 ranks) | 12 | 78b994c5 |
| 57 | std/matrix/ linear algebra (matmul mattranspose matnorm matdet matadjugate matrank matgauss matsolve matinverse matlu matqr mateigen) — float, NO trig (safe). Audit verified ALL invariants on fresh 3x3 (A·inv=I, LU=PA, QR=A + Q orthonormal, A·x=b, A·adj=det·I, eigen trace/det). matlu singular-solve FIXED (returns []); added msv_make/mi_make constructors | 12 | 63552467 |
| 58 | std/poly/ polynomials (polyadd polymul polydiv polyeval polygcd polyderiv polyroots polyinterp polymodp chebyshev bernstein polyfromroots) — exact int-coeff arith + round-trip (q·b+r=a, from_roots eval≈0, GCD divides both). polyroots REJECT FIXED: pr_quadratic float-slot bug (inline sqrt) + pr_cubic trig-precision (replaced with bisection+deflate) → re-verified [1,2,3] eval≈0. Medium notes documented (polymodp large-p overflow, polyfromroots int-only roots) | 12 | 769255fe |
| 59 | std/interval/ (interval_arith interval_bound interval_overlap allen interval_merge interval_set interval_partition interval_cover interval_scheduling interval_stab interval_map interval_tree2) — integer/exact. Audit ALL 12 ACCEPT (scheduling optimality + weighted DP brute-force-confirmed; set difference/complement + Allen 13-relations verified fresh). Cleanest task. ib_shrink over-shrink guard added; allen point-interval limitation documented | 12 | 12372ee9 |
| 60 | std/textlayout/ (wordwrap textpad truncate_text justify textindent textcenter textfill hangindent reflow textcolumns linebreak texttable) — pure-string, exact KATs verified fresh + edge cases. 11 ACCEPT + textcolumns CONCERN (trailing whitespace on last column) FIXED (rstrip per line). texttable 'nested-loop bug' repro'd FALSE ALARM | 12 | 71b46c27 |
| 61 | std/geo/ grid encodings (pluscode maidenhead quadkey dms latlon_bbox antimeridian utm_zone tile_math grid_ref geo_normalize mgrs_grid geo_morton) — integer/string, NO trig (safe subset). Audit ALL 12 ACCEPT clean: round-trips hold + reference vectors independently verified (Maidenhead JN58td, quadkey 213 bit-checked, UTM zone 32 + Norway exceptions, DMS 40.4461, OSGB round-trip) + pole/antimeridian edge cases | 12 | 34e6a6dc |
| 62 | std/coding/ ECC (reed_muller repetition_code parity2d convolutional viterbi interleaver hadamard_code berger_code crc_param checksum_fletcher constant_weight dual_modular) — integer/bit. Audit ALL 12 ACCEPT clean: error correction verified at EVERY position (RM 8/8, Hadamard 64/64, Viterbi 12/12, TMR 24/24) + all CRC/Fletcher check values recomputed (0x29B1/0xCBF43926/0xF4/0x4B37/0xC8F0/0x024D0127) | 12 | b11f8030 |
| 63 | std/raster/ 2D graphics (bresenham thick_line raster_clip raster_circle raster_ellipse bezier_raster flood_fill polygon_fill raster_rect blit sprite_mask dither) — integer pixel math. Audit: 11 ACCEPT + raster_ellipse CONCERN (boundary pixels lie just outside exact ellipse) — verified NOT a bug (0 dups/0 asym/extremes present, standard rasterization); documented boundary-vs-containment. Bresenham reverse-symmetry, Bezier de-Casteljau, flood 4-vs-8, Bayer(2)=[0,2,3,1] verified | 12 | e7e97985 |
| 64 | std/game2d/ (aabb_collide circle_collide sat_collide spatial_hash quadtree_hit sweep_test grid_astar raycast2d tilemap steering2d collision_resolve motion2d) — float/no-trig/no-HOF. Audit ALL 12 ACCEPT: A* hand-traced optimal (len 9 around fresh maze wall) + valid path + [] no-path; SAT/MTV separates; swept-AABB TOI=gap/vel; raycast hit points exact; reflect v-2(v.n)n + impulse momentum. 2 low doc-notes (id-keying, quadtree boundary) | 12 | 205f64e3 |
| 65 | std/phonetic/ name-matching (nysiis cologne match_rating daitch_mokotoff phonex statcan_code roman_soundex onca lein phonetic_compare) — string algos, canonical reference-vector KATs. 12-skeptic adversarial audit recomputed canonical vectors INDEPENDENTLY (not trusting fleet KATs) + coverage-probed rule-heavy algos. Shipped **10**; **HELD caverphone + double_metaphone** — both are long-rule-table algos (50 / ~200 rules) and BOTH the fleet AND the auditor produced WRONG canonical values (I verified caverphone is canonically wrong on initial-vowel/internal-r/y names — drops initial vowel instead of →A, maps every r→A; DM missing initial-X→S + Spanish-LL, ~40/200 rules). No reference oracle available → deferred to attended effort w/ Apache-Commons/jellyfish oracle. FIXED before ship: phonex px_digit -1 dedup contract (was ignoring `prev`), nysiis ny_translate_char next-lookahead (EV/KN/PH/H/W now honored); cologne umlaut-comment + X-sentinel doc; daitch secondary-branch-dedup limitation documented. Rule-verified: Soundex R163/A261/T522, NYSIIS MACINTOSH→MCANTAS, Cologne 65752682, MRA BYRN, D-M 739400, LEIN digit-table, Jaro MARTHA/MARHTA 0.944 | 10 | ff89cf7c |
| 66 | std/rational/ exact fraction arithmetic (rfrac_core arith compare convert continued farey approx egyptian mixed harmonic vector format) — integer-based [num,den], first-principles reference-vector + round-trip + gcd-reduced-invariant KATs. Safe domain (hand-verifiable, no oracle needed). All 12 KATs green + hand-verified: 1/2+1/3=5/6, CF(415/93)=[4,2,6,7], F_5 (11 terms traced), egyptian(4/13)=[4,18,468], H(4)=25/12, simplest_between(2/7,3/7)=1/3, best_within(3.14,den<=10)=22/7, negatives (floor(-7/2)=-4, parse('-1 3/4')=[-7,4]) all correct | 12 | b012c57c |
| 67 | std/numeral/ number systems (bijective26 cardinal_en ordinal_en base_radix base_between factoradic zeckendorf balanced_ternary negabinary gray_code mixed_radix tally) — integer/string codecs, round-trip + reference-vector KATs (safe, hand-verifiable, no oracle). All 12 KATs green + verified: bijective col 703='AAA', cardinal 999999999999 spelled correctly, factoradic 463=[3,4,1,0,1,0]+Lehmer, Zeckendorf 100=[89,8,3], balanced-ternary 5='1TT', negabinary 6='11010', Gray to/from round-trips 0..31. Fleet died mid-run (session cap) → resumed via resumeFromRunId (cached batches instant, finished factoradic + zeckendorf/baltern/negabinary) | 12 | c27b9077 |
| 68 | std/bits/ scalar bit-manipulation (popcount bitscan bitrotate bitreverse bitfield pow2 lowbits zigzag bitpack bittricks rankselect binary_fmt) — integer-exact, reference-vector + round-trip KATs. Fresh (no dedicated bit-ops lib; bitset=data-structure, morton=2D). NOVA shift/precedence gotchas baked into brief (parenthesize & vs ==, >> is arithmetic, never shift 64). All 12 KATs green + adversarial probe PASS on the full-width/negative danger zone: popcount(-1)=64, popcount(i64_min)=1, log2_floor(1)=0, tk_abs/min/max on negatives, zigzag RT on -1e9. pow2 uses native clz builtin (63-clz) | 12 | f639d0be |
| 69 | std/scheduling/ CPU+disk+page algorithms (sched_fcfs sched_sjf sched_srtf sched_rr sched_priority sched_metrics sched_hrrn disk_fcfs_sstf disk_scan page_fifo page_lru_opt bankers) — integer-exact, textbook reference-vector KATs (hand-traced values). Fresh (no std/scheduling). All 12 KATs green: FCFS avg-wait 17, SRTF canonical [17,5,26,10], RR q=4 [30,7,10], Belady anomaly FIFO 3frames=9/4frames=10, SSTF seek 236, SCAN/LOOK, LRU/Optimal (7), Banker's safe-seq [1,3,4,0,2]+unsafe detection. Fleet self-caught the neg-index insertion-sort footgun (→selection sort) + a LOOK direction bug | 12 | 97ba3a59 |
| 70 | std/probability/ exact discrete probability (prob_dice coin binomial hypergeometric expected bayes geometric derangement birthday odds poker gambler) — integer counts + reduced [num,den] fractions, NO float, all reference vectors hand-verified. Fresh (no std/probability). All 12 KATs green: 2d6 sum-7=[1,6], C(5,2)=10, binomial(4,2,½)=[3,8], hypergeom, Bayes 11/566, derangement(4)=[3,8], birthday(6,3)=[5,9], poker four-kind=624 → [1,4165], gambler's-ruin(1,4,½)=[3,4] + biased r=2 → 6/7 | 12 | a229e43e |
| 71 | std/itertools/ eager list transforms (it_chunk window take_drop flatten zip accumulate group unique interleave partition rotate product) — list/int only, NO float, NO HOF, reference-vector + round-trip KATs. Expands under-served itertools (was 1 module). All 12 KATs green: chunk/window/take/drop/slice/step, zip↔unzip round-trip, prefix sums/products/diffs, RLE encode↔decode round-trip, dedupe order-preserving, interleave/intersperse/roundrobin, split/halve/parity, rotate/cycle, cartesian product. Aliasing-safe (fresh inner list per iter — KATs assert distinct sublist values) | 12 | 2970b263 |
| 72 | std/gametheory/ combinatorial games (subtraction_game sprague_grundy minimax negamax alphabeta matrix_game bulls_cows wythoff staircase_nim stone_game kayles euclid_game) — integer-exact, Grundy/nim + adversarial-search, hand-traced reference KATs. Fresh (nim/tictactoe already in std/game; used 12 different concepts). All 12 KATs green: subtraction {1,2,3}=n%4, mex/nim-sum, minimax=negamax=alphabeta=3 on classic tree, Wythoff P-pos (3,5), Kayles G(4)=1/G(5)=4, stone-game margin, Euclid game. Agent CAUGHT my spec typo: eg_is_winning(13,8) is TRUE not false (13/8>φ → N-position) — re-verified, KAT asserts correct value | 12 | a9d3fe5b |
| 73 | std/dp/ dynamic-programming classics (dp_subset_sum coin_change knapsack lcs lis edit_distance matrix_chain rod_cutting max_subarray min_path_grid partition_equal unique_paths) — integer-exact, hand-verified reference KATs. Fresh (edit_distance/lcs only appear as uses in bio/phonetic/text). All 12 KATs green: coin-change min(63)=6, knapsack=22, LCS/LIS=4, Levenshtein kitten→sitting=3, matrix-chain=18, rod-cutting(8)=22, Kadane=6, min-path=7, partition min-diff, unique-paths C(8,2)=28. Fleet self-caught `del` reserved-keyword (→dcost) + no-multiline-calls + used flat 1D tables to dodge list-of-list aliasing | 12 | 213822b7 |
| 74 | std/greedy/ classic greedy algorithms (activity_selection job_sequencing gas_station jump_game interval_greedy meeting_rooms min_platforms candy task_scheduler stock_buy_sell assign_cookies min_cost_ropes) — integer-exact, hand-verified reference KATs. All 12 KATs green: activity-selection=4, job-seq profit=142, gas-station start=3, jump min=2, min-arrows=2, min-platforms=3, candy=5, task-scheduler=8, stock 1txn=5/multi=7, min-cost-ropes=29. Fleet caught my spec typo (as_max_count([1,2],[3,4]) intervals overlap → 1 not 2). RENAMED interval_scheduling→interval_greedy (prefix isch_→ig_): std/interval/interval_scheduling already exists with same prefix — collision I missed in freshness grep, fixed before commit | 12 | a8af37e1 |
| 75 | std/search/ searching algorithms (binary_search search_bounds rotated_search peak_element ternary_search interpolation_search exponential_search jump_search integer_sqrt search_matrix bitonic_search first_true) — integer-exact, binary-search-family, hand-verified reference KATs. Fresh (no std/search; integer_sqrt distinct iq_ prefix vs math/introot ir_). All 12 KATs green: bsearch first/last, lower/upper bound, rotated [4,5,6,7,0,1,2] min-idx=4, peak-find, isqrt(17)=4/nth_root(32,5)=2/perfect-power, staircase matrix search, bitonic peak, monotone-predicate boundary. Fleet handled binary-search edges: round-up midpoint (no infinite loop), overflow sentinel in nth_root, div-guard in interpolation | 12 | 9df424bb |
| 76 | std/functional/ higher-order functions (fmap ffilter freduce fpredicate fzipwith ftakewhile fgroupby fsortby fiterate fcombinator faccumulate fchain) — integer HOF, functions-as-arguments (VERIFIED works across module boundaries via probe). Expands under-served functional (was 1 module). Constrained to avoid known limits: no closure returns, no lists-of-functions (explicit-arg pipe), sorts use selection/done-flag (no neg-index sentinel). All 12 KATs green: map/filter/foldl-foldr/scanl, all/any/count/find, zip_with, take/drop_while, group_by, stable sort_by, iterate, flip/on/compose, sum_by. Demonstrates NOVA HOF capability | 12 | d9cabaa8 |
| 77 | std/cli/ command-line parsing (cli_flags options positional subcommand getopt longopt kvparse usage validate quotesplit envargs argparse) — pure string/list, NO side effects (env passed in), reference-vector KATs. Expands under-served cli (was 1 module). All 12 KATs green: flag clustering -vvv=3, --opt value/--opt=value, positional extraction w/ value-opts, subcommand, getopt cluster-expand, longopt =split + unambiguous-abbrev, k=v parse, usage formatting, validate, quote-aware split, env fallback, integrated argparse. Fleet flagged a compiler behavior (len() on a string returned from a fn reads 0; direct list-index return corrupts) worked around via single-exit `let v; return v` — matches [[return-null-mistag]] class, REPROd separately (NOT a new bug; return-null-mistag is real but narrow) | 12 | 025f2b4e |
| 78 | std/semver/ semantic versioning (semver_parse compare prerelease bump caret tilde wildcard hyphen comparator satisfies sort coerce) — SemVer 2.0.0, pure string/int, hand-verified vs spec. Fresh (no std/semver). All 12 KATs green: strict parse+validate (no leading zeros), §11 prerelease chain alpha<alpha.1<alpha.beta<beta<beta.2<beta.11<rc.1<release, build-meta ignored in precedence, ^/~/x-range/hyphen/comparator bounds, ||-OR + space-AND ranges, precedence sort (1.10.0>1.9.0), coerce v1.2→1.2.0. semver_hyphen agent re-flagged the known-FALSE-ALARM while-and-nested pattern (used done-flag workaround; harmless) | 12 | 395d2cb5 |
| 79 | std/backtracking/ combinatorial generation (bt_combination_sum sum2 sum3 subsets_dup permutations_dup combinations_k generate_parens palindrome_partition restore_ip word_break letter_combos partition_k) — integer/string, count-focused + exact-small KATs. Fresh (no std/backtracking; distinct from combinatorics permutations/subsets). All 12 KATs green: comb-sum([2,3,6,7],7)=2, comb-sum2=4, unique subsets-dup([1,2,2])=6, unique perms-dup([1,1,2])=3, C(4,2)=6, parens Catalan(3)=5/(4)=14, palindrome-partition('aab')=2 + min-cuts=1, restore-ip('25525511135')=2, word-break count=2, letter-combos('23')=9, partition-k. Fleet caught my spec typo (subset_sum_target([1,2,3],2)=3 not -1, 6÷2 divisible); copy-before-push aliasing handled via pop-based backtracking | 12 | e43ac52e |
| 80 | std/uuid/ RFC 4122/9562 UUIDs (uuid_parse format version nil compare v4 v7 v8 namespace hex shortid bytes) — deterministic (rand-bytes/timestamp taken as INPUT), round-trip + reference-vector KATs. Fresh dir (util/uuid only had uuid4/is_uuid; distinct prefixes, no collision). All 12 KATs green: parse/format 8-4-4-4-12, version+variant extraction, v4/v7/v8 bit-stamping (v7 ts 1234567890123→011f71fb-04cb-7000, round-trips), v3/v5 namespace buffer prep + DNS/URL namespaces, hex32↔canonical, base62 shortid 128-bit round-trip, byte accessors. Agents handled the `{}` string-interpolation hazard via chr(123)/chr(125) | 12 | d0883880 |
| 81 | std/graph/ advanced algorithms (euler_path hamiltonian graph_coloring transitive_closure topological_kahn scc_kosaraju articulation_bridges two_sat boruvka_mst bipartite_matching cycle_enum graph_metrics) — integer, hand-verified on small graphs. Fresh basenames added to std/graph (distinct from existing 12). All 12 KATs green: Euler circuit/path, Hamiltonian K4, chromatic(triangle)=3, Kosaraju SCC, articulation/bridges (path→2 bridges), 2-SAT via implication-SCC, Borůvka MST=6, bipartite matching=3, girth/triangles, tree/bipartite/diameter. Batch C dropped mid-response → articulation_bridges + two_sat fixed by a focused agent (had used `slice()` on LISTS which is the STRING fn — correct list slice is `xs[a:b]`; rewrote with parallel int-array stacks). Re-verified: slice-on-list = wrong-API not compiler bug | 12 | 630e88fb |
| 82 | std/collections/ advanced data structures (union_find segment_tree lazy_segment_tree sparse_table monotonic_stack monotonic_queue difference_array prefix_sum_2d min_stack dsu_weighted binary_lifting cartesian_tree) — integer, stateful (reference-type mutation), hand-verified. Fresh basenames added to collections (Fenwick already covered; distinct). All 12 KATs green: DSU path-compression, segment/lazy-segment range ops, sparse-table RMQ, monotonic NGE/histogram=10 + sliding-window, difference/2D-prefix, O(1) min-stack, weighted-DSU potentials, binary-lifting LCA/kth-ancestor, min-Cartesian-tree RMQ. binary_lifting fixed LOG=floor(log2)+1; cartesian_tree fixed stack via index-assign (per xs[a:b]/no-slice-on-list lesson) | 12 | 0db7a4dd |
| 83 | std/statistics/ descriptive statistics (stat_mode quartiles dispersion shape correlation rank means zscore moving histogram entropy outliers) — float w/ tolerance KATs, distinct from math/stats basics. Fresh dir. All 12 KATs green + INDEPENDENT float spot-check (fresh inputs): Pearson=±1, geometric([2,8])=4, harmonic([1,4,4])=2, Shannon([3,3,3,3])=2, Gini-pure=0 — genuinely correct not curve-fit. Skewness/kurtosis moments, Spearman rank, Tukey quartiles/fences, SMA/EMA/WMA, histogram bins, entropy (ln via atanh series, no log builtin). MULTIPLE agents hit + worked around the float-slot/float-ABI hazard (sc_pearson split→4.87e+155 garbage → single-pass; sd_cv 1e-157 → hardening) — confirms [[reference-nova-float-slot]] class is real; workaround = single-pass/inline/bind-to-lets | 12 | 78acf9ff |
| 84 | std/tree/ rooted-tree algorithms (tree_traversal diameter subtree centroid isomorphism dp_mis dp_cover prufer ancestors path rerooting count) — integer, hand-verified on a fixed reference tree T5, distinct from graph. Fresh dir. All 12 KATs green: pre/post/bfs/euler order, 2-BFS diameter+center, subtree sizes [5,3,1,1,1], centroid/centroids, AHU isomorphism (path≠star), tree-DP MIS=3/MVC=2 (König), Prüfer [0,1,1] + round-trip, parent/depth/kth-ancestor, path/LCA, rerooting sum-distances, leaf/degree counts. Agent caught my spec error: sum_distances(T5)=[6,5,9,8,8] not [7,6,10,9,9] (I was +1; re-verified by hand) | 12 | 9b0b9f71 |
| 85 | std/httphdr/ HTTP header value parsers (http_cache_control content_disposition content_type accept qvalue range etag link cookie auth forwarded header_util) — string-based, RFC-cited, distinct from net/httpheaders (generic). Fresh dir. All 12 KATs green: cache-control directives + max-age, content-disposition filename, content-type mime/charset/boundary, Accept q-value best-match + wildcards (text/*, */*), Range resolve (0-499 / 500- / -500 suffix), ETag weak/strong + If-None-Match *, Link rel parsing (angle-bracket-aware split), Cookie/Set-Cookie attrs, Basic auth (full RFC4648 base64 decode dXNlcjpwYXNz→user:pass) + Bearer, X-Forwarded-For client IP, comma-list/param split (quoted-comma aware) | 12 | c8d98398 |
| 86 | std/geometry/ integer-exact computational geometry (cg_polygon_area point_in_poly polygon_centroid orientation segment_intersect convex_check polygon_simple triangulate point_line rotating_calipers bounding lattice) — ALL integer coords + exact results (2x-areas, squared dists, [num,den] rationals, indices) — NO float/sqrt. Fresh cg_ basenames added to std/geometry. All 12 KATs green: shoelace area₂ₓ=8, ray-cast point-in-poly, rational centroid [1,1,1], orientation cross, segment-intersect (proper/collinear/touch), convexity, self-intersection (bowtie), ear-clip triangulate (n-2 tris, area₂ₓ sum), point-to-line dist²=[4,1], diameter²=8, bbox, Pick's-theorem interior=1. Staged explicitly (existing dir) | 12 | bdc0e3b2 |
| 87 | std/permutation/ group algebra (perm_compose inverse cycles order sign apply power inversions fixed conjugate from_cycles derangement) — one-line notation, integer-exact, distinct from combinatorics enumeration. Fresh dir. All 12 KATs green: compose (p∘q)[i]=p[q[i]]=[2,0,1], inverse, canonical cycle decomposition [[0,1,2],[3,4]], order=lcm=6, sign=(-1)^(n-cycles), gather/scatter apply + round-trip, binary-exp power (+neg/inverse), inversion count, fixed points, conjugacy by cycle-type, from/to cycles, derangement subfactorial D(4)=9. order(pk)=order(p)/gcd formula | 12 | 37e94882 |
| 88 | std/geometry3d/ integer-exact 3D computational geometry (cg3_vector orientation plane line3 tetrahedron aabb3 sphere triangle3 barycentric3 distance3 convex3 lattice3) — [x,y,z] int coords, exact (6x-volumes, 4x-areas, squared dists, [num,den] rationals) — NO float/sqrt. Fresh dir. All 12 KATs green: cross/dot, scalar-triple orient, plane from 3 pts, point-to-line dist², tetra vol₆ₓ=1 (unit), AABB, sphere-contains + sphere-intersect (exact case-split no sqrt), triangle area²/normal, in-tetrahedron barycentric, point-segment/plane dist², convex-polyhedron half-spaces, 3D collinear/coplanar + gcd3 lattice | 12 | a5816fe9 |
| 89 | std/complex/ complex numbers (complex_arith div norm pow convert polar gaussian_gcd gaussian_prime two_squares poly_eval rotate geometry) — [re,im] mostly integer-EXACT (only magnitude uses float). Fresh dir. All 12 KATs green: (1+2i)(3+4i)=-5+10i, exact div [11,-2,5], norm², i^k powers ((1+i)²=2i), to_string '3-2i'/'i', magnitude 5.0 (float+tol), Gaussian-integer Euclidean gcd_norm, Gaussian-prime test (rules: axis p≡3 mod4 OR prime norm), sum-of-two-squares 25→[[0,5],[3,4]], Horner poly-eval (x²+1 at i=0), rotate-by-i, distance²/collinear | 12 | d3e2f011 |
| 90 | std/vector/ n-dimensional exact vector algebra (vec_arith dot cross3 distance projection angle combination reduce span gram lattice compare) — integer vectors, ALL results integer or reduced-rational [num,den] EXACT — NO float/sqrt. Fresh dir (distinct from geometry's fixed vector2/3). All 12 KATs green: add/sub/scale/negate/zeros, dot=32 + norm²=25 + orthogonality, 3D cross=[0,0,1] + scalar-triple=1 + area₄ₓ, L2²/L1/Linf distances (25/7/4), projection-coeff [num,den] + exact Cauchy-Schwarz parallel test, cos²-angle=[1,2] + acute/obtuse/orthogonal predicates, linear combination Σcᵢvᵢ, gcd-reduce to primitive [4,6,8]→[2,3,4], rank-over-Q via division-free Gaussian elim ([[1,2],[2,4]]→rank 1), Gram determinant via Bareiss = squared volume (diag(2,3)→36), signed lattice det + unimodular ([[2,1],[1,1]]→1, swap→−1), lexicographic/dominance/componentwise min-max. Fraction-free integer linalg, grep-verified zero float literals | 12 | 0cf90b6e |
| 91 | std/tensor/ integer-exact n-dimensional arrays / ndarray (tsr_create index reshape transpose elementwise broadcast reduce matmul concat slice pad convolve) — FRESH category, foundational for the AI pillar. Rep = [flat_row-major, shape]; all ops integer-EXACT, no float. All 12 KATs green + INDEPENDENT ADVERSARIAL VERIFICATION (4 verifiers, ~140 FRESH cases distinct from KATs, default-REJECT, 10/12 exercised 3-D): create/from_nested↔to_nested (incl 3-D [2,2,2]), strides([2,3,4])=[12,4,1] + ravel/unravel round-trip, reshape (flat-invariant) + expand/squeeze, transpose 2-D + 3-D full axis-reversal + general permute [2,0,1], elementwise add/sub/Hadamard/scale/neg, broadcasting numpy right-align rules incl to-a-COLUMN-vector + add_bc, reduce sum/prod/max/argmax + sum_axis + floor-mean, integer matmul [[1,2],[3,4]]×→[[19,22],[43,50]] + matvec/dot/outer/trace, concat/stack/vstack/hstack (axis-1 interleave verified), slice/row/col/diagonal/take, pad-border/roll/flip/tile, conv1d correlate-vs-convolve (kernel-flip) + conv2d VALID on RECTANGULAR img + cumsum/diff. My is_symmetric spec had a bug ([[1,2],[2,1]] IS symmetric); fleet correctly reconciled + added distinguishing cases | 12 | 3850c904 |
| 92 | std/logic/ propositional & boolean logic (logic_truthtable ops gates cnf dpll resolution quine karnaugh nnf boolexpr kleene properties) — FRESH category, ALL exact & verifiable by brute-force 2^n enumeration (zero oracle risk). truth-table-as-int over n≤4 + clause/literal CNF. Covers: tt var/const/eval/taut/sat/count, boolean algebra (and/or/xor/not/nand/nor/implies/iff), digital gates + half/full-adder/mux/majority, CNF/DNF eval + canonical minterm/maxterm forms + Horn, DPLL SAT (is_sat/solve/count), propositional resolution refutation, Quine-McCluskey prime implicants + minimal cover (Petrick), Karnaugh Gray-code + subcube grouping, formula-AST NNF/CNF/DNF/simplify (verified by 2^n equivalence), Pratt boolexpr parser (precedence !>&>^>|), Kleene K3 3-valued logic, Post-class properties (monotone/linear-ANF/selfdual/symmetric/dual). All 12 KATs green (each embeds its own enumeration cross-check) + INDEPENDENT ADVERSARIAL VERIFICATION 12/12: dpll/resolution/quine/karnaugh/properties re-verified by my own from-scratch brute-force oracle in the main loop (SAT count/solve on 11 adversarial CNFs incl n4-UNSAT; refute==unsat on 10; minimize+PI reproduce T + PRIMALITY over n=2..4; subcube oracle over all 255 n=3 subsets; Post-class over all 256 n=3 + n=4 sample) — the other 7 via completed independent verifier probes (LOK). Fleet caught a real NOVA parse gotcha: consecutive `if(bit==1);if(bit==0)` mis-parses as if-elif → explicit-let fix | 12 | 7f493be1 |
| 93 | std/grammar/ formal context-free grammar theory (grammar_core nullable first follow ll1 derive cyk cnf earley elim regular props) — FRESH category = the PRODUCTION-RULE / Chomsky-hierarchy grammar side (distinct from std/automata=machines and std/parsing=practical toolkit). Symbol=string (uppercase=nonterminal), epsilon=empty rhs, production=[lhs,rhs]. Textbook-grounded: dragon-book expr grammar FIRST/FOLLOW EXACT, Hopcroft-Ullman CYK 'baaba', ambiguous E→E+E parse_count=Catalan. Covers: nonterminal/terminal/nullable(fixpoint)/FIRST/FOLLOW sets, LL(1) table+conflict-detection+predictive parse, bounded general recognizer/generator, CYK (CNF), full CFG→CNF conversion (5 dragon-book steps), Earley recognizer + parse-count/ambiguity, grammar cleanup (remove-epsilon/units/useless + immediate-left-recursion elim + left-factoring), regular-grammar↔NFA, classification (CNF/GNF/Chomsky-type/ambiguity). All 12 KATs green + INDEPENDENT ADVERSARIAL VERIFICATION (4 verifiers, own bounded recognizers as ground truth, fresh grammars balanced-parens/aⁿbⁿ/left-recursive up to len 6): earley 259 cases + Catalan parse-counts, derive 254, cyk 254, cnf 38 (bounded language-equivalence incl len-5 binarization), elim 470, first/follow/nullable/ll1/props all ACCEPT. **grammar_regular REJECTED → FIXED: grr_to_nfa dropped unit productions A→B (0 leading terminals → 0 transitions); fixed with epsilon-edge + epsilon-closure in grr_nfa_accepts (handles unit chains + unit-to-epsilon + unit cycles); KAT 10 added as regression, re-verified** | 12 | ff297ccd |
| 94 | std/lambda/ lambda calculus (lambda_term shift subst beta eta normal church bool encode ski named combinators) — FRESH category, completes the computation-model trilogy (std/automata=machines + std/grammar=productions + std/lambda=functions). de Bruijn indices; EXACT shift/subst/beta-contract formulas; normal-order reduction bounded. Ground truth = REDUCTION + decoding Church encodings. Covers: term ADT + is_closed, de Bruijn shift/lift/free-vars, capture-avoiding substitution, beta (normal-order step/normalize/is-nf), eta reduction, reduction strategies (normal/applicative/WHNF + Church-Rosser confluence), Church numerals + succ/plus/mult/exp, Church booleans/pairs/if, Church pred/sub/iszero/lists, SKI + bracket abstraction, named terms + alpha-equality + de Bruijn conversion, combinators I/K/S/B/C/W/omega/Y. All 12 KATs green + INDEPENDENT ADVERSARIAL VERIFICATION: church 70 cases (5×5 plus/mult grids + exp) vs REAL integer arithmetic, encode 44 (pred/sub 21/iszero/length), bool 28 (full truth tables), shift/subst/term/beta/eta/normal all ACCEPT (own de Bruijn refs + confluence). **lambda_ski REJECTED → FIXED: bracket abstraction was broken for NESTED lambdas (emitted app(K, lam body) treating an inner lambda as a constant; also re-processed the K-lambda representation). Rewrote as compile-body-first + opaque combinator atoms + expand-at-end (verified compile(K)=S(K K)I); KAT 6 added; re-verified abstract(t) behaves identically to t on K/S/compose/W/4-deep.** combinators (I/K/S/B/C/W laws + SKKx/BIIx/CKab) + named (nested to_debruijn/alpha-equality/free-names) independently re-verified in main loop | 12 | 5bb37ae6 |
| 95 | std/relation/ binary relations & relation algebra (relation_core ops props closure equiv order lattice function power set count components) — FRESH category = discrete-math relation THEORY (equivalence/order/lattice/closures-as-algebra/function-properties/counting), distinct from std/graph = graph ALGORITHMS. Relations as n×n boolean matrices; everything EXACT & verifiable by enumeration over small n. Covers: matrix/pairs rep + domain/range, union/intersect/diff/complement/converse/composition, reflexive/symmetric/antisymmetric/transitive/asymmetric/connex predicates, reflexive/symmetric/transitive(Warshall) closures, equivalence classes + partition↔relation round-trip, partial/total/strict order + Hasse covering + min/max, lattice lub/glb/is-lattice/top/bottom/chain, function is-fn/injective/surjective/bijective/inverse/compose, relation powers/reachability/transitive-reduction, finite set algebra + powerset/cartesian, Bell/Stirling2 counting + relation-count formulas, connected components. All 12 KATs green (Bell 0..6, Stirling2(4,2)=7, 3-cycle TC=universal) + INDEPENDENT ADVERSARIAL VERIFICATION over 40+ random relations each with brute oracles: closure 537 cases (BFS reachability), props 1032, ops 1261, equiv 136 (union-find), order 255, lattice 138 (bound-set enum), function 1167, components 942 (BFS), count 91 (RGS partition enumeration), set 1024 (membership brute). **relation_power REJECTED → FIXED: rpow_transitive_closure_mat had a Floyd-Warshall bug (tc[i][k] and M[k][j] instead of tc[k][j]) → rpow_reachable/is_reachable missed multi-hop paths discovered at later pivots (0→3→1→2 missed 2). Fixed to use evolving tc; KAT 7 added; re-verified vs BFS on the bug case + cycles + 40 random relations.** | 12 | eeb6dba4 |
| 96 | std/typesystem/ unification + Hindley-Milner type inference (type_core subst unify scheme check infer hm arrow alpha pretty higher curry) — FRESH category = NOVA's OWN core machinery expressed as a library; Curry-Howard link to std/lambda. Type rep [tvar]/[arr]/[con]; subst = [k,type] pairs; fresh vars via mutable ctr cell. Textbook ground truth compared UP TO ALPHA. Covers: type ADT + ftv, substitution + compose, ROBINSON unification with occurs-check, type schemes (generalize/instantiate), simply-typed checking, Algorithm W (principal types), full HM with LET-POLYMORPHISM, arrow utilities (arity/curry/uncurry), alpha-equivalence + canonical renaming, pretty print/parse round-trip, higher kinds (*→*), Curry-Howard proof checking (I/K/S inhabit their types). All 12 KATs green (infer id→a→a, occurs-check rejects λx.x x, let id=λx.x in id id typechecks) + INDEPENDENT ADVERSARIAL VERIFICATION: unify SOUNDNESS gate 700 cases (mgu actually unifies, own apply), infer combinator battery S/K/I/B/C/W/twice up to alpha, hm let-polymorphism, scheme 10, subst 86, core 76, curry 16, alpha 28, arrow ACCEPT, pretty round-trip 7. **type_check REJECTED → FIXED, + type_infer & type_hm had the SAME LATENT bug: env lookup scanned oldest-first while bindings append at the end, so an inner binding could not shadow an outer same-named one (λx.λx.x wrongly inferred a→b→a instead of a→b→b; combinator battery used distinct names so it hid). Fixed all 3 to scan newest-first; KAT 10 (shadowing) added to type_check; re-verified shadowing on all 3 in main loop.** | 12 | ea891edb |
| 97 | std/proof/ proof theory (proof_formula proof_axioms proof_deduction proof_hilbert proof_natded proof_sequent proof_tableau proof_search proof_fitch proof_normalize proof_terms proof_intuition) — FRESH category = formal proof theory (propositional). Formula ADT + truth-table oracle (pfm_eval/is_tautology/is_sat), Hilbert-style proof checker (3 axiom schemas A1/A2/A3 + MP + schema-matching via metavar unification), natural deduction (construction + checking), sequent calculus LK (full rule checking for ax/and/or/imp/not/weak L+R), analytic tableaux (alpha/beta expansion with signed formulas, validity+satisfiability+contradiction), proof search (truth-table provability + tableau certificate + countermodel generation), Fitch-style proofs, proof normalization, Curry-Howard proof terms, intuitionistic logic (Kripke semantics up to 3 worlds, classical↔intuitionistic separation), deduction theorem, axiom catalogues. All 12 KATs green + INDEPENDENT ADVERSARIAL VERIFICATION: 4 probes (32 checks) — tableau↔truth-table cross-check on 12 formulas (LEM/deMorgan/Peirce/hypsyl/bot/top/sat), intuitionistic logic (LEM/DNE/Peirce correctly NOT intuitionistically valid, K/S/exfalso/conjelim/disjintro correctly ARE), Hilbert proof checker (valid 5-step p→p proof accepted, 3 invalid proofs rejected: non-axiom/wrong-MP/forward-ref), search countermodel verification (countermodel for p→q actually falsifies + no countermodel for tautology). ALL PASS — first clean adversarial gate in 5 tasks. | 12 | 047c247f |
| 98 | std/text/bbcode BBCode markup parser & utilities (bbcode_lex bbcode_parse bbcode_strip bbcode_to_html bbcode_to_text bbcode_validate bbcode_color bbcode_quote bbcode_list bbcode_url bbcode_code bbcode_nest) — full BBCode processing toolkit. Lexer (tokenizer with open/close/text tokens, malformed brackets degrade to text), recursive-descent parser (iterative stack-based AST builder with mismatch-recovery), strip (lex→parse→AST walk, correctly preserves unmatched brackets as literal text), to_html (single-pass recursive-descent converter: b/i/u/s/url/img/color/size/quote/code/list with nesting-aware close matching), to_text (plain-text rendering preserving URL/quote/code/list semantics), validate (LIFO nesting checker with error messages + position reporting), color (named→hex + validation), quote (stack-based extraction with depth/flatten), list (item extraction + ordered/unordered build), url (extract/build/safety-check), code (extract/escape/strip-outside), nest (depth/tag-counts/flatten/find/replace). All leaf except parse(→lex) and strip(→lex+parse). All 12 KATs green + INDEPENDENT ADVERSARIAL VERIFICATION: 3 probes (48 checks) — to_html edge cases (case-insensitive tags, nested-same-tag, code-literal preservation, URL-with-query-string, unclosed/stray pass-through, void img, list ul/li; 12 checks), validate cross-check (valid/invalid/crossing/unclosed/stray/star-item/deep-nesting/case-insensitive/attr-tag/error-count; 15 checks), lex→parse→strip pipeline (strip idempotence on 8 inputs, token structure verification, AST shape, attribute extraction, malformed-bracket degradation, unclosed-tag preservation; 21 checks). ALL PASS. | 12 | *(this commit)* |

## GAP-MAP #2 BACKLOG (2026-07-16, grep-verified over 240-module tree) — tasks 21-50, LOW-risk first
**LOW risk (int/string/bytes, deterministic/round-trip KATs) — build first:**
- std/net/lineproto: resp2 resp3 irc syslog5424 syslog3164 ftp_reply beanstalkd memcache_text telnet_opt gopher whois_parse smtp_line
- std/bits/bits64: rotl rotr reverse next_pow2 is_pow2 set/clear/test/toggle_bit lowest/highest_set popcount clz/ctz
- std/bits/bitfield: mask extract extract_signed insert test clear_field replace pack2/3 unpack2/3 field_range
- std/bits/morton3d: encode decode x/y/z parent children neighbors distance (z-order curves)
- std/devtool/semver: parse compare valid bump range_caret/tilde/hyphen satisfies max_satisfying sort diff coerce
- std/devtool/gitignore: parse_line compile_pat match_path negation anchor double_star dir_only load match_list layer_merge explain from_string
- std/music/theory: interval scale chord mode keysig circlefifths tuning_equal/just/cents rhythm arpeggio progression
- std/functional/combinators: curry2/3 partial1/2 flip once juxt always complement converge evolve memoize_n
- std/functional/result: res_ok/err/is_ok/is_err/map/flat_map/map_err/unwrap_or/and_then/or_else opt_from/map
- std/compress/univcodes: elias_omega fibonacci_code unary exp_golomb truncated_binary start_step_stop comma interleaved_elias abs_binary byte_aligned_huff bitpack frame_of_reference
- std/net/binproto: mqtt_fixed mqtt_connect mqtt_publish mqtt_sub mqtt5_props coap_msg coap_opt coap_block stomp_frame stomp_build stomp_heartbeat pop3_reply  (LOW-MED)
- std/data/subtitles: srt_parse/build/shift vtt_parse/build/shift sub_timecode/merge/split/filter/reindex/roundtrip
- std/data/feeds: rss_parse atom_parse opml_parse/build m3u_parse/build pls_parse/build po_parse/build feed_item feed_roundtrip  (LOW-MED)
- std/game/cards: card deck deal hand_sort hand_classify hand_compare showdown blackjack cribbage war bridge_suit solitaire
- std/game/maze: grid recursive_backtracker kruskals prims aldous_broder wilsons sidewinder binary_tree eller hunt_kill solve_bfs solve_astar
- std/text/bbcode: lex parse strip to_html to_text validate color quote list url code (+nest)
- std/math/tseries: rolling_mean/var/std/min/max diff pct_change seasonal_naive trend_linear anomaly_zscore/iqr kat  (LOW-MED)
- std/sync/coordination: barrier phaser promise event_flag rwlock once_cell mailbox broadcast work_queue timeout_chan select_first gate  (LOW-MED; respect process/channel model)

**MED risk (float / complex / data tables) — build after:**
- std/compress/adaptive (adaptive-huffman/LZSS/LZ78/BWT-pipeline), std/physics/mechanics, std/physics/waves_em,
  std/chem/formulas, std/geo/spherical, std/geo/projection (float+atan2 CARE), std/audio/synth, std/devtool/uuid_ext
  (needs md5/sha1), std/text/markdown, std/text/wikifmt, std/data/geodata, std/numeric/spline, std/numeric/polyregress,
  std/game/rating (elo/glicko). NOTE geo/physics float gotchas: coerce int*1.0, bind sub-exprs to float lets.

## RHYTHM NOTE (2026-07-14): run DEPTH-1 (one 12-agent fleet at a time). Depth-2 (24 agents) drained the account
## session cap twice. One fleet at a time is the sustainable pace. **18 full tasks committed clean (216 modules).**
## New categories added this campaign: std/media, std/parsing, std/term, std/cipher. Saturated (grep first):
## collections, math, text, numeric, data, encoding, fmt-area, random-sampling.

*(History: weekly limit hit 2026-07-13 mid-task-14 → owner switched accounts → resumed, 7 hash modules re-ran from
cache, no work lost. Then depth-2 hit this session cap.)*

## BACKLOG (grep-verified, ordered low-risk-first)
| # | Task | Risk | Value | Modules |
|---|---|---|---|---|
| 9 | std/net/dns | LOW | VERY HIGH | dns_name(compression) dns_header dns_question dns_rr dns_rdata_a dns_rdata_txt dns_rdata_mx dns_rdata_soa dns_rdata_srv dns_message dns_types dns_query(EDNS0) |
| 10 | std/functional/combinators | LOW | HIGH | hof partial curry compose pipe flip scan predicate group once memoize thunk (grep-verify each vs builtins/itertools) |
| 11 | std/text/phonetics | LOW | HIGH | nysiis caverphone double_metaphone cologne phonex fuzzy_score phonetic_sim name_match phonetic_index phonetic_cluster phonetic_sort phonetic_dedup |
| 12 | std/time/depth | LOW | HIGH | isoweek period strftime strptime rrule holiday businessday pduration age quarter weeknum daycount |
| 13 | std/parsing/cli (NEW cat) | LOW | HIGH | cli_schema cli_parse cli_help cli_short cli_long cli_subcommand cli_required cli_default cli_type cli_flag cli_positional cli_env |
| 14 | std/parsing/expr | LOW-MED | HIGH | expr_tokenize expr_shunt expr_eval expr_pratt expr_ops expr_unary expr_func expr_var expr_prec expr_paren expr_assoc expr_error |
| 15 | std/net/uritemplate | LOW | HIGH | ut_parse ut_vars ut_expand ut_simple ut_reserved ut_fragment ut_label ut_path ut_query ut_form ut_explode ut_matrix (RFC 6570) |
| 16 | std/collections/heaps | LOW-MED | HIGH | maxheap indexedpq daryheap binomialheap fibheap pairingheap heapmerge heapsort heapkth medianheap intervalheap heapselect |
| 17 | std/collections/ordmap_ext | LOW | HIGH | triemap skipmap arc_cache lfu_cache circularbuf quotientfilter weightedunion seglazy fenwick2d sparsetable orderstat multiset |
| 18 | std/text/patch | LOW-MED | HIGH | diff_myers diff_patience unified_emit unified_parse hunk_parse hunk_apply patch_apply patch_reverse patch_validate diff3_merge conflict_format patch_stats |
| 19 | std/os/system | LOW | HIGH | pathutil fileutil glob tempfile loghandler argsext termctl hexdump endianio bufreader linereader envconfig (mind Win+Linux) |
| 20 | std/net/inetproto | LOW-MED | HIGH | inet_checksum ipv4_header ipv6_header tcp_header udp_header icmp_echo arp ethernet vlan pseudo_header ip_options tcp_options |
| 21 | std/hash/breadth | MED | HIGH | crc8 crc16 crc64 crc_param fnv64 xxh32 xxh64 murmur3_32 murmur3_128 siphash hashcombine checksum (64-bit rotl needs logical-shift helper; avoid crc32/fnv1a32/djb2/sdbm/fletcher/pearson/jenkins/adler32 — exist) |
| 22 | std/data/asn1 | MED | HIGH | der_oid der_int der_seq der_set der_octetstr der_bitstr der_utctime der_bool der_null der_length der_tag ber_parse |
| 23 | std/data/bson | MED-HIGH | HIGH | bson_encode bson_decode bson_doc bson_array bson_int bson_double bson_string bson_bool bson_null bson_date bson_binary bson_objectid |
| 24 | std/parsing/combinators | MED | HIGH | pc_result pc_primitives pc_char pc_seq pc_alt pc_many pc_map pc_between pc_sepby pc_lazy pc_token pc_expr |
| 25 | std/parsing/jsonpath | MED | HIGH | jfp_lex jfp_parse jfp_filter jfp_slice jfp_recurse jfp_eval jfp_union jfp_wildcard jfp_index jfp_name jfp_root jfp_apply |
| 26 | std/net/smtp_framing | LOW | MED-HIGH | smtp_command smtp_response smtp_ehlo smtp_mailfrom smtp_rcptto smtp_data smtp_auth smtp_status imap_line imap_tag pop3_line pop3_resp |
| 27 | std/data/byteserial (xdr+netstring+ubjson) | LOW-MED | MED-HIGH | xdr_int xdr_uint xdr_hyper xdr_string xdr_opaque xdr_array netstring_enc netstring_dec ubj_encode ubj_decode ubj_optimized ubj_marker |
| 28 | std/geometry/vector | MED | HIGH | affine2d bezier_adv svgpath delaunay qrcode turtle transform3d polyclip quadtree bbox3d bspline earcut |
| 29 | std/numeric/distributions | MED (float) | HIGH | normaldist poisson binomial tdist chisqdist fdist betadist gammadist exponential uniform lognormal weibull |
| 30 | std/numeric/scientific | MED (float) | HIGH | specialfn(gamma,erf,beta) ttest chisqtest anova ranktest spline optimize polyroots bessel legendre quadrature interp1d |
| 31 | std/text/unicode | HIGH (data tables) | CRITICAL | utf16 utf32 casefold2 unicat ucwidth2 ucseg uninorm_nfc uninorm_nfd uninorm_nfkc script_id bidi_class combining_class |

**Extend past 31:** re-run gap-analysis for a second wave (depth in graph/geometry/dsp, plus new categories:
regex engine, template engines, markup parsers, scientific units, bignum-decimal depth) to reach ~100 tasks.

**Status legend:** update this table's DONE section (commit hash) as each task lands — same discipline as
[[feedback_canonical_docs_living_plan]]. Full arc deferred to campaign end per [[feedback_execution_rhythm_30task]].
