# NOVA CORE GAP LEDGER — what the compiler/core is lacking (2026-07-03)

> **Purpose.** The 9 frameworks (Forge, Reactor, Cortex, Mesh, Prism, Pulse, Sentinel, Edge, Ops)
> all stand on ONE core: the self-hosted compiler + the C runtime + the type system. **A framework
> inherits every flaw of the core, multiplied by its own size.** So the core must be *good* before
> we pour years into frameworks. This file is the single, honest, prioritized list of what "good"
> still requires.
>
> **Source.** Derived from the 26-agent deep audit of 2026-07-03 (all 140 docs + the 21,967-line
> `nova_compiler.nova` + the ~20,900-line `nova_runtime.c` + Forge + CI + benchmarks read at
> file:line depth, with adversarial claim-checking and a six-language judge panel). Every gap below
> carries evidence. Nothing here is vibes.
>
> **Scope.** CORE ONLY (compiler, type system, runtime, perf, platform, toolchain, process).
> Framework-specific work lives in FORGE_STATUS.md / per-framework plans, not here.
>
> **The one-line state.** The core is *real and rare* — it self-hosts to a byte-identical fixpoint,
> hits C-class scalar speed with zero annotations, and runs a real concurrency runtime. As of
> **2026-07-06** the foundation is materially stronger: **Tier 0 (runtime soundness) is 100% closed**
> and **Tier 1 is essentially done — the type checker is now SOUND BY DEFAULT** (strict is the default;
> it no longer fails open; 1.0–1.3 + 1.5 closed). **Tier 3 (expressiveness) turned out to be LARGELY
> ALREADY BUILT** — generics, traits/interfaces (with bounds, default methods, dynamic dispatch,
> conformance), and exhaustive-match ADTs all exist and pass tests (the audit's "unbuilt" was stale;
> only optional comptime 3.4 remains). So the real remaining core work is narrower than the audit
> implied: **Tier 2** (non-scalar perf — the float-array-on-escape cliff, 2.2) and **Tier 4**
> (concurrency), plus reach/toolchain (Tiers 5–6). Every one is fixable.

---

## ★ LIVE STATUS (2026-07-07) — read this before trusting the audit tone below

The body of this file keeps the original 2026-07-03 audit voice ("the future isn't bright if…"),
but much of it is now **done or was stale**. Current reality, verified by fixing + reconverging +
by reading the compiler and running probes:

| Tier | Was audited as | ACTUAL state now |
|---|---|---|
| **0 Runtime soundness** | 6 live UB/UAF/wrong-answer holes | **✅ UB/UAF class 100% CLOSED** (0.1–0.7). **🔶 One residual LEAK found 2026-07-07 (0.8):** struct heap fields leak on drop outside arenas (non-owning + dead free-case); NOT a UB/UAF, arena-masked (Forge safe); false comment fixed + pinned, XL type-directed fix deferred |
| **1 Type-system soundness** | default checker *fails open* (GATE 2 a lie) | **✅ DONE — SOUND BY DEFAULT** (1.0–1.3, 1.5); strict is default + fail-closed; 10-test negative gate |
| **2 Perf** | float arrays 8–120× C (impossible for Cortex/Pulse) | **2.2 S4.2 ESCAPE-SURVIVAL ✅ SHIPPED** — escaped float array auto **160×C→~1.2×C** (guarded loop-versioning pre-pass; DEFAULT-ON transparent (zero-config), reconverged byte-identical WITH the transform on the compiler itself, 1121/0 both modes; disable via NOVA_S4_ESCAPE=0). 2.5 perf-tracking ✅. Remaining: 2.3 HOF (XL), 2.1 i64 ceiling. |
| **3 Expressiveness** | *"no generics / no traits / no ADTs — the biggest jump"* | **✅ LARGELY ALREADY BUILT** — generics, traits (bounds/default/dyn-dispatch/conformance), exhaustive-match ADTs all exist + pass tests. Only optional comptime (3.4) missing. |
| **4 Concurrency** | N>1 regresses; races; cycles leak | **Correctness gated** (N>1 CI green); **4.7 RC-cycle leak CONFIRMED** (evidence). Real fixes (race, cycle-collector, preemption) = **L–XL, supervised** (deadlock/corruption risk). |
| **5 Platform** | Windows + WSL-once | Accurate; ARM/macOS/GPU/WASM-framework = **XL / need hardware**. |
| **6 Toolchain** | onboarding 3/10; tools unwired | **6.5 front door ✅**, **6.2 REPL ✅**, **6.4 LSP ✅** (self-hosted real-inferer LSP, extension wired + binary refreshed); `nova fmt` works. Only **6.1 package manager** remains (L + external registry infra). |
| **7 Process** | bus-factor 1; tools unwired; orphaned tests | **7.3 ✅ dented** (+471 orphaned tests gated via manifest = 182 core-language + 289 Forge-lib batteries, all verified in the 1121-test parallel harness; +4 negative tests → gate 14/14), **7.4 ✅ mitigated** (loud -SkipReconverge banner). 7.1/7.2/7.5 = discipline (human). |

**Net:** the *foundation* (Tiers 0–1) is trustworthy, expressiveness (Tier 3) is mostly shipped,
**Tier 2 perf's #1 gap (S4.2 float-on-escape) is now SHIPPED default-on** (reconverged byte-identical
with the transform on the compiler itself, 1121/0 both modes), and **toolchain (Tier 6) + process
(Tier 7) are largely closed** (only 6.1 package manager remains, needs external registry infra). The
genuinely-open CORE work left is **Tier 4 (concurrency — 4.7 cycle collector designed/ready, races,
preemption)** — careful supervised changes where a mistake reintroduces a soundness/corruption class,
so NOT to be rushed; **Tier 5 platform** needs ARM/macOS hardware; and Tier 2's secondary items
(2.3 HOF closures XL, 2.1 i64 ceiling). Commits this campaign: 0.3 `57c4b9d6`, 0.7 `5cd3cbfb`, 1.0 `8fe0394b`, 1.1 `f4fee2b1`,
1.2 `6b87d32c`, 1.3 `f748602d`, 2.5 `e7841fa4`/`907820dd`, 6.5 `e21f1b05`, Tier-3 re-verify `9506c039`,
6.2 `662a46d2`, 6.4 `cafe3162`, 6.1-scope `1d656213`, 7.3 `ad731e7c`, 1.5/7.3-neg `d0db3726`, 7.4 `d0e9880b`.

---

## How to read this

Each gap has: **what it is · evidence (file:line) · which frameworks it blocks · effort · fix direction.**
Effort key: **S** = days · **M** = weeks · **L** = 1–3 months · **XL** = multi-month campaign.
Tiers are a *build order*, not a priority ranking of importance — you cannot safely do Tier 2 perf
work while Tier 0 soundness holes make every result untrustworthy.

---

## TIER 0 — Runtime soundness holes (FIX FIRST; the core is not trustworthy until these close)

These are live, in-code today. They violate the project's own no-UB / no-UAF bar. A framework built
on them ships their crashes to every user of that framework. **Nothing else matters until these are gone.**

| # | Gap | Evidence | Blocks | Effort |
|---|-----|----------|--------|--------|
| 0.1 | **✅ CLOSED (a0514dc1).** Self-assign use-after-free. Fixed: inc-before-dec in `nova_rt_list_set`/`nova_rt_dict_set`. Repro `_gap01_rt_unit.c` (ASAN heap-UAF before, clean after); both FULLRC modes green. | `nova_runtime.c:1577-1579`, `:2474-2476` | ALL | S |
| 0.2 | **✅ CLOSED (dfd71645).** Division/modulo UB. Fixed: int `/`→`nova_rt_div`, `%`→`nova_rt_mod` (both box-aware + guard x/0→0 + INT_MIN/-1 wrap) in both codegen paths + compound `/=`/`%=`. RECONVERGED byte-identical (gen5.ll==gen6.ll); both FULLRC modes 649/0; perf gate native. | compiler ire+codegen div/mod sites | ALL | S |
| 0.3 | **✅ CLOSED (57c4b9d6).** Both halves fixed. `%` on boxed `any` (dfd71645, →`nova_rt_mod`, box-aware; boxed 7.5%2.0→1.5). Unary `-` on boxed NOW FIXED: root cause was the **register-type pass** ([nova_compiler.nova:14268](../nova-compiler/test_programs/nova_compiler.nova#L14268)) typing a `neg` of a non-float operand as `int` (missing the `any`-fallback every binary op has at :14266) → both the negate (`sub i64 0, <boxed_ptr>`) and print dispatch used the raw-int path → garbage. Fix: type-pass emits `any` for neg-of-`any`; ire codegen routes the `any` path through box-aware `@nova_rt_neg`; known-`int` path keeps native `sub i64 0`. Probe `_negprobe.nova`: `-fs[0]`→-7.5, `-x`→-3.25, `-d["p"]`→-2.5, `-xs[0]`→-42, pure-int neg exact. RECONVERGED byte-identical; both FULLRC modes green; perf native. | compiler codegen (any path) | ALL (esp. Cortex/Pulse numeric) | S |
| 0.4 | **✅ CLOSED (a0514dc1).** Wild reads. Fixed: NULL + `nova_mem_find_tag` guard on `nova_rt_list_len`/`dict_len` (return 0 on mismatch; zero perf cost — never emitted by codegen, `len()`→`len_any`). Repro `_gap0405_rt_unit.c` (segfault before, 0 after). | `nova_runtime.c:1659-1662`, `:2871-2874` | ALL | S |
| 0.5 | **✅ CLOSED (a0514dc1).** String-pool double-dec OOB write. Fixed: free only on 1→0, never re-push an already-freed slot, bound the push. Repro `_gap05_only.c` (segfault before, top bounded after). | `nova_runtime.c:508-518` | ALL | S |
| 0.6 | **✅ CLOSED (pre-existing, value-model overhaul).** `nova_is_likely_float` (now `nova_runtime.c:4384`) uses the BOX TAG as sole authority — the unsound magnitude heuristic is gone; a large int in an `any` context is unambiguously an int (floats always boxed at every widen point). Verified no magnitude guess remains. | `nova_runtime.c:4384` | ALL numeric | M |
| 0.7 | **✅ CLOSED (5cd3cbfb) — found+fixed 2026-07-06 (sibling of 0.3).** Boxed-float **truthiness** was wrong: a boxed `0.0` is a non-null pointer, so `branch`/`not`/`and`/`or` (lowering to `icmp ne/eq i64 <cond>, 0`) read it as **truthy** when `0.0` is falsy. Fix: new box-aware `nova_rt_truthy` (boxed float → `d != 0.0`; other boxed → truthy; raw int → `v != 0`) + `ir_infer_block` inserts the coercion for any `not`/`and`/`or` operand and any `branch` condition whose register type is `any`/`val` (the boxed/dynamic egress labels). The emitter is UNTOUCHED — its raw icmp now runs on a clean 0/1 int. Typed `int`/`bool`/`float` conditions keep the raw icmp → **byte-identical** (verified: int-only program diffs identically to the 0.3 compiler, only the unused preamble decl added). Probe `_truthprobe.nova` now: `if 0.0`→falsy, `if 2.5`→truthy, `not 0.0`→true, dict `0.0`→falsy — all correct. `_negprobe` intact under the 0.7 compiler. RECONVERGED byte-identical; both FULLRC modes green. | `nova_compiler.nova` ir_infer_block (~:14995 not/and/or, :15031 branch) | ALL (esp. Cortex/Pulse numeric conditionals) | S |
| 0.11 | **🔶 OPEN — re-surfaced 2026-07-07 by DOGFOODING (HIGH severity: silently WRONG numbers).** **Float-returning helper reads an UNINITIALIZED float slot → garbage (~e-156), layout-dependent.** Building showcase_stats, `stddev(xs)=sqrt(variance(xs))` returned `3.08e-156` instead of `sqrt(2)=1.41421`, cascading to `pearson r = inf`. Two tells it is an uninitialized READ, not math: (1) `variance(xs)` prints `2.0` correctly the line before; (2) adding a `print` inside `stddev` **makes it correct** (layout shift). The documented workaround (bind sub-exprs to `let`s) **does NOT fix it** here. This is the same class as the long-known `geo_bearing`/`atan2` float-return garbage (memory `reference_nova_float_codegen_geo_bearing`), now with a fresh repro. HEISENBUG (minimal extraction masks it), so `_floatret_uninit_test.nova` preserves the pattern CI-safely (reports + exit 0). WORKAROUND in real code: avoid sqrt-in-a-float-returning-helper; e.g. compute R²=cov²/(vₓ·v_y) instead of Pearson r (showcase_stats does this — robust 3/3). Real fix = float-return/float-slot codegen must zero-init or correctly wire the return slot (float ABI, S1 area); XL, needs a dedicated codegen session with LLVM-IR diffing of the working-vs-garbage layouts. | `nova_compiler.nova` float ret/call slot codegen (S1 float ABI) | ALL float-heavy numeric code (Cortex/stats/sim) | XL |
| 0.12 | **✅ CLOSED (found+fixed 2026-07-08 by adjacent-behavior probing — same class as 0.9).** **`list + any` segfaulted when the dynamic operand held a non-list.** The `+` lowering routes to `nova_rt_list_concat` when EITHER operand is statically a list (`lt=="list" or rty=="list"`), and that function deref'd both as `NovaList*` with no tag check — so `[1,2] + dict["missing"]` (missing → int 0) NULL/wild-deref'd → segfault. Probing `+` after fixing the compare family surfaced it (`_listconcat_probe`). Fix (RUNTIME-only, `nova_runtime.c:2216`): tag-check both at the top of `nova_rt_list_concat` — list+list concatenates; anything else is a DEFINED `nova_panic("'+' with a list requires both operands to be lists")` (matches `mul`/bytes convention), never a wild deref. list+list (static + via `any`) unaffected. No compiler change → **no reconverge**; validated both modes on list-heavy tests + pinned by `_listconcat_mixed_test.nova`. (str-concat `str OR str` at :14665 is SAFE — `nova_rt_add`→`nova_rt_str_concat_safe` is box-aware; verified by `_arith_mixed_probe`.) | `nova_runtime.c:2216` `nova_rt_list_concat` | code doing `list + <dynamic>` | S |
| 0.10 | **✅ CLOSED (2026-07-09) — RUNTIME-ONLY FIX, no reconverge.** Root cause was the `_no_rc` insert elision: `nova_rt_list_append_no_rc` and `nova_rt_dict_set_no_rc` stored elements/keys **uncounted** (no `rc_inc`), while the destructor (`nova_rc_free`) and mutation ops (`list_set` inc-before-dec, `list_remove`/`pop` dec) all COUNTED on removal. This violates the universal RC invariant **INSERT RETAINS, removal/destruction releases**. A string allocated at rc=1 was inserted uncounted into both a list and a dict (both proven local → `_no_rc` routing); then `list_set`'s correct inc-before-dec drove rc 1→0 → freed the string while the dict still keyed on it → UAF (confirmed ASAN: heap-use-after-free, freed at `nova_rt_list_set:1598`, used at `rc_inc(tmp)`). **Fix (2 lines, `nova_runtime.c`):** `nova_rt_list_append_no_rc` now delegates to `nova_rt_list_append` (counted), and `nova_rt_dict_set_no_rc` delegates to `nova_rt_dict_set` (counted). This matches the existing WASM shim (`_wrt_min.c:85`) which already did the delegation. The compiler still routes proven-local inserts to these symbols on its fast path; behavior is now identical to the counted siblings. No `nova_compiler.nova` change → **no reconverge needed**. Repro `_alias_swap_leak_test.nova` now hard-asserts and passes both NORMAL and FULLRC. Full 1146-test regression 0 FAIL both modes (validated). Note: the `_no_rc` elision was a perf optimization predicated on a false precondition ("caller proven local" ≠ "no other counted alias exists") — a correct future elision would require proving BOTH no other counted alias AND no in-place mutation of the container. Tracked as a future perf item; correctness takes priority. | `nova_runtime.c:1483` (list_append_no_rc) + `nova_runtime.c:2538` (dict_set_no_rc) | ALL code that builds a list+dict with shared heap strings then mutates the list | S |
| 0.9 | **✅ CLOSED (found+fixed 2026-07-07 by DOGFOODING — a real crash on ordinary code).** **`any == string` / `any != string` segfaulted.** The eq/neq codegen took the raw `inttoptr`+`strcmp` path whenever EITHER operand was statically `str` (`lt=="str" OR rty=="str"`). A DYNAMIC operand (`any`/`val`) can hold a non-pointer — e.g. `dict["missing"]` yields int `0` — so `strcmp` dereferenced the `0` → crash. This bites the MOST mundane code: any looked-up header/query/config/JSON value compared to a string literal (`if req.headers["x"] == "secret"`). Surfaced immediately when building the TaskFlow showcase. Fix: lowering `str OR str` → **`str AND str`** ([nova_compiler.nova:14733](../nova-compiler/test_programs/nova_compiler.nova#L14733)), so str-vs-dynamic falls through to the box-aware `nova_rt_eq`/`nova_rt_neq` (tag-checks — a small-int operand returns not-equal without deref, `nova_runtime.c:4583`). `str==str` keeps the fast strcmp path; `str==int` (both static) is already a compile error upstream → the only behavior change is the dynamic case (which crashed, so no valid program relied on it). Probe `_eq_mixed_test.nova` (any/list/nested-dict vs string, all correct, no crash); crud demo + TaskFlow pass. **RECONVERGED byte-identical (gen5.ll==gen6.ll, `0678EFE5…`).** **SIBLING also fixed (2026-07-08): the ORDERING ops `< <= > >=` had the identical `str OR str` strcmp-deref bug** — `any < string` (e.g. `dict["missing"] < "x"`) segfaulted the same way. Same fix at [nova_compiler.nova:14754](../nova-compiler/test_programs/nova_compiler.nova#L14754) (`str OR str`→`str AND str`) → str-vs-dynamic routes to box-aware `nova_rt_lt/le/gt/ge` (→ `nova_rt_cmp`, `nova_runtime.c:4557`: strcmp only when BOTH strings, else unbox+int-compare — never derefs). Probe `_ord_mixed_test.nova`; sort/report/calc unaffected. Reconverged byte-identical. This closes the ENTIRE mixed dynamic-vs-string comparison family (eq/neq + all four orderings). | `nova_compiler.nova` eq/neq (~:14733) + ordering (~:14754) lowering | ALL (any web/JSON/config code) | S |
| 0.8 | **🔶 OPEN — found 2026-07-07 (NOT a UB/UAF; a resource leak).** **Struct heap fields leak on drop outside arenas.** A struct's managed fields (list/dict/string/nested-struct/bytes) are NEVER released when the struct dies: `rc_free`'s `switch(NOVA_RC_TAG)` sees a struct's **packed** tag `(nslots<<3)\|5` (e.g. 21 for 2 slots), never `case NOVA_MEM_STRUCT`(=5), so real structs fall to `default:` (header freed, fields leaked). Confirmed by `_struct_field_leak_test.nova`: int-only struct `delta=0` (header path SOUND), list-field struct `delta=4000` (2 objs/iter leak); `arena_masked_delta=4000` too (function scopes don't auto-arena). **Blast radius:** non-arena struct churn (compute/game/CLI loops, the compiler itself). **NOT** Forge — request handlers run in a per-request arena (structs are `ARENA_BIT` → freed wholesale; `forge_readiness` live_delta→0 proves it). **Why the naive fix is WRONG (and dangerous):** `make_struct` ([nova_compiler.nova:16766](../nova-compiler/test_programs/nova_compiler.nova#L16766)) stores fields **raw, with NO `rc_inc`** — the struct is **non-owning**, so simply enabling the dec loop double-frees every aliased field; and struct slots mix raw int/float with pointers, so blind-dec feeds float bits to `rc_dec` (the same unsoundness that makes kind=2 float-lists skip dec). **Correct fix (XL):** type-directed ownership — emit `rc_inc` for MANAGED fields on construct + a per-type managed-slot bitmap (`type_hash→bitmap`, parallel to the existing reflection registry) so `rc_free` dec's ONLY pointer slots; same for `make_closure` captures. **Shipped 2026-07-07 (this finding):** corrected the false "make_struct rc_inc's fields" comment in `rc_free` (was a landmine — would lead a maintainer to enable the double-free) + `_struct_field_leak_test.nova` pins the baseline. Fix itself deferred to a dedicated session (foundation-critical refcount change; validate with ASAN + double-free probe + 1121/0 both modes + reconverge). | `nova_runtime.c` rc_free `case NOVA_MEM_STRUCT` (~:9908) + `make_struct` emission | non-arena struct churn (Cortex/Reactor/CLI/self-compile) | XL |

> **PROGRESS (2026-07-06):** **Tier 0 is now 100% CLOSED (0.1–0.7).** **0.1, 0.4, 0.5** fixed in `a0514dc1`
> (C-unit repros proving UAF/OOB before, clean after). **0.6** already closed (value-model box-tag
> float discrimination). **0.2 (div/mod UB)** fixed in `dfd71645` — RECONVERGED byte-identically and
> both FULLRC modes 649/0. **0.3** is now FULLY CLOSED (`57c4b9d6`): the `%`-on-boxed half in
> `dfd71645`, and the unary-`-`-on-boxed half here — the root cause was the *register-type pass* typing
> a neg-of-`any` as `int` (missing the `any`-fallback its sibling binary ops have), so both the negate
> and the print dispatch ran on a raw-int reinterpretation of a boxed pointer. Fixed by giving neg the
> same `any`-fallback + routing the `any` path through box-aware `@nova_rt_neg`; RECONVERGED
> byte-identically, both FULLRC modes green, perf native. Every Tier-0 crash/UB/corruption hole is
> closed. **0.7 (same day):** probing the 0.3 fix's siblings surfaced boxed-float *truthiness* — a boxed
> `0.0` is a non-null pointer, so `if`/`not`/`and`/`or` read it as truthy. Fixed with box-aware
> `nova_rt_truthy` coercion inserted (in `ir_infer_block`) only for `any`/`val` conditions — typed
> int/bool code is byte-identical. `_truthprobe.nova` now all-correct. So all of Tier 0 (0.1–0.7),
> including every wrong-answer edge, is closed.
>
> **UPDATE 2026-07-07 — 0.8 found (a LEAK, not a UB/UAF).** Investigating the RC free path (to de-risk
> the 4.7 cycle collector) surfaced that `rc_free`'s `case NOVA_MEM_STRUCT` is **dead code** for the
> common case: `switch(NOVA_RC_TAG)` sees a struct's packed tag `(nslots<<3)|5` (≠5), so structs fall to
> `default:` and their managed heap fields are **never dec'd → leaked**. This is *consistent* (not a bug
> in isolation) with `make_struct` storing fields **without `rc_inc`** — structs are non-owning, so the
> two halves match as a leak-but-safe model (no double-free today). It was invisible because (a) Forge
> runs in per-request arenas (structs freed wholesale — `forge_readiness` proves flat), and (b) no test
> covered struct-field drop. **Blast radius = non-arena struct churn** (compute/game/CLI/self-compile).
> **Shipped now:** the false "make_struct rc_inc's fields" comment in `rc_free` (a landmine that would
> lead a maintainer to enable a double-free) is corrected + `_struct_field_leak_test.nova` pins the
> baseline (int-struct=0 proving the header path is sound; list-struct=4000 the field leak). **The real
> fix is XL** (type-directed ownership: `rc_inc` managed fields on construct + per-type managed-slot
> bitmap so free dec's only pointer slots — struct slots mix raw int/float, so blind-dec is float-unsound)
> and is **deferred to a dedicated session** — a foundation-critical refcount change must not be rushed;
> validate with ASAN + a double-free (aliased-field) probe + 1121/0 both FULLRC modes + reconverge.
>
> **Closures confirmed same family (`_closure_capture_leak_test.nova`).** `make_closure`
> ([nova_compiler.nova:16947](../nova-compiler/test_programs/nova_compiler.nova#L16947)) also stores
> captures **raw, no `rc_inc`**, and additionally marks each captured local's slot **ESCAPED** (:16954)
> so W5b does NOT drop that local (prevents a dangling capture). Net: a closure's captured heap value
> **leaks GUARANTEED** on the closure's death (even a sibling local that would otherwise be dropped) —
> measured `sibling=2001` (~1/iter), `frame=4000` (~2/iter); `acc` correct → memory-SAFE, just leaky.
> So the aggregate model is uniformly "store raw + dead free-case" for BOTH structs and closures; the
> type-directed fix (inc managed slots on build + per-type managed-slot bitmap to dec on free) must
> close both together (enabling the dec alone would double-free struct fields, which are non-owning).

**Why the future isn't bright if ignored:** every one of these is a crash or silent-corruption that a
framework user hits in normal code (`arr[i]=arr[i]` and `x/0` are things people *write*). You cannot
market "secure by default" (Sentinel) or "production-grade" on a runtime with wild reads. **Total Tier 0 ≈ 2–4 weeks.** This is the highest-leverage fortnight in the project.

---

## TIER 1 — Type-system soundness (the "genius compiler" promise is quietly false today)

NOVA's whole thesis is *the compiler is the genius* (GATE 2: 95% zero-annotation, inference catches
bugs). **As of 2026-07-06 the default checker is SOUND — it no longer fails open** (1.0+1.1+1.2 below).
Remaining Tier-1 items are diagnostics/expressiveness (1.3 occurs-check, 1.4 enum payloads), not the
core fail-open hole. Kept below with ✅ status for the record.

> **★ 1.0 (PREREQUISITE, THE REAL 1.1/1.2 BLOCKER) — heterogeneous-return inference: ✅ FIXED
> (8fe0394b, 2026-07-06). Unblocks 1.1/1.2.**
> Final (working) design after two reverted attempts: (a) **ti_ DEFERRED collect-and-join** — an
> un-annotated ("open") fn COLLECTS its return-expr types in `ti_ret_seen` (they're often unresolved
> vars at return time) and, AFTER the fn's `ti_solve` resolves them, `ti_ret_join_bind` joins the
> RESOLVED types: int/float/bool are mutually-coercible numerics (a float fn with a `return 0` stays
> float), a differing NON-numeric concrete widens to `any`, unit/nothing/var absorbed; concrete
> agreement preserved (no perf loss). (b) **Track genuinely-heterogeneous fns** (`ti_fn_hetero`, set
> when the join widened on a real conflict). (c) **IR frt fixpoint SKIPS the refine for hetero fns**
> (`not contains(ti_fn_hetero, fname)`), so a heterogeneous fn stays `any` in `frt` → callers dispatch
> `print_any`/box correctly. Crucially the IR-side `ir_analyze_return_type` is otherwise UNCHANGED —
> an earlier JOIN there perturbed the S4 floatlist/param-type fixpoint and de-typed float list params
> (math3d/physics2d e-156); the hetero-skip achieves the same result without touching that analysis.
> Validated: `classify(1)`→"hello" (print), `_t1probe`/`_anystr`/`_hetreturn_test` correct,
> math3d/physics2d **ok** (0 FAILs), **strict self-compile 0 errors** (all 7 false-rejections gone).
> Reconverged. `_hetreturn_test` guards it in the regression suite.
>
> *(historical) DIAGNOSIS + two reverted attempts below, kept for the reasoning trail:*
> Attempting the budget fix surfaced the real blocker: return-position inference is **strict at BOTH
> levels** (ti_ HM + the IR `ir_analyze_return_type`), so a function whose `return`s disagree in type is
> **false-REJECTED** instead of widening to `any` — 7 cases in the compiler's own source (`eval_expr`
> `-> any`; `move_expr_uses`/`ire_emit_function`/`nova_pkg_*`/`nova_fmt_file_ast` = `return 0` early-exit
> + unit fall-through). The budget hole *masks* all 7. Also **fails small real programs in DEFAULT mode**
> (`_anyret_repro.nova`) — a genuine defect independent of the budget. **Owner-ratified decision ("do
> what you think"): widen un-annotated heterogeneous returns to `any`** (zero-ceremony; `any` is
> first-class), PRESERVING the inferred concrete type when returns agree (no perf loss) and keeping
> STRICT for a concrete annotation.
>
> **ATTEMPT 1 (reverted; patch saved `NOVA_DESIGN/tier1_return_inference_ATTEMPT.patch`):** two mirrored
> tolerant JOINs — ti_ `ti_ret_constrain` + IR `ir_analyze_return_type`. It compiled heterogeneous
> returns, made strict self-compile 0-errors, and reconverged byte-identically — BUT **broke `math3d` +
> `physics2d` (float `e-156` garbage = a raw double read as a boxed pointer).** ROOT CAUSE of the
> regression: the ti_ side used **EAGER `ti_bind`**, but return-expression types are frequently
> unresolved **type VARIABLES** at return-processing time; the eager code *skipped* var-typed returns
> (only binds concrete), so a float fn's return var never got connected → mis-typed → boxed.
>
> **CORRECT APPROACH (for next session):** the ti_ side must be **DEFERRED**, not eager — keep a
> per-fn `ti_ret_seen` list; for an open (un-annotated) fn, COLLECT each return-expr type (and the
> fall-through) WITHOUT constraining ret_var; then AFTER the function's `ti_solve` (which resolves the
> vars) compute the JOIN of the *resolved* types (first concrete wins; a second DIFFERENT concrete →
> `any`; `any`/`unit`/`nothing`/`var` absorbed) and bind ret_var once. The IR side
> (`ir_analyze_return_type`) join is CORRECT AS DESIGNED IN THE PATCH: state machine `concrete`/`conflict`
> — an `any`/`val` return does NOT force widening (concrete wins), only two DIFFERENT concrete kinds →
> `any` (this is what keeps float fns float). Repros: `_anyret_repro.nova`, `_hetreturn_test.nova`,
> `_t1probe.nova`, `_anystr.nova`; strict error dump `_strict_errors_full.log`. Until 1.0 lands, 1.1/1.2
> remain blocked (any budget increase re-surfaces the 7 false-rejections).

- **1.1 Fail-open unification budget — ✅ FIXED (f4fee2b1, 2026-07-06).** Was: a single GLOBAL
  5,000-unification cap; after it, `ti_unify` became a silent no-op for the rest of the file, so the
  compiler's own 9,664-unification self-compile left its later code UNCHECKED. Fix: `ti_solve` now
  resets `ti_unify_count` **per drain** in BOTH modes (was strict-only) — each declaration gets its own
  ~5,000-unification budget instead of being starved by a global cap. Unblocked by 1.0 (the return-
  inference fix removed the false-rejections that resetting used to surface). Compiler self-compiles
  clean (0 err); RECONVERGED byte-identical (gen5.ll==gen6.ll SHA A7C9AE2B…); perf-native; regression
  617 PASS / 0 real failures; both FULLRC modes green. Default still silent-skips past 5,000/drain (that
  residual — a single >5,000-unification declaration — is closed by 1.2's fail-closed strict-default).
- **1.2 Strict is now the DEFAULT — ✅ FIXED (6b87d32c, 2026-07-06).** The "`IrInst` arity"
  blocker comment was STALE — 1.0 removed the return-inference false-rejections. Flipped `_ti_strict`
  default (`nova_compiler.nova:10300`: on unless `NOVA_TI_STRICT=0`). The sound path is now default:
  per-drain budget (1.1) + FAIL-CLOSED on exhaustion (a type error, not a silent-accept) — the checker
  **never silently stops checking**. The "genius compiler" (GATE 2) is no longer quietly false.
  Compiler self-compiles clean (0 err) with strict DEFAULT; RECONVERGED byte-identical (gen5.ll==gen6.ll
  SHA E21C2313…); perf-native; **FULL regression under strict-default = 644 PASS / 0 real failures / 0
  strict rejections**; both FULLRC modes green. Opt out with `NOVA_TI_STRICT=0` (historical fail-open).
- **1.3 Occurs-check — ✅ FIXED (f748602d, 2026-07-06).** `ti_occurs` was already a correct
  occurs-check called at both var-bind sites — but on a cycle it *silently* skipped the bind (var left
  unbound → degraded to `any`) instead of diagnosing. Now emits a real error: "recursive type: this
  expression would have an infinite type (a value cannot contain itself)". Nominal recursive structs
  (`type Node ... next: Node`) don't create var-cycles, so they're unaffected. New guard
  `_negty_recursive.nova` (self-application `x(x)` → rejected) wired into the negative-type gate.
  Compiler self-compiles clean; RECONVERGED byte-identical (SHA 260E7F8A…); CI ALL GREEN (651 PASS / 0
  FAIL both modes). Message uses no `ti_type_str` on the cyclic type (would infinite-loop).
- **1.4 Untyped enum payloads.** User-enum payloads carry no static type. **Status (2026-07-06 recheck):
  PARTIAL — better than filed.** Concrete-annotated variant payloads ARE typed (the variant constructor
  is `nt_fn(field_types, nt_struct(vname))`, `nova_compiler.nova:12496-12509`), and generic type params
  in payloads flow through `tgmap`/`ti_ann_to_type_g` (generics work — see below). Remaining gap =
  exhaustiveness + typed payload flow through `match` arms for the general case. **Effort: M**, folds
  into 3.3 (ADTs + exhaustive match).
- **1.5 Negative type-error tests — ✅ DONE.** `_neg_type_tests.ps1` (wired into `nova_ci.ps1` stage
  2k): 5 wrong programs each REJECTED with the right code — arity (E1003), return/arg type (E1001),
  unknown ident (E1002), recursive type (1.3). Soundness regressions are now visible.

**Why it matters:** "unchecked code degrades to `any` → raw-bit reinterpretation" is the exact pipeline
that produces the Tier 0 CVE class. Tier 1 is the *upstream* of Tier 0. **STATUS 2026-07-06: the
type checker is now SOUND BY DEFAULT (1.0–1.3, 1.5 done). 1.4 folds into Tier 3.3. Tier 1 is
essentially closed** — the "fails open" hole that made GATE 2 a lie is gone.

> **★ TIER 3 GENERICS RE-CHECK (2026-07-06): the "no generics" claim was STALE.** NOVA already has
> real user-facing generics: `fn<A,B> swap(p)`, `fn<T,U> my_map(xs: List<T>, f) -> List<U>`, generic
> filter/reduce/fold, generic structs (type params in fields via `tgmap`), trait BOUNDS
> (`ti_extract_bounds`), and HM let-polymorphism (`ti_generalize`/`ti_instantiate`). Tests
> `generics_test` + `generics_edge_test` (+ advanced/soundness/edge suites) PASS in CI both modes. So
> Tier 3.1 is **largely IMPLEMENTED**, not absent — the remaining Tier-3 work is specific
> (exhaustive-match ADTs 3.3, interfaces/traits-as-values 3.2, comptime 3.4), not "add generics from
> scratch". Re-scope Tier 3 accordingly.

---

## TIER 2 — Performance ceilings (the core is C-fast only on a narrow subset)

C-class scalar/struct math is real (~1.0–1.04× clang -O2, verified). But the moment code leaves that
subset, NOVA falls off a cliff — and the frameworks that need speed live off the cliff.

- **2.1 The uniform i64 value model is the ceiling.** Everything is a 64-bit handle → trivial inference
  and Python-simplicity (the win), but floats-in-lists are *boxed* and cross-function structs can't be
  register-split (no SROA). Cross-function struct code is capped at ~1.0–1.2× C (falsification 285846e).
  This is the **root cause** of 2.2 and 2.3, not a separate bug.
- **2.2 Float arrays — RE-MEASURED 2026-07-06 (the "8–120×" was one-sided; the real story is
  CONDITIONAL on ESCAPE).** Direct measurement (`_fa_bench*.nova`, sum of a 2M float array × 50 reps,
  vs clang `-O2` C):
  - **Homogeneous float list, no escape → 260 ms vs 108 ms C = 2.4× C.** The S4 floatlist fast path
    fires: `nova_rt_list_get_f` (raw double, no box-chase) + native `fadd double`. The residual 2.4× is
    per-element *call* overhead (vs an inlined load) + C's SIMD vectorization, NOT boxing.
  - **Same loop but the list ESCAPES** (passed to a helper → S4 taint drops the fast path for soundness,
    a callee could make it heterogeneous) **→ 30,361 ms = 281× C.** Reads fall to the generic boxed
    `nova_rt_index_get`. **THIS is the cliff** — and it's the real 2.2, not "every numeric loop".
  So: self-contained numeric loops are fine (2.4×); the catastrophic case is a float array that escapes
  and is then hot-read. **Fix = S4.2: a runtime elem-kind guard + native load on the tainted-but-still-
  homogeneous path (deopt), so escape doesn't force boxing.** **Blocks: Cortex/Pulse/Reactor** whenever
  arrays cross function boundaries (i.e. always, in real code). **Effort: L** (scoped S4.2), not the full
  XL raw-`double[]` rewrite — the fast path already exists, it just needs to survive escape.
  **★ FULL IMPLEMENTATION DESIGN (2026-07-07): `S4_TYPED_ARRAYS_DESIGN.md` §"S4.2 ESCAPE-SURVIVAL".**
  Grounded by measurement (160×C now) + codegen recon (injection = `nova_compiler.nova:14836`/`:9181`).
  Key results: the runtime `elem_kind==2` invariant is SOUND + survives escape (so this is Tier-0-safe
  by construction — the gap is compiler TYPING, not runtime); there is NO sound read-level fix
  (`float+string`=concat forbids "coerce to double"), so the fix is **loop-versioning** — guard on
  `list_is_kind2(xs)`, and in the fast branch a `_floatlist_view(xs)` shadow re-establishes `floatlist`
  typing on a fresh untainted register → `list_get_f`+native `fadd`; the else branch is byte-identical to
  today. Open crux to validate on gen4 first: accumulator flow-sensitivity (may need a native-float temp
  + reconcile). Staged; iterate on gen4 (cheap), reconverge once.
- **2.3 HOF/closure arithmetic fully dynamic.** `map`/`filter`/`pmap` callbacks use runtime dispatch,
  ~50–100ns/op — and the tutorial *encourages* `map`/`filter` while the perf doc warns against them
  (a contradiction users will hit). Evidence: `where_nova_is_slow.nova:73-103`. **Effort: XL** (S5
  monomorphization — see note).
- **2.4 The fix is one campaign, and it doubles as a feature.** S3 (struct SROA) + S4 (raw float
  arrays) + S5 (monomorphic specialization, the "Julia endgame") close 2.1–2.3. S2 was attempted and
  reverted once (`PERFORMANCE_SPECIALIZATION.md`) — the retry is scoped there. **This is the single
  most valuable core investment** because it's *both* the perf fix *and* the generics enabler (Tier 3.2).
- **2.5 Continuous perf tracking — ✅ FIXED (e7841fa4, 2026-07-06).** The harness already
  existed (`bench/run_bench.ps1` times 5 benches → appends `{ts,commit,bench,mode,elapsed_ms,rc}` to
  `history.jsonl`; `bench/regression_check.ps1` compares the last 2 commits, fails >threshold%). The
  gap was that it wasn't RUN. Now wired into `nova_ci.ps1` as stage 2/3b (append + compare), **NON-FATAL**
  (times here are tiny/0–42 ms and memory-noisy, so a wobble must not red the CI; the FATAL codegen guard
  is `_perf_gate.ps1`). Baseline refreshed (`0cd6e97f`). Every perf change is now *measured*. NOTE: the
  benches are small; a future improvement is bigger workloads for higher signal. `-SkipPerfTrack` to skip.

**Why the future isn't bright if ignored:** Cortex and Pulse are *defined* by numeric throughput. If
float arrays stay 100× slow, those two frameworks can never be built honestly — you'd be shipping a
"NumPy killer" that loses to NumPy by 100×. **Tier 2 is the mountain: plan it as a dedicated multi-month arc, not a sprint.**

---

## TIER 3 — Language expressiveness — ★ RE-VERIFIED 2026-07-06: LARGELY DONE (ledger was badly stale)

> **This whole tier was audited as "unbuilt" — but 3.1/3.2/3.3 all already EXIST and pass tests.**
> Verified by reading the compiler + compiling/running probes with the current (strict-default) build.

- **3.1 Generics — ✅ EXIST.** `fn<A,B> swap(p)`, `fn<T,U> my_map(xs: List<T>, f) -> List<U>`, generic
  filter/reduce/fold, `List<T>` syntax, **generic structs** (type params in fields via `tgmap`), **trait
  BOUNDS** (`ti_extract_bounds`), and HM let-polymorphism (`ti_generalize`/`ti_instantiate`). Tests
  `generics_test`/`generics_edge_test`/advanced/soundness PASS. The "no generics" claim was wrong.
- **3.2 Interfaces/traits — ✅ EXIST.** `trait` decls, impls, **trait bounds on generics**
  (`trait_bounds_test` PASS), **default methods** (`phase75_default_trait_test`), **dynamic dispatch**
  (`phase75_dyn_trait_test` → "DYN TRAIT TEST PASSED"), and **conformance enforcement**
  (`trait_conformance_test` → `error[E1006]: type 'Square' does not fully implement trait 'Shape':
  missing name()`). Was "unbuilt" — wrong.
- **3.3 ADTs + exhaustive match — ✅ EXIST.** Exhaustiveness is ENFORCED with `error[E1009]`:
  non-exhaustive match on **Option** ("missing None arm"), **Result** ("missing Err"), and **user enums**
  ("missing Blue"). Typed variant payloads carry types (`nt_fn(field_types, nt_struct(vname))`). So 1.4
  is effectively covered here too. Was "would make... would close 1.4" — it already does.
- **3.4 (Optional) comptime.** Genuinely not built (Zig-style compile-time execution). **Effort: L.**
  Remains the one real Tier-3 item, and it's optional. `const`-fn folding exists (CI `#35 const-fn-eval`
  gate) as a partial.

**Net: Tier 3 is NOT the "biggest jump available" — it's mostly shipped.** The real remaining core work
is **Tier 2** (the float-array-on-escape perf cliff, 2.2) and **Tier 4** (concurrency). This materially
re-prioritizes the roadmap. *(Follow-up: refine generic error MESSAGES + add a couple exhaustiveness/
trait negative tests to the CI gate so these don't silently regress — S.)*

**Discipline note:** absorb the *idea*, unify it into the three primitives — do NOT bolt on each
language's syntax as a separate feature. That path is how NOVA becomes C++ (explicitly forbidden in
CLAUDE.md). The test for every addition: *does this make the language simpler and more unified, or does
it add a feature sitting beside the others?* If "beside" → reject.

---

## TIER 4 — Concurrency & runtime architecture (Reactor/Mesh live or die here)

- **4.1 N>1 I/O regresses.** Multi-carrier web I/O measured at **0.76–0.82×** single-core — more cores
  makes it *slower*. Evidence: audit scheduler + Go-judge findings. **Blocks: Forge at scale, Mesh,
  Reactor.** **Effort: L.** Must be fixed before claiming multi-core.
- **4.2 Linux FD_SETSIZE overflow.** `select()` with fd ≥ 1024 corrupts the carrier stack on the claimed
  Linux target (glibc `fd_set` not resized). Evidence: `nova_runtime.c:27`, `:6387`, `:6394`.
  **Effort: M.** Move to `poll`/`epoll` fully. CVE-class on Linux.
- **4.3 Cross-thread channel-send data race (N=1 default).** Sending from a non-carrier OS thread takes
  a path that touches scheduler state without the right lock. Evidence: `:7838`, `:7896`, `nova_sched_wake_one` `:4810`, `nova_rq_push` `:6019-6037`. **Effort: M.**
- **4.4 No preemption.** A CPU-bound task starves its carrier (cooperative only). Evidence:
  `forge_otp.nova:113`. **Blocks: Reactor (frame budget), Erlang-parity claims.** **Effort: XL.**
- **4.5 No per-process heaps.** One shared C address space + copy-on-send, not true isolation → a
  corruption is not contained (unlike BEAM). This is the honest limit behind "process isolation IS
  memory safety." **Effort: XL** (architectural). Decide explicitly whether to close or to re-scope the claim.
- **4.6 `remote_spawn` is a stub / unauthenticated RPC-by-name.** **Blocks: Mesh (distributed), Ops.**
  **Effort: L–XL.**
- **4.7 RC cycles leak forever — CONFIRMED; DESIGNED 2026-07-07 (re-scoped L→XL).** Re-reproduced:
  `_cycle_leak.nova` (1000× `Node{nxt=self}`) under FULLRC+HEAP_PROFILE → `struct count=1000 bytes=32000`
  still-live at exit. **Grounded the runtime for a trial-deletion collector** (CPython-style — needs NO
  root scanning; the RC count itself reveals external refs): refcount = `int32` at `ptr-8`; child
  enumeration already exists per type in `nova_rc_free` (`:9908`) — LIST `data[i]` (skip kind==2), DICT
  `keys/vals[i]`, STRUCT slots `1..NOVA_STRUCT_NSLOTS-1`, CHANNEL ring. **Algorithm:** copy rc→gc_refs
  for each tracked obj; subtract internal refs (visit children, dec gc_refs of children that are tracked);
  gc_refs>0 = externally-reachable root → propagate reachability; unreachable = cyclic garbage → free.
  **WHY XL not L:** (1) NO existing live-object REGISTRY — the heap profiler tracks only COUNTS
  (`nova_mem_live`), so the collector must BUILD object tracking (a maintained pointer set on every
  container alloc/free — invasive hot-path change, thread-safe under N>1). (2) The **safe-free phase is
  UAF-risky** — freeing a batch of mutually-referencing garbage cascades child-decs that can double-free
  or dangle mid-sweep (CPython pins refcounts + tp_clear; NOVA needs the equivalent). Build it OPT-IN
  (gated like S4.2) + validate on `_cycle_leak` + adversarial (nested/mixed cycles, NO false-free of live)
  before any reconverge. Repro `_cycle_leak.nova`. **Supervised XL** (a wrong safe-free = UAF worse than
  the leak). Deferred — real, but a slow RAM leak, not a crash.

**Why it matters:** Reactor (games) needs a frame budget → needs preemption (4.4). Mesh (distributed)
needs real remote spawn (4.6). Forge-at-scale needs N>1 to not regress (4.1). These are not polish.

---

## TIER 5 — Platform reach ("runs anywhere" is Windows + WSL-once today)

- **5.1 No ARM/aarch64.** Fibers silently no-op on ARM. **Blocks: Edge (IoT), Apple Silicon, mobile.** **Effort: L.**
- **5.2 macOS never run** (epoll-only assumptions). **Effort: M–L.**
- **5.3 WASM frontend is a value-model proof, not a framework.** No SPA framework, no in-browser
  concurrency, no headless-browser CI. **Blocks: Prism (GUI-in-browser), the full-stack identity.** **Effort: XL.**
- **5.4 GPU is one hardcoded OpenCL vadd kernel** — no kernel-lowering compiler path. Evidence:
  `nova_runtime.c` ~`:20436-20523`. **Blocks: Cortex (AI accel), Reactor (rendering).** **Effort: XL.**
- **5.5 No GUI/windowing runtime at all.** **Blocks: Prism, desktop Reactor games.** **Effort: XL** (new runtime bridge — note: this is *runtime/platform* work the compiler enables but does not replace).
- **5.6 No CI on any platform but the dev's Windows box.** Linux was verified *once* manually on WSL2.
  **Effort: M.** "Runs anywhere" is unproven without per-platform CI.

---

## TIER 6 — Toolchain & developer experience (adoption dies here even if the core is perfect)

- **6.1 Package manager — the ONE genuinely-open Tier-6 gap (verified 2026-07-07, accurate not stale).**
  MORE than a "driver stub": `nova init`/`new`/`get`/`install`/`clean`/`test` all exist, plus a github
  registry (`nova_pkg_download`: `index.toml`→`source` URL, then a `raw.githubusercontent.com/nova-lang/
  pkg-<name>` fallback). **What's truly missing:** (1) **no dependency SOLVER** — `nova_pkg_install`
  fetches only the DIRECT deps in `nova.toml`; a downloaded package's OWN deps are never fetched (no
  transitive closure), and the `pkg_version` arg is parsed but IGNORED (no semver constraint / conflict
  detection / backtracking); no lockfile. (2) **no populated registry** — the URL points at an empty
  `nova-lang/packages` repo. **Path:** `index.toml` carries a `[dependencies]` block → `install`
  recurses over the transitive closure with a visited-set (cycle guard) + semver resolution + a
  `nova.lock`. **Effort: L**, and the registry itself is EXTERNAL infra (owner's github + published
  packages) that cannot be stood up autonomously → **supervised**. Without this there is no ecosystem,
  and no ecosystem = no adoption (Python's actual moat).
- **6.2 REPL — ✅ FIXED (2026-07-07).** The claim "No REPL" was STALE: a real REPL was already
  implemented (`repl.nova` — `nova repl`, v0.1, banner/`:quit`/`:reset`/`:session`, multi-line `fn`
  continuation, definition-vs-expression classification). It was BROKEN by two bugs, both now fixed:
  (a) **session-state loss** — my own Tier-1 unused-variable lint (E1000) fired on every replayed
  session `let` that a later line didn't reference, so a definition never compiled "OK" → was never
  re-added to the session → the REPL forgot all state. Fix (repl.nova, no compiler change): emit a
  silent `_ = NAME` read for each replayed binding (`_`-prefixed names are E1000-exempt; the `_ = x`
  discard counts as a read) plus a visible `print(NAME)` echo of each new binding. (b) **runtime
  resolution** — `nova_find_runtime` resolved the DEV-layout `nova repl` to the 7-week-stale flat
  `nova-compiler/nova_runtime.c` (144 fns) instead of the canonical `test_programs/output/nova_runtime.c`
  (793 fns), so `repl.nova` failed to link (`undefined nova_rt_stdout_write`/`nova_rt_stdin_read_n`).
  Fix: **synced** the flat copy to canonical (it is *meant* to mirror it — same includes, and
  `bootstrap.ps1`/`bench_gate5.ps1` all `Copy-Item output\nova_runtime.c nova_runtime.c`; it simply
  went stale in May). Verified end-to-end with NO `NOVA_RUNTIME`: `let x=5`→`5`, `x*10`→`50`,
  `let z=x+y`→`12`, `z*z`→`144`; error recovery (session survives a bad line), redefinition
  (`let x=99`→`x*2`=`198`), `:reset` genuinely clears. Runtime-only + standalone `repl.nova` → no
  reconverge. **Remaining polish (not blocking):** unbounded session growth on redefinition (last-wins,
  correct but keeps old `let x` lines); no readline history/tab-completion.
- **6.3 `nova debug` is fake; `nova build/test/fmt` run simple driver versions** (sophisticated tools
  unwired). `nova fmt` DOES work (AST-reprint, CI gate 2i); `nova repl` now works (6.2). **Effort: M.**
- **6.4 LSP — ✅ substantially FIXED (2026-07-07; was STALE).** The claim "LSP runs the May-20 Kotlin
  type-inferer → false positives on spawn/channels/ORM/generics" is FALSE. A self-hosted `nova lsp`
  (in `nova_compiler.nova`: `initialize`/`hover`/`definition`/`completion`/`publishDiagnostics`) already
  exists, and its **diagnostics call the real current inferer** — `lsp_collect_diagnostics` runs
  `tokenize` → `parse_program` → `ti_infer_program_named` (the SAME front-end that compiles programs,
  incl. all Tier-0/1 hardening). Proven end-to-end: a genuine arity error → `E1003` diagnostic at the
  right line; a **correct** `spawn`+`channel` program → **zero** diagnostics (no false positive);
  hover shows the real signature `add(a: int, b: int) -> int` (CI gate #32 `_s32_hover_check`). The
  VS Code extension already launches this (`nova-vscode/out/extension.js`: `command: nova-compiler.exe`,
  `args: ['lsp']`) — the Kotlin `LspAnalyzer.kt`/`LspServer.kt` in `bin/`+`src/` are LEGACY dead code
  the extension no longer uses. **Fixed the one real residual:** the extension shipped a **Jun-28**
  `bin/nova-compiler.exe`/`nova-lsp.exe` (pre-Tier-0/1), so editor users got week-stale, less-sound
  diagnostics — refreshed both to the current reconverged `gen3_test.exe` (Jul-7, strict-default) and
  re-verified diagnostics+hover against the shipped binary. **Remaining (release-only, not a code gap):**
  repackage the `.vsix` from the refreshed `bin/` on the next extension release; optionally delete the
  dead Kotlin LSP sources.
- **6.5 Onboarding docs — front door ✅ FIXED (e21f1b05, 2026-07-06).** `CLAUDE.md` said
  "Current Phase: Phase 0" and "compiler in Java" — both false (NOVA is a self-hosted ~22k-line NOVA
  compiler). Replaced with an accurate **Current State** block (self-hosted, byte-identical fixpoint,
  Tier 0/1 done, CORE_GAPS is the live plan) + corrected the "compiler in Java → SELF-HOSTED in NOVA"
  line with the real build/reconverge/commit loop. A new dev now lands on the truth. **Remaining (not
  done):** the broader onboarding polish (a real quickstart, an ONBOARDING.md) — tracked, lower urgency.
- **STRENGTH to keep:** error messages are genuinely best-in-class (E-codes, source snippets, "did you
  mean?"). Do not regress this.

---

## TIER 7 — Process & discipline (the meta-risk the audit ranked #1)

Not code — but the audit's single highest risk, because it *manufactures* all the tiers above.

- **7.1 Bus factor 1 at AI velocity.** Peaks of 51–116 commits/day; no human can review that. This is
  *why* Tier 0 holes and untested features exist. **Fix:** slow down; make review the bottleneck, not commits.
- **7.2 Rapid-dev ships syntax-checked-only code into the canonical framework** — including security
  features (admin perms/audit were shipped as stubs never wired into any route: `forge_admin.nova:334-364`).
  **Fix:** no security-facing code merges without a functional test.
- **7.3 Orphaned tests — ✅ MAJOR DENT (2026-07-07).** ~529 `*_test.nova` files were on disk but not in
  the regression gate → those features could regress silently. Built `_classify_orphans.ps1` (a
  compile+link+run+exit0 classifier, kill-on-timeout) and swept the **204 core-language orphans**
  (excluded ~325 forge/demo/db/server/heavy that need the server-test harness). Result: **190 initially
  classified CLEAN**, then run through the REAL harness — **182 pass (verified in the parallel harness,
  NORMAL; clean in FULLRC) and are now GATED** via a data-file manifest (`_orphan_coverage_manifest.txt`)
  loaded by `_run_final_regression.ps1` (`$all_tests`), matching "one manifest; every test in the gate".
  **Two lessons banked:** (1) the first classifier only checked STDOUT, but NOVA's `assert_*` prints
  `FAIL assert` to STDERR and exits 0 — so 8 genuinely-failing tests were mis-marked CLEAN; the harness
  (which checks stderr) caught them and the classifier is now fixed to check both streams. (2) Those 8
  are NOT flaky — they genuinely fail, testing known-partial/env-dependent features (`wasm_*`: native
  `wasm_run(add.wasm)` returns −1, the WASM exec path is unwired here → Tier 5.3; `_glob`: cwd-dependent
  glob finds 0 matches; `gguf`/`ml_ops`: data/compute asserts). Correctly EXCLUDED (a gate test that
  fails = red CI); per "in the gate or deleted" they should be fixed or retired later. The 14 non-clean
  orphans are correctly out too: negative
  tests (`ffi_unsafe_required`/`multi_error`/`trait_bounds_fail`/`trait_unknown`/`phase75_*` — these
  belong in the negative gate, follow-up), distributed-stubs, and FFI/net/time tests. **Then a SECOND
  sweep of the 337 non-server forge-lib orphans** (the Forge "batteries" — algorithms/DS/crypto-math,
  KAT-gated): **289 clean in both RC modes AND verified passing in the parallel harness** (the full
  1121-test run went 1121 PASS / 0 FAIL — zero contention flakes) → all 289 gated too. **Manifest now =
  471 tests** (182 core + 289 forge). The 48 excluded forge orphans are correctly out (negative tests,
  FFI needing C sources, or server-ish hangs). **Remaining:** ~40 server/demo orphans (need the serial
  server-test phase) + the handful of genuinely-failing tests (wasm/glob/ml — to fix or retire). Net:
  **+471 previously-unprotected features now gated.** **Effort: M** (mechanical).
- **7.4 `nova_ci -SkipReconverge` bypass — ✅ mitigated (2026-07-07).** The bypass no longer skips the
  self-hosting check SILENTLY: `nova_ci.ps1` now prints a prominent WARNING banner ("reconverge skipped —
  safe only for test/doc-only changes; never gate a compiler/runtime commit with this"). The human
  discipline (don't skip on a merge that touches the compiler) is still required — a script can't detect
  "merge" — but the bypass is now loud + auditable instead of a one-line silent note.
- **7.5 Doc sprawl:** ≥6 docs each claim to be canonical. **Fix:** one core plan (this file) + one Forge
  plan (FORGE_STATUS) + supersession banners on the dead ones.

---

## The build order (what to actually do, in sequence)

1. **Tier 0 (2–4 wks): close every runtime soundness hole.** The core becomes *trustworthy*. Nothing
   else is safe to build on until this is done.
2. **Tier 1 (3–5 wks): make the default type checker sound** (reset the budget, land strict-by-default,
   write negative tests). The core becomes *honest*.
3. **Tier 7 in parallel, forever: slow down, test before ship, one plan.** This stops new Tier 0/1 holes
   from being created faster than you close them.
4. **Tier 3.1+3.2 (L): generics + interfaces.** The core becomes *expressive* enough for clean framework
   APIs — and this is the prerequisite for gRPC-from-types, typed repos, plugins.
5. **Tier 2 (XL, dedicated arc): S3→S4→S5 specialization.** The core becomes *generally fast* — and S5
   is the same work that makes generics zero-cost. This unlocks Cortex/Pulse/Reactor.
6. **Tier 4 (L–XL): fix N>1, then preemption/distribution** as specific frameworks (Reactor, Mesh) demand them.
7. **Tier 5/6 as each framework requires** its platform (Edge→ARM, Prism→GUI/WASM, Cortex→GPU) and as
   adoption requires the toolchain (package registry, REPL).

**Rule:** do not start a framework whose blocking core gap is still open. Reactor needs Tier 2 + 4.4.
Cortex needs Tier 2 + 5.4. Prism needs 5.3/5.5. Forge (the one that's real) needs Tier 0/1 + 4.1 to be
production-grade. **Forge is the right framework to keep pushing precisely because its remaining blockers
are the core gaps everything else also needs.**

---

## If you only do FIVE things this quarter

1. **Close Tier 0** — the runtime is not trustworthy until these 6 holes are gone. (~2–4 wks)
2. **Make the type checker sound by default** (Tier 1.1–1.3) + write negative tests (1.5). (~3–5 wks)
3. **Add generics + interfaces** (Tier 3.1/3.2) — the biggest expressiveness jump, and it unblocks clean
   APIs across every framework. (L)
4. **Commit to the S3→S4→S5 specialization arc** (Tier 2.4) — the one campaign that fixes performance
   *and* makes generics zero-cost. Cortex/Pulse/Reactor are impossible without it. (XL — start it, don't finish it this quarter)
5. **Slow the commit rate and gate every merge** (Tier 7) — this is what makes the other four *stick*
   instead of being re-broken next week.

---

## The honest bottom line

The core is **real and rare**, but it is **not yet trustworthy (Tier 0/1), not yet general (Tier 2),
and not yet expressive (Tier 3).** Every one of those is fixable — none is a dead end. The frameworks
are the *reward*; the core is the *foundation*, and right now the foundation has cracks that a
9-framework tower would turn into a collapse. Fix the foundation in the order above and the future is
bright. Pour framework code onto the current core and you multiply its cracks nine times over.

**Build the compiler until it's trustworthy, sound, fast, and expressive. Then the frameworks are cheap.**
