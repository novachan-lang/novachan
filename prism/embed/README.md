# prism/embed — the escape hatch, contained

Not started.

| Module | Purpose |
|---|---|
| `prism_embed.nova` | Sandboxed native/iframe escape hatch (spec §17, features #110 #131) -- the one place a Prism app can host content Prism itself doesn't own, with the sandbox boundary explicit and typed |
