# Build artifacts. forge/ committed 1,013 files with .ll/.exe interleaved among sources
# (see PRISM_STATUS.md "REPOSITORY STRUCTURE") -- prism/ is git-ignored from the first commit
# so that sprawl never starts here.
*.ll
*.exe
*.o
*.obj
*.pdb
*.wasm
*.wat
