#include <windows.h>
#include <setjmp.h>
static void CALLBACK test_entry(LPVOID param) {
    jmp_buf buf;
    __try {
        if (setjmp(buf) == 0) {
            *(volatile int*)0 = 42;
        }
    } __except(GetExceptionCode() == EXCEPTION_STACK_OVERFLOW
              ? EXCEPTION_EXECUTE_HANDLER : EXCEPTION_CONTINUE_SEARCH) {
        _resetstkoflw();
    }
}
int main(void) { return 0; } folderNew folder_seh_test.c
