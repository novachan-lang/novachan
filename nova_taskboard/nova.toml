[package]
name = "nova-taskboard"
version = "0.1.0"
nova-version = "0.1.0"
description = "Project & task management web app — Forge + PostgreSQL demo"
author = "mangesh.mane"
license = "Apache-2.0"

[dependencies]
