// Sample type errors to test error message quality

// 1. Adding int and string
name = "Alice"
age = 30
greeting = "Hello " + age

// 2. Wrong arg count (not caught yet)
fn add(a, b)
    return a + b

result = add(1, 2, 3)
