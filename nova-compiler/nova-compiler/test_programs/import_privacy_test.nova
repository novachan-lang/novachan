// Test that _ prefixed functions are NOT importable
import shapes

a1 = shapes.circle_area(10)
print(a1)
a2 = shapes.rect_area(3, 4)
print(a2)
// shapes._secret() should NOT be accessible — if it compiles, the privacy check failed
