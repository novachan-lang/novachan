fn main()
    let xs = [1.5, 2.5, 3.5, 4.5]
    let sq = map(xs, fn(x) x * x)
    print(sq)
    let big = filter(xs, fn(x) x > 3.0)
    print(big)
    let ns = [1, 2, 3, 4, 5]
    let dbl = map(ns, fn(x) x * 2)
    print(dbl)
    let f = fn(x) x + x
    print(map(ns, f))
    print(map(xs, f))
