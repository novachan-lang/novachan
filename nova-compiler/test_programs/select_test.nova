// Select: receive from whichever channel has data first
ch1 = channel()
ch2 = channel()

spawn fn()
    send(ch1, 10)

spawn fn()
    send(ch2, 20)

result = select(ch1, ch2)
idx = result[0]
v = result[1]

result2 = select(ch1, ch2)
v2 = result2[1]
print(v + v2)
