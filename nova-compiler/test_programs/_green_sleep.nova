fn main()
    let lo = 99
    let i = 0
    while i < 15
        let t0 = time_ms()
        sleep(10)
        let e = time_ms() - t0
        if e < lo
            lo = e
        i = i + 1
    print("min elapsed over 15 sleeps of 10ms: " + str(lo))
