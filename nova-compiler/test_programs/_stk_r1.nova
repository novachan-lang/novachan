fn main()
  let t = {}
  t["frames"] = []
  let f = {}
  f["name"] = "main"
  f["enter_ms"] = time_ms()
  f["exit_ms"] = 0
  push(t["frames"], f)
  let depth = 0
  for fr in t["frames"]
    if fr["exit_ms"] == 0
      depth = depth + 1
  assert(depth == 1, "d1")
  print("r1 ok")
