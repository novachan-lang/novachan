type Vec2
    x: Int
    y: Int

fn vec2_add(a: Vec2, b: Vec2) -> Vec2
    Vec2 { x: a.x + b.x, y: a.y + b.y }

fn main()
    let a = Vec2 { x: 3, y: 4 }
    let b = Vec2 { x: 10, y: 20 }
    let c = vec2_add(a, b)
    print(str(c.x))
    print(str(c.y))
