fn greet(name)
    "Hello, {name}!"

fn describe(n)
    "The number is {n}"

fn formatResult(label, value)
    "{label} = {value}"

print(greet("World"))
print(greet("NOVA"))
print(describe(42))
print(describe(100))
print(formatResult("sum", 1000))
print(formatResult("pi", 3))
