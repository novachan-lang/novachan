// Debug: test private function call from same module
import std/physics/friction

fn main()
    // Call incline_normal with angle 0 and 90
    let n0 = incline_normal(1.0, 0.0)
    let n90 = incline_normal(1.0, 90.0)
    print("incline_normal(1,0) = " + str(n0))
    print("incline_normal(1,90) = " + str(n90))
