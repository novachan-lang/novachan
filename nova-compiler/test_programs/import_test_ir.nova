import mathlib

let r1 = mathlib.add(3, 4)
print(r1)

let r2 = mathlib.multiply(5, 6)
print(r2)

let r3 = mathlib.square(7)
print(r3)

let r4 = mathlib.add(mathlib.multiply(2, 3), mathlib.square(4))
print(r4)
