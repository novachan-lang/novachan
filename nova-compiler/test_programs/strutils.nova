fn greet(name)
    "Hello, " + name + "!"

fn shout(msg)
    upper(msg)

fn repeat_str(s, n)
    let result = ""
    let i = 0
    while i < n
        result = result + s
        i = i + 1
    result
