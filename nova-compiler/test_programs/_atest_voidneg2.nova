// NEGATIVE: a void @test violates the `-> bool` contract; the `tn() == true` runner must make it a hard
// compile error (previously it silently reported a PASSING void-assert test as "0/1 failed").
@test
fn t_void()
    print("ran")

fn main()
    print(str(__nova_run_tests()))
