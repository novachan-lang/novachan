// TRACK-A repro module: two structs that SHARE a field name "a" at DIFFERENT slots.
// XS has a at slot 1; YS has a at slot 2. A factory returns XS.
type XS
    a: int
    b: int

type YS
    b: int
    a: int

fn make_xs() -> XS
    XS(10, 20)

fn make_ys() -> YS
    YS(30, 40)
