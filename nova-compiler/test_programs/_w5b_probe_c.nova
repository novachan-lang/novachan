// C: intra-frame ALIAS -- two local list slots hold the same handle, neither
// returned, neither an escape the analysis looks for. Predict W5b DOUBLE-FREE.
fn f() -> int
    let s = [10, 20, 30]
    let s2 = s
    return len(s) + len(s2)
fn main()
    print(f())
    print("C_DONE")
