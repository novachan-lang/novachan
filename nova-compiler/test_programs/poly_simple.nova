fn first(items: list) -> int
    return items[0]

fn main()
    let nums = [42, 99]
    let strs = ["hello", "world"]
    let r1 = first(nums)
    let r2 = first(strs)
    print(str(r1))
    print(str(r2))
