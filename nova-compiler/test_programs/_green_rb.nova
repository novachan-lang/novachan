fn main()
    let n = 0
    let i = 0
    while i < 30
        let t = {}
        let stored = time_ms()
        t["k"] = stored
        let rb = t["k"]
        if rb != stored
            n = n + 1
        i = i + 1
    print("mismatches: " + str(n) + " of 30")
