// MUST FAIL: non-exhaustive match on a user enum (missing Blue) — E1009.
enum Color
    Red
    Green
    Blue
fn name(c) -> string
    match c
        Red => "red"
        Green => "green"
fn main()
    print(name(Red))
