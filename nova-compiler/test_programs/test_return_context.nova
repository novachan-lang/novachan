fn test(d: dict, k: string)
    if contains(d, k)
        print(f"already there: {d[k]}")
        return 0
    let x = k + " = \"" + "val" + "\"\n"
    print(x)

fn main()
    let d = {"foo": "bar"}
    test(d, "foo")
    test(d, "baz")
