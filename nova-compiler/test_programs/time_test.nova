let start = time_ms()
sleep(10)
let end = time_ms()
let elapsed = end - start
print("elapsed_ms >= 10: " + str(elapsed >= 10))

let t1 = clock_ns()
let t2 = clock_ns()
print("clock_ns monotonic: " + str(t2 >= t1))

print("time_ms > 0: " + str(start > 0))
