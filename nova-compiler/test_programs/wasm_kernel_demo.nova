fn fib(n)
    if n < 2
        n
    else
        fib(n - 1) + fib(n - 2)
fn sumsq(k)
    let total = 0
    let i = 1
    while i <= k
        total = total + i * i
        i = i + 1
    total
fn main()
    fib(20)
    sumsq(10)
