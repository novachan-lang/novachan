fn main()
    let xs = [3, 1, 4, 1, 5, 9]
    let ys = xs.map(fn(n) n * 2)
    print(ys)
    
    let s = "hello world"
    print(upper(s))
    print(len(s))
    
    let parts = split(s, " ")
    print(parts)
