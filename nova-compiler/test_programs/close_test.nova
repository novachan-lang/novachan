// Channel close: sender closes channel after sending values
ch = channel()

spawn fn()
    send(ch, 1)
    send(ch, 2)
    send(ch, 3)
    close(ch)

// Read all values before close
a = receive(ch)
b = receive(ch)
c = receive(ch)
print(a + b + c)
