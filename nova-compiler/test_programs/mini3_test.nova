type Point
    x: Int
    y: Int

p = Point { x: 10, y: 20 }
print(p.x)
print(p.y)

type Person
    name: String
    age: Int

alice = Person { name: "Alice", age: 30 }
print(alice.name)
print(alice.age)
