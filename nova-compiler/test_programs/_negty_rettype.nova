// MUST FAIL: return type annotation says int but function returns string
fn greet(name: string) -> int
    "Hello " + name   // E1001: type mismatch — expected int, found string

fn main()
    print(greet("world"))
