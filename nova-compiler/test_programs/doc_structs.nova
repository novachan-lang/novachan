type Point
    x: Int
    y: Int

fn distance(a, b) -> int
    let dx = a.x - b.x
    let dy = a.y - b.y
    dx * dx + dy * dy

fn main()
    let p1 = Point { x: 0, y: 0 }
    let p2 = Point { x: 3, y: 4 }
    print(f"distance squared: {distance(p1, p2)}")
