// Investigate: does NOVA persist a module-level mutable global across fn calls (native)?
let counter = 0
fn bump()
    counter = counter + 1
fn main()
    bump()
    bump()
    bump()
    print("counter=" + str(counter))
