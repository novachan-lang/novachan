// The workaround shape: bind the closure field to a local, THEN call it.
type Calc
    op: any
    label: string

fn main()
    let c = Calc(fn(a, b) a + b, "add")
    let f = c.op
    print(f(3, 4))
    let g = c.op
    print(g(5, 7))
    print(c.label)
