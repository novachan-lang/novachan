// MUST FAIL TO COMPILE: 'nmae' is a TRANSPOSITION of 'name' on struct Wc.
import forge_orm
type Wc
    id: int
    name: string
    qty: int
fn main()
    let db = unwrap(orm_open("sqlite://_ormcol.db"))
    let xs: list<Wc> = orm_all(db, "SELECT id, nmae, qty FROM Wc", [])
    print(len(xs))
