// This SHOULD fail: get_message returns a string (from make_msg),
// but we try to use it as a number. If forward-call inference works,
// this should be a type error.

fn get_message()
    return make_msg()

fn make_msg()
    return "hello"

fn main()
    let x = get_message() + 5
    print(x)
