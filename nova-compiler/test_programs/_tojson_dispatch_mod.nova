// Module under test: an ordinary NON-generic struct, no @redact, nothing special.
type MacCreds
    mc_user: string
    mc_pass: string

fn mac_creds_new(u: string, p: string) -> MacCreds
    MacCreds(u, p)
