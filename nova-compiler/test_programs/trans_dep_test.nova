import trans_dep_mid

fn main()
    assert(td_mid_doubled() == 84, "transitive: mid calls base value * 2")
    assert(td_mid_shout("nova") == "HELLO NOVA", "transitive: mid calls base greet + upper")
    print("trans_dep_test: all passed")
