// NEGATIVE: using a value after move(...) must be a COMPILE ERROR.
fn main()
    let a = [1, 2, 3]
    let b = move(a)
    print("b=" + str(len(b)))
    print("len=" + str(len(a)))       // E1003: used after move
