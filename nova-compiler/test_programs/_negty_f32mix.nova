// LOCK-4 inc3c: mixing f32 and f64 in ARITHMETIC is a width mismatch. C's implicit float
// promotion is a well-known footgun; NOVA requires the conversion to be written.
fn main() -> int
    let a: f32 = 1.5
    let b: f64 = 2.5
    print(str(a + b))
    return 0
main()
