type Box
    v: int

fn first() -> int
    let a: Box = "err in first"
    a.v

fn second() -> int
    let b: Box = "err in second"
    b.v

fn third() -> int
    let c: list = 99
    len(c)

fn main()
    print(first() + second() + third())
