// Debug: test cos directly
fn main()
    let pi = 3.14159265358979
    let theta90 = pi / 2.0
    let c = cos(theta90)
    print("cos(pi/2) = " + str(c))
    let theta0 = 0.0
    let c0 = cos(theta0)
    print("cos(0) = " + str(c0))
