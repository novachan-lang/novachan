// POSITIVE: two modules with DISTINCT exported names -> no false collision, compiles + runs.
import _l11_mod_alpha
import _l11_mod_gamma
fn main() -> int
    let ok = alpha_uniq() + gamma_a() + gamma_b()
    if ok == 310
        print("PASS _l11_pos")
    else
        print("FAIL _l11_pos=" + str(ok))
    return 0
