fn greet(name: string) -> string
    let msg = "Hello, " + name + "!"
    msg

fn main()
    let x = 10
    let y = 20
    let sum = x + y
    let greeting = greet("NOVA")
    print(greeting)
    print("sum = " + str(sum))
