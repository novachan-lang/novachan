# Files NOT uploaded to Railway for `railway up`. The build only needs:
#   Dockerfile, railway.json, ai_serve_linux.ll, output/nova_runtime.c
# Keep the secret and Windows-only binaries out of the upload.
.env
*.exe
