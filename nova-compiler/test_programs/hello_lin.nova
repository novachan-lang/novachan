// Cross-platform smoke test: pure compute, deterministic output.
// sum(1..100) = 5050 (Gauss). Used to validate the NOVA Linux build via Docker.
fn main()
    let total = 0
    let i = 1
    while i <= 100
        total = total + i
        i = i + 1
    print("sum={total}")
