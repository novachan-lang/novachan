fn main()
    let t = {}
    t["start"] = time_ms()
    let j = 0
    while j < 6
        sleep(10)
        let now = time_ms()
        let start = t["start"]
        let e = now - start
        print("now=" + str(now) + " start=" + str(start) + " elapsed=" + str(e))
        j = j + 1
