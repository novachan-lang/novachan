enum Status
    Ok()
    Failed()

fn show(s) -> string
    match s
        Ok(_) => "ok"
        Failed(_) => "failed"

assert(show(Ok()) == "ok", "user enum Ok variant")
assert(show(Failed()) == "failed", "user enum Failed variant")

print("user enum exhaustiveness test passed")
