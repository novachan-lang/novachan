// Bidirectional communication: parent sends work, child sends result back
request = channel()
response = channel()

spawn fn()
    val = receive(request)
    send(response, val * val)

send(request, 7)
answer = receive(response)
print(answer)
