type V
    x: float
    y: float
fn dot(a, b)
    a.x * b.x + a.y * b.y
fn run(n)
    s = 0.0
    a = V(1.5, 2.5)
    b = V(3.0, 4.0)
    i = 0
    while i < n
        s = s + dot(a, b)
        i = i + 1
    s
print(run(1000))
