fn maxOf(a, b)
    if a > b
        a
    else
        b

fn minOf(a, b)
    if a < b
        a
    else
        b

fn absVal(x)
    if x < 0
        -x
    else
        x

print(maxOf(10, 20))
print(minOf(10, 20))
print(absVal(-42))
print(3 + 4 * 5)
print(100 / 3)
print(100 % 7)
