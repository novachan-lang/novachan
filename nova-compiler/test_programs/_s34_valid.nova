// squares demo
fn square(n: int) -> int
    return n*n

fn main()
    let total = 0
    let i = 1
    // accumulate squares
    while i<=5
        total = total + square(i)
        i = i+1
    print(str(total))
