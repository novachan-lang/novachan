// phase1 fan-in only
let n = 3000
let hub = channel()
let i = 0
while i < n
    spawn fn()
        send(hub, 1)
    i = i + 1
let total = 0
let got = 0
while got < n
    total = total + receive(hub)
    got = got + 1
assert(total == n, "p1")
print("p1 OK sum=" + str(total))
