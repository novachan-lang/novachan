fn make_pair(xs, i) -> list
    let pair = [xs[i], xs[i+1]]
    return pair

fn main()
    let a = ["--port", "8080"]
    let p = make_pair(a, 0)
    print("p[0]='" + p[0] + "'")
    print("p[1]='" + p[1] + "'")
    print("len p[1]=" + str(len(p[1])))
