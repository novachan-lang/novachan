// #16 negative test N1: string assigned to a struct-typed binding (concrete vs concrete).
// MUST fail to compile with a type mismatch.
type Point
    x: int
    y: int

fn main()
    let p: Point = "hello"
    print(p.x)
