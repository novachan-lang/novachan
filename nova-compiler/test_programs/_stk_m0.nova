fn main()
  let f = {}
  f["name"] = "main"
  f["exit_ms"] = "zero"
  let lst = []
  push(lst, f)
  let depth = 0
  for fr in lst
    if fr["exit_ms"] == "zero"
      depth = depth + 1
  assert(depth == 1, "d1")
  print("m0 ok")
