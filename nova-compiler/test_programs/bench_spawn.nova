// Spawn overhead benchmark: create many short-lived processes
// Spawn 100 workers, each sends result back
total = 0
i = 0
while i < 100
    ch = channel()
    spawn fn()
        send(ch, i * i)
    val = receive(ch)
    total = total + val
    i = i + 1
print(total)
