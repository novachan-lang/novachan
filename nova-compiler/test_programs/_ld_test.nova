fn get_pair() -> list
    [42, "hello"]

fn main()
    let (a, b) = [10, 20]
    assert_eq(a, 10)
    assert_eq(b, 20)
    let (x, y) = get_pair()
    assert_eq(x, 42)
    assert_eq(y, "hello")
    let (_, second) = [99, 77]
    assert_eq(second, 77)
    print("let_destruct: all passed")
