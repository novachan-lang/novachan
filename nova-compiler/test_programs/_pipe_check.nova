let nums = [1, 2, 3, 4, 5]
let doubled = nums |> map(fn(x) x * 2)
print(doubled)

let result = "hello world" |> upper() |> len()
print(result)

let total = [1, 2, 3, 4, 5] |> sum()
assert(total == 15, "pipe sum")
print("pipe tests passed")
