type Node
    n_val: int
    n_next: any

fn main()
    let b0 = live_count()
    let i = 0
    while i < 1000
        let a = Node(1, 0)
        let b = Node(2, 0)
        a.n_next = b
        b.n_next = a
        i = i + 1
    print("cycle delta over 1000 iters = " + str(live_count() - b0))
