fn check_len(s) -> int
    return len(s)

fn main()
    let xs = ["a", "bb", ""]
    let i = 0
    while i < len(xs)
        let s = xs[i]
        print("s='" + s + "' len=" + str(check_len(s)) + " direct=" + str(len(s)))
        i = i + 1
