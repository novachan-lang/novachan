fn main()
    let outer = [[1,3],[5,8],[6,10],[15,20]]
    print("len=" + str(len(outer)))
    let i = 0
    while i < len(outer)
        let ii = i
        let iv = outer[ii]
        print("iv[" + str(ii) + "]=" + str(iv[0]) + "," + str(iv[1]))
        i = i + 1

main()
