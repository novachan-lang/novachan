fn worker(ch: channel, id: int)
    let result = id * id
    send(ch, result)

fn main()
    let ch = channel()
    spawn worker(ch, 7)
    let val = receive(ch)
    print("received: " + str(val))
    assert(val == 49, "expected 49")
    print("SPAWN CALL FORM PASSED")
