type V
    x: float
    y: float
    z: float

fn dot(a, b)
    a.x * b.x + a.y * b.y + a.z * b.z

fn main()
    let a = V(1.5, 2.5, 3.5)
    let b = V(0.5, 1.5, 2.5)
    let s = 0.0
    let i = 0
    while i < 200000000
        s = s + dot(a, b)
        i = i + 1
    print(str(s))
