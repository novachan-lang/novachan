import std/calendar/chinese_zodiac

fn main()
    // ── chinese_zodiac KAT vectors ───────────────────────────────────────────

    // 2020 = Rat
    let a1 = chinese_zodiac(2020)
    if a1 != "Rat"
        print("FAIL chinese_zodiac chinese_zodiac(2020): got " + a1 + " want Rat")
        exit(1)

    // 2024 = Dragon  (2020+4 → idx 4)
    let a2 = chinese_zodiac(2024)
    if a2 != "Dragon"
        print("FAIL chinese_zodiac chinese_zodiac(2024): got " + a2 + " want Dragon")
        exit(1)

    // 2008 = Rat  (2020-12 = 2008; idx = (2008-2020) mod 12 = (-12) mod 12 = 0)
    let a3 = chinese_zodiac(2008)
    if a3 != "Rat"
        print("FAIL chinese_zodiac chinese_zodiac(2008): got " + a3 + " want Rat")
        exit(1)

    // 2021 = Ox
    let a4 = chinese_zodiac(2021)
    if a4 != "Ox"
        print("FAIL chinese_zodiac chinese_zodiac(2021): got " + a4 + " want Ox")
        exit(1)

    // 2022 = Tiger
    let a5 = chinese_zodiac(2022)
    if a5 != "Tiger"
        print("FAIL chinese_zodiac chinese_zodiac(2022): got " + a5 + " want Tiger")
        exit(1)

    // 2023 = Rabbit
    let a6 = chinese_zodiac(2023)
    if a6 != "Rabbit"
        print("FAIL chinese_zodiac chinese_zodiac(2023): got " + a6 + " want Rabbit")
        exit(1)

    // 2025 = Snake
    let a7 = chinese_zodiac(2025)
    if a7 != "Snake"
        print("FAIL chinese_zodiac chinese_zodiac(2025): got " + a7 + " want Snake")
        exit(1)

    // 2032 = Rat again  (2020+12=2032)
    let a8 = chinese_zodiac(2032)
    if a8 != "Rat"
        print("FAIL chinese_zodiac chinese_zodiac(2032): got " + a8 + " want Rat")
        exit(1)

    // ── chinese_element KAT vectors ──────────────────────────────────────────

    // 2024 = Wood (last digit 4)
    let e1 = chinese_element(2024)
    if e1 != "Wood"
        print("FAIL chinese_zodiac chinese_element(2024): got " + e1 + " want Wood")
        exit(1)

    // 2025 = Wood (last digit 5)
    let e2 = chinese_element(2025)
    if e2 != "Wood"
        print("FAIL chinese_zodiac chinese_element(2025): got " + e2 + " want Wood")
        exit(1)

    // 2026 = Fire (last digit 6)
    let e3 = chinese_element(2026)
    if e3 != "Fire"
        print("FAIL chinese_zodiac chinese_element(2026): got " + e3 + " want Fire")
        exit(1)

    // 2028 = Earth (last digit 8)
    let e4 = chinese_element(2028)
    if e4 != "Earth"
        print("FAIL chinese_zodiac chinese_element(2028): got " + e4 + " want Earth")
        exit(1)

    // 2020 = Metal (last digit 0)
    let e5 = chinese_element(2020)
    if e5 != "Metal"
        print("FAIL chinese_zodiac chinese_element(2020): got " + e5 + " want Metal")
        exit(1)

    // 2022 = Water (last digit 2)
    let e6 = chinese_element(2022)
    if e6 != "Water"
        print("FAIL chinese_zodiac chinese_element(2022): got " + e6 + " want Water")
        exit(1)

    // ── chinese_sexagenary KAT vectors ───────────────────────────────────────

    // 1984 = position 1 (Jiazi anchor)
    let s1 = chinese_sexagenary(1984)
    if s1 != 1
        print("FAIL chinese_zodiac chinese_sexagenary(1984): got " + str(s1) + " want 1")
        exit(1)

    // 2024 = (2024-1984) mod 60 + 1 = 40 + 1 = 41
    let s2 = chinese_sexagenary(2024)
    if s2 != 41
        print("FAIL chinese_zodiac chinese_sexagenary(2024): got " + str(s2) + " want 41")
        exit(1)

    // 1985 = position 2
    let s3 = chinese_sexagenary(1985)
    if s3 != 2
        print("FAIL chinese_zodiac chinese_sexagenary(1985): got " + str(s3) + " want 2")
        exit(1)

    // 2043 = 1984+59 = position 60
    let s4 = chinese_sexagenary(2043)
    if s4 != 60
        print("FAIL chinese_zodiac chinese_sexagenary(2043): got " + str(s4) + " want 60")
        exit(1)

    // 2044 = 1984+60 = position 1 (full cycle)
    let s5 = chinese_sexagenary(2044)
    if s5 != 1
        print("FAIL chinese_zodiac chinese_sexagenary(2044): got " + str(s5) + " want 1")
        exit(1)

    print("PASS _chinese_zodiac_test")
