fn main()
    let ch = channel()
    spawn fn()
        send(ch, 10)
        send(ch, 20)
        send(ch, 30)
        close(ch)
    let total = 0
    for val in ch
        total = total + val
    assert(total == 60, "for-in-channel sum")
    print("FOR-IN-CHANNEL TEST PASS")
