type A
    x: int

type B
    y: int

fn A__greet(a: A) -> string
    return "a"

fn B__greet(b: B) -> string
    return "b"

fn main()
    let a = A(1)
    let things = [a]
    let r = things[0].greet()
    print(r)
