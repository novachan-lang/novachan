// MUST FAIL TO COMPILE: 'qtyy' is one insertion away from 'qty'.
import forge_orm
type Wc2
    id: int
    name: string
    qty: int
fn main()
    let db = unwrap(orm_open("sqlite://_ormcol.db"))
    let xs: list<Wc2> = orm_all(db, "SELECT id, name, qtyy FROM Wc2", [])
    print(len(xs))
