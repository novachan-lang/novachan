fn add_one(x: int) -> int
    return x + 1

fn multiply(a: int, b: int) -> int
    return a * b

fn main()
    let p = fn_ptr("add_one")
    print("fn_ptr returned: " + str(p))
    if p != 0
        print("PASS: fn_ptr returned non-zero address")
    else
        print("FAIL: fn_ptr returned 0")
