@deprecated
fn old_api() -> string
    return "old result"

@deprecated("use new_process instead")
fn old_process(x: int) -> int
    return x * 2

fn main()
    let r = old_api()
    assert(r == "old result", "deprecated fn still works")

    let p = old_process(5)
    assert(p == 10, "deprecated with msg works")

    print("DEPRECATED ANNOTATION TEST PASS")
