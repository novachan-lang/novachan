type Pair
    a: string
    b: int

let items = []
push(items, Pair("hello", 1))
push(items, Pair("world", 2))

let first = items[0]
match first
    Pair(a, b) =>
        print(a)
        print(str(b))

let second = items[1]
match second
    Pair(a, b) =>
        print(a)
        print(str(b))
