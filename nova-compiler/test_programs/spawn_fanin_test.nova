// Multiple spawns writing to the same channel (fan-in)
ch = channel()

spawn fn()
    send(ch, 100)

spawn fn()
    send(ch, 200)

spawn fn()
    send(ch, 300)

a = receive(ch)
b = receive(ch)
c = receive(ch)
// Order is non-deterministic, but sum is always 600
print(a + b + c)
