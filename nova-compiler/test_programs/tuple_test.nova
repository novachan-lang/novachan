pair = (10, 20)
(a, b) = pair
print(a)
print(b)

fn swap(t)
    (x, y) = t
    (y, x)

result = swap((3, 7))
(p, q) = result
print(p)
print(q)

fn min_max(a, b)
    match a < b
        1 => (a, b)
        _ => (b, a)

(lo, hi) = min_max(42, 17)
print(lo)
print(hi)
