// Probe: can a NOVA top-level fn be exported from the wasm module + called by the host (JS)?
// The handler is identified by its EXPORT NAME (a string) -- no fn pointers / closures / table.
extern fn host_flag(v: int)

fn on_click()
    unsafe host_flag(42)

fn main()
    unsafe host_flag(1)
