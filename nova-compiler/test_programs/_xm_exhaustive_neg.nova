// NEGATIVE PROBE: Fix 1.1 (cross-module exhaustiveness).
// This file MUST be REJECTED with E1009 — the match omits XmBlue on an enum
// declared in an IMPORTED module. Before the fix it compiled clean and the
// missing arm silently returned "".
import _xm_soundness_mod

fn main()
    let c = _xm_soundness_mod.xm_make_red()
    match c
        XmRed(v) => print("red")
        XmGreen(v) => print("green")
