// Bootstrap test 13: closures and higher-order functions
fn apply(f, x)
    return f(x)

fn double(n)
    return n * 2

print(apply(double, 5))

fn make_adder(n)
    fn adder(x)
        return x + n
    return adder

let add10 = make_adder(10)
print(add10(5))
print(add10(20))
