assert(1 == 1, "one equals one")
assert(2 + 2 == 4, "basic math")
assert(10 > 5, "comparison")

let x = 42
assert(x == 42, "variable binding")

fn add(a: int, b: int) -> int
    a + b

assert(add(3, 4) == 7, "function call")

print("All assertions passed!")
