fn main()
    let xs = []
    let i = 0
    while i < 1000
        xs.push(float(i))
        i = i + 1
    let r = 0
    let total = 0.0
    while r < 100000
        let ys = xs.map(fn(v) v * 2.0 + 1.0)
        total = total + ys[0]
        r = r + 1
    print(str(total))
