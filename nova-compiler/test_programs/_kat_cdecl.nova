@cdecl
fn my_compare(a: int, b: int) -> int
    if a < b
        return -1
    if a > b
        return 1
    return 0

@cdecl
fn on_event(code: int) -> int
    code * 2

@export
fn lib_version() -> int
    1

fn main()
    print("unused in library mode")
