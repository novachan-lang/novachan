// Negative test (compile-must-fail): calling an extern function without an
// enclosing 'unsafe' should be rejected by the type checker. This test is
// driven by _ffi_unsafe_required_probe.ps1 which expects compilation to FAIL.
extern fn strlen(s: string) -> int

fn main()
    let n = strlen("hello")
    print(n)
