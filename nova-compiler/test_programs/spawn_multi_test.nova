// Multiple values through a single channel (producer-consumer)
ch = channel()

spawn fn()
    send(ch, 10)
    send(ch, 20)
    send(ch, 30)

a = receive(ch)
b = receive(ch)
c = receive(ch)
print(a)
print(b)
print(c)
print(a + b + c)
