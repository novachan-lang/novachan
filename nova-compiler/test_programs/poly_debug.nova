fn first(items: list) -> string
    return str(items[0])

fn main()
    let nums = [42, 99]
    let strs = ["hello", "world"]
    let r1 = first(nums)
    let r2 = first(strs)
    print("r1=" + r1 + " r2=" + r2)
