type Token
    kind: string
    value: string

let tokens = []
let words = ["hello", "world", "foo"]
for w in words
    push(tokens, Token("word", w))

print(str(len(tokens)))
match tokens[0]
    Token(kind, value) =>
        print(kind)
        print(value)
