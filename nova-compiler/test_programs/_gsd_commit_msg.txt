test(async): gen_server MODEL demo -- counter composing mailbox + selective receive + timeout + spawn

Proves NOVA's Erlang/Elixir primitives (shipped in 6fc488c selective receive + f16c3c0 timeout)
compose into OTP's flagship behaviour with NO framework -- just the language. A counter process
owns private state and serves cast (incr, fire-and-forget), call (get, request/reply), and stop
messages in a `receive` loop with an idle `after` timeout; the state is reachable only via a
message, so process isolation IS the encapsulation. 100 casts -> count 100; the call replies 100;
stop reports the final state.

Pure NOVA -- no compiler/runtime change. Registered in _run_final_regression.ps1 as a permanent
guard; passes NORMAL + FULLRC + ASAN-clean.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
