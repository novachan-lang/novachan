// Diagnostic: isolate f-string vs regular string interpolation
// Test 1: regular string with no interpolation
let a = "hello"
print(a)

// Test 2: regular string concatenation (no interpolation)
let b = "hello" + " " + "world"
print(b)
