type P
    x: float
    y: float

fn getany()
    let d = {}
    d["v"] = 2.5
    d["v"]

fn main()
    let p = P(3, 4)
    print(str(p.x + p.y))
    let q = P(getany(), 1.5)
    print(str(q.x + q.y))
