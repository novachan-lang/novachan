import std/compress/gzip_member

fn main()
    let buf = []
    push(buf, 31)
    push(buf, 139)
    push(buf, 8)
    push(buf, 8)
    push(buf, 0)
    push(buf, 0)
    push(buf, 0)
    push(buf, 0)
    push(buf, 0)
    push(buf, 3)
    let f = gz_flags(buf)
    print("flags=" + str(f))
    // now call the flag fns
    let r4 = gz_flag_fname(buf)
    print("fname=" + str(r4))
    // try direct bit check with imported flags fn
    let f2 = gz_flags(buf)
    let bit = (f2 & 8)
    print("f2=" + str(f2) + " bit=" + str(bit))
    print("PASS _gzdbg5_test")
