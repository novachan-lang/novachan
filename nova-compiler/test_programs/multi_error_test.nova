// Test: 3 parse errors in different functions
fn good()
    print("this is fine")

fn broken1()
    let x = (1 + 2    // missing closing )
    print(x)

fn also_good()
    print("also fine")

fn broken2()
    let y = [1, 2,    // missing closing ]
    print(y)

fn last_good()
    print("last good")

fn broken3(a b)       // missing comma between params
    print(a)
