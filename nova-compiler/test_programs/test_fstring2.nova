// Test: f-string with escaped quotes inside interpolation
fn main()
    let config = {}
    config["host"] = "localhost"

    // This should also work - backslash quote inside interpolation
    print(f"host = {config[\"host\"]}")

    print("done")

main()
