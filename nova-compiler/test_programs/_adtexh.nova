enum Colour
    Red()
    Green()
    Blue()

// MISSING the Blue arm and no _ -- must still be a compile error
fn name(c: Colour) -> string
    match c
        Red() => "red"
        Green() => "green"

fn main()
    print(name(Red()))
