fn first(items) -> string
    return str(items[0])

fn main()
    let strs = ["hello", "world"]
    let r2 = first(strs)
    assert(r2 == "hello", "first(strs) should be hello, got: " + r2)
    print("OK: " + r2)
