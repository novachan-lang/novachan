fix(runtime): M:N Stage 0 -- in_rq hard guard (single-owner-per-task) + proves no latent double-enqueue

First step of the per-carrier work-stealing deque scheduler redesign (NOVA_DESIGN/MN_PER_CARRIER_DEQUE_DESIGN.md,
a vetted 5-agent design). NO behavior change -- pure enforcement + a proof.

Promotes the in_rq DOUBLE-PUSH detector in nova_rq_push from a watchdog-only log to a HARD guard at N>1: a
double-push == a double-enqueue, which at N>1 means two carriers run the same task -> double live_dec (live-count
negative -> premature termination) / double monitor-notify / double mailbox drain / a reclaim UAF. abort() makes
that a LOUD deterministic crash instead of silent corruption. Gated `g_watchdog_on || g_carrier_count > 1` -> ZERO
cost at N=1 (the only production mode). Also documents the load-bearing lock order (ch->lock OUTER, recursive
g_sched_lock INNER, per-carrier deque locks LEAF) that the later stages obey.

★ PROOF (the high-information part): ran green_scale (10k tasks x2 phases) AND a 100k-yield reschedule storm, BOTH
at NOVA_CARRIERS=4 + NOVA_SCHED_WATCHDOG=1 + ASAN, with the guard ON. The guard NEVER fired -> there is NO latent
double-enqueue in the current N>1 scheduler (green_scale passes clean; the reschedule storm only lock-convoys, never
double-enqueues). This refines the N>1-reclaim-defer finding (the UAF was not a simple double-enqueue in these
paths) and gives a permanent enforcement that will catch any double-enqueue the stealing stages (B+) might introduce.

Gate: N=1 reconverged gen5.ll==gen6.ll + 551/551 both modes (the guard is inert at N=1); N>1 green_scale N=4 +
watchdog + ASAN clean. Next: Stage A (per-carrier deque infrastructure, pop-only wiring, deques empty -> no behavior
change).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
