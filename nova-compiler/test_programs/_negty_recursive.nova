// MUST FAIL: self-application x(x) requires x to have an infinite type (x : x -> ?).
// The occurs-check must reject this (recursive/infinite type), not silently accept.
fn selfapp(x)
    x(x)

fn main()
    print(selfapp(selfapp))
