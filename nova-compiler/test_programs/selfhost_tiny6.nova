type Token
    kind: string
    value: string

fn make_token(k: string, v: string) -> Token
    Token(k, v)

let t = make_token("IDENT", "hello")
match t
    Token(kind, value) =>
        print(kind + " " + value)
