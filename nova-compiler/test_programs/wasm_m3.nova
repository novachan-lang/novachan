// A real NOVA computation (a loop summing 1..10 = 55) running in wasm32, with a
// DYNAMIC string result (str(s)) printed via the JS-hosted runtime bridge.
fn main()
    let s = 0
    let i = 1
    while i <= 10
        s = s + i
        i = i + 1
    print(str(s))
