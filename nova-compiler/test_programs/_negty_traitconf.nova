// MUST FAIL: a type declared to implement a trait but missing a method — E1006.
trait Greeter
    fn greet(self) -> string
    fn name(self) -> string
type Bot : Greeter
    id: int
fn Bot.greet() -> string
    "hi"
fn main()
    let b = Bot(1)
    print(b.greet())
