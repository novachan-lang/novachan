// Spawn capturing multiple variables from outer scope
ch = channel()
x = 10
y = 20
name = "nova"

spawn fn()
    send(ch, x + y)

result = receive(ch)
print(result)

// Second spawn capturing different set
ch2 = channel()
factor = 5

spawn fn()
    send(ch2, result * factor)

print(receive(ch2))
