[package]
name = "nova-compiler"
# Keep in sync with nova-compiler/VERSION (the single source of truth; TOML can't read files
# dynamically, so this one is manual -- also read at runtime by nova_find_version()).
version = "0.1.0"
nova-version = "0.1.0"
description = "The NOVA self-hosted compiler and toolchain"
author = "mangesh.mane"
license = "Apache-2.0"
repository = "https://bitbucket.org/manemangesh/nova"

[dependencies]
