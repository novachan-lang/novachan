// forge_closestpair -- thin wrapper over std/geometry/closestpair (canonical implementation).
import std/geometry/closestpair

fn closest_pair_dist2(points: list) -> int
    return cp_closest_sq(points)

