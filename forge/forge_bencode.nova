// forge_bencode -- thin wrapper over std/data/bencode (canonical implementation).
import std/data/bencode

fn bencode_encode(v) -> string
    return bnc_encode(v)

fn bencode_decode(s: string)
    return bnc_decode(s)

