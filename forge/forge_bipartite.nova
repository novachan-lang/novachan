// forge_bipartite -- thin wrapper over std/graph/bipartite (canonical implementation).
import std/graph/bipartite

fn bipartite_coloring(edges: list) -> dict
    return bp_coloring(edges)

fn is_bipartite(edges: list) -> bool
    return bp_is_bipartite(edges)

