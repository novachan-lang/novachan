// forge_reservoir -- thin wrapper over std/sketch/reservoir (canonical implementation).
import std/sketch/reservoir

fn reservoir_sample(items: list, k: int, seed: int) -> list
    return rs_sample(items, k, seed)

