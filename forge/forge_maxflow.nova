// forge_maxflow -- thin wrapper over std/graph/maxflow (canonical implementation).
import std/graph/maxflow

fn max_flow(edges: list, source, sink) -> int
    return mf_max_flow(edges, source, sink)

