// forge_shlex -- thin wrapper over std/text/shlex (canonical implementation).
import std/text/shlex

fn shlex_split(s: string) -> list
    return sh_split(s)

fn shlex_count(s: string) -> int
    len(shlex_split(s))

