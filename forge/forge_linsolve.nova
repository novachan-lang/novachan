// forge_linsolve -- thin wrapper over std/numeric/linsolve (canonical implementation).
import std/numeric/linsolve

// Returns the solution vector, or null when A is singular (see ls_solve).
fn solve(A: list, b: list) -> any
    return ls_solve(A, b)

